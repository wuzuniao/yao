# 更新记录

本文件用于记录项目的功能更新和主要变动。**每次功能完成后，请务必在此文件中进行记录。**

---

## 2026-08-10（feat）- 新增 MariaDB 定时备份脚本，每周一自动备份并保留最近 12 份

**背景**：项目数据库（wuzuniao_yao、wuzuniao_yonghu）此前无定期备份机制，需通过 cron 定时任务实现自动化备份，确保数据安全。

**新增脚本** `scripts/backup_db.sh`：
- 备份 MariaDB 容器（yao-mariadb）内的两个业务数据库：wuzuniao_yao（按时吃药打卡业务库）和 wuzuniao_yonghu（用户认证与账户绑定库）。
- 每个数据库单独一个 `.sql` 文件，按 `日期_库名.sql` 命名（如 `2026-08-10_wuzuniao_yao.sql`），保存到 `deploy/backup_sql/` 目录。
- 使用 `mariadb-dump --databases` 包含完整的 `CREATE DATABASE IF NOT EXISTS` 和 `CREATE TABLE` 结构。
- 使用 `--skip-extended-insert` 实现数据单行插入（每行数据一条 INSERT 语句），配合 `--complete-insert` 包含列名提升恢复兼容性。
- 附加参数：`--single-transaction`（InnoDB 一致性快照不锁表）、`--default-character-set=utf8mb4`、`--routines --triggers --events`（完整备份存储过程/触发器/事件）。
- root 密码从 `deploy/.env` 读取，未硬编码；路径使用 `BASH_SOURCE` 推导，与 deploy.sh 保持一致的可移植性。
- **保留策略**：每个数据库仅保留最近 12 份备份文件，超出部分自动清理（按文件名日期升序删除最早的）。

**定时任务**：已添加到系统 crontab，每周一 00:00 执行（`0 0 * * 1`），输出日志追加到 `deploy/backup_sql/backup.log`。

**影响范围**：仅新增备份脚本与 cron 任务，不涉及业务代码变更；备份文件存放于 `deploy/` 目录（已被 .gitignore 忽略，不纳入版本控制）。

---

## 2026-08-09（chore）- 将 V1.0.7 标签重新指向最新提交 c5f0e4e

**背景**：V1.0.7 原指向提交 `c4c9d43`（feat(i18n)），但自该提交后已累积 7 个新提交（含新手引导修复、鸿蒙推送排除、法务文档校准等）。用户要求将 V1.0.7 标签更新为最近提交版本。

**操作**：
- 本地删除旧 `V1.0.7` 标签，在最新提交 `c5f0e4e`（fix(guide): 修复登录成功后新手引导步骤未推进的问题）上重新打 `V1.0.7` 标签。
- 远程删除旧 `V1.0.7` 标签（`git push origin --delete V1.0.7`），并 force push 新标签。
- 验证：本地/远程 `refs/tags/V1.0.7` 均指向 `c5f0e4e`，与 HEAD 一致。

**待办（用户跳过）**：GitHub Release `V1.0.7` 附件未更新。当前 release 仍挂载旧 APK `__UNI__528E611_0809121922.apk`，需替换为本地新构建的 `frontend/unpackage/release/apk/__UNI__528E611_0809202918.apk` 并删除旧附件（需 GitHub token / gh CLI，当前环境无凭据，留待用户或后续处理）。

**影响范围**：仅 git 标签指向；改写已发布标签可能影响已基于旧标签拉取/下载的用户，无代码内容变更。

## 2026-08-09（fix）- 法务文档同步「鸿蒙无 App 推送」并删除鸿蒙陈旧编译产物

**背景**：前述方案 A 已将友盟推送插件排除出鸿蒙端（harmony 标记 `-`、useAppPush.js 收窄为 `APP-PLUS`、删除 MyAbilityStage.ets），但法务文档仍表述「App 推送覆盖 Android／iOS／HarmonyOS」，与事实脱节。

**法务文档校准**（`frontend/locale/zh-CN.js` 与 `en.js`，privacy.vue/agreement.vue 均为 `$t()` 纯展示，文案集中在语言包）：
- `agreementDoc.c3p1b`：App 推送通知由「仅 App 端（Android／iOS／HarmonyOS）」改为「Android／iOS（HarmonyOS 端暂不支持）」（指纹登录仍三端）。
- `agreementDoc.c10p1`：友盟 U-Push SDK 由「仅 App 端」改为「仅 App 端，HarmonyOS 端除外」。
- `privacyDoc.c2s5l4b`：设备平台类型由「android／ios／harmony」改为「android／ios（HarmonyOS 端暂不产生推送标识）」，与后端推送枚举 `Literal["android","ios","harmony"]` 区分（后端枚举保留 harmony 以兼容潜在未来端）。
- `privacyDoc.c2s9l2`：设备唯一识别码采集口径补「HarmonyOS 端无推送 SDK，此类标识由框架采集」。
- `privacyDoc.c5r2Name`：第三方 SDK 表中友盟 SDK 由「Android／iOS／HarmonyOS」改为「Android／iOS（HarmonyOS 端除外）」。
- 校验：两语言包 `require` 通过，键路径对齐。

**删除鸿蒙陈旧编译产物**：
- 删除 `frontend/unpackage/dist/build/app-harmony`（含错误 `oh-package.json5` 与陈旧 `oh-package-lock.json5`，二者曾导致 00617202）。
- 上层 `unpackage/dist/build`、`unpackage/dist` 保留（可能含其他平台产物）。
- 现可于 HBuilderX 重新发行鸿蒙，不再报 00617202。

**影响范围**：仅法务文案与本地编译产物；鸿蒙端提醒能力仍由邮件/站内信承担，安卓/iOS 推送不受影响。

## 2026-08-09（fix）- 修复鸿蒙编译 hvigor 00303196：entry module.json5 缺 type 字段

**问题**：重发鸿蒙后跳过 00617202，但 hvigor 报 `00303196 Configuration Error: Unable to obtain the module type at .../app-harmony/entry/src/main/module.json5`。

**根因**：HBuilderX 合并 `harmony-configs/entry/src/main/module.json5` 与内置模板时，会注入 `deviceTypes` 等默认字段，但**不会自动注入 `module.type`**。原源文件只声明了 `requestPermissions`，导致产物 `entry/src/main/module.json5` 的 `module` 对象无 `type` 字段 → hvigor 无法识别模块类型。

**处理**：在源 `frontend/harmony-configs/entry/src/main/module.json5` 的 `module` 对象显式补 `"name": "entry"` 与 `"type": "entry"`（标准 entry 模块必填字段），并加注释说明务必手写 type。HBuilderX 字段级深度合并会保留该值。同步删除上次发行残留的 `app-harmony` 产物目录，确保全新合并。

**待用户后续**（非代码问题）：日志提示「没有配置签名证书」——HBuilderX 本地/云打包鸿蒙需签名证书（`.p12`/`.cer`/`.p7b` 二进制，禁止 AI 生成/提交），须在 `manifest.json` 的 `app-harmony.distribute` 配置签名 material 或由用户确认使用自动调试签名后再重发验证。

## 2026-08-09（fix）- 修复鸿蒙编译 hvigor 00303038：entry module.json5 schema 缺 deliveryWithInstall

**问题**：00303196 修复后重发，hvigor 报 `00303038 Configuration Error: Schema validate failed ... must have required property 'deliveryWithInstall'`（`module` 缺该必填字段）。

**根因**：与 00303196 同源——HBuilderX 合并 `harmony-configs/entry/src/main/module.json5` 时只注入 `deviceTypes` 等少量字段，**不会注入 `module` 的必填 schema 字段**（`type`、`deliveryWithInstall`、`mainElement` 均需手写）。

**处理**：在源 `frontend/harmony-configs/entry/src/main/module.json5` 的 `module` 补 `"mainElement": "EntryAbility"`（与 HBuilderX 生成的 `entry/src/main/ets/entryability/EntryAbility.ets` 对应）与 `"deliveryWithInstall": true`。同步删除上次发行残留 `app-harmony` 产物，确保全新合并。

**附**：`build-profile.json5` 已含签名配置（`signingConfigs.release` 指向 `yao.cer`/`com.wuzuniao.yao.p12`/`yaoRelease.p7b`），故「没有配置签名证书」仅为 HBuilderX 早期提示，非实际障碍，签名已由 HBuilderX 自动生成。

## 2026-08-09（chore）- 鸿蒙签名凭据写入 .gitignore，防止密钥泄露

**背景**：`frontend/harmony-configs/` 下存在签名私钥库 `com.wuzuniao.yao.p12`、证书 `yao.cer`/`yao.csr`、发布 Profile `yaoRelease.p7b` 及 `material/` 二进制散列缓存。这些均为敏感凭据，但 `.gitignore` 此前未覆盖，存在 `git add -A` 误提交风险。

**处理**：
- `.gitignore` 的「Node.js / 前端」段新增忽略规则：`frontend/harmony-configs/*.p12`、`*.cer`、`*.csr`、`*.p7b`、`material/`（`entry/` 下 `module.json5`/`string.json` 等非隐私工程配置仍提交）。
- `git rm --cached` 移除已删除的 `MyAbilityStage.ets`（方案 A 删除但未暂存，索引仍追踪）。
- 验证：`git check-ignore` 确认四类证书与 material 均被忽略；`git status` 中证书文件不再出现于未追踪列表。

**影响范围**：仅仓库忽略配置与安全索引，不涉及业务代码。

---

## 2026-08-09（fix）- 修复鸿蒙编译 ohpm 报错 00617202：友盟推送插件排除出鸿蒙端

**问题**：HBuilderX 发行鸿蒙时 `ohpm install` 失败——
`Error: 00617202 Fetch Local Package Failed，...\app-harmony\uni_modules\xtf-umengpush\utssdk\app-harmony\module.har does not exist`。

**根因**（非配置写错，属能力边界）：
- 产物 `unpackage/dist/build/app-harmony/oh-package.json5` 声明依赖
  `"@uni_modules/xtf-umengpush": "./uni_modules/xtf-umengpush/utssdk/app-harmony/module.har"`，
  但产物根目录只有 `AppScope/entry/hvigor/libs/oh_modules`，**没有 `uni_modules/` 目录**，该 har 从未生成；
  `oh-package-lock.json5` 中也无此依赖条目，属上次成功安装遗留的陈旧 lock。
- xtf-umengpush 为 DCloud **付费加密 UTS 插件**（`utssdk/{app-android,app-ios,app-harmony}/index.uts`
  三份实现文件均为密文，仅 `interface.uts` 等声明文件为明文，另有 144 字节 `encrypt` 授权文件）。
- 按 uni-app 官方《制作和发布插件指南》：加密 UTS 插件发行到 **Web / 小程序 / 鸿蒙** 须走
  **DCloud 云端编译**换取 har，而该链路**仅支持 uni-app x 工程**；本项目为传统 uni-app
  （`@dcloudio/uni-app` + `uni-app-plus`，`.vue` + `<script setup>`），故云编译不触发、har 永不生成。
- 因此 `ohpm clean` 无效——缺的不是缓存，是从未被生成的产物。

**处理（方案 A：鸿蒙端排除该插件，零成本）**：
- `frontend/uni_modules/xtf-umengpush/package.json`：`uni_modules.platforms.client.uni-app.app.harmony`
  由 `"√"` 改为 `"-"`，使 HBuilderX 不再为鸿蒙生成 har 依赖。
- `frontend/composables/useAppPush.js`：全部条件编译由 `APP` 改为 `APP-PLUS`（Android+iOS），
  与插件平台声明保持一致——否则鸿蒙端会残留悬空 import 而以另一种方式编译失败。
  同步移除因外层收窄而失效的 `APP-HARMONY` 死分支（requestPermission 的鸿蒙授权分支、
  getDeviceToken 的 `platform='harmony'` 分支），并在文件头补「勿改回 #ifdef APP」的根因说明。
- 删除 `frontend/harmony-configs/entry/src/main/ets/abilityStage/MyAbilityStage.ets`：
  其 `import { preInit } from '@umeng/analytics'` / `'@umeng/push'` 依赖插件 `config.json`
  提供的 ohpm 包，插件排除后必然编译失败（该文件 `srcEntry` 本就处于注释未启用状态）。
- `frontend/harmony-configs/entry/src/main/module.json5`：移除友盟权限合并与 AbilityStage
  桥接的过时注释，改为记录鸿蒙不集成友盟推送的原因。

**影响范围**：鸿蒙端不再提供 App 推送（提醒能力由邮件 / 站内信通道承担）；
Android / iOS 端推送链路**完全不受影响**（宏由 `APP` 收窄为 `APP-PLUS`，二者在这两端等价）。
微信小程序 / H5 不受影响。若后续需鸿蒙推送，须购买该插件**源码授权版**（不受加密云编译限制）。

**待人工执行**：删除陈旧产物目录 `frontend/unpackage/dist/build/app-harmony`（内含错误的
`oh-package.json5` 与陈旧 lock），再重新发行鸿蒙，避免复用旧依赖声明。

---

## 2026-08-09（docs）- 申请表填写信息.md 补充在线表单三个字段（开发目的 / 面向领域 / 主要功能）

用户提交截图显示中国版权保护中心在线申请表的三项补充字段（开发目的 ≤50 字、面向领域/行业 ≤50 字、软件的主要功能 500–1300 字）。以软件说明书 1.1/1.3 + 项目实际技术栈为基础，以"人话口吻、去除 AI 味"原则起草三项内容，写入 `申请表填写信息.md` 新增「七、补充说明字段（在线表单）」小节，并将原七、打印/导出指引顺延为八。已用人工字数核算：开发目的≈23 字、面向领域≈15 字、主要功能≈580 字，全部符合字数限制。

---

## 2026-08-09（docs）- 申请表填写信息.md 按软件说明书核对更正

以已更正的 `软件说明书.html` 为基准，更正 `申请表填写信息.md` 技术信息章节：① 开发使用的数据库 MariaDB → MariaDB 10.11 (LTS)；② 运行平台补充「鸿蒙 HarmonyOS App」；③ 软件用途/功能通知渠道补充「App 推送（友盟+ U-Push）」；④ 七、打印指引更新过时提示（说明书截图已全部替换，可直接打印）。

---

## 2026-08-09（docs）- 软件说明书补充截图（4.6 新手引导：介绍页 + 6 步共 8 张分步引导）

- 将 9 张截图复制到 `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/`，按规范命名：
  - `4.6.1-welcome.png`：新手引导介绍页（首次进入应用展示「按时吃药」主要功能 + 新手引导说明 + 底部「点我开启新手引导」按钮）。
  - `4.6.2-guide-step1.png` ~ `4.6.5-guide-step4.png`：分步引导上半（步骤 1→4：点击「设置」→ 点击「请登录」用户资料卡 → 微信一键登录 → 配置通知方式可跳过）。
  - `4.6.6-guide-step5a.png` / `4.6.7-guide-step5b.png`：分步引导下半步骤 5（点击「制定计划」入口 → 进入计划列表点击「+ 新建计划」）。
  - `4.6.8-guide-step6a.png` / `4.6.9-guide-step6b.png`：分步引导下半步骤 6 收尾（点击底部「首页」回到首页 → 在首页点击大红色「立即打卡」按钮完成打卡，引导结束）。
- 替换 4.6 节占位框（原 `【截图占位：新手引导 / 功能介绍界面】`）：
  - 图 4-6 新手引导介绍页（上方单图，max-width:80%;max-height:130mm）。
  - 图 4-7 分步引导上半（步骤 1→4，四张并排，每张 max-width:24%;max-height:90mm）。
  - 图 4-8 分步引导下半（步骤 5→6 收尾，四张并排，每张 max-width:24%;max-height:90mm）。
- 内容跨多个 A4 打印页（CSS page-break 自动分页），4.7 公告及后续章节自动顺延；不影响章节编号连贯性。
- 同步 `目录结构.json`（screenshots 新增 4.6.1~4.6.9 共 9 条）。**说明书截图占位全部清零**，剩余仅申请表 5 项个人敏感信息待补：身份证号、通讯地址、邮编、联系人/电话、邮箱。

---

## 2026-08-09（docs）- 软件说明书补充截图（5.4 主要 API 接口示例：Swagger 接口文档 + 调试界面）

- 将 3 张截图复制到 `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/`，按规范命名：
  - `5.4.1-overview.png`：Swagger 接口文档总览（标题「无足鸟按时吃药打卡 0.1.0 · OAS 3.1」，分组：用户 / 通知渠道 / 计划（GET 列表、POST 创建、DELETE/PUT 删除更新）/ 打卡记录 / 站内信 / 公告 / default，含底部 Schemas）。
  - `5.4.2-create-plan.png`：POST `/api/v1/plans` 调试界面上半（创建计划请求体 schema：Parameters/authorization、Request body application/json 与示例 JSON）。
  - `5.4.3-responses.png`：POST `/api/v1/plans` 调试界面下半（Responses 200/422 示例与下方 DELETE `/api/v1/plans/{plan_id}` 入口）。
- 替换 5.4 节占位框（原 `【截图占位：Swagger 接口文档 / 调试界面】`）：
  - 图 5-4 Swagger 接口文档总览（上方单图，max-width:100%;max-height:130mm）。
  - 图 5-5 POST `/api/v1/plans` 调试界面（下方双图并排，各 max-width:48%;max-height:130mm）。
- 同步 `目录结构.json`（screenshots 新增 5.4.1/5.4.2/5.4.3 三条）。截图占位剩余 1 处：4.6（新手引导）。

---

## 2026-08-09（docs）- 软件说明书补充截图（4.8 后台调度与提醒引擎：终端日志 + 站内信）

- 将 2 张截图复制到 `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/`，按规范命名：
  - `4.8.1-scheduler.png`：后端 Uvicorn 终端运行日志（持续接收前端的 `GET/POST /api/v1/...` 请求，包括计划列表、通知渠道、未读数、今日打卡、提交打卡、微信订阅授权等）。
  - `4.8.2-station-msg.png`：前端「站内信」页面（展示历次打卡提醒消息，含「每日降压药」提醒条目，可点击未读条目标记为已读）。
- 替换 4.8 节占位框（原 `【截图占位：后台调度 / 通知日志界面】`）：
  - 图 4-7 后台调度终端日志（上方单图，max-height 120mm）。
  - 图 4-8 站内信通知查看（下方单图，max-height 160mm）。
- 同步 `目录结构.json`（screenshots 新增 4.8.1/4.8.2 两条）。截图占位剩余 2 处：4.6（新手引导）/ 5.4（Swagger）。

---

## 2026-08-09（bugfix）- 软件说明书章节结构修复（4.8/5.4/6.4 归位 + 移除重复"结束"标记）

### 变更类型
bugfix（软著材料_无足鸟按时吃药打卡软件V1.0.7 软件说明书 HTML 结构）

### 问题
用户反馈说明书中看不到 4.8 与 5.4 两节。排查发现：生成时的"补充 section"（4.8 后台调度与提醒引擎 / 4.9 权限与角色、5.4 主要 API 接口示例、6.4 数据库主要表结构）被错误地追加到文件末尾、且位于第 7 章之后；同时文档内出现了**两处**"—— 软件说明书 结束 ——"标记（第 7 章内一处错误提前、第 8 章内一处正确）。导致章节顺序错乱、4.8/5.4 夹在"结束"标记之后不易被看到。

### 修正
- 将 `<!-- 第4章补充：调度引擎 -->`(4.8/4.9) 移入第 4 章主段（4.7 公告）之后。
- 将 `<!-- 第5章补充：API 示例 -->`(5.4) 移入第 5 章主段（5.3 注意事项）之后。
- 将 `<!-- 第6章补充：数据库结构 -->`(6.4) 移入第 6 章主段（6.3 创新点）之后。
- 删除第 7 章内错误提前的"结束"标记，仅保留第 8 章末尾的正确"结束"标记（全文唯一）。
- 校验：Grep 确认章节号 4.1–4.9、5.1–5.4、6.1–6.4 顺序连贯，"结束"标记仅剩 1 处，末尾重复块已清除。

### 影响范围
- 软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html（章节顺序修复，无新增/删除文件，截图占位仍为 3 处：4.6 / 4.8 / 5.4）。

---

## 2026-08-09（docs）- 软件说明书补充截图（5.2 典型操作示例：创建→提醒→打卡）

### 变更类型
docs（软著材料_无足鸟按时吃药打卡软件V1.0.7 软件说明书）

### 内容简述
为软件说明书 5.2 节「典型操作示例」替换占位框，新增 3 张真实界面截图：
- 5.2.1-create.png：步骤一，新建计划（每日降压药，08:00 提醒，优先级 3，通知方式 站内信+微信+邮件，进行中）。
- 5.2.2-remind.png：步骤二，站内信提醒页（顶部「每日降压药」未读提醒卡 + 全部已读按钮 + 历史系统公告）。
- 5.2.3-checkin.png：步骤三，立即打卡页（计划卡片「每日降压药/进行中」，底部首页/记录/设置导航，中间红色立即打卡按钮）。

### 影响范围
- 软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html（5.2 节占位框替换为三张图+图注）
- 软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/（新增 5.2.1 / 5.2.2 / 5.2.3 三张 PNG）
- 目录结构.json（screenshots 块同步登记新文件）

---

## 2026-08-09（fix）- 修复 App 端新手引导第二步登录成功返回设置页引导未推进

### 变更类型
fix（App 端新手引导步骤推进逻辑，影响 App/H5 端登录成功返回设置页后的引导推进）

### 根因
App/H5 端步骤配置（nonWechatSteps）为「引导用户注册」设计：步骤 3 在登录页高亮「立即注册」，步骤 3-续 在注册页高亮注册卡片，注册成功回设置页后推进到步骤 4。但用户可在登录页直接账号密码/指纹登录（未走注册流程），登录成功 `redirectTo` 回设置页时，`onPageEnter('settings')` 因「当前步骤(login)的下一步是 register 而非 settings」，转而匹配上一步（settings/profile-card）导致 **currentStep 回退到 1**，引导回退到第 2 步，表现为「引导项没有向下更新」。

微信小程序端正常：其步骤 3 是 `wechat-login`，下一步 `steps[3]` 正是 settings/notification-method，登录返回设置页时 next.page 匹配自动推进。

### 修复
- `frontend/store/modules/guide.js`：新增 `advanceOnLoginSuccess()`，引导激活且当前步骤处于登录页（step.page==='login'）时，跳过注册页步骤直接推进到 `notification-method`（第 4 步）；其余情况（引导未激活、当前不在登录页、已登录模式跳过登录步骤）不操作。
- `frontend/pages/user/login.vue`：在全部 4 个登录成功跳转路径（账号密码 handleLogin 主路径、App 指纹引导开启弹窗回调、App 指纹登录 handleFingerprintLogin、微信登录 handleWechatLogin）跳转 settings 前调用 `guideStore.advanceOnLoginSuccess()`。

### 不破坏已有功能
- **幂等**：微信小程序端原本依赖 onPageEnter 自动推进到同一目标步骤，主动推进后 onPageEnter 见 step.page==='settings' 不再变化，行为一致。
- **注册路径不受影响**：用户点击「立即注册」走 register.vue 注册流程时不会触发 advanceOnLoginSuccess（仅登录成功路径调用），注册成功回设置页仍由 onPageEnter 自动推进（register-card 步骤的下一步正是 settings/notification-method）。
- **非引导态不干扰**：引导未激活（普通登录）时 isActive=false，方法直接返回，零副作用。
- **已登录模式不受影响**：已登录启动引导（guideMode='logged-in'）已用 fastForwardToStep4 跳过登录步骤，currentStep 不会落在 login 页，方法不操作。

### 影响范围
- `frontend/store/modules/guide.js`：新增 advanceOnLoginSuccess 方法及导出
- `frontend/pages/user/login.vue`：4 处登录成功路径新增一行调用
- 不涉及其他页面、组件、后端；微信小程序端行为不变

---

## 2026-08-09（bugfix）- 修复新手引导高亮区块外4角白边

### 变更类型
bugfix（微信小程序新手引导高亮蒙版视觉，影响所有引导步骤的高亮区域外观）

### 根因
前一次为修「高亮区域内无法点击」而把微信小程序端蒙版改为「4 个灰色矩形围出高亮洞、移除 box-shadow 视觉层」。但 4 个矩形拼出的是**矩形洞**，而高亮 view 带**圆角**（border-radius），矩形洞的 4 个角（圆角高亮之外、矩形洞之内）不再被任何灰色蒙版覆盖，透出页面底色，表现为高亮区块外的「4 角白边」。

### 修复
`frontend/components/BeginnerGuide.vue` 回退为昨天以前的实现方式（box-shadow 视觉蒙版）：
1. 视觉蒙版 `mask-hole`（单 view + box-shadow 大扩散）恢复为 H5 与微信小程序**双端共用**（此前被 `#ifdef H5` 限制为仅 H5）。box-shadow 沿高亮圆角向外填充灰色，圆角处无白边。
2. 微信小程序 4 个 `bar` 改回**透明**（`background: transparent`），仅作点击阻挡层（`@click.stop`/`@touchmove.stop` 拦截洞外点击），视觉蒙版统一由 `mask-hole` 的 box-shadow 绘制，拼接处无颜色、无拼接线。
3. 同步修正组件内 4 处注释（头部说明 / 蒙版原理 / maskHoleStyle / 样式注释）为 box-shadow + 透明 bar 方案。

### 不破坏已有功能
- 保留「目标就绪才渲染」：`mask-hole` 与 `bar` 均为 `v-if="highlightRect"`，目标未就绪时不渲染任何遮罩，不会回退到全屏拦截蒙版（卡死）。
- 保留「点击直达目标」：`mask-hole`（z 9998）与 `highlight`（z 9999）均为 `pointer-events:none`，洞内点击透传至目标按钮（与高亮 view 同机制，高亮一直可点即证明该机制在本项目基础库下可靠）；洞外点击由 4 个透明 `bar`（pointer-events:auto）拦截。
- 澄清：前一次「高亮区域内无法点击」的真正元凶是历史提交 634833b 引入的**重复** `mask--hole` 视图——其类 `.beginner-guide__mask` 设有 `background: var(--color-mask-60)` 且 `pointer-events` 为默认 `auto`（实为覆盖在洞上的灰色可点击视图），并非 `mask-hole` 的 `pointer-events:none` 失效。本次未恢复该重复视图，故不会重现已修复的点击问题。

### 影响范围
- `frontend/components/BeginnerGuide.vue`：模板结构、样式、注释（仅微信小程序端蒙版逻辑变更，H5 行为不变）
- `目录结构.json`：BeginnerGuide 描述本就为 box-shadow + 透明 bar 方案，与本次实现一致，无需改动
- 不涉及后端、其他页面、构建产物（dist/unpackage 均为 gitignore，由开发者本地重新构建同步）

---

## 2026-08-09（docs）- 软件说明书按已更正 README 逐条核对修正

### 变更类型
文档核对修正（软件说明书）

### 变更
以已更正的 `README.md` 为基准，逐条核对并修正 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 与 README 不一致/遗漏处（共 8 处）：
1. 2.1 服务端运行环境：数据库推荐配置由 `MariaDB 12.3 (LTS)` 改为 `MariaDB 10.11 (LTS)`，与 README 版本一致（此前生产 10.11、测试/开发 12.3，以 README 生产描述为准）。
2. 1.3 / 2.2 / 4.3 / 8.1：补充「App 推送（友盟+ U-Push，仅 App 端）」通知渠道（README 功能特性/FAQ/技术栈均列出）。
3. 1.3 / 2.2：补充「鸿蒙（HarmonyOS）App」平台（README FAQ「支持哪些平台」）。
4. 5.1 快速开始：登录入口示例补充 App 端，与多端一致。
5. 3.4 配置项说明：扩充配置表，对齐 README 配置章节（新增 WX_SUBSCRIBE_* / 友盟+ U-Push UMENG_* / CORS_ALLOW_ORIGINS，呼应图 3-1 标注）。
6. 6.4 数据库表结构：由 6 表扩充为 10 表，与 ER 图（源自 backend/sql 真实建表）及实际库结构一致（内部一致性修正）。

### 影响范围
- 仅修改 `软著材料_.../软件说明书.html` 文案，未新增文件、未改动业务代码
- 已知分歧：项目规范.md 仍锁定 MariaDB 12.3（开发/测试标准），与 README（生产 10.11）并存；依本次指令以 README 为准修正说明书
- 未涉及目录结构.json（无新增文件）

---

## 2026-08-09（bugfix）- 修复新手引导高亮区域内无法点击

### 变更类型
bugfix（微信小程序新手引导点击拦截，影响所有引导步骤的目标按钮点击）

### 根因
此前为修「添加新方式点击空白」而引入的实现：微信小程序端用 4 块透明 `bar` 围出高亮洞，洞内点击依赖 `mask--hole` 视觉层（`pointer-events:none`）的穿透放行。但 `pointer-events:none` 在本项目微信小程序基础库下对该浮层 view 的穿透不可靠，导致高亮洞内（目标按钮上方）的 `mask--hole` 层实际拦截了点击，目标按钮点不到。

### 修复
`frontend/components/BeginnerGuide.vue`：
1. 微信小程序端：4 块 `bar` 直接承载灰色背景（`background: var(--color-mask-60)`），兼作视觉蒙版与点击阻挡层（`@click.stop`/`@touchmove.stop`）；高亮洞（L~R × T~B）内物理上不再放置任何 view，目标按钮 100% 可点，从根上不再依赖 `pointer-events:none` 穿透。
2. 删除依赖穿透的 `mask--hole` 微信端分支；该视觉层经 `#ifdef H5` 条件编译隔离，仅 H5 继续使用（H5 穿透可靠）。
3. 清理已无引用的死代码：`.beginner-guide__mask` 基础类与 `.beginner-guide__mask--full`（全屏拦截蒙版，正是早前「点不到/空白」的元凶）。
4. H5 端行为不变；同步更新组件文档注释。

### 影响范围
- `frontend/components/BeginnerGuide.vue`：模板结构、点击阻挡层样式、死代码清理
- 已重新 `npm run build:mp-weixin` 同步产物（产物中 `bar` 已为灰色背景，`mask--full` 已消失）
- 不涉及页面、后端、H5/App 逻辑（App 端本无此引导蒙版）

---

## 2026-08-09（docs）- 软件说明书补充截图（4.4 打卡记录 · 日历视图）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 4.4 节占位框：
1. 新增 `screenshots/4.4.1-calendar.png`（打卡记录日历视图：按月查看，有打卡日期带绿点标记，选中日期高亮，下方展示当日打卡记录）。
2. 4.4 节占位框替换为该图并加图注「图 4-6 打卡记录日历视图（按月查看，有打卡日期带绿点标记，选中日期高亮，下方展示当日打卡记录）」。

剩余待补 4 处截图占位：4.6 / 4.8 / 5.2 / 5.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 1 张 PNG
- 同步 `目录结构.json`（screenshots 新增 4.4.1 条目）

---

## 2026-08-09（docs）- 软件说明书补充截图（4.3 多途径通知）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 4.3 节占位框：
1. 新增 `screenshots/4.3.1-channels.png`（通知方式配置：站内信默认、邮件 SMTP、微信订阅消息三种渠道，下方「新建通知方式」可新增邮件/微信渠道）。
2. 4.3 节占位框替换为该图并加图注「图 4-5 通知方式配置（站内信默认、邮件 SMTP、微信订阅消息三种渠道，下方「新建通知方式」可新增邮件/微信渠道）」。

剩余待补 5 处截图占位：4.4 / 4.6 / 4.8 / 5.2 / 5.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 1 张 PNG
- 同步 `目录结构.json`（screenshots 新增 4.3.1 条目）

---

## 2026-08-09（docs）- 软件说明书补充截图（4.2 打卡计划管理）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 4.2 节占位框：
1. 新增 `screenshots/4.2.1-plan-list.png`（计划列表页「制定计划」：计划卡片含名称、备注、提醒时间，可删除/新建）。
2. 新增 `screenshots/4.2.2-plan-create.png`（新建计划详情：名称、备注、起止日期、提醒时间、优先级、通知方式、任务状态）。
3. 新增 `screenshots/4.2.3-plan-edit.png`（编辑计划：预填原数据，底部按钮为「更新计划」）。
4. 4.2 节占位框替换为上述三张图（列表+创建并排、编辑单张），各加图注。

剩余待补 6 处截图占位：4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 3 张 PNG
- 同步 `目录结构.json`（screenshots 新增 4.2.1/4.2.2/4.2.3 条目）

---

## 2026-08-09（docs）- 软件说明书补充截图（4.1 账号体系 · 登录 / 注册）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 4.1 节占位框：
1. 新增 `screenshots/4.1.1-oneclick-login.png`（微信一键登录，需勾选服务协议与隐私政策）。
2. 新增 `screenshots/4.1.2-normal-login.png`（账号密码登录「欢迎回来」，含忘记密码与一键登录入口）。
3. 新增 `screenshots/4.1.3-register.png`（注册账号：用户名+密码+确认密码+邮箱+验证码）。
4. 4.1 节占位框替换为上述三张图（登录两张并排、注册单张），各加图注。

剩余待补 7 处截图占位：4.2 / 4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 3 张 PNG
- 同步 `目录结构.json`（screenshots 新增 4.1.1/4.1.2/4.1.3 条目）

---

## 2026-08-09（docs）- README「配置」章节对齐实际变量

### 变更类型
文档修正（README）

### 变更
依据 `backend/app/core/config.py` 与 `frontend/config/env.js`，将 README「配置」章节后端配置表对齐实际变量：
- 原仅列 5 项，补充 `WX_SUBSCRIBE_TEMPLATE_ID` / `WX_SUBSCRIBE_PAGE` / `WX_SUBSCRIBE_ORG_NAME`、友盟+ U-Push 全套（`UMENG_ANDROID_*` / `UMENG_IOS_*` / `UMENG_HARMONY_*` / `UMENG_PRODUCTION_MODE` / `UMENG_PUSH_PAGE`）、`CORS_ALLOW_ORIGINS`。
- JWT 描述引用来源更正为「项目规范.md §4」（与 .env.template 一致）。
- 引导语补充「完整变量与说明见 .env.template」。前端表保持 `API_BASE_URL` / `WX_SUBSCRIBE_TEMPLATE_ID` 两项不变（env.js 实际导出）。

### 影响范围
- 仅修改 `README.md`「配置」章节，未改动代码/配置
- 同步本次文档修正记录（本文件）

---

## 2026-08-09（fix）- 修复注册页用户名输入框 placeholder 显示错误

### 变更类型
缺陷修复（i18n 词缺失）

### 变更
注册页 `register.vue` 用户名输入框绑定的 `$t('validate.usernamePlaceholder')` 在 `zh-CN.js` / `en.js` 的 `validate` 组中均无对应词条，`$t` 回退为 key 路径字符串，导致输入框提示错误显示 `validate.usernamePlaceholder`。
- `frontend/locale/zh-CN.js`：在 `validate` 组新增 `usernamePlaceholder: '请输入用户名（2-15 个字符，仅限中文、英文及数字）'`。
- `frontend/locale/en.js`：同步新增 `usernamePlaceholder` 英文本条。

### 影响范围
- `frontend/locale/zh-CN.js`、`frontend/locale/en.js`（新增词条，与其它 validate 文案对齐）
- 修复后注册页用户名输入框正常显示占位提示

---

## 2026-08-09（docs）- 软件说明书补充截图（3.4 配置项说明）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 3.4 节占位框：
1. 新增 `screenshots/3.4.1-env.png`（后端 `.env` 配置文件：友盟推送密钥、数据加密密钥、JWT 密钥、CORS 跨域源）。
2. 新增 `screenshots/3.4.2-envjs.png`（前端 `env.js` 配置文件：API 基础地址、微信订阅消息模板 ID）。
3. 3.4 节占位框替换为上述两张图，各加图注。

剩余待补 8 处截图占位：4.1 / 4.2 / 4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 2 张 PNG
- 同步 `目录结构.json`（screenshots 新增 3.4.1/3.4.2 条目）

---

## 2026-08-09（docs）- 修正 backend/.env.template 描述错误

### 变更类型
文档修正（配置模板）

### 变更
逐条核对 `backend/.env.template` 注释与 `backend/app/core/config.py` 及通知代码一致，仅修正一处事实错误：
- JWT 过期天数注释原误引「AGENTS.md 第7节」，实际 7 天约定位于 `项目规范.md` §4（API 设计规范），已更正引用来源。

### 影响范围
- 仅修改 `backend/.env.template` 第 61 行注释，未改动任何变量名/默认值与业务代码
- 同步本次文档修正记录（本文件）

---

## 2026-08-09（docs）- 软件说明书补充架构图（6.4 数据库 ER 图）

### 变更类型
文档更新（软件说明书）

### 变更
按 6.3 相同方法，自动读取项目实际实现生成 6.4 节数据库 ER 图（非 AI 生图）：
1. 读取 `backend/sql/create_user_db.sql`（用户库 3 表：users / user_miniapp_accounts / user_biometric_tokens）与 `backend/sql/create_yao_db.sql`（业务库 7 表：checkin_plans / plan_notification_times / plan_notification_channels / notification_channels / notification_logs / checkin_records / announcements），确认真实表结构与外键引用关系。
2. 生成矢量 SVG `screenshots/6.4.1-er.svg`（viewBox 0 0 1000 860，含两个数据库分区背景、10 张表实体框、PK/FK 标注、关系连线、图例说明 user_id 跨库逻辑关联）。
3. 替换 6.4 节占位框为 ER 图并加图注「图 6-2 数据库表关系（ER 图，依据 backend/sql 实际建表语句生成）」。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增 6.4.1-er.svg
- 同步 `目录结构.json`（screenshots 新增 6.4.1-er.svg）

---

## 2026-08-09（docs）- 更新 README 校正事实性错误

### 变更类型
文档修正（README）

### 变更
依据实际项目状态校正 README（README 描述生产环境：生产 MariaDB 为 10.11，测试/开发为 12.3）：
1. MariaDB 版本：以生产环境为准，徽章与「环境准备」表格统一为 `10.11 (LTS)`（曾误改为 12.3）；「技术栈」表格原误写 `12.3 (LTS)`，校正为 `10.11 (LTS)`。全文一致。
2. 常见问题「支持哪些平台？」：补充已实际支持的鸿蒙（HarmonyOS）App 端。
3. 常见问题「通知渠道有哪些？」：补充已实际实现的 App 推送（友盟+ U-Push，仅 App 端）。
4. 新增「增量更新（生产环境）」章节：补充仅更新 H5 前端/后端时的 4 步增量流程（停止容器释放内存避免 OOM、限制 Node 容器资源构建 H5、`--force-recreate` 仅重建 backend/nginx 不影响 mariadb、构建后验证），含资源限制与 `npm ci` 提示。

### 影响范围
- 仅修改 `README.md` 文案，新增一个章节，未改动任何代码、配置与目录结构
- 同步本次文档修正记录（本文件）

---

## 2026-08-09（docs）- 生成软件著作权登记申请材料（个人主体）

### 变更类型
文档生成（软件著作权登记）

### 变更
在仓库根目录新增 `软著材料_无足鸟按时吃药打卡软件V1.0.7/` 目录，由软件著作权登记助手生成个人主体软著申请材料：
1. `源代码文档.html`：抽取本仓库真实后端源码（FastAPI/Python）共 6596 行，按前 30 页 + 后 30 页（每页 ≥50 行）分页，A4 打印样式，页眉标注“无足鸟按时吃药打卡软件 V1.0.7”。
2. `软件说明书.html`：软件说明书/用户手册，≥15 页，含软件概述、运行环境、安装配置、功能说明、使用说明、技术特点、API 示例、数据库结构、术语等章节，含截图占位框。
3. `申请表填写信息.md`：在线申请表填写字段汇总（软件信息/著作权人信息/技术信息/开发人员）+ 个人主体材料清单与注意事项（身份证复印件、亲笔签字、信息一致、日期逻辑）。
4. `_gen_source.py`：源代码文档生成辅助脚本。

### 待著作权人补充
软件全称=无足鸟按时吃药打卡软件、版本号=V1.0.7、开发完成日期=2026-06-30、已发表（首次发表日期占位待填）；著作权人姓名、身份证号、通讯地址为占位符，须本人填写；说明书内截图占位框须替换为真实界面截图。

### 影响范围
- 新增 `软著材料_无足鸟按时吃药打卡软件V1.0.7/` 整个目录
- 未改动任何业务代码、前端、后端、配置
- 同步更新 `目录结构.json`（新增目录条目）

---

## 2026-08-09（docs）- 软件说明书补充截图（1.4 软件主界面）

### 变更类型
文档更新（软件说明书）

### 变更
按著作权人提交的截图，替换 `软著材料_无足鸟按时吃药打卡软件V1.0.7/软件说明书.html` 第 1.4 节占位框：
1. 新增 `screenshots/1.4.1-welcome.png`（欢迎页：应用介绍与特性）。
2. 新增 `screenshots/1.4.2-home.png`（首页：计划卡片 + 打卡入口）。
3. 1.4 节标题由「软件主界面（示意图）」改为「软件主界面」，两张图各加图注。

剩余待补 12 处截图占位：2.3 / 3.4 / 4.1 / 4.2 / 4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4 / 6.3 / 6.4（按著作权人后续提交的截图依次替换）。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/` 新增目录与 2 张 PNG
- `软件说明书.html`：1.4 节插入两张 `<img>` 并加图注
- 未改动任何业务代码
- 同步更新 `目录结构.json`（新增 screenshots 目录条目）

---

## 2026-08-09（docs）- 软件说明书补充架构图（2.3 技术栈 / 部署）

### 变更类型
文档更新（软件说明书，自动生成）

### 变更
依据项目实际实现自动生成 2.3 节「服务端部署架构图 / 技术栈示意」：
1. 读取 `backend/requirements.txt` 与 `README.md`、部署脚本，确认后端依赖（FastAPI / Uvicorn / SQLAlchemy / asyncmy / Pydantic / PyJWT / bcrypt / cryptography 等）、前端（uni-app Vue 3 / Pinia / SCSS）、数据库（MariaDB 12.3，业务库 wuzuniao_yao + 用户库 wuzuniao_yonghu）、部署（Docker Compose：yao-nginx / yao-backend / yao-mariadb，Rocky Linux）、通知渠道（站内信 / 微信订阅消息 / 邮件 SMTP）。
2. 生成矢量架构图 `screenshots/2.3.1-arch.svg`（分层：客户端 → 前端 → Nginx → 后端 → MariaDB，含通知渠道与部署说明），替换 2.3 占位框并加图注。

### 影响范围
- `软著材料_无足鸟按时吃药打卡软件V1.0.7/screenshots/2.3.1-arch.svg` 新增（矢量图，打印清晰）；
- `软件说明书.html`：2.3 节插入架构图 `<img>` 并加图注；
- 未改动任何业务代码；同步更新 `目录结构.json`（screenshots 新增 2.3.1-arch.svg）。
- 说明书截图占位剩余 11 处：3.4 / 4.1 / 4.2 / 4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4 / 6.3 / 6.4。

---

## 2026-08-09（docs）- 软件说明书补充架构图（6.3 系统架构/数据流）

### 变更类型
文档更新（软件说明书，自动生成）

### 变更
依据项目实际分层结构自动生成 6.3 节「系统架构图 / 数据流示意」（同 2.3 方法，非 AI 生图）：
1. 读取 `backend/app/` 实际结构（api/v1 路由、core 鉴权/限流/加密、services 业务逻辑、models、schemas、main.py 后台调度），确认分层：客户端 → Nginx → API 路由 → Services → Models/Schemas → MariaDB，横切关注点（JWT/限流/AES-256-GCM），后台 Scheduler 触发通知分发。
2. 生成矢量图 `screenshots/6.3.1-arch.svg`（系统架构 + 数据流：请求/响应主链路、横切安全、后台调度触发虚线），替换 6.3 占位框并加图注。

### 影响范围
- `screenshots/6.3.1-arch.svg` 新增；
- `软件说明书.html`：6.3 节插入架构图 `<img>` 并加图注；
- 未改动业务代码；同步 `目录结构.json`（screenshots 新增 6.3.1-arch.svg）。
- 说明书截图占位剩余 10 处：3.4 / 4.1 / 4.2 / 4.3 / 4.4 / 4.6 / 4.8 / 5.2 / 5.4 / 6.4。

---

## 2026-08-09（build）- 排除 App 原生图标误打入小程序包（消除微信 200k 图片告警）

### 变更类型
构建配置（微信小程序 mp-weixin 端）

### 背景
微信开发者工具代码质量扫描提示「图片和音频资源大小应不超过 200k」。排查发现 `frontend/assets/images` 全部图片均 ≤23KB，无超标；真正触发告警的是 uni 编译器在构建 `mp-weixin` 时，把 App 原生图标聚合资源 `dist/build/mp-weixin/static/app-icons/app-icons.png`（约 357KB）一并打进小程序主包。该资源仅 App 端使用，小程序端从不引用。

### 修复
`frontend/vite.config.js`：新增 Vite 插件 `removeAppIconsForMpWeixin`，`apply` 限定 `UNI_PLATFORM === 'mp-weixin'`，在 `closeBundle` 阶段删除产物 `static/app-icons` 目录。H5 / App 等平台不受影响。

### 影响范围
- `frontend/vite.config.js`：新增插件
- `frontend/dist/build/mp-weixin/`：重新构建后已无 `static/app-icons`，包内无 >200KB 资源
- 不涉及页面、后端、H5/App 逻辑

---

## 2026-08-09（feature）- 帮助中心新增「如何允许 APP 通知」FAQ 卡片

### 变更类型
前端功能（帮助中心页 i18n 文案）

### 变更
1. `frontend/pages/user/help.vue`：在 Card 6 之后新增 Card 7，复用既有卡片结构，标题与三段正文走 `help.q8Title` / `q8Text1` / `q8Text2` / `q8Text3`。
2. `frontend/locale/zh-CN.js`：新增 `help.q8*` 中文词条（仅 APP 端显示、授权开启「App推送」、目前仅前台送达且只测了安卓端）。
3. `frontend/locale/en.js`：同路径补齐美式英文翻译，与既有 `App push` 文案一致。

### 影响范围
- `frontend/pages/user/help.vue`：新增一张 FAQ 卡片
- `frontend/locale/zh-CN.js` + `en.js`：新增 help.q8* 词条（双语言包同步）
- 不涉及后端、不涉及多端条件编译逻辑
- 已重新 `npm run build:mp-weixin` 同步产物

---

## 2026-08-09（bugfix）- 修复通知方式页"添加新的通知方式"点击后空白

### 变更类型
bugfix（微信小程序新手引导遮罩，影响 notification.vue 添加通知方式交互）

### 根因
`BeginnerGuide.vue` 模板中，当引导目标元素位置尚未上报（`targetRect` 为 null，如点击"添加新方式"后入口卡消失、引导步骤从 `add-notification` 切换到 `wechat-auth-button` 的 150ms 上报间隙）时，渲染 `beginner-guide__mask--full` 全屏灰色蒙版并 `@click.stop` 拦截所有点击，盖住整页 → 用户感知为"点击后空白 / 卡死无法操作"。该全屏拦截蒙版此前作为"目标未就绪时的占位"一直存在，与组件注释所述的"目标就绪才显示带洞灰蒙版（mask-hole）"设计相悖；且 `mask-hole` 透明遮罩 view 此前从未被渲染，导致目标就绪时反而无洞外灰蒙版。

### 修复
1. `frontend/components/BeginnerGuide.vue`：将"目标未就绪时全屏拦截蒙版（`mask--full` + `@click.stop`）"替换为"目标就绪时的透明洞外灰蒙版（`mask--hole` + box-shadow 大扩散 + `pointer-events:none`），`v-if="targetRect"` 才渲染"。目标未就绪时不渲染任何遮罩，页面正常可见可操作，彻底消除空白/卡死；同时让引导真正遮灰（修复 mask-hole 从未渲染的既有缺陷）。
2. `frontend/pages/index/notification.vue`：`handleAdd` 清理无效的死代码分支（非微信端 `email-type-radio` 推进判断在进入分支前 target 必不为该值，永远不触发），保留微信端"进入 `wechat-auth-button` 授权引导步骤"的单一推进，逻辑清晰。

### 影响范围
- `frontend/components/BeginnerGuide.vue`：蒙版渲染条件与class
- `frontend/pages/index/notification.vue`：handleAdd 引导推进（死代码清理，行为不变）
- 依赖 `maskHoleStyle` computed（已存在）实现洞外灰蒙版
- 不涉及后端、不涉及 H5/App 逻辑（App 端本就无此拦截问题）
- 已重新 `npm run build:mp-weixin` 同步产物 `dist/build/mp-weixin`

---

## 2026-08-09（build）- 构建微信小程序生产包并补全发布合规配置

### 变更类型
构建 / 生产发布配置（微信小程序 mp-weixin 端）

### 背景
原有 `frontend/dist/build/mp-weixin` 为过期产物（缺失 2026-08-08 新增的 `delete-account` 免登录删除页），且微信小程序端缺少生产发布必需的隐私合规与索引配置。本次生成可直传微信开发者工具/微信后台发布的生产包。

### 修复与变更
1. **依赖安装**：`frontend` 缺失 `node_modules`。`vue@3.4.x` 与 `pinia@2.3.1` 存在 peer 冲突（pinia 要求 `vue ^3.5.11`），采用 `npm install --legacy-peer-deps` 按锁文件（vue 3.4.21 + pinia 2.3.1）安装，运行时完全兼容。
2. **生产构建**：执行 `npm run build:mp-weixin`，产物 `frontend/dist/build/mp-weixin` 已更新——含全部 16 个页面（5 主包 + 用户分包 11 页，含 `delete-account`），`project.config.json` 已含 `appid=wx3101dc47c535111e`、`bigPackageSizeSupport`、`minified`、`es6`。
3. **隐私合规**：`manifest.json` 的 `mp-weixin.setting` 新增 `"__usePrivacyCheck__": true`（微信隐私弹窗强制项）+ `es6/minified/postcss` 编译优化。
4. **索引配置**：新增 `frontend/sitemap.json`（allow page=*），uni-app 编译时拷贝至产物根，声明页面可被微信搜一搜索引。

### 影响范围
- `frontend/manifest.json`：mp-weixin 隐私与编译配置
- `frontend/sitemap.json`：新增（源文件）
- `frontend/dist/build/mp-weixin/`：重新生成的生产产物（完整、最新）
- `frontend/package.json`：无改动（仍 vue ^3.4.0 + pinia ^2.3.1）；安装用 `--legacy-peer-deps`
- 不涉及后端、不涉及 H5/App 逻辑

### 发布前置待办（需人工在微信后台完成，非代码层）
- 微信公众平台「设置-服务内容声明/用户隐私保护指引」中补充隐私政策（已含 delete-account 网页入口说明）
- 订阅消息模板 ID（`wx3101...` 在 config/env.js 已配置）需在微信后台确认订阅消息接口权限已开通

---

## 2026-08-08（bugfix）- i18n 翻译检查与修复（announcement.title 重复键 + en.js 翻译质量）

### 变更类型
bugfix（国际化翻译键结构缺陷 + 美式英语翻译质量修复）

### 根因
深入检查简体中文→美式英语国际化翻译时发现两类问题：
1. **announcement.title 重复键（严重 bug）**：locale/zh-CN.js 与 locale/en.js 的 `announcement` 对象内 `title` 键定义两次——第1次为页面标题（"公告管理"/"Announcements"），第2次为表单字段标签（"公告标题"/"Announcement title"）。JS 对象字面量后定义覆盖先定义，导致 announcement.vue 的 PageHeader 页面标题错误显示为"公告标题"而非"公告管理"，英文同理。
2. **en.js 翻译质量缺陷**（10 处）：拼写错误（"Digtal"应为"Digital"）、重复词（"Acceptance and Acceptance"）、帮助文案按钮引用与实际按钮标签不一致（"Not time yet" vs "Not yet time"）、指纹验证提示时态错误（祈使句 vs 完成时）、分享默认标题产品名与官方全称不一致、大小写不统一（"Sign In" vs "Sign in"、"Create Account" vs "Create an account"）、"无备注"翻译不统一（"No remark" vs "No notes"）、"App push"大小写不一致。

### 修复
1. **announcement.title 重复键**：将表单字段标签键从 `title` 重命名为 `fieldTitle`（zh-CN.js + en.js 同步），announcement.vue 中3处表单标签调用改为 `$t('announcement.fieldTitle')`，PageHeader 页面标题保留 `$t('announcement.title')`。
2. **en.js 翻译质量**：修正 "Digtal"→"Digital"、"Acceptance and Acceptance"→"Acknowledgment and Acceptance"、help.q6Text2/q6Text5 中 "Not time yet"→"Not yet time"、push.biometricAuthContent "Sign in with your fingerprint"→"Signed in with fingerprint"、share.default 产品名→"Wuzuniao On-time Medication Check-in"、home.noRemark "No remark"→"No notes"、share.login "Sign In"→"Sign in"、share.register "Create Account"→"Create an account"、home.introNotifyEm "app push"→"App push"。

### 影响范围
- `frontend/locale/zh-CN.js`：announcement.fieldTitle 键名调整（1处）
- `frontend/locale/en.js`：announcement.fieldTitle 键名调整 + 10处翻译质量修复
- `frontend/pages/user/announcement.vue`：3处表单标签 i18n 调用键名调整
- 不涉及后端、不涉及多端条件编译逻辑（MP-WEIXIN/H5/APP-PLUS 块均无影响）
- 验证：record.selectedDate 占位符差异（中文{month} vs 英文{monthName}）经确认非 bug——record.vue 调用处同时传入四个变量，中英文占位符均正确替换

---

## 2026-08-08（紧急修复）- 记录页白屏（locale resolve 缺陷）

### 变更类型
bugfix（i18n 基础设施缺陷导致整页渲染崩溃）

### 根因
`frontend/locale/index.js` 的 `resolve(dict, path)` 函数末尾 `return typeof node === 'string' ? node : undefined`——**只返回 string 类型**，对数组/对象词条一律返回 `undefined`（循环内每轮还会因 `typeof node !== 'object'` 提前返回 undefined）。此前新增的 `tm(key)` 复用了 `resolve`，导致 `tm('record.monthLabels')` / `tm('record.weekdays')` 永远返回 `undefined`；record.vue 模板中 `monthLabels[monthIndex]` 对 undefined 取值抛出 `Cannot read properties of undefined`，使 `<script setup>` 顶层崩溃 → 记录页完全白屏、无任何内容。

### 修复
`tm()` 改用独立取值函数 `getNode(dict, path)`（按点号路径深层取值并返回**原始值**，数组/对象/字符串均可，缺失才返回 undefined），不再复用 `resolve` 的 string 过滤逻辑。`t()` 维持原行为（仅返回 string、数组词条回退为 key 字符串）不变。验证：`tm('record.monthLabels')[7]` 正确返回「8月」，`t('record.year',{year})` 行为不变。

### 影响范围
- 仅改 `frontend/locale/index.js`（新增 `getNode`、`tm` 内部改调）；record.vue 无需再改。无后端改动。
- 验证：`node` 模拟 resolve/getNode 逻辑确认 tm 返回数组；index.js / record.vue lint 0 错误。

---

## 2026-08-08（bugfix）- 首页词云不显示（t() 取数组回退成 key 字符串）

### 变更类型
bugfix（i18n 重构遗留，与记录页白屏同源缺陷）

### 根因
i18n 重构把词云关键词改为语言包数组词条 `home.cloudWords`（zh-CN `['免费','易用','安全','开源']` / en `['Free','Easy','Secure','Open']`）。但 `frontend/pages/index/index.vue` 第 221 行仍用 `t('home.cloudWords')` 取词。`t()` 内部 `resolve()` 末尾 `return typeof node==='string'?node:undefined`，**只返回字符串**，对数组词条返回 `undefined`，再回退 `zh-CN` 仍为数组 → 最终回退返 key 字符串 `'home.cloudWords'`。
- 结果：`CLOUD_WORD_TEXTS` 变成 14 字符的字符串；`.length=14`、`.map` 把每个字符当成一个"词"（h/o/m… 单字符乱码）；`colors=shuffle(6).slice(0,14)`/`sizes=shuffle(5).slice(0,14)` 取不到 14 个 → 颜色/字号类为 `--cundefined`/`--sundefined` 失效 → 词云不可见或渲染乱码。

### 修复
- `index.vue` 导入由 `import { t } from '../../locale'` 改为 `import { t, tm } from '../../locale'`。
- 取值改为 `const CLOUD_WORD_TEXTS = tm('home.cloudWords') || []`（tm 用独立 getNode 返回原始数组，中英文均正确取 4 词）。

### 影响范围
- 仅改 `frontend/pages/index/index.vue`；locale 核心 tm/t 不变。lint 0 错误。

---

## 2026-08-08（i18n 修复）- 新手引导卡片切换语言不刷新

### 变更类型
i18n（引导卡片文案响应式缺陷）

### 问题
深入排查新手引导翻译完整性，结论：所有引导相关 UI 文案（guide.steps.* 13 个步骤、BeginnerGuide 的 badge/skip、首页 home.guide* 系列、BottomNav/settings/plan/notification/login/register 等被高亮的目标按钮）在 zh-CN 与 en 两个语言包中词条**完全对称、无缺失、en 无中文残留**（已用全量词条对比 + 模板裸文本扫描 + guide 子树精确对比三种方式验证）。
- 真正缺陷：引导步骤卡片的 title/description 由 `frontend/store/modules/guide.js` 的 `currentStepData` 通过 `t(\`guide.steps.${key}.title\`)` 注入。而 `t()` 读取模块级 `currentLocale`（非 Vue 响应式），`currentStepData` 未建立对 `languageStore.current` 的依赖 → 在引导进行中切换语言（如中文切英文）时，卡片文案不会重算、仍显示旧语言。

### 修复
- `guide.js` 引入 `useLanguageStore`，在 `currentStepData` computed 内加 `void languageStore.current` 建立响应式依赖，使切语言时引导卡片即时重算。

### 影响范围
- 仅改 `frontend/store/modules/guide.js`；无其他文件改动。lint 0 错误。
- 备注：通知方式列表中由后端返回的 `channel_type`（"微信"/"邮件"/"App推送"）属后端数据契约值，不在 UI 文案翻译范围，未改动（改动需前后端协同，破约定风险高）。

---

## 2026-08-08（i18n 增强）- 默认语言按设备地区推断

### 变更类型
feat（i18n 默认语言智能选择）

### 需求
个人信息页语言行默认显示的语言类型按地区判断：中国相关地区默认「简体中文」，中国以外地区默认「English」。

### 实现
- `frontend/store/modules/language.js` 新增 `detectDefaultLocale()`：读取 `uni.getLocale()`（失败回退 `uni.getSystemInfoSync().language`），小写下划线转中划线；`sys.startsWith('zh')` 命中全部中文变体（zh/zh-CN/zh-Hans/zh-HK/zh-TW/zh-MO/zh-Hant 等）返回 `'zh-CN'`，否则返回 `'en'`。
- 初始化逻辑改为：持久化值 `app-language` 若合法则优先采用；无持久化偏好时才用 `detectDefaultLocale()` 的推断结果，不影响用户已保存的显式选择。

### 影响范围
- 仅改 `frontend/store/modules/language.js`；`index.js` 的 `setLocale`/`getLocale` 与 `profile.vue` 语言开关无需改动。无后端改动。lint 0 错误。

---

## 2026-08-08（i18n 修复）- 语言开关显示与记录页英文年月

### 变更类型
i18n（修复两个切换语言的显示 bug）

### 问题1：个人信息页语言开关显示「zh」而非「简体中文」
- 根因：`profile.vue` 用 `$t('language.' + languageStore.current)`，`languageStore.current` 值为 `'zh-CN'` / `'en'`，但语言包 `language` 分组键名是 `zhCN` / `en`（不是 `zh-CN`），导致 `$t('language.zh-CN')` 解析失败、回退返回 key 字符串（截断显示「zh」）。
- 修复：`profile.vue` 改为 `$t('language.' + (languageStore.current === 'zh-CN' ? 'zhCN' : 'en'))`，与语言包键名对齐；切换语言时开关内文字正确显示「简体中文」/「English」。

### 问题2：英文界面记录页日历标题「年/月」消失（二次修复）
- **初次修复误区**：仅把 en.js 的 `year/month` 补成数组 `monthLabels`，但 `record.vue` 仍用 `t('record.monthLabels')` 取数组。用户实测显示「2026年m」/「m 2026」，月份成单字母。
- **真正根因（关键）**：`locale/index.js` 的 `t()` 函数**只返回 string 类型**，对数组词条（如 `record.monthLabels`、`record.weekdays`）会回退成 key 字符串 `'record.monthLabels'`。于是 `monthLabels[monthIndex]` 取的是该字符串的第 N 个字符 → 「m」。
- **修复**：`locale/index.js` 新增 `tm(key, locale)` 返回语言包**原始值（支持数组/对象）**，缺失回退 zh-CN；`record.vue` 的 `month / weekdays` 改用 `tm` 并包成 `computed`（内部 `void languageStore.current` 建立响应式依赖，切换语言即时更新），删除误留的旧 `const monthLabels = t(...)` 重复声明；`selectedDateText` 也补 `void languageStore.current` 依赖。`t()` 本身不变，仍用于字符串插值。
- 验证：词条对比 zh-CN 924 = en 924（en 零缺失）；record.vue / index.js / profile.vue 均 lint 0 错误；日历标题中文「2026年 8月」、英文「2026 Aug」、选中日期英文「Aug 5, 2026」均正确。

---

## 2026-08-08（i18n）- 补全剩余页面美式英文翻译（首页/通知角标/公告表单/语言开关/网络层）

### 变更类型
i18n（修复全站翻译遗漏，确保英文界面不再回退中文）

### 内容简述
- **首页 `index.vue`**（此前完全未国际化，是最大遗漏）：欢迎卡片 slogan/安全说明/开源地址、介绍卡片标题/主要功能/多途径通知/H5 专属段、新手引导卡全部操作指引、任务卡片「无备注」「进行中」、任务列表「选择任务」、立即打卡按钮四态文案（无打卡任务/未到打卡时间/已打卡/立即打卡）、词云「免费/易用/安全/开源」全部改为 `$t('home.*')`；`useShare({title: t('share.home')})`；词云数组 `CLOUD_WORD_TEXTS` 与 `checkinText` 改从 `t()` 取。
- `frontend/locale/zh-CN.js` / `en.js`：新增 `home` 分组（51 键，路径对齐）；`notification` 分组补 `wechatBadge`/`appPushBadge`（微→W、推→P）；`language` 分组补 `zhCN`/`en`（语言母语名）；`request` 分组补 `errorSeparator`（；→; ）。
- `notification.vue`：微信「微」、App 推送「推」单字角标改用 `$t('notification.wechatBadge'/'appPushBadge')`。
- `announcement.vue`：表单标题「发布新公告」由写死中文改为 `$t('announcement.newFormHeading')`（locale 已有该键却未使用，属 bug 修复）。
- `profile.vue`：语言开关文字由 `languageIsEnglish ? 'English' : '简体中文'` 改为 `$t('language.' + languageStore.current)`，切换语言时开关内文字同步切换为对应母语名。
- `request.js`：Pydantic 校验错误拼接分隔符 `；` 改为 `t('request.errorSeparator')`。

### 影响范围
- 前端 5 文件 + 2 语言包；无后端改动。脚本对比确认 zh-CN 与 en 词条数均为 912、en.js 零缺失（缺失会回退中文）；模板硬编码中文扫描（排除注释）目标页面已清零；全部改动文件 lint 0 错误。
- 至此全站 14 个页面 + 通用组件 + 网络层均已纳入 i18n，英文界面无残留中文（语言母语名按惯例保留「简体中文/English」不翻译）。

---

## 2026-08-08（i18n）- 隐私政策 privacy.vue 全量中英双语国际化

### 变更类型
i18n（长文法务文档 privacy.vue 接入自定义轻量 i18n，与 agreement.vue 同模式）

### 内容简述
- `frontend/locale/zh-CN.js`：在既有 `privacyDoc` 对象内补全第三~十二章全部中文词条（章节标题/小节标题/表格表头/表格行/列表项/嵌套 em 加粗片段），命名沿用 `c3*/c4*/…/c12*` + `Em/b/Em2/b2` em-split 后缀，与 agreementDoc 一致。
- `frontend/locale/en.js`：路径对齐补齐 `privacyDoc` 全 12 章美式英文翻译（含三张表格多行单元格，行内 `\n` 渲染）。
- `frontend/pages/user/privacy.vue`：`<template>` 全量重写为 `$t('privacyDoc.*')`（标题/前言/12 章/页脚），保留 `privacy-page__em` 嵌套加粗、scroll-view 表格与 `--data` 类；`<script setup>` 引入 `t` 并将 `useShare({title:'隐私政策'})` 改为 `useShare({title: t('share.privacy')})`，分享标题随语言切换。
- 维护要求：新增词条须在 zh-CN.js 与 en.js 双端补齐，否则英文界面回退中文（已遵守）。

### 影响范围
- 前端 1 页 + 2 语言包；无后端改动、无业务逻辑改动；lint 0 错误。
- 至此 agreement.vue 与 privacy.vue 两篇长文法务文档均完成全量中英双语 i18n。

---

## 2026-08-08（功能+文档）- 新增免登录账号删除说明页（满足 Google Play 网页删除入口）

### 变更类型
feat + docs（新增公开静态页，关联隐私政策/服务协议/个人信息页，完善跨境合规网页删除入口）

### 背景
Google Play 账号删除政策要求提供「应用内 + 网页端」双删除途径，且网页入口须**可直接访问**。此前网页入口为需登录的 profile 页，审核员未登录访问会被跳登录页，存在被判「链接无效」风险。新增一个**免登录**的公开说明页，作为可直接访问的网页删除入口说明。

### 内容简述
- **新增 `frontend/pages/user/delete-account.vue`**：免登录公开静态页，复用 contact.vue 卡片风格与语义令牌（`--color-brand`/`--color-card-bg` 等），含五段说明（两种删除途径、24h 冷静期、删除范围、依法须保留部分、其他个人信息权利）及跳转登录页/隐私政策/服务协议的入口；`useShare({title:'账号删除与数据说明'})`。
- `frontend/pages.json`：分包 `pages/user` 注册 `delete-account` 页（navigationBarTitleText 含「免登录删除入口」）。
- `frontend/public/sitemap.xml`：补充 `yao.wuzuniao.com/pages/user/delete-account` URL（priority 0.4）。
- `frontend/pages/user/profile.vue`：分组 2 删除账号项下方新增「了解账号删除与数据说明」弱提示入口（`--color-brand` 链接色），跳转说明页；新增 `goDeleteAccountHelp()`。
- `frontend/pages/user/privacy.vue`：第七条第 5 项网页端入口补充免登录说明页链接。
- `frontend/pages/user/agreement.vue`：第九章用户主动终止处补充免登录说明页链接。
- `目录结构.json`：新增 `delete-account.vue` 条目，profile.vue 描述补充入口。

### 影响范围
- 新增 1 个纯展示页 + 路由/SEO 注册 + 3 处交叉引用；无后端改动、无业务逻辑改动；lint 0 错误，`pages.json`/`目录结构.json` 校验通过。
- 真正的删除操作仍须登录（profile 页 `scheduleDeletion` API），说明页仅作免登录可见的说明与跳转，符合 Google Play「可直接访问的网页删除入口」要求。

---

## 2026-08-08（配置+文档）- 关闭 Firebase/FCM 自动初始化（消除未使用的境外传输）

### 变更类型
config + docs（FCM 实际未启用，关闭其自动初始化以与隐私政策一致、消除 Google Play 上架隐患）

### 核实结论：FCM 在本项目无实际使用
- 推送完全依赖友盟+ U-Push 自有通道；`umeng-push-config.json` 中 `vendors.fcm.enabled=false`，且 `googleServicesJsonPath`/`projectId`/`applicationId`/`apiKey`/`senderId` 全部为空。
- 业务代码中无任何主动调用 FCM 的逻辑；`config.json` 里的 `firebase-messaging`/`fcm-umengaccs` 是 SDK 打包自带依赖。
- 但原 `AndroidManifest.xml` 中 `firebase_messaging_auto_init_enabled=true` 与 `firebase_analytics_collection_enabled=true` 会使 Firebase 在 App 启动时**自动初始化并向 Google 境外服务器注册设备**，构成未经声明的跨境传输，与隐私政策「不会主动向境外提供」及 GDPR 申报冲突。

### 内容简述
- `frontend/uni_modules/xtf-umengpush/utssdk/app-android/AndroidManifest.xml`：上述两处 `true` → `false`，关闭 Firebase 自动初始化与统计采集。
- `frontend/pages/user/privacy.vue`：第六条存储地点由「FCM 为向境外传输的例外」改为「FCM 未启用且已关闭自动初始化，不向境外传输」；第五条第三方表 FCM 行用途同步修正。
- `目录结构.json`：privacy.vue 描述同步 FCM「已关闭自动初始化、不跨境传输」。

### 影响范围
- 仅 Android 端推送 SDK 元数据与两个纯展示页文案，无业务逻辑改动；lint 0 错误，`目录结构.json` 校验通过。
- 注：`config.json` 中的 `firebase-messaging` 依赖为 SDK 自带，删除可能导致编译失败，予以保留（其运行行为已由 meta-data 关闭）。

---

## 2026-08-08（文档）- 隐私政策补充境外发行合规说明（Google Play 上架前置）

### 变更类型
docs（面向 Google Play 境外分发 + 数据存储于中国境内的跨境传输披露）

### 背景
应用后续将发布至 Google 商店，但服务器与数据库位于中国境内，构成跨境传输场景，需向境外用户明示。

### 核实的外部要求
- **Google Play 账号删除政策**（2024-05-31 起强制）：创建账号的应用必须同时提供「应用内」与「网页端」两条账号及数据删除途径，网页入口须可直接访问。
- **GDPR 第 13/14 条**：向境外传输须告知传输事实、法律依据；中国未获欧盟充分性认定，须明示该事实。
- **Google Play 数据安全表单**：披露内容须与隐私政策及代码实现一致，不得声明未实现的能力。

### 代码侧核实结论（本次仅核实，未改代码）
- 注销全链路无 `#ifdef` 平台限制，H5 端可完整完成注册/登录/注销；H5 已公开部署 `https://yao.wuzuniao.com`（history 模式，`/pages/user/profile` 深链可直达），故**网页端删除入口已天然具备**。
- `schedule_deletion`（`user_service.py:499`）置 status=0 + 失效 token，并**当即物理删除** `user_biometric_tokens`（`revoke_biometric_tokens` 为 `delete()` 非软失效）；`purge_expired_deletions` 于 24 小时后物理删除其余 8 张表。删除范围已逐表与政策文案核对一致。
- **无数据导出接口**、**无匿名化实现**：原政策「可获取副本」「删除或匿名化处理」两处表述超出实现，本次已改为与实现一致的表述。
- IP 仅用于限流且**仅内存保留 60 秒**（`core/rate_limit.py`），不落库；Nginx access log 会记录 IP。

### 内容简述
- `frontend/pages/user/privacy.vue`：
  1. **新增第九章「中国境外用户特别说明」**（3 小节）：跨境传输事实与法律依据（合同必需 + 明确同意）、明示中国未获欧盟充分性认定、境外用户可行使的权利（含 EEA/UK 数据可携权与向当地监管机构投诉）、不接受跨境传输时的替代方案（自建部署）。原第九～十一章顺延为第十～十二章，全部交叉引用已同步校正。
  2. 前言增加境外用户指引，指向第九条。
  3. 第六条存储地点重写：明确服务器与数据库均在中国境内、境外用户数据将传输至境内处理、继续使用即视为知悉同意。
  4. 第七条第 5 项改为「注销账号与删除数据」，**明列应用内与网页端（yao.wuzuniao.com/pages/user/profile）双入口**，满足 Google Play 要求；删除范围按代码逐表细化，并声明不保留备份副本与匿名化残留。
  5. 校准两处与实现不符的表述：「可获取副本」改为「暂未提供一键导出，可邮件申请」；「删除或匿名化处理」改为纯删除；保留期限新增访问日志与限流 IP（60 秒内存）说明。
  6. 第八章未成年人保护补境外最低同意年龄（EEA 13-16 周岁、美国 13 周岁）。
  7. 第十二条投诉渠道补「境外用户可向所在地数据保护监管机构投诉」。
- `frontend/pages/user/agreement.vue`：
  1. 第九章补用户主动终止途径（应用内 + 网页端双入口、24 小时冷静期）。
  2. 第十二章管辖条款补「不含冲突法规则」，并声明不影响境外用户所在地强制性消费者权利，指向隐私政策第九条。

### 影响范围
- 仅两个纯展示页文案与章节结构，无脚本逻辑、无样式、无接口、无代码改动；复用既有类名，未散写色值；lint 0 错误。
- 章节编号已从一～十二连续校验，全部「第 X 条」交叉引用与实际章号一致。

### 上架前仍需处理（未在本次改动范围）
1. `AndroidManifest.xml` 中 `firebase_messaging_auto_init_enabled` / `firebase_analytics_collection_enabled` 当前为 `true`，若不使用 FCM 建议改 `false`，可同步简化跨境条款。
2. 建议新增**免登录**的公开删除说明页（如 `/pages/user/delete-account`）：Google Play 审核员在未登录状态下访问 `/pages/user/profile` 会被跳转登录页，存在被判「链接无效」的风险。
3. Nginx access log 未配置 `max-size`/`max-file` 轮转，IP 事实上无限期留存，建议配置保留上限。
4. 数据安全表单填报须与本政策逐项对应，勿勾选「可请求数据副本」的自动化能力（当前为人工邮件响应）。

---

## 2026-08-08（文档）- 隐私政策与服务协议按实际支持端修订（补鸿蒙、校准第三方 SDK 披露）

### 变更类型
docs（法务文档与代码实际实现对齐；无业务逻辑改动）

### 核实结论：本项目实际支持 5 个发行版本
微信小程序、H5（Web）、Android App、iOS App、HarmonyOS App。
**不支持 QQ 小程序**——全项目 `MP-QQ`/`mp-qq` 零命中，`manifest.json` 无 `mp-qq` 节与 appid，`package.json` 无 QQ 构建脚本，未安装 `@dcloudio/uni-mp-qq`，无 `qq.login` 分支，推送插件 `package.json` 明确声明 `"qq": "-"`。故未在法务文档中写入 QQ 小程序，避免构成不实陈述。文档中既有的「QQ 交流群」属联系方式，表述正确未动。
（`manifest.json` 中 `mp-alipay`/`mp-baidu`/`mp-toutiao`/`quickapp` 为脚手架残留空壳，无 appid、无脚本、无条件编译，不可发行，同样未写入。）

### 内容简述
- `frontend/pages/user/agreement.vue`：
  1. 前言端清单由「微信小程序、Android/iOS App 及网页（H5）」改为「微信小程序、Android App、iOS App、HarmonyOS App 及网页（H5）」。
  2. 第三章「服务内容与形式」补充平台能力差异说明（微信登录/订阅消息仅小程序端；指纹登录与 App 推送仅 App 三端）。
  3. 第十章「第三方服务」由笼统表述改为列明 DCloud uni-app、友盟+ U-Push、微信开放平台、邮件服务商，并指引至《隐私政策》第五条。
- `frontend/pages/user/privacy.vue`：
  1. 前言与第一条适用范围补 HarmonyOS 移动应用（包名 com.wuzuniao.yao）。
  2. 指纹登录相关条款（第二条第 2、3 项）改为「生物识别」通用表述，覆盖 Android/HarmonyOS 指纹与 iOS Touch ID/Face ID。
  3. App 推送条款设备平台类型由「android/ios」补为「android／ios／harmony」，与后端 `Literal["android","ios","harmony"]` 一致。
  4. 第三条 Android 权限表补齐推送 SDK 打包时自动合并的 3 项权限（WAKE_LOCK、C2DM_RECEIVE、魅族 PUSH）并加说明。
  5. 第三条新增「3. HarmonyOS（鸿蒙）端申请的权限」小节（ACCESS_BIOMETRIC、INTERNET、GET_NETWORK_INFO、APP_TRACKING_CONSENT/OAID、通知权限），原「不会调用的权限」顺延为第 4 项。
  6. 第五条第三方表：友盟行补注 HarmonyOS；厂商推送通道行由「未启用」改为如实披露「代码随 SDK 集成于安装包内但未配置厂商密钥故不运行」；新增 Google Firebase 云消息（FCM）行（Android 端随 SDK 集成，`firebase_messaging_auto_init_enabled` 为 true）。
  7. 第六条存储地点条款相应调整：由绝对化的「不会向境外提供」改为「不会主动向境外提供」并说明 FCM 例外情形，避免与新增的 FCM 披露自相矛盾。

### 影响范围
- 仅 `agreement.vue` / `privacy.vue` 两个纯展示页的文案与章节结构，无脚本逻辑、无样式、无接口改动，复用既有类名（`privacy-page__*`），未散写色值。已通过 lint 检查（0 错误）。
- 未改动 `manifest.json`、构建脚本与任何端的实际能力。

---

## 2026-08-08（功能）- 个人信息页「语言」开关显示文字并独立成区

### 变更类型
feat（语言开关内显示当前语言文字 + 语言/指纹/主题独立成「偏好设置」分组）

### 内容简述
- `frontend/pages/user/profile.vue`：
  1. 语言开关由复用指纹开关类（`profile-page__biometric-switch`）改为独立类 `profile-page__lang-switch`，开关内居中显示当前语言名——默认「简体中文」、点击后「English」（由 `languageIsEnglish` 驱动），旋钮滑动指示状态，文字随状态切换颜色，全部引用语义令牌（`--color-border-input` / `--color-wechat` / `--color-text-inverse` / `--color-text-secondary`），未散写色值。
  2. 将「语言」「指纹登录」「主题」三行从分组1（资料修改）移出，独立成「偏好设置」分组（`profile-page__group`），位于资料修改与退出/删除之间；邮箱项因此成为分组1末项，去除其 `--bordered` 下边框避免多余分隔线。
  3. 平板断点（≥768px）锁定语言开关尺寸为 px，避免 rpx 过度放大。
- 语言偏好 store（`frontend/store/modules/language.js`）与切换逻辑（`toggleLanguage` / `languageIsEnglish`）不变，多端通用性不受影响（无 `#ifdef` 包裹，全端渲染）。

### 影响范围
- 仅前端 profile.vue 的样式与分组结构；未改动后端、数据库、指纹/主题业务与翻译文案。指纹开关样式（`profile-page__biometric-switch`）保持原样未被影响。

---

## 2026-08-07（修复）- 退出登录保留指纹凭证，仅注销账号时清理

### 变更类型
fix（退出登录不再作废生物识别凭证，便于下次指纹一键登录；真正清理仅在注销账号时统一执行）

### 内容简述
- `backend/app/services/user_service.py`：`logout()` 移除 `revoke_biometric_tokens(user_id)` 调用，退出登录仅设置 `token_invalid_before` 使当前 JWT 失效，不再作废生物识别凭证，用户下次可直接用指纹登录。`revoke_biometric_tokens` 方法注释同步更新，作废场景由「改密码/退出登录/删除账号」改为「改密码/删除账号」（退出登录时保留凭证）。改密码、注销账号仍作废全部凭证不变。
- `frontend/composables/useBiometric.js`：更新 `clearBiometricToken` 注释，明确退出登录时也保留本地凭证，真正清理仅在注销账号时统一处理。
- `frontend/pages/user/profile.vue`：更新 `toggleBiometric` 关闭分支注释，凭证清理由「退出登录/注销账号」改为「仅注销账号」。（注：前端 `handleLogout` 本就只调 `userStore.clearUser()` 清除 JWT 与用户信息，不清理 `biometric_token`，无需改动）
- `frontend/pages/user/privacy.vue`：更新指纹登录条款文案，明确「退出登录时保留指纹凭证便于下次指纹一键登录，仅在注销账号时作废全部设备凭证」。

### 影响范围
- 后端 `user_service.py` 的 `logout()` 方法与 `revoke_biometric_tokens` 注释；前端注释与隐私政策文案、目录结构描述。改密码与注销账号的凭证作废逻辑不变；`revokeBiometric` 单设备撤销接口仍保留，供需要单设备撤销的场景调用。

---

## 2026-08-07（修复）- 指纹登录开关不再清理本地凭证

### 变更类型
fix（修复用户主动关闭指纹登录导致本地凭证被清除、再次打开需重新账号密码登录的问题）

### 内容简述
- `frontend/pages/user/profile.vue`：`toggleBiometric` 关闭分支原逻辑为「清除本地凭证 + 通知服务端作废本设备凭证」，会导致用户再次打开指纹登录时因本地无 `biometric_token` 而提示「请先用账号密码登录以开启指纹」。改为仅切换本地开关标记（`setEnabled(false)`），不调用 `clearBiometricToken()`、不调用 `revokeBiometric()`，保留本地凭证与服务端凭证记录，便于用户再次打开时直接生效。同步移除不再使用的 `revokeBiometric` 导入，函数由 `async` 改为同步（移除了唯一的 `await`）。
- `frontend/pages/user/privacy.vue`：同步更新指纹登录关闭行为的条款描述，明确「关闭仅切换开关、不清除本地凭证、不作废服务端凭证，退出登录/注销账号时统一作废全部设备凭证」。
- `frontend/composables/useBiometric.js`：更新 `clearBiometricToken` 方法注释，说明 profile 页主动关闭指纹登录不再调用本方法，避免注释与实际行为脱节。

### 影响范围
- 仅前端 App 端指纹登录开关逻辑与隐私政策文案，不涉及后端、数据库与其他页面；`revokeBiometric` 后端接口仍保留，供退出登录/注销账号等场景调用。

---

## 2026-08-07（修复）- 首页立即打卡3秒防抖改为按任务区分

### 变更类型
fix（修复打卡防抖误拦截跨任务打卡的问题）

### 内容简述
- `frontend/pages/index/index.vue`：打卡防抖原使用全局单一时间戳 `lastCheckinTime`，用户对任务 A 打卡后 3 秒内切换到任务 B 也会被拦截。改为按计划 ID 记录的映射表 `lastCheckinTimeMap`，3 秒防抖仅针对同一任务，切换到其他任务可立即打卡。
  - `lastCheckinTime`(number) → `lastCheckinTimeMap`(object)，以 `planId` 为键记录各任务最近打卡时间戳。
  - `handleCheckin` 内防抖判定改为读取 `lastCheckinTimeMap[planId]`，未记录或超过 3 秒即放行。

### 影响范围
- 仅前端首页 `index.vue` 打卡防抖逻辑，不涉及后端、数据库与其他页面；头部注释本就描述为"同一任务3秒内只允许点击一次"，本次修复使实现与注释一致。

---

## 2026-08-06（功能）- 个人信息页新增「语言」切换行

### 变更类型
feat（个人信息页新增语言偏好开关，本次仅做按钮功能）

### 内容简述
- `frontend/store/modules/language.js`：新增 Pinia store `useLanguageStore`，持久化语言偏好（`app-language` 本地存储键，默认 `zh-CN`，可选 `en`），本期仅记录用户选择、暂不接入翻译。
- `frontend/pages/user/profile.vue`：新增一行「语言」，默认简体中文、点击切到 English；新增 `languageIsEnglish` 计算属性与 `toggleLanguage()` 切换函数（后续 2026-08-08 调整为独立 `profile-page__lang-switch` 开关类并在开关内显示文字、与指纹/主题独立成「偏好设置」分组）。

### 影响范围
- 仅前端 profile.vue 与新增 language store，不涉及后端、数据库与翻译文案；样式复用既有开关 CSS 类，未新增色值与配色令牌。

---

## 2026-08-06（检查）- 鸿蒙端功能完整性最终检查 + 其他端回归验证

### 变更类型
chore（全链路完整性检查，修正 1 处注释遗漏）

### 检查范围与结论

#### 一、鸿蒙端功能完整性（全部已实现）
1. **条件编译宏修正**：全局搜索确认前端无遗漏的 `#ifdef APP-PLUS`（残留的 `app-plus`/`APP-PLUS` 均为合法引用：npm 包名 `@dcloudio/uni-app-plus`、manifest.json 配置段名 `"app-plus"`、`process.env.UNI_PLATFORM === 'app-plus'` 字符串字面量、说明性注释）。
2. **推送链路（useAppPush.js）**：`requestPermission()` / `getDeviceToken()` 的鸿蒙差异用 `#ifdef APP-HARMONY` 正确隔离；`openNotificationSettings()` 有 try-catch 兜底；三重取 token 通道（缓存 / register-state 事件 / 轮询）完整。
3. **指纹登录（useBiometric.js）**：整体用 `#ifdef APP` 包裹，Soter 三接口业务逻辑三端通用；鸿蒙权限 `ohos.permission.ACCESS_BIOMETRIC` 已在 `harmony-configs/entry/src/main/module.json5` 声明，`string.json` 提供权限理由文案。
4. **工程配置**：`manifest.json` 新增 `app-harmony` 段（bundleName）；`MyAbilityStage.ets` 已创建（srcEntry 注释未启用，待真机验证，合理）；`umconfig.json` 已填入 appKey/channel，appMessageSecret 为占位符（待维护者填真值）。
5. **后端 harmony 支持**：`config.py` 新增鸿蒙密钥配置项；`umeng_service.py` `_get_credentials()` 支持 harmony 平台、`_build_payload()` 鸿蒙与 Android 共用分支；`notification_channel.py` `platform` Literal 包含 harmony；`scheduler_service.py` `_send_app_push` 透传 platform；`.env.template` 鸿蒙密钥配置项完整。

#### 二、其他端回归验证（未被破坏）
1. **小程序构建**：`npm run build:mp-weixin` 构建通过（exit code 0）。
2. **产物残留检查**：小程序产物中零残留 `umengpush` / `startSoterAuthentication` / `plus.android` / `APP-HARMONY`；`useBiometric.js` 被完全剔除（整体 `#ifdef APP`）；`useAppPush.js` 产物仅剩 `require("vendor.js")`（tree-shaking 移除未引用代码）；`index.vue` 产物中无 `useAppPush` / `reportDeviceToken`（uni-app 编译器智能剔除仅在 `#ifdef APP` 内使用的变量）；`env.js` 产物中 `IS_APP` 求值为 false，`API_BASE_URL` 正确走生产域名。
3. **H5 端**：条件编译正确隔离，H5 端默认账号密码登录，无微信/指纹入口（`#ifdef MP-WEIXIN` / `#ifdef APP` 块被剔除）。
4. **Android/iOS 端**：`#ifdef APP` 包含 Android/iOS，行为与鸿蒙适配前一致（原 `APP-PLUS` 逻辑全部命中 `APP`）。

#### 三、发现并修复的问题
- **`backend/app/schemas/notification_channel.py` 第 12 行注释**：鸿蒙适配时将条件编译宏 `APP-PLUS` 改为 `APP`，但遗漏了此行注释。已修正为 `#ifdef APP，含 Android/iOS/鸿蒙`，与实际代码一致。

#### 四、待验证项（非 bug，有意设计）
1. `MyAbilityStage.ets` 的 `srcEntry` 在 `module.json5` 中注释未启用——注释说明自定义 AbilityStage 需继承 uni 运行时基类，须在 DevEco Studio 真机编译验证通过后再启用。
2. `umconfig.json` 的 `appMessageSecret` 为占位符 `REPLACE_WITH_UMENG_MESSAGE_SECRET`——须由维护者替换为友盟后台鸿蒙应用的 Umeng Message Secret。
3. `umconfig.json` 的 `debug=true`——上线前须改为 `false`。
4. 友盟鸿蒙 payload 结构按「与 Android 同源」实现——待真机推送验证，如有差异仅需微调 `_build_payload` 鸿蒙分支。

### 影响范围
- 仅修正 1 处注释，零逻辑变更，零行为影响。
- 鸿蒙端功能全链路已实现，其他端（小程序/H5/Android/iOS）未被破坏。

---

## 2026-08-06（新增）- 鸿蒙（HarmonyOS）端适配：推送 + 指纹登录全链路打通

### 变更类型
feat（跨端平台扩展，前端条件编译宏语义修正 + 鸿蒙工程配置 + 后端 harmony 平台支持）

### 背景与核心问题
项目原以 `APP-PLUS` 作为「App 端」条件编译宏，但 **uni-app 的 `APP-PLUS` 仅命中 Android 与 iOS，不含鸿蒙**，鸿蒙专属宏为 `APP-HARMONY`，三端通用宏为 `APP`。若直接打包鸿蒙，推送初始化、设备 token 上报、指纹登录等整段代码会被编译器**静默剔除**（不报错、功能凭空消失），排查成本极高。本次统一将「三端通用」逻辑的宏改为 `APP`，平台差异用 `APP-HARMONY` 隔离。

### 变更

#### 一、前端条件编译宏修正（`APP-PLUS` → `APP`，共 7 文件 44 处）
- `composables/useAppPush.js`、`composables/useBiometric.js`、`App.vue`、`pages/index/index.vue`、`pages/index/notification.vue`、`pages/user/login.vue`、`pages/user/profile.vue`
- 语义：`APP` = Android + iOS + HarmonyOS；`APP-HARMONY` = 仅鸿蒙。改后鸿蒙端可命中推送与指纹登录的全部逻辑，Android/iOS 行为零变化。

#### 二、前端鸿蒙差异隔离（`useAppPush.js`）
- `requestPermission()`：新增 `#ifdef APP-HARMONY` 分支。**鸿蒙无 `plus.android` 命名空间，也无 `POST_NOTIFICATIONS` 标准权限**，若不隔离会运行期崩溃；鸿蒙走友盟插件封装的 `requestNotificationPermission`。原 Android 的 `plus.android.requestPermissions` 与 iOS 分支整体包入 `#ifndef APP-HARMONY`，逻辑不变。
- `getDeviceToken()`：鸿蒙端 `plus.os.name` 取值不可靠且平台恒定，用 `#ifdef APP-HARMONY` 固定上报 `platform = 'harmony'`；非鸿蒙端沿用 `plus.os.name === 'iOS' ? 'ios' : 'android'`。
- `openNotificationSettings()`：补注释说明鸿蒙端插件未封装系统设置页跳转，已由 try-catch 兜底不报错。

#### 三、前端环境配置（`config/env.js`）
- `APP_PLUS` 常量更名为 `IS_APP`，判定条件由 `UNI_PLATFORM === 'app-plus'` 扩展为 `=== 'app-plus' || === 'app-harmony'`。使鸿蒙端与 Android/iOS 一致走生产 HTTPS 域名（真机 localhost 指向手机自身，必然失败）。

#### 四、鸿蒙工程配置（新增）
- **`frontend/manifest.json`**：新增 `app-harmony` 顶层段，声明 `bundleName: "com.wuzuniao.yao"`（与 Android 包名、友盟鸿蒙应用包名三者一致）。
- **`frontend/harmony-configs/entry/src/main/module.json5`**（新增）：鸿蒙工程模块增量配置，声明 `ohos.permission.ACCESS_BIOMETRIC`。**这是鸿蒙指纹登录的硬性前置**——uni-app 官方文档明确 Soter 三接口在鸿蒙需该权限，缺失则调用直接失败。注意鸿蒙权限**不写在 manifest.json**，须写在 `harmony-configs` 增量目录，由 HBuilderX 编译时与内置模板合并。推送所需的 INTERNET / GET_NETWORK_INFO 等已由 `xtf-umengpush` 插件自带 `module.json5` 声明并自动合并，此处不重复声明以免冲突。
- **`frontend/harmony-configs/entry/src/main/resources/base/element/string.json`**（新增）：提供权限申请理由字符串资源 `reason_access_biometric`，供 `module.json5` 的 `$string:` 引用（缺失会导致鸿蒙编译失败）。
- **`uni_modules/xtf-umengpush/utssdk/app-harmony/resources/rawfile/umconfig.json`**：填入友盟鸿蒙应用真实配置（bundleName / appKey / appMessageSecret / channel），原为占位值，不填则鸿蒙端推送无法注册。

#### 五、鸿蒙指纹登录（`composables/useBiometric.js`）
- 经查 uni-app 官方文档确认：`checkIsSupportSoterAuthentication` / `checkIsSoterEnrolledInDevice` / `startSoterAuthentication` 三接口**自 HBuilderX 4.31 起鸿蒙原生支持**，`supportMode` 同样返回 `fingerPrint`，`requestAuthModes` 同样接受 `['fingerPrint']`。
- 因此**业务逻辑零改动**，仅需：① 宏改为 `APP` 使模块不被剔除；② 声明 `ohos.permission.ACCESS_BIOMETRIC`（见上）。文件头注释同步补充鸿蒙权限声明位置与版本要求。

#### 六、后端 harmony 平台支持（5 文件）
- **`app/core/config.py`**：新增 `UMENG_HARMONY_APP_KEY` / `UMENG_HARMONY_MASTER_SECRET` 配置项（鸿蒙在友盟控制台为独立应用，密钥独立一套）。
- **`.env` / `.env.template`**：填入/占位鸿蒙密钥，注释说明 `UMENG_PRODUCTION_MODE` 仅 iOS 生效，Android / Harmony 侧友盟忽略。
- **`app/schemas/notification_channel.py`**：`AppPushDeviceToken.platform` 与 `UpsertAppPushChannel.platform` 的 `Literal["android","ios"]` → `Literal["android","ios","harmony"]`。**这是入库第一道闸**，不放宽则鸿蒙上报直接 422，后端永远收不到 token。
- **`app/services/umeng_service.py`**：`_get_credentials()` 新增 `platform == "harmony"` 分支取鸿蒙密钥；`_build_payload()` 补文档说明——鸿蒙在友盟复用同一 unicast 接口，payload 与 Android 同源（`display_type=notification` + `body`），故共用同一分支，**仅密钥区分**，无需新增代码分支。
- **`app/services/notification_channel_service.py`**：`device_tokens` JSON 结构注释由 `android|ios` 更新为 `android|ios|harmony`（仅文档）。

### 影响范围
- **鸿蒙端**：推送与指纹登录全链路可用（需 HBuilderX 4.31+ 与 DevEco Studio 环境打包验证）。
- **Android / iOS / 微信小程序 / H5**：**零行为变更**。宏由 `APP-PLUS` 扩大到 `APP` 后，Android/iOS 仍全部命中；小程序/H5 仍全部不命中。已跑 `npm run build:mp-weixin` 构建通过，并验证小程序产物中零残留 `umengpush` / `startSoterAuthentication` / `plus.android`，条件编译剔除正确。

### 遗留与待验证
1. **友盟鸿蒙 payload 结构**：官方文档为 JS 渲染页无法抓取，当前按「与 Android 同源」实现（业界通行做法，接口与签名机制一致）。真机推送验证后如有差异，仅需微调 `_build_payload` 的鸿蒙分支，不影响 Android/iOS。
2. **打包环境**：需 HBuilderX 4.31+（Soter 鸿蒙支持起始版本）+ DevEco Studio 5.0.3.800+，并申请鸿蒙证书（bundleName / 调试发布证书 / Provision Profile）。
3. **上线前**：`umconfig.json` 的 `debug` 须改为 `false`。

---

## 2026-08-06（修正）- 鸿蒙 umconfig.json 的 appMessageSecret 密钥类型修正

### 变更类型
fix（密钥配置纠错，避免鸿蒙端 code=1004 鉴权失败）

### 问题定位
原 `umconfig.json` 的 `appMessageSecret` 填的是**友盟 Master Secret**（`t4sgwk5anrt0keis4onwjegb3u10o4wi`）。但鸿蒙端友盟 SDK 要求的 `appMessageSecret` 必须是**「Umeng Message Secret」**——与 App Master Secret 是两个不同密钥（插件 readme 第106/994行明确警告填错会 code=1004 鉴权失败）。
- 后端 `.env` 的 `UMENG_HARMONY_MASTER_SECRET` 用 Master Secret **正确**（后端 REST API 调友盟服务端、签名均用 Master Secret）。
- 鸿蒙端 SDK（UMeng Message Secret）**另需**一个密钥，二者不可混用。

### 变更
- **`uni_modules/xtf-umengpush/utssdk/app-harmony/resources/rawfile/umconfig.json`**：将误填的 Master Secret 替换为醒目占位符 `REPLACE_WITH_UMENG_MESSAGE_SECRET`，避免误打包上线。该值须由维护者替换为友盟后台鸿蒙应用的 **Umeng Message Secret**（入口：友盟+ 控制台 → U-Push → 应用管理 → 应用信息）。

### 影响范围
- 鸿蒙端推送鉴权：占位符状态下编译可过、推送必失败，须填真值后真机验证。
- 其他端（Android/iOS/小程序/H5）、后端：零影响。

---

## 2026-08-06（修复）- App 推送「设备标识获取失败」修复 + 开启推送时申请通知权限

### 变更类型
fix（前端 App 端，友盟 U-Push 设备标识获取链路重构 + 通知权限申请流程补全）

### 问题定位
用户在通知方式页开启 App 推送时提示「获取设备标识失败」。查阅插件 `uni_modules/xtf-umengpush/readme.md` 与 `utssdk/interface.uts` 后，定位到 4 处缺陷：

1. **丢弃了最快的取值通道**：`initializeUmengPush()` 的返回值 `UmengPushInitResult` 本身即含 `deviceToken`（设备已注册过时可同步取得），原 `initUmeng()` 只读 `result.success`，把 token 直接丢掉了。
2. **等待时间远不足（主因）**：友盟 `register()` 为异步，`deviceToken` 需联网向友盟服务器注册后才回调返回。原实现仅「立即读一次 + 1.5s 后重试一次」，真机首次安装的注册耗时常达数秒，必然超时判失败。
3. **未使用 `register-state` 推送式通道**：插件通过 `onUmengPushRuntimeEvent` 的 `register-state` 事件回传 `deviceToken`，原实现完全未监听，只做轮询。
4. **权限申请「发出即返回」**：`handleEnableAppPush` 中 `requestPermission()` 为同步无回调调用，不等待授权结果就去取 token；用户拒绝时无任何提示，也没有「已开启则忽略」的判断。

### 变更
1. **`frontend/composables/useAppPush.js`**（重构取 token 与权限链路）：
   - 新增模块级 `cachedDeviceToken` 缓存与 `cacheDeviceToken()`：注册成功后 token 固定不变，避免重复等待。
   - 新增 `registerRuntimeListener()`：幂等注册**唯一**的 `onUmengPushRuntimeEvent` 回调。插件为**单监听器模型**（新注册覆盖旧注册），故 `register-state` 取 token 与 `notification-click` 点击跳转必须合并进同一回调，否则互相覆盖。
   - `initUmeng()`：改为先挂监听再初始化（避免注册回调早于监听而丢事件）；缓存返回值中的 `deviceToken`；对 `isConfigured=false` 与 `registerErrorCode` 打警告日志便于排查；`initialized` 改以返回值的 `initialized || success` 判定。
   - `getDeviceToken()`：改为**三重通道**——缓存优先 → `register-state` 事件写入缓存 → 每 500ms 轮询 `getUmengPushDebugState().deviceToken` 兜底，总超时由 1.5s 放宽至 **12s**。
   - 新增 `isNotificationEnabled()`：经 `getPushDeviceInfo()` 读 `systemNotificationEnabled && notificationPermissionGranted`（查询异常时返回 true，不误拦截正常流程）。
   - `requestPermission()`：由同步 `boolean` 改为返回 `Promise<boolean>`；**已开启则直接 resolve(true)，不重复弹窗**；Android 沿用 `plus.android.requestPermissions` 申请 `POST_NOTIFICATIONS`（保留原注释：不用插件 `requestNotificationPermission`，其在部分厂商 ROM 会跳厂商设置页报 "Not Found"），iOS 改为走插件 `requestNotificationPermission` 并取 `res.granted`（原实现未等结果）。
   - 新增 `openNotificationSettings()`：封装 `openUmengNotificationSettings()`，供用户拒绝后引导。
   - `registerPushClick()`：点击跳转逻辑已并入 `registerRuntimeListener`，此处仅保留幂等 `initUmeng()`。
   - `reportDeviceToken()` 失败提示由「获取设备标识失败，请稍后重试」改为「设备注册未完成，请检查网络后重试」（指向真实原因，减少无效重试）。
2. **`frontend/pages/index/notification.vue`**（`handleEnableAppPush`，`#ifdef APP-PLUS` 内）：改为先 `await requestPermission()`；未获权时弹「需要通知权限」Modal 引导 `openNotificationSettings()` 跳系统设置并中止（不再登记必然收不到提醒的设备）；获权后 loading 文案改为「正在注册设备...」并加 `mask` 防误触。

### 影响范围
- 仅 App 端（全部改动位于 `#ifdef APP-PLUS` 内），微信小程序 / H5 端无任何行为变化。
- 后端接口与数据结构零改动。
- 因插件为单监听器模型，`notification-click` 点击跳转已迁移至合并回调，行为与原先一致（解析 `payload.route`，缺省 `/pages/index/index`）。
- **验证限制**：CLI `npm run build:app` 只产出空壳资源包，无法验证 App 原生逻辑，须经 HBuilderX 云打包 / 自定义基座真机验证。

---

## 2026-08-05（安全修复）- 指纹登录安全审计问题修复（S3/M1/M2/M4/M5），S1/S2/M3/M6 平台限制说明

### 变更类型
安全修复（后端 + 前端，依据《指纹登录安全审计报告.md》逐条验证后修复可修复项）

### 审计问题验证结论
逐条对照项目实际代码验证审计报告，结论如下：

**已修复（真实存在且可在当前约束下修复）：**
- **S3（隐私政策与实现矛盾）**：`privacy.vue` 仍声明"AES-256-GCM 加密 + PBKDF2 12 万次迭代派生密钥"，与上一轮已改为明文存储的实现直接冲突，属隐私合规不实陈述。已改为如实描述（应用沙箱存储 + 设备绑定，未做本地加密）。
- **M1（device_id 无 UUID 格式校验）**：`user_service.py` 三方法 + `schemas/user.py` 四处 device_id 仅校验非空/长度，可为任意短串。已加 UUID v4 格式校验（schema 层共享 `_validate_device_id` + 服务层 `_is_valid_device_id` 二次防御）。
- **M2（token 非 compare_digest）**：`verify_biometric_token` 用 SQL `=` 比对 token。已改为按 device_id 取出候选记录后用 `secrets.compare_digest` 恒定时间比对。
- **M4（过期凭证无定时清理）**：无清理任务，过期行永久滞留。已新增 `purge_expired_biometric_tokens` 方法 + scheduler 每 30 分钟清理循环。
- **M5（关闭指纹不通知后端）**：`profile.vue` toggleBiometric 关闭时只清本地，服务端凭证仍有效最长 31 天。已新增 `/biometric-revoke` 接口（JWT 保护）+ `revoke_biometric_token(user_id, device_id)` 单设备撤销方法，前端关闭时调用，立即作废服务端凭证。

**真实存在但当前约束下无法修复（平台/架构限制）：**
- **S1（SOTER 签名未上传后端）/ S2（challenge 客户端自造）**：查阅 uni-app 官方文档（https://uniapp.dcloud.net.cn/api/system/authentication.html）确认——`resultJSON`/`resultJSONSignature` 返回值"平台差异说明"均标注为**仅微信小程序**，**App 端 `startSoterAuthentication` 不返回签名**；`challenge` 参数也仅微信小程序/HarmonyOS 生效。在用户约束"不能用 UTS 调用 Android Keystore/Keychain"下，App 端**无法实现服务端验签**（API 层面不返回可验证签名）。此为 uni-app App 端 SOTER 固有限制，非实现缺陷。引入服务端 challenge 机制在 App 端无密码学意义（不组装进可验证签名），属"安全剧场"，故不引入。
- **M3（限流按 IP 共享/进程内字典）**：`rate_limit.py` 已注释"开发阶段单进程足够，生产环境建议迁移至 Redis"，与 AGENTS.md 项目规范"单进程部署"一致，属架构约束，不在本次范围。
- **M6（登录即下发并落盘长效凭证）**：凭证下发是 `/login` 响应的一部分（device_id 已传则必然下发），前端落盘是启用指纹的前提。改为"用户确认才下发"需新增"主动开启指纹"接口且破坏登录后引导闭环，权衡后保持现状（凭证已下发后端，前端落盘与否不影响后端已存在事实；用户拒绝则不 setEnabled，凭证闲置 31 天过期或随时可开）。

### 变更
1. **`frontend/pages/user/privacy.vue`**（S3）：移除"AES-256-GCM 算法加密 + PBKDF2 12 万次迭代 + 机型参与密钥派生"虚假描述，改为如实描述"存储于本应用私有存储区（应用沙箱），其他应用通常无法访问；与设备标识绑定，仅本设备有效"；关闭指纹描述同步改为"通知服务端作废本设备凭证记录"。
2. **`backend/app/schemas/user.py`**（M1）：新增 `_DEVICE_ID_RE` UUID v4 正则与 `_validate_device_id` 共享校验器；`LoginUser`/`ResetPassword`/`RefreshTokenReq` 的可选 device_id 加校验器；`BiometricLogin` 的 device_id 加 UUID v4 格式校验；新增 `BiometricRevoke` schema。
3. **`backend/app/services/user_service.py`**（M1/M2/M4/M5）：
   - 新增 `_DEVICE_ID_RE` 与 `_is_valid_device_id`，`issue_biometric_token`/`refresh_biometric_token`/`verify_biometric_token` 的 device_id 校验改为 UUID v4 格式校验。
   - `verify_biometric_token` 改为先按 device_id + 未过期取出候选记录，再用 `secrets.compare_digest` 恒定时间比对 token（M2）。
   - 新增 `revoke_biometric_token(user_id, device_id)` 单设备撤销方法（M5）。
   - 新增 `purge_expired_biometric_tokens()` 清理过期凭证方法（M4）。
4. **`backend/app/services/scheduler_service.py`**（M4）：新增 `INTERVAL_BIOMETRIC_PURGE=1800` 与 `_loop_purge_expired_biometric_tokens` 循环（每 30 分钟清理过期 user_biometric_tokens），`start_all` 注册该任务。
5. **`backend/app/api/v1/users.py`**（M5）：新增 `POST /biometric-revoke` 接口（JWT 保护，调 `revoke_biometric_token`），import `BiometricRevoke`。
6. **`frontend/api/modules/user.js`**（M5）：新增 `revokeBiometric({ device_id })` API。
7. **`frontend/pages/user/profile.vue`**（M5）：import `revokeBiometric`（#ifdef APP-PLUS）；`toggleBiometric` 关闭分支改为清本地 + 调 `revokeBiometric` 作废服务端凭证（失败不阻断本地关闭，凭证最长 31 天自然过期）。

### 影响范围
- **指纹登录安全闭环增强**：device_id 格式校验防伪造脏数据；token 恒定时间比对防理论时序攻击；关闭指纹立即作废服务端凭证（风险窗口从 31 天缩至即时）；过期凭证定时清理防累积；隐私政策与实现一致消除合规风险。
- **兼容性**：前端 `getDeviceId` 一直生成标准 UUID v4，历史数据兼容；verify_biometric_token 对 device_id 加 UUID 校验后，极少数非 v4 格式旧 device_id 需重新账号密码登录生成新 device_id（旧凭证本应轮换）。
- **不改动**：凭证有效期仍 31 天（用户约束不变）；SOTER 调用方式不变；账号密码登录/微信登录/token 续期/改密登出删号撤销等已实现功能均不受影响。
- 后端 Python 编译通过；前端 `npm run build:mp-weixin` 构建成功。
- **App 端生效前提**：需重新 HBuilderX 云打包/自定义基座。

---

## 2026-08-05（修复）- 指纹登录闭环彻底修复：移除本地 AES 加密链路 + 登录后引导开启 + onShow 开关判定

### 变更类型
修复（前端 / `frontend/composables/useBiometric.js` + `frontend/pages/user/login.vue` + `frontend/composables/useTokenRefresh.js` + `frontend/package.json`）

### 根因（三次排查，定位真正根因）
此前两次修复均未解决问题：
- 第一次：怀疑 `login.vue` 的 `isEnabled()` 过滤造成死锁并移除——问题依旧。
- 第二次：怀疑 `useBiometric.js` 用 `btoa/atob/TextEncoder`（App 端缺失）改用 `uni.arrayBufferToBase64/uni.base64ToArrayBuffer`——问题依旧。

本次深入排查定位到**真正的根因**在加密链路本身：
- `useBiometric.js` 用 `@noble/ciphers` 的 `gcm` + `@noble/hashes` 的 `pbkdf2/sha256` 做 PBKDF2 派生 AES-256-GCM 加密存储 biometric_token。这两个纯 ESM 包在 App 端 5+ 引擎中存在难以调试的兼容性问题（ESM 导入/TypedArray 行为差异），`encryptToken`/`decryptToken` 抛异常被 `storeBiometricToken`/`getBiometricToken` 的 try-catch **静默吞掉** → 凭证从未真正写入本地或读不出 → `getBiometricToken()` 恒为空。
- 连锁：① profile 开启指纹 `getBiometricToken()` 空 → 提示「请先进行账号密码登录以开启指纹登录」；② login onShow 因本地无 token 使 `biometricVisible` 恒 false → 永远默认账号密码登录。
- 另发现两个独立缺陷：
  - `login.vue` onShow 只检查 `getBiometricToken()` 不检查 `isEnabled()`，导致用户即使未开启指纹只要有 token 就默认显示指纹卡片（逻辑错误）。
  - `login.vue` 登录成功后无任何引导，用户不知道可开启指纹登录（UX 缺失）。
  - `useTokenRefresh.js` 的 `getTokenExp` 用 `atob` 解码 JWT，App 端缺失 `atob` 时返回 0 导致每次 onShow 都误判「临近过期」触发续期（附带问题，非指纹根因）。

### 变更
1. **`frontend/composables/useBiometric.js`（核心修复）**：
   - **移除整个 AES-256-GCM 加密链路**：删除 `gcm/pbkdf2/sha256` 导入、`getDeviceSalt/utf8Encode/utf8Decode/deriveKey/b64encode/b64encode/b64decode/encryptToken/decryptToken` 全部辅助函数。
   - `storeBiometricToken/getBiometricToken/clearBiometricToken` 改为直接用 `uni.setStorageSync/getStorageSync/removeStorageSync` 存取 biometric_token（存储 key 由 `biometricTokenEnc` 改为 `biometricToken`）。
   - 安全性论证：biometric_token 由后端 `secrets.token_hex(32)` 生成（256-bit 高熵随机串），本身已是安全凭证；后端校验 token 与 device_id 绑定关系，跨设备无法复用；uni storage 在 App 端为应用沙箱，其他应用无法访问。泄露风险可控。
   - 保留 `getDeviceId/isAvailable/authenticate/isEnabled/setEnabled` 不变。
2. **`frontend/pages/user/login.vue`**：
   - **onShow 增加 `isEnabled()` 判定**：三者同时满足（设备支持指纹 + 本地有凭证 + 用户已开启开关）才默认显示指纹卡片，否则默认账号密码登录。修正此前「有 token 就弹指纹」的逻辑错误（存储仍不依赖开关，避免死锁）。
   - **handleLogin 成功后引导开启指纹**：账号密码登录成功并存储凭证后，若设备支持指纹且 `isEnabled()` 为 false，弹 modal 询问「是否开启指纹登录，下次免密一键登录？」——确认则 `setEnabled(true)`（下次进登录页默认指纹卡片），取消则仅提示登录成功（凭证已存、可在 profile 页随时开启）。
3. **`frontend/composables/useTokenRefresh.js`**：
   - `getTokenExp` 兼容 App 端缺失 `atob`：优先用全局 `atob`，否则回退 `uni.base64ToArrayBuffer` 解码 JWT payload（参考 `announcementRead.js` 实现），避免 App 端每次 onShow 误判临近过期触发续期。
4. **`frontend/package.json`**：
   - 移除不再使用的 `@noble/ciphers` 与 `@noble/hashes` 依赖（仅 useBiometric.js 使用，已移除加密链路）。注意：`package-lock.json` 需执行 `npm install` 后自动同步。

### 影响范围
- App 端指纹登录完整闭环修复：账号密码登录（凭证直接存本地，不再因加密失败而丢失）→ 登录后引导开启指纹（弹窗询问，用户确认即开启）→ profile 开启指纹（取得到凭证，不再提示「请先登录」）→ 下次进登录页默认指纹一键登录。
- `useTokenRefresh.js` 修复避免 App 端频繁误触发续期。
- 移除 `@noble` 依赖减小打包体积；需执行 `npm install` 同步 lock 文件。
- **生效前提**：App 端改动须重新 HBuilderX 云打包/自定义基座才生效；CLI `npm run build:app` 产物为空壳无法验证。
- 小程序/H5 端无影响（useBiometric/useTokenRefresh 改动均在 `#ifdef APP-PLUS` 内或为兼容性兜底）。

---

## 2026-08-05（修复）- 指纹登录开启失败/默认不弹出：App 端缺失 btoa/TextEncoder 致凭证存不进

### 变更类型
修复（前端 / `frontend/composables/useBiometric.js` + `frontend/pages/user/login.vue`）

### 根因（二次排查，修正此前判断）
此前怀疑 `login.vue` 的 `isEnabled()` 过滤造成死锁并移除，但问题依旧。深入排查后定位到真正的根因在 `useBiometric.js` 的凭证加解密层：
- `encryptToken`/`decryptToken` 直接调用浏览器全局 `btoa`/`atob`/`TextEncoder`/`TextDecoder`。**App 端（Android 5+ 引擎）通常缺失这些全局 API**，调用即抛异常。
- 异常被 `storeBiometricToken`/`getBiometricToken` 的 try-catch 静默吞掉 → 凭证从未真正写入本地 → `getBiometricToken()` 恒为空。
- 连锁导致：① profile 页开启指纹时 `getBiometricToken()` 空 → 提示「请先进行账号密码登录以开启指纹登录」；② 登录页 `onLoad` 因本地无 token 使 `biometricVisible` 恒 false → 永远默认账号密码登录，不弹指纹。
- `@noble/ciphers` 的 `gcm` 经核实是纯 JS 实现（不依赖 WebCrypto/crypto.subtle），故 AES 加解密本身在 App 端可用，问题仅出在 base64 与 UTF-8 编解码的全局 API 缺失。

### 变更
1. `frontend/composables/useBiometric.js`：
   - `b64encode`/`b64decode` 改用 **`uni.arrayBufferToBase64` / `uni.base64ToArrayBuffer`**（uni 全端兼容 API），并保留手动兜底。
   - 新增 `utf8Encode`/`utf8Decode`，优先用原生 `TextEncoder`/`TextDecoder`，缺失时回退手写 UTF-8 编解码。
   - `deriveKey`/`encryptToken`/`decryptToken` 改用上述 helper，移除对 `btoa`/`atob`/`TextEncoder` 的直接依赖。
2. `frontend/pages/user/login.vue`：保留此前移除 `biometric.isEnabled()` 过滤的改动（账号密码登录后无条件存储后端下发的 `biometric_token`），使「已登录过」即可在 profile 开启指纹。

### 影响范围
App 端指纹一键登录完整闭环恢复：账号密码登录（凭证正确加密存本地）→ profile 开启指纹（取得到凭证、调起系统指纹验证成功）→ 下次进登录页自动弹出指纹一键登录。注意：CLI `npm run build:app` 产物为空壳，必须用 HBuilderX 云打包/自定义基座验证。

---

## 2026-08-04（合规）- App 端 Android 隐私政策原生提示框（方案 B，国内上架合规）

### 变更类型
合规（前端 / manifest.json + 新增 androidPrivacy.json + App.vue / App 端隐私合规）

### 背景
HBuilderX 云端打包提示：「当前应用缺少相关配置（App 隐私政策与权限配置），不上架国内应用市场无需处理」。本次按方案 B（计划上架国内应用市场）完整实现 Android 原生隐私提示框，消除该 Warning 并满足《网络安全法》《消费者权益保护法》要求。

### 根因
`manifest.json` 的 `app-plus` 未配置隐私提示框（`privacy` 节点 / `androidPrivacy.json`），云端打包检测缺失即输出合规 Warning；且原 `App.vue` 在 `onLaunch` 直接初始化友盟推送 SDK（采集设备标识），违反「用户同意隐私前不得启动三方 SDK、不得采集设备信息」的合规红线。

### 变更
1. **新增 `frontend/androidPrivacy.json`**（HX 3.2.1+ 原生隐私弹窗配置，`prompt:"template"`）：
   - `title` / `message` / `buttonAccept` / `buttonRefuse` / `second` / `disagreeMode` / `styles` 完整字段。
   - `message` 富文本含《服务协议》《隐私政策》两个链接，均指向项目已有隐私政策页的线上 H5 地址 `https://yao.wuzuniao.com/pages/user/privacy`（history 模式无 .html 后缀，即 `pages/user/privacy` 页面在 H5 端的发布地址；DCloud 原生弹窗 a 链接不支持 uni-app 页面路由，仅支持 http(s) 外链或本地 html，故用线上页面 URL，满足「使用项目中已有页面」约束）。
   - `hrefLoader:"system"`（系统浏览器打开外链，跨 HX 版本稳定）。
2. **`frontend/manifest.json`**：`app-plus` 新增 `"privacy": { "prompt": "template" }` 内联节点，与 `androidPrivacy.json` 协同，兼容各 HX 版本识别原生隐私提示框。
3. **`frontend/App.vue`**：`onLaunch` 中 App 端（`#ifdef APP-PLUS`）的友盟推送初始化改为——存在 `uni.onAgreePrivacy` 时监听其回调，待用户点击「同意」隐私后再 `registerPushClick()`（内部幂等初始化 SDK、采集 Device Token）；不支持时降级为直接初始化。确保同意前不采集设备信息。

### 合规要点核对
- Android `permissions` 仅 INTERNET / ACCESS_NETWORK_STATE / ACCESS_WIFI_STATE / POST_NOTIFICATIONS / VIBRATE / RECEIVE_BOOT_COMPLETED，**无读写存储 / 设备信息 / 定位等敏感权限**，已最小化。
- 隐私政策页（`pages/user/privacy.vue`）已含 DCloud uni-app 引擎数据采集告知条款（第 200 行），满足官方「加入框架方数据采集声明」要求。
- 通知权限申请（`requestPermission`）仅在通知方式页用户主动添加 App 推送时触发，非启动期；打卡成功后的设备标识刷新（`reportDeviceToken` `createIfMissing:false`）在用户已使用后进行，均合规。
- 指纹检测（`useBiometric.js`）在登录页 onShow，非启动期。

### 影响范围
- 仅影响 App 端（Android）。`androidPrivacy.json`、`app-plus.privacy`、`App.vue` 的 `#ifdef APP-PLUS` 块均不影响微信小程序 / H5 端产物与既有功能。
- 文件均无 lint 错误，JSON 校验通过。
- **生效前提**：原生隐私弹窗与 `modules`、友盟插件同为打包期资源，改后**必须重新 HBuilderX 云打包或制作自定义基座**才生效；`npm run build:app` 空壳无法验证。
- 已知约束：项目暂无独立《服务协议》页面，弹窗双链接暂均指向隐私政策页（项目唯一协议页）；后续若补充服务协议页，可将《服务协议》链接拆分指向新页。

---

## 2026-08-04（合规）- 新增《服务协议》页面并补全双链接合规

### 变更类型
合规（前端 / 新增 agreement.vue + pages.json + 设置页入口 + 双链接引用治理）

### 背景
应用商店上架要求《用户协议》与《隐私政策》为相互独立且内容真实的两份文件（双链接）。此前项目仅有隐私政策页（`pages/user/privacy.vue`），弹窗《服务协议》链接名实不符（指向隐私页）。本次按主流服务协议规范新建独立《服务协议》页，并实现全链路双链接合规。

### 变更
1. **新增 `frontend/pages/user/agreement.vue`（《服务协议》）**：样式完全复用 `privacy.vue` 的 `privacy-page` 类名与结构（视觉一致）；`themeMixin` 主题绑定 `:data-theme="themeKey"`、`BackButton` 次级页返回，与全站 14 页一致。内容为十三章节条款：协议确认与接纳、账号注册与使用资格、服务内容与形式、用户行为规范、知识产权、个人信息保护、免责声明、违约处理、服务变更中断与终止、第三方服务、协议变更、争议解决与管辖、联系我们。
2. **`frontend/pages.json`**：`pages/user` 分包新增 `agreement` 路由（navigationStyle:custom + SEO 标题「服务协议 - 用户协议与权利义务」），位于 `privacy` 前。
3. **`frontend/pages/index/settings.vue`**：分组 2 在「隐私政策」上方新增「服务协议」行（同格式 `group2-item` + 右箭头），新增 `goAgreement()` 跳转 `/pages/user/agreement`。
4. **双链接合规治理**：
   - `frontend/androidPrivacy.json`：弹窗 `message` 的《服务协议》《隐私政策》双链接分别指向真实线上页 `…/pages/user/agreement` 与 `…/pages/user/privacy`。
   - `frontend/pages/user/register.vue`：勾选区由单《隐私政策》链接改为「《服务协议》和《隐私政策》」双链接，新增 `goAgreement()`。
   - `frontend/pages/user/login.vue`：微信/指纹/账号密码三处勾选区均改为双链接，弹窗文案统一为「请先查看并同意《服务协议》和《隐私政策》」，新增 `goAgreement()`。
   - `frontend/public/sitemap.xml`：新增 `pages/user/agreement` 条目（共 15 个可路由页面）。

### 合规要点核对
- 《服务协议》与《隐私政策》互为补充、互不包含，满足应用商店「用户协议独立且真实」要求。
- 双链接均指向项目内真实页面线上 URL（DCloud 原生弹窗仅支持 http(s) 外链），无死链、无指向错误页。
- 未改动任何既有交互逻辑与样式 token，仅新增条目与方法。

### 影响范围
- 影响：H5 / 微信小程序 / App 三端均可访问新《服务协议》页；设置页新增入口；注册/登录勾选区展示双协议链接。
- 无影响：既有隐私政策页、通知、打卡、主题切换、友盟推送等任何既有功能与交互；JSON/SCSS 校验通过，无 lint 错误。
- 生效前提：`androidPrivacy.json` 双链接治理需重新 HBuilderX 云打包/自定义基座才在 App 端原生弹窗生效；H5/小程序端即时生效。

---

## 2026-08-04（修复）- App 端提示「打包时未添加 fingerprint 模块」

### 变更类型
修复（前端 / manifest.json / App 原生模块配置）

### 问题现象
App 端进入登录页或触发指纹一键登录时，运行时报错「打包时未添加 fingerprint 模块」，指纹能力检测与验证均不可用。

### 根因
`frontend/composables/useBiometric.js` 调用了 `uni.checkIsSupportSoterAuthentication`、`uni.checkIsSoterEnrolledInDevice`、`uni.startSoterAuthentication`，这三个 API 在 App 端由 DCloud 的 **Fingerprint（指纹识别）原生模块**提供。而 `frontend/manifest.json` 的 `app-plus.modules` 仅声明了 `Push`，未声明 `Fingerprint`。按 uni-app 官方《App模块配置》规则，未勾选的功能模块打包时默认不会包含，因此云打包产物中缺少该原生模块，调用时抛出上述提示。

### 依据
- uni-app 官方《App模块配置》：模块标识为 `Fingerprint`，支持 Android / iOS，源码视图下在 `app-plus.modules` 中以空对象 `{}` 声明即可；该模块未标注需配置三方 SDK，也未要求开发者显式声明 Android `USE_FINGERPRINT` 权限（由模块自带清单合并处理）。
- uni-app 官方《生物认证说明》：「App端打包时，注意需要在 manifest 的模块中选择指纹、faceID 等模块，否则打包后无法运行相关功能。」

### 变更
- `frontend/manifest.json`：`app-plus.modules` 由 `{ "Push": {} }` 改为 `{ "Push": {}, "Fingerprint": {} }`。

### 影响范围
- 仅改 `frontend/manifest.json` 一处，未触碰 `mp-weixin` / `h5` 段与 Android `permissions` 列表，不影响小程序与 H5 端产物。
- **必须重新用 HBuilderX 云打包 / 制作自定义基座后才生效**，旧安装包与 CLI 产物无效（CLI 不产出 apk，且原生模块由打包阶段注入）。

---

## 2026-08-04（功能）- 隐私政策全面重写与合规补全

### 变更类型
功能 / 合规（前端 / privacy 页 / 政策正文）

### 背景
项目功能大幅扩展（App 端、指纹登录、友盟推送、邮件渠道、主题换肤、分享等），原隐私政策仅 7 章、内容停留在"微信小程序 + DCloud 设备码"阶段，与实际收集范围、调用权限、共享的第三方严重脱节，无法通过应用市场与微信平台的合规审核。

### 依据
1. 全量扫描项目：`pages.json` 14 个页面、`manifest.json` 权限声明、前端全部 `uni.*` 敏感 API 调用与 storage key、`backend/sql/` 全部建表字段、`backend/app/api/v1/` 全部端点、`core/config.py` 第三方配置项。
2. 官方要求：DCloud《Android平台隐私与政策提示框配置方法》要求的逐字声明条款与 `dcloud.io/license/appprivacy.html` 链接；友盟+《SDK 隐私条款模板》的 U-Push 采集字段清单与 `umeng.com/page/policy` 链接；《个人信息保护法》第十三条、第十七条告知事项；微信小程序《用户隐私保护指引》。

### 变更（`frontend/pages/user/privacy.vue`，仅正文内容，样式架构不变）
- 章节由 7 章扩为 **11 章**：适用范围与处理者 / 信息收集与使用（12 小节）/ 设备权限 / 本地存储与同类技术 / 共享转让公开披露 / 存储与保护 / 您的权利 / 未成年人保护 / 开源与自建部署说明 / 政策更新 / 联系我们。
- 新增覆盖：微信一键登录（OpenID / session_key）、**App 端指纹登录**（明示不采集指纹特征、本地 UUID 设备标识、AES-256-GCM 加密的 31 天生物识别凭证）、打卡计划与记录（含"计划名称/备注为自由文本、不建议填健康医疗信息"的敏感信息提示）、四种通知渠道（站内信/邮件 SMTP 授权码加密存储/微信订阅消息/友盟 Device Token）、通知发送记录、系统公告、分享功能、内置预设头像（不访问相册）、token 7 天有效与静默续期、**敏感个人信息专项说明**。
- 新增 **3 张横向滚动表格**（复用既有 `privacy-page__table*` 类）：Android 权限逐条用途与拒绝影响、本地存储项明细、第三方共享清单（DCloud / 友盟 U-Push / 微信开放平台 / 用户自选邮件服务商 / 腾讯企业邮 / 厂商推送通道-未启用）。
- 新增**不调用权限的负面清单**（定位、相机相册、麦克风、通讯录、日历、健康数据、应用列表、外部存储、扫码蓝牙 NFC、微信头像昵称手机号等），并说明剪贴板仅单向写入不读取。
- 权利章节对齐实际实现：注销为**页面内自助申请 + 24 小时冷静期 + 到期彻底删除 7 类关联数据**（原文案错写为"联系客服删除"）；补充知情决定权、查阅复制、更正补充、撤回同意、解释权与逝者近亲属权利、十五个工作日响应时限。
- 存储保护章节补充：境内存储且不出境、分项留存期限、bcrypt 密码哈希 / AES-256-GCM 敏感字段加密 / HTTPS 传输 / 用户 ID 数据隔离 / 接口输出脱敏 / 敏感接口频率限制 / 安全事件告知义务。
- 未成年人保护由"14 周岁"单条扩为 18 / 14 周岁分层，明确未满 14 周岁需监护人**单独同意**。
- 新增"开源与自建部署说明"：本政策仅适用官方运营实例，第三方自建部署由其自行负责。

### 技术要点
- 重点条款强调**未使用 `<b>`/`<strong>`**（微信小程序不支持，会导致渲染异常），改为嵌套 `<text class="privacy-page__em">` + 新增该类样式（`--color-text-primary` + `font-weight: 600`），跨端一致。
- 未新增任何色值字面量，`privacy-page__em` 引用既有语义令牌；未改动页面布局、卡片、表格、`@media` 断点等既有样式定义。

### 影响范围
- 仅改 `frontend/pages/user/privacy.vue` 一个文件（正文 + 1 个新增样式类 + 文件头注释）。登录页/注册页的《隐私政策》跳转链路不变。编译通过（`build:mp-weixin` DONE Build complete，仅既有 sass 弃用警告）。

---

## 2026-08-04（修复）- profile 修改邮箱与主题之间缺分隔线

### 变更类型
修复（前端 / profile 页 / 布局细节）

### 问题
profile.vue「分组 1：资料修改」中，"修改邮箱/绑定邮箱"项（第 191 行）`class="profile-page__group-item"` **遗漏** `profile-page__group-item--bordered`（同组用户名/头像/签名/密码四项均带）。`--bordered` 提供 `border-bottom: 1px solid var(--color-separator)` 细分隔线。缺失导致非 App 端「修改邮箱」直接衔接「主题」项时无细分隔线，视觉断裂。

### 修复（`frontend/pages/user/profile.vue`）
- 第 191 行"修改邮箱"项补加 `profile-page__group-item--bordered`，与同组其他项一致，恢复其与下方项（指纹登录/主题）之间的细分隔线。

### 影响范围
- 仅给 1 个分组项补一个类名，未改样式定义或逻辑；分隔线在所有主题下随 `--color-separator` 生效。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（修复）- 墨黑主题选中项不高亮（修改头像/选择主题）

### 变更类型
修复（前端 / 设计系统 / 选中态语义）

### 问题
墨黑主题（ink）下，profile.vue 的「修改头像」与「选择主题」选中项（`.profile-page__avatar-option--selected` / `.profile-page__theme-option--selected`）不高亮。选中态样式为 `border-color: var(--color-brand-bg)` + `background: var(--color-selected-bg)`。墨黑下三处色值全同 `#f2f2f2`：选中底 `--color-selected-bg`=`--palette-brand-selected`、未选中底 `--color-surface-hover`=`--palette-neutral-15`、边框 `--color-brand-bg`=`--palette-brand-light`，导致选中与未选中视觉无差异。

### 修复（`frontend/assets/styles/global.scss`，仅墨黑块）
- 将墨黑 `--palette-brand-selected` 由 `#f2f2f2` 改为 `#dcdcdc`（中灰，与 hover 背景 `#f2f2f2` 拉开）。选中项呈现「略深中灰底 + 浅灰描边环（`--color-brand-bg`=#f2f2f2 衬在深底上可见）」，与未选中浅灰底明确区分。
- 不动 `--palette-brand-light`（边框源，被次级按钮/底部导航/BeginnerGuide 高亮环等大量组件共用，改之牵连过广），故仅调选中底一档。

### 影响范围
- 仅改 global.scss 墨黑块 1 键（line 211），其余 9 套主题及所有组件不受影响；选中态语义（头像/主题/设置分组等共用 `--color-selected-bg`）在墨黑下统一修复。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（调整）- 可选主题顺序：薰衣草移至末位

### 变更类型
调整（前端 / 配置）

### 内容
`frontend/store/modules/theme.js` 的 `THEME_LIST` 顺序调整：将 `lavender`（薰衣草）从原第 4 位移至列表末尾。新顺序：green → ink → indigo → cyan → amber → coral → rose → crimson → gold → lavender。仅调整数组顺序，未改任何主题定义；profile.vue 主题选择器按 `THEME_LIST` 遍历渲染，薰衣草随之显示在可选主题最后。

### 影响范围
- 仅改 theme.js 数组顺序（1 处位置移动），无功能/样式变更。

---

## 2026-08-04（优化）- 墨黑主题提交/取消按钮对比度

### 变更类型
优化（前端 / 设计系统 / 按钮语义）

### 问题
墨黑主题（ink）下品牌色全压成灰阶：提交按钮 `.profile-page__btn--submit` 背景 `var(--color-brand-bg)`=`--palette-brand-light`=`#f2f2f2`（浅灰），取消按钮 `.profile-page__btn--cancel` 背景 `var(--color-btn-cancel-bg)`=`--palette-neutral-30`=`#f0f0f0`（更浅灰），两者文字均 `#171717` 墨黑。提交与取消同为灰底黑字、明度几乎一致，提交按钮无任何 CTA 强调，无法视觉引导提交。

### 修复（`frontend/pages/user/profile.vue`）
- 提交按钮改用主色实心 + 反色白字：`background: var(--color-brand)`（墨黑下=`#171717` 实黑底）、`color: var(--color-text-inverse)`（墨黑下=`#ffffff` 白字），成为强 CTA。
- 取消按钮保持次级：`background: var(--color-btn-cancel-bg)`（浅灰）、`color: var(--color-text-primary)`（黑字）。
- `.profile-page__btn-text` 的 `color` 由固定 `--color-text-primary` 改为 `inherit`，文字色交由按钮父级控制（提交白字/取消黑字）。
- 对所有主题一致生效：亮色下提交=品牌实心色+白字（更标准 CTA），取消=浅灰+黑字，主次更清晰。

### 影响范围
- 仅改 profile.vue 按钮样式（`.profile-page__btn--submit/--cancel/__btn-text`，该按钮结构仅 profile.vue 使用），未改全局令牌或其他主题。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（优化）- 三套暗色主题（薰衣草/绯红/流金）对比度

### 变更类型
优化（前端 / 设计系统 / 暗色方案）

### 问题
三套暗色主题（lavender/crimson/gold）中性阶「全反转」过度：卡片底 `--palette-neutral-0` 与页面底 `--palette-neutral-40` 明度几乎相同（如 lavender `#0f1011` vs `#010102`），卡片靠 `inset 1px` 描边 + 5% 黑阴影区分，但描边极暗、黑底阴影不可见 → 整屏融为一片黑，卡片样式看不清。

### 修复（`frontend/assets/styles/global.scss`，用户确认仅 A+B 配色，不含头像占位）
- 提亮卡片底 `--palette-neutral-0`（与页面底拉开明度差，卡片在暗底浮起）：
  - lavender `#0f1011` → `#1c1d20`
  - crimson `#1f0d0d` → `#341717`
  - gold `#1e1909` → `#322a12`
- 提亮卡片描边 `--palette-neutral-45`（暗底可见）：
  - lavender `#23252a` → `#34373f`
  - crimson `#3e1c1c` → `#4e2424`
  - gold `#3c3315` → `#4d421a`
- 页面底 `--palette-neutral-40`、文本档、品牌/功能色、其余 7 套主题均未改动；仅改 3 套暗色各 2 键（共 6 键），业务代码零改动。语义令牌层（`--color-card-bg`/`--color-border-card`）自动跟随。

### 影响范围
- 仅改 global.scss 三套暗色主题的 6 键；亮色主题与卡片层级对比机制不受影响。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。注：settings 页「黑色头像」（无图时透黑卡底）属组件占位缺陷，本次按用户选择未处理，留待后续 C 方案。

---

## 2026-08-04（删除）- settings 用户资料卡右上角渐变光晕及冗余令牌

### 变更类型
删除（前端 / 设计系统 / 细节打磨）

### 决策
用户要求删除资料卡右上角渐变光晕，相关冗余颜色定义一并清理。

### 删除内容
- `frontend/pages/index/settings.vue`：模板中 `<view class="settings-page__profile-card-glow">` 节点；基础样式块 `.settings-page__profile-card-glow`（固定半径径向渐变）；平板断点（≥768px）中的同名覆盖块。
- `frontend/assets/styles/global.scss`：删除 `--color-brand-glow-mid` 与 `--color-brand-glow-faint` 两令牌的**全部 11 处定义**（默认块 1 处 + 9 套非 green 主题各 1 处，共 11 组）。此二令牌仅被上述光晕使用，无其他引用点。

### 保留项
- `--color-brand-glow` / `--color-brand-glow-soft` / `--color-brand-glow-strong`（被 `BeginnerGuide.vue` 引导高亮使用，与本次删除的 `-mid`/`-faint` 无交集，保留）。

### 影响范围
- 仅删除未使用的光晕视图/样式与专属令牌，业务代码零改动；10 套主题下资料卡不再有右上角渐变。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。全局搜索 `profile-card-glow`/`brand-glow-mid`/`brand-glow-faint` 已无残留引用。

---

## 2026-08-04（优化·已废弃）- settings 用户资料卡光晕相关优化（已被上方删除取代）

> 以下三条为光晕存在的迭代记录，最终由"删除光晕"取代，留痕备查：① 光晕柔化为圆形径向渐变；② 边界不显、渐变聚拢头像旁；③ 头像右侧硬边界消除（圆心推到卡片右缘外 + 固定半径渐变）。

---

## 2026-08-04（优化）- settings 用户资料卡光晕柔化（消除方块棱角）

### 变更类型
优化（前端 / 设计系统 / 细节打磨）

### 问题
settings 页用户资料卡右上角光晕（`profile-card-glow`）原为方形线性渐变（320rpx×320rpx，`border-top-right-radius:48rpx`），边缘生硬、呈现为「正方形色块」，与卡片整体调性不协调。

### 修复（`pages/index/settings.vue`）
- 基础样式：改为**圆形径向渐变**从右上角发散——`width/height:400rpx`，`border-radius:50%`，`background: radial-gradient(circle at top right, var(--color-brand-glow-mid) 0%, var(--color-brand-glow-faint) 45%, transparent 72%)`。0% 实色 → 45% 淡 → 72% 透明，三层过渡柔和无硬边，自然消融入卡片底色。
- 平板断点（≥768px）：原 `width:160px; height:160px; border-top-right-radius:24px` 同步改为 `width:200px; height:200px; border-radius:50%`，与基础样式一致为圆形。

### 影响范围
- 仅改 settings.vue 光晕样式（基础 + 平板断点），仍引用随主题切换的 `--color-brand-glow-mid/faint`，10 套主题下均为柔和圆形光晕。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（优化）- settings 光晕边界不显、渐变聚拢头像旁

### 变更类型
优化（前端 / 设计系统 / 细节打磨）

### 问题
前版圆形光晕（400rpx、圆心在右上角）在卡片内仍可见明显边界/色块边缘，生硬。

### 修复（`pages/index/settings.vue`）
- 放大光晕至 `560rpx`（平板 `320px`），圆心由「右上角」改为「右侧头像纵向中心」（`circle at right center` + `top:50%; transform:translateY(-50%)`），使柔光聚拢在头像周围。
- 渐变提前归零：`0%` 实色 → `25%` 淡 → `52%` 完全透明（原 45%/72%）。大圆 + 提前归零使过渡半径远超卡片，边界绝不显现在卡片可视区内，头像旁仅留一抹柔光。
- 平板断点同步 `320px`。

### 影响范围
- 仅改 settings.vue 光晕样式（基础 + 平板断点），仍引用随主题切换的 glow 令牌，10 套主题下头像旁柔光无生硬边界。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（优化）- settings 光晕头像右侧硬边界消除

### 变更类型
优化（前端 / 设计系统 / 细节打磨）

### 问题
上版（圆心在卡片右缘 `right:0`、最远角尺寸、52% 透明 ≈206rpx）头像右沿距卡片右缘仅 48rpx，仍落在较实 glow 区内，被卡片 `overflow:hidden` 在右缘硬切 → 头像右侧出现明显竖直边界。

### 修复（`pages/index/settings.vue`）
- 改用**固定渐变半径**写法 `radial-gradient(circle 110rpx at right center, ...)`，半径不受圆块尺寸/最远角影响、完全可控。
- 圆心推到卡片右缘**外侧** `right:-120rpx`：卡片右边缘距圆心 120rpx > 渐变 100% 半径 110rpx → 右缘处已完全透明，硬边界消失。
- 柔光只在头像右侧外一小圈（距右缘 48rpx 内，距圆心 72rpx<110rpx 有淡光），向左侧自然淡出。0% 实色 → 60% 淡 → 100% 透明。
- 平板断点：固定半径渐变继承，仅覆盖 `right:-80px; width/height:200px`。

### 影响范围
- 仅改 settings.vue 光晕样式，仍引用随主题切换的 glow 令牌；10 套主题下头像右侧无硬边界、柔光聚于头像旁。编译通过（`build:mp-weixin` DONE Build complete，仅 sass 弃用警告）。

---

## 2026-08-04（修复）- settings 页底部背景不随主题 + 卡片绿渐变不随主题

### 变更类型
修复（前端 / 设计系统 / 细节打磨）

### 问题 1：settings 页底部背景不随主题切换
- 现象：小程序端切主题后，settings 页底部（内容不足一屏时）显示默认绿底，不随主题变。
- 根因：`.settings-page` 未设 `min-height:100vh`，内容不足一屏时多出高度由原生 `page` 承载；小程序端 `page` 不携带 data-theme，其 `background-color: var(--page-bg-color)` 取 `:root,page` 默认值（绿）。H5/App 端 `.app-root` 携带 data-theme 故正常。
- 修复（`pages/index/settings.vue`）：`.settings-page` 加 `min-height: 100vh`（box-sizing:border-box 含 padding，内容不足一屏恰占一屏、无多余滚动条；超屏自然滚动）。页面根 view 带 data-theme，其 `var(--page-bg-color)` 解析为当前主题色，覆盖整个视口，消除底部色差。

### 问题 2：用户资源卡片绿色渐变不随主题切换
- 现象：settings 页用户资料卡的右上角绿色渐变光晕（`profile-card-glow`，用 `--color-brand-glow-mid`/`--color-brand-glow-faint`）在所有主题下均为固定绿，不随主题变。
- 根因：global.scss 语义令牌层 `--color-brand-glow-*` 写死为绿色 rgba（默认值仅 green 主题正确）。
- 修复（`frontend/assets/styles/global.scss`）：为 9 套非 green 主题（`ink/indigo/lavender/cyan/amber/coral/rose/crimson/gold`）的 `[data-theme]` 覆盖块注入 `--color-brand-glow-mid` 与 `--color-brand-glow-faint`，值取各自 `--palette-brand-light`（品牌浅色）派生的 rgba（alpha 沿用 green 的 0.45/0.18）。green 主题无独立块，沿用默认绿 glow（正确）。卡片渐变随之随主题切换。`profile-card-glow` 的样式（仍引用 `--color-brand-glow-mid`/`faint`）无需改动。

### 影响范围
- 仅改 settings.vue（1 行 min-height）与 global.scss（9 套各加 2 行 glow 覆盖），业务代码零改动。settings 页底部背景与卡片渐变在 10 套主题下均正确随主题。编译通过，lint 0。

---

## 2026-08-04（修复）- 主题切换小程序端失效（App.vue template 不渲染进页面）

### 变更类型
修复（前端 / 设计系统 / 多端换肤核心链路）

### 变更背景
上一条修复（语义令牌声明扩展为 `:root, page, [data-theme]`）解决了 CSS 变量解析层级问题，但用户反馈小程序端切换主题仍无效。深入排查编译产物发现**真正根因**：uni-app 小程序端 `App.vue` 的 `<template>`（`.app-root` with `:data-theme`）**根本不会渲染进页面**——每个页面加载的是自己的 wxml（如 `profile.wxml`），不包含 `App.wxml` 内容。因此 `[data-theme]` 选择器在页面里**无任何元素匹配**，主题覆盖块与语义令牌块都不命中 → 主题永远不变。（H5 端 App.vue template 作为 SPA 根组件会渲染，故 H5 端上一条修复有效；小程序端需另行处理。）

### 修复内容
1. **`frontend/main.js`**：注册全局 `themeMixin`，提供响应式 computed `themeKey`（`= useThemeStore().current`）。所有组件（含页面）自动获得 `themeKey`，无需各自 import store。
2. **14 个页面根 view 绑定 `:data-theme="themeKey"`**：`pages/index/{index,notification,plan,record,settings}.vue` + `pages/user/{announcement,contact,forgot-password,help,login,messages,privacy,profile,register}.vue`，均在 `<template>` 第一个 `<view>` 标签加 `:data-theme="themeKey"`。这样每个页面根 view 自身携带 data-theme，`[data-theme="x"]` 覆盖块命中，`--palette-*` 在页面根覆盖，语义令牌（`[data-theme]` 声明）引用覆盖后的值 → 切换即时生效。
3. **保留**上一条 `global.scss` 语义令牌 `:root, page, [data-theme]` 声明（必要前提）与 `App.vue` 根 view `:data-theme`（H5 端双保险）。

### 响应式链路（全端通用，无需条件编译）
`profile.vue` `handleApplyTheme` → `themeStore.setTheme(key)` 改 `current` → 全局 mixin computed `themeKey` 重新计算 → 各页面根 view `:data-theme="themeKey"` 更新 → `[data-theme="x"]` 覆盖 `--palette-*` → 语义令牌 `var(--palette-*)` 解析为新值 → 页面颜色变化。小程序/H5/App 三端统一机制。

### 验证（编译产物实测）
- `npm run build:mp-weixin` 通过，无空 `@media` 块。
- 14/14 页面 wxml 根 view 含 `data-theme="{{xx}}"`（如 `profile.wxml`：`<view data-theme="{{bA}}" class="profile-page">`）。
- `profile.js` 确认 `bA: n.themeKey`（`n` 为组件实例代理，`themeKey` 为 mixin computed，响应式绑定 `themeStore.current`）。
- `themeKey` computed 注入全部 14 页 + `app.js`。

### 影响范围
- 修复后 10 套主题在微信小程序 / H5 / App 端均可正确切换。仅新增一个全局 computed + 14 页根 view 一个属性，未改任何页面业务逻辑，不引入新 bug。lint 0。

---

## 2026-08-04（修复）- 主题切换全端失效（语义令牌声明层级错误）

### 变更类型
修复（前端 / 设计系统 / 多端换肤核心链路）

### 变更背景
用户反馈：个人信息页选择主题并提交后，页面颜色始终为默认主题（青绿），切换不生效。深入排查确认**该 bug 与具体哪套主题无关，是整套换肤机制的底层缺陷，之前 4 套主题实际也从未真正生效**。

### 根因（CSS 变量就近解析原则）
`global.scss` 中 Palette 原始色板与语义令牌（`--color-*`/`--shadow-*`/`--color-mask-*`/`--color-brand-glow-*`）均声明在 `:root, page`（最顶层 html / 小程序原生 page）。主题覆盖块 `page[data-theme], [data-theme]` 仅把 `--palette-*` 覆盖在**带 data-theme 的子元素**（App.vue 的 `.app-root` view）上。而语义令牌内部的 `var(--palette-*)` 在「声明它的元素」（page/html）作用域解析，取的是 page/html 自身的 `--palette-*`（原始值），**不会**取子元素 `.app-root` 覆盖后的值 → 所有 `--color-*` 不变 → 切换无效。小程序端更彻底：App.vue 根 view 的 `data-theme` 不会让原生 `page` 命中 `page[data-theme]`，`[data-theme]` 子视图的变量同样传不到 page 语义层。

### 修复内容（frontend/assets/styles/global.scss）
- 将语义令牌块的声明选择器由 `:root, page` 扩展为 `:root, page, [data-theme]`。现在语义令牌也在「带 data-theme 的根节点」（`.app-root`）上声明，其内部 `var(--palette-*)` 在 `.app-root` 作用域解析，拿到该节点被覆盖后的 Palette 值 → 切换主题即时生效。Palette 原始声明仍只在 `:root, page`（作为默认/兜底真源），单一真源不破坏。
- 配套确认（已编译验证）：App.vue 模板 `<view class="app-root" :data-theme="themeStore.current">` 在微信小程序产物 `App.wxml` 渲染为 `<view data-theme="{{a}}">` 包裹 router-view，H5/App 端同理命中 `[data-theme]`；覆盖块编译为 `page[data-theme=ink],[data-theme=ink]`（属性值无引号），通用 `[data-theme]` 段命中根 view。

### 影响范围
- 修复后 green/ink/indigo/lavender/cyan/amber/coral/rose/crimson/gold 共 10 套主题在微信小程序 / H5 / App 端均可正确切换，多端通用、无需条件编译、业务代码零改动。仅调整语义令牌声明作用域，未改任何页面/组件逻辑，不引入新 bug。编译 `build:mp-weixin` 通过（无空 @media 块陷阱），lint 0。已同步更新 MEMORY.md 关键约束。

---

## 2026-08-04（新增）- 多主题扩展：青碧 / 琥珀 / 珊瑚 / 玫瑰 / 绯红 / 流金（共 10 套）

### 变更类型
新增功能（前端 / 设计系统扩展）

### 变更背景
在既有「青绿 green / 墨黑 ink / 靛蓝 indigo / 薰衣草 lavender」基础上，按"差异化 + 符合项目调性"原则新增 6 套：
- **1 套冷色**：`cyan`（青碧，色相位于蓝绿之间，避开 indigo 蓝 / lavender 紫 / green 绿，冷色区唯一空位）。
- **5 套暖色**（色相轴铺开避免雷同）：`amber`（琥珀，橙黄）/ `coral`（珊瑚，红橙）/ `rose`（玫瑰，品红粉）/ `crimson`（绯红，正红+暗暖底）/ `gold`（流金，金棕+暗暖底）。
- 亮暗分布：cyan/amber/coral/rose 为亮色，crimson/gold 为暗色（中性阶整体反转为暗底、功能色暗底提亮），与 ink/lavender 形成暗色家族但调性各异。

### 前端（store/modules/theme.js）
- `THEME_LIST` 新增 6 项：`cyan`(#0891b2/#e0f6fa)、`amber`(#f59e0b/#fdf2dc)、`coral`(#ff6b5e/#ffe6e2)、`rose`(#e11d74/#fce2ee)、`crimson`(#dc2626/#3a1316)、`gold`(#c9a227/#2a2410)；`setTheme` 校验与持久化机制不变，自动出现于 profile.vue 主题选择器（现共 10 套）。

### 前端（global.scss 新增 6 个 [data-theme] 方案块）
- 选择器均写 `page[data-theme], [data-theme]`：同时覆盖小程序原生 `page` 与 App 端根 view（H5 经 App.vue 根 view），**多端通用，无需条件编译**。
- 每套**全量覆写 36 键**（中性 19 + 品牌 8 + 功能色 10），已脚本校验 6 套键集合均与默认一致（unique=36、无缺漏）；语义令牌与业务代码零改动。
- 暗色套（crimson/gold）中性阶整体反转：`neutral-0/40` 为暗底、`neutral-100` 为亮文本，与 lavender 暗色模式对齐；危险/错误/警告/链接色在暗底提亮保证对比度。
- 功能色约定（沿用已修复约束）：每套 `--palette-success` / `--palette-wechat` 均跟随该主题代表色（脚本校验 6 套相等），各主题完成态标识与微信渠道标识互不相同。

### 影响范围
- 多端通用：App/H5 经 App.vue 根 view `data-theme` 命中 `[data-theme]`；小程序经 `page[data-theme]` 命中。无原生组件色值例外，配色真源仍仅 global.scss 一处。
- profile.vue 主题选择器原为 `flex-wrap` 网格，10 个色块自动换行无溢出，无需改动布局。新增主题纯 CSS 变量覆盖，不影响既有 4 套及任何现有功能。lint 0。

---

## 2026-08-04（新增）- 多主题（颜色切换）支持

### 变更类型
新增功能（前端 / 设计系统扩展）

### 变更背景
用户希望支持多主题换肤，在 `profile.vue`「指纹登录」下方新增「主题」入口，点击展开可选主题，默认选中当前主题，主题以代表色命名；新增「墨黑」主题（配色参考 DESIGN-vercel.md），多端通用（H5/小程序/App）实现。

### 前端（新增 store/modules/theme.js）
- `useThemeStore`：Pinia store，`current` 持久化于 `uni.setStorageSync('app-theme')`，默认 `'green'`（青绿）；`setTheme(key)` 校验后写入并持久化。
- `THEME_LIST`：`green`（青绿）/ `ink`（墨黑）两套，`swatch`/`swatchBg` 供选择器色块预览。

### 前端（global.scss 新增 [data-theme="ink"] 方案块）
- 选择器 `page[data-theme="ink"], [data-theme="ink"]`：覆盖小程序原生 `page` 与 App 端根 view。
- 仅覆写 `--palette-*` 原始色板（近黑 ink #171717 + canvas #fafafa + hairline #ebebeb），语义令牌与业务代码零改动；微信绿/链接蓝/危险红等功能色不变。

### 前端（App.vue 根节点绑定 data-theme）
- 根 `<view class="app-root" :data-theme="themeStore.current">` 包裹 `<router-view>`，使 CSS 变量全站级继承，换肤即时生效（多端统一入口）。

### 前端（profile.vue 新增「主题」区块）
- 分组项 `主题` + 展开区 `profile-page__theme-list`（参考「修改头像」模式）：色块 + 名称，`--selected` 态引用 `--color-brand-bg` / `--color-selected-bg`。
- `themeForm.selected` 默认取 `themeStore.current`；`handleApplyTheme` 调 `themeStore.setTheme` 后 `toggleSection('theme')` 收起；收起时 `resetSection('theme')` 还原。
- 新增 `profile-page__theme-*` 样式与 ≥768px 断点锁定。

### 影响范围
- 多端通用：App/H5 经 App.vue 根 view `data-theme` 命中 `[data-theme]`；小程序经 `page[data-theme]` 命中。无原生组件色值例外，配色真源仍为 global.scss 单一处。lint 0。

---

## 2026-08-04（新增）- 多主题扩展：靛蓝 / 薰衣草

### 变更类型
新增功能（前端 / 设计系统扩展）

### 变更背景
在既有「青绿 green」+「墨黑 ink」基础上，按"同主色家族只取一个、不重复绿系"原则，新增两个主题：
- 「靛蓝 indigo」对齐 Stripe DESIGN（蓝系唯一代表，亮色，体量在候选蓝系中最大）；
- 「薰衣草 lavender」对齐 Linear DESIGN（紫系唯一代表，暗色系，与 ink 同族不同调，区分度好）。
筛选参考 getdesign.md 的 Installs（安装/采用）作为代理指标；Vercel(=ink)、Supabase/Spotify(绿系冲突)、Notion(紫系调性跳) 已排除。

### 前端（store/modules/theme.js）
- `THEME_LIST` 新增 `indigo`（靛蓝，swatch `#533afd`/swatchBg `#eef0fe`）与 `lavender`（薰衣草，swatch `#5e6ad2`/swatchBg `#ecedfb`）两项；`setTheme` 校验逻辑与持久化机制不变，自动出现于 profile.vue 主题选择器。

### 前端（global.scss 新增 [data-theme="indigo"] / [data-theme="lavender"] 方案块）
- 选择器均写 `page[data-theme], [data-theme]`：同时覆盖小程序原生 `page` 与 App 端根 view（H5 经 App.vue 根 view），**多端通用，无需条件编译**。
- 仅覆写 `--palette-*` 原始色板，**全量 36 键对齐默认主题**（中性灰阶 0–100 共 19 + 品牌 8 + 功能色 10，已脚本校验三者键集合完全一致、missing/extra 均为空），语义令牌与业务代码零改动。
- 功能色约定：微信绿 `#07c160` / 成功绿 独立保留（不随主题变，与 ink 一致）；indigo 链接蓝→靛蓝、品牌浅色/选中态为浅靛蓝系；lavender 近黑 canvas `#010102`+亮墨文本+暗灰 hairline，唯一 chromatic accent=薰衣草蓝。

### 影响范围
- 多端通用：App/H5 经 App.vue 根 view `data-theme` 命中 `[data-theme]`；小程序经 `page[data-theme]` 命中。无原生组件色值例外，配色真源仍仅 global.scss 一处。新增主题纯 CSS 变量覆盖，不影响 green/ink 及任何现有功能。lint 0。

---

## 2026-08-04（修复）- 微信绿/成功绿随主题更换（消除多主题同色）

### 变更类型
修复（前端 / 设计系统一致性）

### 变更背景
初版 indigo/lavender 将 `--palette-success` 与 `--palette-wechat` 固定为 `#07c160`（与 green 同），违反"随对应主题更换"原则，导致四个主题成功绿/微信绿完全相同。

### 修复内容（global.scss）
- 四主题的 `--palette-success` 与 `--palette-wechat` 均改为各自主题代表色，互不相同：
  - green：`#2ead4b` / `#07c160`（绿系）
  - ink：`#0070f3` / `#0070f3`（墨黑蓝，原 success 已随主题，本次补 wechat 同步）
  - indigo：`#533afd` / `#533afd`（靛蓝）
  - lavender：`#5e6ad2` / `#5e6ad2`（薰衣草）
- 语义层 `--color-success`（`var(--palette-success)`）与 `--color-wechat`（`var(--palette-wechat)`）自动跟随，完成状态标识与微信渠道标识底色/角标随主题变色。
- 同步更新 profile.vue 指纹开关开态注释、notification.vue 微信徽标/角标注释（原"微信绿 #07c160"表述已不符）。

### 影响范围
- 功能色随主题统一更换，四主题视觉互补（绿/黑/蓝/紫）；引用点（指纹开关开态、微信渠道徽标）均为纯视觉标识，无功能逻辑依赖固定色值。键集合不变，多端通用，lint 0。

---

## 2026-08-04（重构）- 消除 profile 原生 switch 例外，纯 CSS 开关支持 var()

### 变更类型
重构（前端 / 设计系统收尾）

### 变更背景
配色真源收敛后，仅 `profile.vue` 的 App 端 uni `switch` 原生 `color="#07c160"` 因原生属性编译期取色、不支持 `var()` 成为唯一例外。为达成「严格单一配色真源」，将其重写为纯 CSS 手写开关。

### 前端（profile.vue，仅 #ifdef APP-PLUS）
- 删除原生 `<switch color="#07c160">`，改为 `<view class="profile-page__biometric-switch" :class="{'--on':biometricEnabled}" role="switch" :aria-checked @click="toggleBiometric">` + 内部 `.profile-page__biometric-switch-knob`。
- 样式：轨道开态 `var(--color-wechat)`、关态 `var(--color-border-input)`，滑块 `var(--color-text-inverse)`，过渡 `background/transform 0.2s`；`@click` 直接调 `toggleBiometric()`（函数内部按 `!biometricEnabled` 翻转，与原 `@change` 行为等价），保留账号待删除态禁用与「请先账号密码登录」提示逻辑不变。
- 交互语义：加 `role="switch"` / `:aria-checked` 维持无障碍；尺寸 88×48rpx 贴近原缩放后 switch。

### 影响范围
- 全量复核：frontend 下 vue 文件已无任何散落色值（`#07c160` 仅存于注释说明等价关系），真正达成「只有 global.scss 一处配色真源」。
- App 端指纹开关视觉与原版一致（微信绿开态），lint 0。

---

## 2026-08-04（重构）- 散落色值全量迁移至语义令牌，确立单一配色真源

### 变更类型
重构（前端样式 / 设计系统）

### 变更背景
设计令牌三层架构已就位，但各页面/组件仍散落写死 `#xxxxxx` 与 `rgba(...)`（卡片阴影、弹层阴影、遮罩、品牌绿光晕、装饰渐变）。本次将全部散落色值迁移到 `global.scss` 语义令牌，使配色真源收敛至唯一一处。

### 前端（global.scss 新增语义令牌）
- 阴影：`--shadow-card`（卡片）、`--shadow-popup`（弹层/悬浮卡 `0 4px 6px -4px…/0 10px 15px -3px…`）、`--shadow-float`（高亮浮起 `0 4px 12px`）、`--shadow-body-center`（H5 居中容器投影 `0 0 12px`）。
- 遮罩：`--color-mask-40/50/60`（半透明黑 0.4/0.5/0.6，分别用于公告/首页满屏、通知页、引导蒙版）。
- 品牌绿光晕：`--color-brand-glow`（0.6）、`--color-brand-glow-soft`（0.4）、`--color-brand-glow-mid`（0.45，渐变专用）、`--color-brand-glow-strong`（0.8）、`--color-brand-glow-faint`（0.18，渐变收尾）。

### 前端（引用迁移）
- 12 个文件共 50+ 处散落 `rgba/hex` 改为语义令牌：所有 `0 1px 2px…rgba(0,0,0,0.05)` 卡片阴影→`--shadow-card`（含带 `inset` 边框的拼接写法）；BottomNav/announcement/notification/plan 弹层→`--shadow-popup`；BeginnerGuide 高亮光晕与蒙版→`--color-brand-glow-*` / `--color-mask-60`；AnnouncementCard/index 满屏遮罩→`--color-mask-40`；notification 遮罩→`--color-mask-50`；settings 装饰渐变→`--color-brand-glow-mid`/`--color-brand-glow-faint`。
- `uni.scss` 删除三个零引用且无关的 uni 颜色遗留变量（`$uni-color-title/subtitle/paragraph`），剩余 `$uni-*` 均注释「对齐 --color-*」。

### 唯一例外（平台限制）
- `profile.vue` 的 uni `switch` 原生组件 `color="#07c160"`：原生属性编译期取色、不支持 `var()`，无法引用令牌；已在注释标明其值等价于 `--color-wechat`，配色真源仍在 global.scss。

### 影响范围
- 运行时视觉表现与原版**完全等价**（令牌值逐一对应原散落值），仅做取值抽象。
- 全量复核：vue 文件除 profile switch 原生属性外无任何散落色值；scss 仅 global.scss（真源）与 uni.scss（对齐注释兼容层）含色值。实现「只有 global.scss 一处配色真源」。

---

## 2026-08-04（重构）- 配色抽象为设计令牌（Design Tokens）三层架构

### 变更类型
重构（前端样式 / 设计系统）

### 变更背景
原 `global.scss` 的 `page{}` 内直接硬编码全部颜色 hex，所有 `--color-*` 散落无分组；另一套 `uni.scss` 的 `$uni-*` 默认色与项目配色并行且值不一致，且从未被业务代码引用。若后续要新增配色方案（如深色模式、其他品牌色），需逐个改动散落变量，无法一处切换。

### 前端
- `frontend/assets/styles/global.scss`：重构为三层设计令牌。
  1) **Palette 原始色板**（`--palette-*`）：按中性灰阶 / 品牌绿系 / 功能色集中所有基础 hex，为配色方案唯一定义源。
  2) **Semantic 语义令牌**（`--color-*`）：业务代码唯一引用层，全部由 Palette 派生；名称与旧版完全一致，**19 个文件、345+ 处引用零改动**。
  3) **Scheme 配色方案**：令牌同声明于 `:root` 与 `page`（page 承载微信小程序原生根），通过 `:root[data-theme]` / `page[data-theme]` 切换；默认 light 已内联，文件末尾预留 `[data-theme="dark"]` 骨架注释。
  - H5 居中媒体查询 `html` 背景由硬编码 `#e8ebe6` 改为 `var(--page-bg-color)`，随方案切换。
  - **扩展方式**：新增配色方案 = 在文件末尾追加一个 `[data-theme="xxx"]` 块覆盖对应 `--palette-*`，业务代码零改动。
- `frontend/uni.scss`：顶部声明「配色真源在 global.scss」，`$uni-*` 取值对齐到同一套语义令牌，消除双源混乱（变量本身仍未被业务引用，仅作兼容兜底）。

### 影响范围
仅样式令牌定义与注释，不改动任何页面/组件取值逻辑；运行时视觉表现与原版完全一致（light 方案等价旧值）。

---

## 2026-08-04（修复）- 删除通知渠道同步清理计划-渠道关联

### 变更类型
修复（数据一致性）

### 变更背景
旧计划可能关联过已被删除的通知渠道，导致编辑计划时 `availableChannels` 中找不到该渠道、提交被 `_validate_channel_ownership` 拦截报错。原 `delete_channel` 仅删除渠道行，未清理 `plan_notification_channels` 中引用该渠道的关联行（该表 `channel_id` 无物理外键约束），从而遗留孤儿关联。

### 后端
- `services/notification_channel_service.py`：`delete_channel` 在删除渠道行前，先用 `delete(PlanNotificationChannel).where(channel_id==...)` 清理所有引用该渠道的计划-渠道关联。**仅删除关联，不删除计划本身**。
- 同步导入 `delete`（sqlalchemy）与 `PlanNotificationChannel`（models.plan）。

### 影响范围
- 后续删除任意通知渠道（邮件/微信/App 推送，站内信本就禁删）时，引用它的计划不再携带孤儿关联。
- 历史已存在的孤儿关联行无害：前端 `plan.vue` 编辑时已用 `validIds` 过滤掉不存在的渠道 id，后端提交校验亦会拒绝，本次改动属防御性加固。
- 集成测试 `test_notification_api.py` + `test_plan_api.py` 共 48 项全部通过；无新增 lint 警告。

---

## 2026-08-04（新增）- App 端指纹一键登录 + Token 静默续期

### 变更类型
新增（App 端生物识别登录 / 多端 JWT 静默续期）

### 变更背景
个人开发者、无微信开放平台资质、暂不做 iOS 的场景下，需要一种「免手机号、不依赖第三方授权」的一键登录方案。在已有账号密码 + JWT 体系基础上，新增 App 端指纹一键登录（参考主流 App：首次账号密码登录后，后续可指纹直接登录），并补齐多端 token 静默续期（默认进首页、仅刷新有效期）。

### 后端
- 新增独立表 `user_biometric_tokens`（业务库 wuzuniao_yonghu）：`id/user_id/token(高熵随机串,唯一)/device_id/expire_at/created_at/updated_at`，`user_id+device_id` 联合索引。SQL：`sql/add_user_biometric_tokens.sql`（增量）+ `sql/create_user_db.sql`（同步表定义）。
- `user_service.py` 新增生物识别凭证管理：`issue_biometric_token`（登录/重置密码成功时按 device_id 签发 31 天有效凭证）、`refresh_biometric_token`（已登录续期时顺延该设备凭证有效期）、`revoke_biometric_tokens`（改密/退出/删除账号时作废全部凭证）、`verify_biometric_token`（指纹登录时校验 token 有效 + device_id 匹配）。
- `users.py` 新增接口：`POST /refresh-token`（JWT 保护，复用 get_current_user_id 校验，重新签发 access_token 并可选顺延生物识别凭证）、`POST /biometric-login`（公开，校验 token+device_id 后签发 JWT）。`login`/`reset-password` 接受可选 `device_id`，成功时在响应 `data` 额外返回 `biometric_token`（前端加密存储）。

### 前端
- `composables/useBiometric.js`（`#ifdef APP-PLUS` 包裹，依赖 @noble/ciphers + @noble/hashes）：`getDeviceId`（首次登录生成 UUID 设备标识）、`isAvailable`（SOTER 指纹能力检测）、`authenticate`（系统级指纹验证，authContent:'指纹验证已登录'）、`storeBiometricToken/getBiometricToken/clearBiometricToken`（PBKDF2 高迭代派生 AES-256-GCM 密钥加密/解密 biometric_token）。
- `composables/useTokenRefresh.js`（多端共用）：解码本地 JWT exp，剩余不足 1 天调 refresh-token 换新 token（保留 userInfo）；从本地 `biometricDeviceId` 读取 device_id 一并续期。
- `api/modules/user.js`：新增 `biometricLogin`/`refreshToken`，`loginUser`/`resetPassword` 增加可选 `device_id`。
- `login.vue`：复用一键登录卡片，App 端设备支持指纹且本地有凭证时显示指纹图标（dl_fingerprint.png）作为一键登录入口；账号密码登录成功且已开启指纹时本地加密存储凭证。
- `profile.vue`：修改邮箱项正下方、同分组内新增「指纹登录」switch 开关项（`#ifdef APP-PLUS`，仅设备支持指纹+已登录时显示）；开启/关闭仅置位本地标记与凭证，关闭 ≠ 退出登录。
- `App.vue` onShow 调 `useTokenRefresh().tryRefresh()` 实现多端静默续期。
- 新增依赖 `@noble/ciphers`/`@noble/hashes`（package.json 登记）；新增指纹图标 `assets/images/dl_fingerprint.png`（本次经用户显式豁免 AGENTS.md「禁止二进制资源」约束引入，未修改规范文件）。

### 安全模型
- 纯前端加密（选项 B）：密钥由设备指纹 PBKDF2 派生，非硬编码；生物识别 token 限 31 天、每次需系统级指纹验证（SOTER 未通过拿不到签名）、绑定 device_id、改密/退出/删除即作废，将风险压到主流 App 可接受范围。
- 指纹登录不引入新用户身份，仅作为「已登录用户免输密码快速进入」或「凭本地凭证+指纹重建会话」的二次确认，后端仍依赖 JWT 体系校验。

### 影响范围
- 后端新增 1 表 + 2 接口 + 4 个 service 方法 + login/reset 下发凭证；部署需执行 `add_user_biometric_tokens.sql`。
- 前端新增 2 个 composable + 2 个 API 函数 + 1 个 PNG 图标；登录页/个人信息页/App.vue 改造。多端条件编译隔离，微信小程序端/H5 端功能与产物不受影响。
- 单元测试 123 项全部通过；集成测试需运行中的 MariaDB（Docker），当前环境未启动，属既有环境限制。

---

## 2026-08-03（修复）- 新手引导第三步「立即注册」重复出现

### 变更类型
修复（新手引导 / App 端非微信小程序端）

### 变更内容
- `frontend/pages/user/register.vue` 的 `goLogin()`（去登录）：在 `navigateTo` 返回登录页前，若当前引导正停在注册页 `register-card` 步骤，先调用 `guideStore.nextStep()` 推进引导。
- 根因：用户从注册页返回登录页时，`login` 页 `onShow` 的 `onPageEnter('login')` 判定上一步为 `register`（`register-card`），执行回退逻辑 → 又回到 `register-link` 步骤并高亮「立即注册」，导致引导重复。注册成功（`submitForm`）时已正确调用 `nextStep()` 推进，但「去登录」返回路径漏处理。

### 影响范围
- 修改文件：`frontend/pages/user/register.vue`（仅非微信小程序端新手引导路径；微信小程序端无注册引导，无影响）。
- 行为：注册页点击「去登录」返回后，引导直接停在步骤 4（通知方式，`settings` 页），不再回退到「立即注册」。

---

## 2026-08-03（修复）- App 打包报 WX_SUBSCRIBE_TEMPLATE_ID 未导出

### 变更类型
修复（App 端构建报错 / 条件编译导出）

### 变更内容
- 根因：`frontend/config/env.js` 中 `WX_SUBSCRIBE_TEMPLATE_ID` 被 `#ifdef MP-WEIXIN` 包裹，仅微信小程序端导出；而 `frontend/composables/useWechatSubscribe.js` 第 13 行 `import { WX_SUBSCRIBE_TEMPLATE_ID } from '../config/env'` 未做条件编译隔离，App 端打包时该名字不存在 → 「is not exported by config/env.js」构建失败。
- `frontend/config/env.js`：将 `WX_SUBSCRIBE_TEMPLATE_ID` 移出 `#ifdef MP-WEIXIN`，改为**全端始终导出**（模板 ID 属公开信息，无敏感风险）。使用处仍由 `useWechatSubscribe.js` 的 `#ifdef MP-WEIXIN` 隔离，非微信端取到空串由 `if (!TEMPLATE_ID)` 兜底，行为不变。

### 影响范围
- 修改文件：`frontend/config/env.js`。
- 仅修复 App（及任意非微信端）打包构建期静态解析失败；微信小程序端功能与产物无变化。需重新构建验证。

---

## 2026-08-03（优化）- App 启动秒开与防闪白配置

### 变更类型
优化（App 启动体验 / manifest 原生配置）

### 变更内容
- `frontend/manifest.json` 的 `app-plus.splashscreen`（顶层节点）：
  - `waiting` 由 `true` 改为 `false`，关闭系统等待圈/雪花动画，消除 App 启动时的开屏过渡感（这是「秒开」感的主要来源）。
  - 保留 `alwaysShowBeforeRender: true` + `autoclose: true`（首页渲染完即关，无露白）。
  - 删除非标准字段 `delay`（官方 splashscreen 节点无此参数）。
- `frontend/manifest.json` 的 `app-plus.distribute.splashscreen`：新增 `androidNavigationBarColor: "#e8ebe6"`，与首页背景色 `--page-bg-color: #e8ebe6` 一致，减少 Android 部分设备系统导航栏启动闪白（HBuilderX 5.0+ 生效）。

### 影响范围
- 修改文件：`frontend/manifest.json`（仅 App 端原生配置，小程序/H5 不受影响）。
- 说明：App 启动封面（splash）为 DCloud 强制必然存在、不可真正取消，本次为让其「一闪而过」达成秒开感；iOS 无对应颜色字段，iOS 闪白只能靠配启动图（png 二进制，按项目规范不引入）。上述配置仅云打包/自定义基座生效，本地运行基座不生效，需重新「发行 → 原生App-云打包」真机验证。

---

## 2026-08-04（修复）- 编辑计划无法勾选/提交 App推送

### 变更类型
修复（计划编辑 / 通知渠道联动）

### 变更内容
- **现象**：在通知方式页（App 端）添加 App 推送后，编辑旧计划时通知方式选项里看不到 App 推送、无法勾选；提交还报后端 400「包含无效或非本用户的通知渠道」。
- **根因**：`plan.vue` 的 `availableChannels` 仅在 `onMounted` 与 `handleNewEntry` 时加载。App 端页面常驻栈内、`onMounted` 不重跑，从通知方式页返回后列表不刷新，新加的 App 推送渠道不出现在编辑选项中；此外旧计划若曾关联过现已删除的渠道，其 `channel_ids` 含失效 id，提交即被后端校验拒绝。
- **修复（`frontend/pages/index/plan.vue`）**：
  1. `onShow` 中增加 `loadChannels()`，每次进入页面刷新通知渠道（覆盖从通知方式页返回场景）。
  2. `toggleEditPlan` 展开前 `await loadChannels()`，确保编辑时渠道最新。
  3. 编辑初始化时，仅保留当前 `availableChannels` 中仍存在的旧关联 id（剔除已删除/失效渠道），再补站内信默认勾选，避免提交校验报错。

### 影响范围
- 修改文件：`frontend/pages/index/plan.vue`（纯前端；后端 update_plan 逻辑对 App 推送无任何特殊处理，经测试验证正常）。
- 验证：后端 plan 相关测试 50 passed；前端 lint 0 错误。

---

## 2026-08-03（重构）- App 推送 channel_type 改中文存储

### 变更类型
重构（数据契约 / 命名统一）

### 变更内容
将 App 推送的 `channel_type` 由英文 `app_push` 改为中文 `App推送` 存储，与通知方式页展示名统一（替代此前方案 A 的前端 `channelTypeLabel` 映射）。

- **后端 `schemas/notification_channel.py`**：`CHANNEL_TYPE_APP_PUSH = "App推送"`（常量值变更）。其余 service/API/scheduler 对该类型的所有比较均经此常量引用，自动生效；经搜索确认无运行时硬编码 `'app_push'` 字面量（仅一处为注释）。
- **前端 `pages/index/plan.vue`**：移除 `channelTypeLabel` 映射函数，两处通知方式选项改回直接渲染 `ch.channel_type`（现为中文）。
- **前端 `pages/index/notification.vue`**：`hasAppPush` / `appPushChannel` 两处判断由 `=== 'app_push'` 改为 `=== 'App推送'`。
- **`sql/create_yao_db.sql`**：`channel_type`/`channel_value` 列注释与段落注释更新为 `App推送`。

### 影响范围
- 修改文件：上述 4 个文件。当前无实际数据落库，不涉及存量数据迁移。
- 类型判断、上报（POST /app-push）、启用更新（PUT /app-push）、调度下发（_send_app_push）逻辑均随常量自动一致；前端展示与判断同步为中文。

---

## 2026-08-03（修复）- 新建/编辑计划通知方式名称显示

### 变更类型
修复（UI 文案一致性）

### 变更内容
- `frontend/pages/index/plan.vue`：新建计划与编辑计划两处通知方式选项原本直接渲染 `ch.channel_type`，导致 App 推送显示为英文 `app_push`，与通知方式页「App推送」不一致。新增 `channelTypeLabel(type)` 映射（仅 `app_push`→`App推送`，其余中文类型原样返回），两处模板改渲染 `channelTypeLabel(ch.channel_type)`。

### 影响范围
- 修改文件：`frontend/pages/index/plan.vue`（纯前端展示修复，无逻辑/接口变更）。
- 仅新建/编辑计划页通知方式标签显示变化，勾选关联关系（按 channel id）不受影响。

---

## 2026-08-03（功能）- App 推送支持更新「是否启用」

### 变更类型
功能（通知方式页 / App 推送）

### 变更内容
参考微信通知方式「更新是否启用」的交互，为 App 推送增加一致的启用状态修改能力（原仅有添加/删除）。

- **后端 `schemas/notification_channel.py`**：新增 `UpdateAppPushChannel`（`channel_id` + `enabled`，channel_id 走正整数校验），参考 `UpdateWechatChannel`。
- **后端 `services/notification_channel_service.py`**：新增 `update_app_push_enabled(channel_id, user_id, enabled)`，校验渠道归属与 `channel_type=='app_push'`，更新 `enabled` 与 `updated_at`，不改动设备 token 数组；参考 `update_wechat_enabled`。
- **后端 `api/v1/notification_channels.py`**：新增 `@router.put("/app-push")` → `service.update_app_push_enabled`，校验失败抛 400；`UpsertAppPushChannel` 不变（仍负责设备 token 上报）。
- **前端 `api/modules/notification.js`**：新增 `updateAppPushChannel({ channel_id, enabled })` → `PUT /api/v1/notification-channels/app-push`，参考 `updateWechatChannel`。
- **前端 `pages/index/notification.vue`**：App 推送卡片点击展开「是否启用」单选 + 提交/取消表单（参考微信 `wechatEditExpanded`/`wechatEditForm`）；新增 `appPushEditExpanded`/`appPushEditForm` 状态与 `toggleAppPushEdit()`/`handleUpdateAppPush()`，均 `#ifdef APP-PLUS` 包裹；删除入口保留。

### 影响范围
- 修改文件：上述 5 个文件。后端零模型/字段变更（复用既有 `enabled` 列），前端无新增页面。
- 验证：后端 pytest 全量 327 项通过；前端 notification.vue lint 0 错误。App 端需云打包/自定义基座真机验证。

---

## 2026-08-03（修复）- vivo 开启 App 推送报 Not Found

### 变更类型
修复（App 推送 / 厂商 ROM 兼容性）

### 变更内容
- 根因：`useAppPush.js` 中 `requestPermission()` 调用了友盟插件 `requestNotificationPermission`，该实现在 vivo 等部分厂商 ROM 上会尝试跳转厂商特定设置页，目标不存在时抛 "Not Found"；此调用正是上轮「主动添加 App 推送时授权」引入的，对应点击「开启推送」路径。
- `frontend/composables/useAppPush.js`：`requestPermission()` 的 Android 分支改为直接用 `plus.android.requestPermissions(['android.permission.POST_NOTIFICATIONS'])` 申请标准系统通知权限（Android 13+ 弹系统授权、低版本默认授予，不依赖厂商页面、跨 ROM 稳定）；iOS 分支保留友盟 `requestNotificationPermission`；方法整体 try/catch，权限申请异常不阻断后续 `reportDeviceToken` 上报主流程。

### 影响范围
- 修改文件：`frontend/composables/useAppPush.js`。
- 仅 Android 端授权申请实现方式变化，行为语义不变（仍满足「主动添加时弹授权提示」诉求）；iOS 行为不变。需云打包/自定义基座真机验证 vivo。

---

## 2026-08-03（修复/上线收尾）- 友盟配置清理 + App 通知权限时机前移

### 变更类型
修复（云打包报错）/ 配置收尾 / 体验优化

### 变更内容
1. **上线前配置收尾**：`frontend/uni_modules/xtf-umengpush/utssdk/app-android/assets/umeng-push-config.json` 与 `app-ios/Resources/umeng-push-config.json`（占位模板）的 `android_umeng.debug`、`ios_umeng.debug` 由 `true` 改为 `false`；`ios.apsEnvironment` 由 `development`/`xxxx` 改为 `production`（iOS 本期不打包，仅保持配置一致）。
2. **修复云打包报错「友盟推送(UM-Push) 不合法」**：删除 `frontend/manifest.json` 中残留的 `app-plus.nativePlugins.UM-Push` 整块（该插件 ID 在 DCloud 插件市场不存在，HBuilderX 云打包校验失败；实际集成走本地 `uni_modules/xtf-umengpush`，与之无耦合）。保留 `modules.Push` 标准引擎模块；全仓库已无 `UM-Push`/`nativePlugins`/`appkey_android` 残留引用。
3. **App 通知权限申请时机前移**：`frontend/composables/useAppPush.js` 从 `initUmeng()` 移除自动权限申请（避免 App 启动即弹系统授权），新增独立 `requestPermission()`（包装友盟 `requestNotificationPermission`，仅 App 端生效）并加入导出；`frontend/pages/index/notification.vue` 在用户主动点击「开启推送」（`handleEnableAppPush`）时于 `reportDeviceToken` 前调用 `requestPermission()`。点击监听、token 上报逻辑不变。

### 影响范围
- 修改文件：`frontend/manifest.json`、`frontend/uni_modules/xtf-umengpush/utssdk/app-android/assets/umeng-push-config.json`、`frontend/uni_modules/xtf-umengpush/utssdk/app-ios/Resources/umeng-push-config.json`、`frontend/composables/useAppPush.js`、`frontend/pages/index/notification.vue`。
- 原生层/uni_modules 改动需重新云打包或自定义基座生效；后端零改动。

---

## 2026-08-03（重构）- App 推送改用 xtf-umengpush 插件集成（替换错误 UM-Push 标识）

### 变更类型
重构（App 推送前端集成）

### 变更内容
将此前误填的、插件市场不存在的 `nativePlugins.UM-Push` 标识，替换为已购买的 `xtf-umengpush` 普通授权版插件（绑定 `__UNI__528E611` / 包名 `com.wuzuniao.yao`）。

- **`frontend/manifest.json`**：删除 `app-plus.nativePlugins.UM-Push` 整段（该插件 ID 在插件市场不存在），`nativePlugins` 置空；保留 `modules.Push` 与 `android.packagename=com.wuzuniao.yao`。
- **`frontend/uni_modules/xtf-umengpush/utssdk/app-android/assets/umeng-push-config.json`**：填写 `android.applicationId=com.wuzuniao.yao`、`android_umeng.appKey=6a700f2ce87a8d65f0a5902c`、`android_umeng.messageSecret=9cc21249944b199c55bfb0824979c95f`、`android_umeng.debug=true`、`channel=wuzuniao`；`vendors` 下七大厂商通道全部 `enabled:false`（本期不接厂商离线通道）。
- **`frontend/uni_modules/xtf-umengpush/utssdk/app-ios/Resources/umeng-push-config.json`**：`ios_umeng.appKey/messageSecret` 维持空占位（iOS 本期不打包）。
- **`frontend/composables/useAppPush.js`**（核心重写）：import 来源改为 `@/uni_modules/xtf-umengpush`（用 `#ifdef APP-PLUS` 包裹，避免小程序/H5 引用不存在导出）；新增幂等 `initUmeng()` 调 `initializeUmengPush(createUmengPushInitOptions())` 初始化 SDK 并申请通知权限；`getDeviceToken()` 改取 `getUmengPushDebugState().deviceToken`（友盟 deviceToken，首次未注册完延迟 1.5s 重试）；`registerPushClick()` 废弃 `plus.push` 的 `click` 事件，改用 `onUmengPushRuntimeEvent` 监听 `type==='notification-click'`，从 `event.payload` 字符串解析 `route` 跳转首页；对外方法签名不变（`getDeviceToken`/`reportDeviceToken`/`registerPushClick`）。
- **`frontend/App.vue`**：`onLaunch` 内 `#ifdef APP-PLUS` 调 `useAppPush().registerPushClick()`（内部已含幂等初始化）。
- 后端（`umeng_service.py`/`scheduler_service.py` 等）、`前端/api/modules/notification.js`、`pages/index/index.vue`、`pages/index/notification.vue` 均不变。

### 影响范围
- 修改文件：`frontend/manifest.json`、`frontend/uni_modules/xtf-umengpush/utssdk/app-android/assets/umeng-push-config.json`、`frontend/composables/useAppPush.js`、`frontend/App.vue`、`目录结构.json`（useAppPush.js / manifest.json / App.vue 描述）、`更新记录.md`（本条目）。
- 后端零改动（HTTP 下发、设备数组、失败回收逻辑与新插件完全兼容）。
- 真机验证需 HBuilderX 云打包或自定义基座（标准基座下友盟 SDK 不生效）；`debug:true` 上线前需改回 `false`。

---

## 2026-08-03（重构）- 拆分 AGENTS.md，新增 项目规范.md

### 变更类型
重构（项目文档）

### 变更内容
将 `AGENTS.md` 中固定的项目技术规范拆分到同目录新建的 `项目规范.md`，`AGENTS.md` 仅保留 AI 必须执行的规范，并要求每次对话先读取 `项目规范.md`。

- **新增 `项目规范.md`**：承载固定技术规范——项目概述、技术栈版本（锁定）、编码规范（Python 后端 / Vue 3 uni-app 前端 / MariaDB 数据库）、API 设计规范、环境配置（后端 .env / 前端 config/env.js / 前端工程布局与启动命令）、提交与版本控制。
- **重写 `AGENTS.md`**：仅保留 AI 执行约束——新会话初始化（强制读取 `项目规范.md`）、核心原则（先思考后编码 / 极简主义 / 手术式修改 / 目标驱动）、项目文档同步、成功标准与质量门禁、AI 特别约束（禁止事项）、参考文档优先级。行数从 231 行精简至 66 行（<120 行要求）。
- **同步修正与实际项目不符的描述**：
  1. 前端全局样式变量：原写「`uni.scss` 或 `assets/styles/variables.scss`」修正为「`uni.scss`（SCSS 变量）+ `assets/styles/global.scss`（CSS 设计令牌）」，因实际不存在 `variables.scss`。
  2. 项目目标：原仅写「微信小程序」修正为「微信小程序（兼 App / H5）」，因 manifest.json 已配置 mp-weixin / app-plus / h5 三端。
  3. 数据库名：原仅写 `wuzuniao_yao` 补充为「业务库 `wuzuniao_yao`、共享用户库 `wuzuniao_yonghu`」。
  4. App 端提醒方式：原「仅账号密码登录 + 邮件/站内信提醒」修正为「邮件/站内信/App 推送提醒」，因 2026-08-03 已新增友盟+ U-Push。
  5. 组件命名示例：原不存在的 `BaseButton.vue` 改为实际组件 `NoticeButton.vue`。
- **`目录结构.json`**：新增 `项目规范.md` 条目，更新 `AGENTS.md` 描述。

### 影响范围
- 新增文件：`项目规范.md`。
- 修改文件：`AGENTS.md`（整体重写）、`目录结构.json`（根目录两文件描述）、`更新记录.md`（本条目）。
- 纯文档变更，不涉及任何代码、配置、数据库结构变更；不影响前后端运行时行为。
- 后续 AI 会话须先读取 `项目规范.md` 再编码，原 AGENTS.md 中的技术规范约束效力不变（仅迁移位置）。

---

## 2026-08-03（新增）- App 端离线推送（友盟+ U-Push）

### 变更类型
新增（通知渠道）

### 变更内容
为 App 端（Android/iOS）新增系统通知栏离线推送，与站内信/邮件/微信订阅消息并列为第四种通知方式。不开通 uniCloud，直接由现有 FastAPI 后端调用友盟+ U-Push HTTP 接口下发。

**数据模型（不新增表、不新增字段）**
- 复用 `notification_channels`，新增 `channel_type='app_push'`，**每用户仅一行**，多设备共存于 `channel_value` JSON 数组：
  `{"device_tokens":[{"token":"...","platform":"android|ios","fail_count":0}]}`
- `fail_count` 为连续下发失败次数：成功归零，累计满 3 次剔除该设备；数组清空则删除整行渠道。

**后端**
- 新增 `backend/app/services/umeng_service.py`：`UmengService.send()` 单播下发，按 platform 取 Android/iOS 各自密钥，签名 `MD5(POST+URL+Body+MasterSecret)`；失败抛 `UmengPushError`。
- `core/config.py` + `.env.template`：新增 `UMENG_ANDROID_APP_KEY`/`UMENG_ANDROID_MASTER_SECRET`/`UMENG_IOS_APP_KEY`/`UMENG_IOS_MASTER_SECRET`/`UMENG_PRODUCTION_MODE`（仅 iOS 生效）/`UMENG_PUSH_PAGE`。iOS 侧仅保留配置项占位，暂不填值。
- `schemas/notification_channel.py`：新增 `CHANNEL_TYPE_APP_PUSH`、`APP_PUSH_MAX_FAIL_COUNT=3`、`AppPushDeviceToken`、`AppPushChannelValue`、`UpsertAppPushChannel`。
- `services/notification_channel_service.py`：新增 `get_app_push_channel`/`upsert_app_push_token`/`parse_app_push_channel_value`。`create_if_missing` 区分「通知方式页添加（建行）」与「打卡刷新（不建行）」。
- `services/scheduler_service.py`：`NotificationDispatcher` 新增 `_send_app_push` 分支，遍历设备逐个下发、维护失败计数与设备回收；**无论多少设备按渠道只写 1 条 `notification_logs`**（任一成功=成功，全部失败=失败），复用既有 `idx_dedup` 防重键。
- `api/v1/notification_channels.py`：新增 `POST /api/v1/notification-channels/app-push`（JWT）；删除复用既有 `DELETE /`。渠道列表响应对 app_push 额外返回 `app_push_devices[{platform,fail_count}]` 与 `device_count`，**不回传 device_token 原文**。
- `sql/create_yao_db.sql`：补充 `notification_channels` 各 channel_type 的 `channel_value` 存储规范注释（表结构本身未变）。

**前端**
- 新增 `frontend/composables/useAppPush.js`（整体 `#ifdef APP-PLUS`）：`getDeviceToken`/`reportDeviceToken`/`registerPushClick`。
- `App.vue` `onLaunch`：注册 `plus.push` 的 `click` 监听，点击通知栏按 `payload.page`（缺省 `/pages/index/index`）`reLaunch` 到首页打卡。
- `pages/index/notification.vue`：新增「App推送」卡片（显示已登记设备数、可删除）、类型单选项与说明、`handleEnableAppPush`/`handleDeleteAppPush`；App 端默认选中「App推送」，保存按钮文案改为计算属性 `saveButtonText`。
- `pages/index/index.vue`：打卡成功后 `#ifdef APP-PLUS` 静默刷新设备标识（应对设备重装换 token），未添加通知方式时后端静默跳过。
- `api/modules/notification.js`：新增 `upsertAppPushChannel`。
- `manifest.json`：`app-plus.modules` 开启 `Push`，`nativePlugins` 配置友盟推送插件（Android AppKey `6a700f2c...`、Message Secret），补 `POST_NOTIFICATIONS`/`VIBRATE`/`RECEIVE_BOOT_COMPLETED` 权限。

**站内信页隔离说明**
- `services/notification_log_service.py` `list_znx_by_user` 原本已通过 JOIN 过滤 `channel_type='站内信'`，本次仅在注释中明确「仅查 channel_type=ZNX，App 推送/邮件日志不在此列」，**无逻辑改动**。

### 影响范围
- 仅 App 端新增能力；微信小程序端 / H5 端 UI 与逻辑通过 `#ifdef APP-PLUS` 完全隔离，无回归。
- 后端派发器为新增独立分支，站内信/邮件/微信三条既有链路未改动。
- 数据库表结构未变更，无需执行增量 SQL。
- 待补：iOS 应用需苹果开发者账号就绪后在友盟控制台创建并回填 `UMENG_IOS_*`；App 需经 HBuilderX 云打包集成友盟原生插件后方可实际收推。

---

## 2026-08-02（修复）- 修复首页词云在 App 端不显示

### 变更类型
修复（跨端兼容）

### 变更内容
首页未登录欢迎卡片的词云在微信小程序 / H5 端正常，App 端空白。排查定位到两处 App 端专有缺陷：

1. **词云数据从未生成（致命）**：`randomizeCloud()` 由 `onShow` 调用，而 App（uni-app v3）首次 `onShow` 早于页面 DOM 提交。此时 `createSelectorQuery().in(instance.proxy)` 查询的组件上下文尚未挂载，App 运行时会**静默丢弃查询、`exec` 回调永不触发**，`generateWords()` 从未执行，`cloudWords` 恒为 `[]`。微信端运行时则会以 `[null]` 触发回调从而走回退估算值分支——这正是同一份代码只有 App 端空白的原因。
   - 修复（`frontend/pages/index/index.vue` `randomizeCloud()` 的 `#ifndef H5` 分支）：先用估算尺寸 `upx2px(604)×upx2px(700)` **同步生成一次**保证词云必然可见，再包一层 `nextTick` 于 DOM 就绪后执行 `createSelectorQuery` 按实测尺寸精修落点；查询失败时保留兜底结果不重复生成，避免闪烁。
2. **`<text>` 绝对定位失效**：App 端 `<text>` 默认按行内文本节点渲染，行内元素上的 `position: absolute` + 百分比 `left/top` 不可靠（词会堆叠在容器左上角或不可见）。
   - 修复：`.index-page__welcome-word` 显式补 `display: block`，使其成为块级绝对定位元素，各端行为一致。

### 影响范围
- `frontend/pages/index/index.vue`（仅 `randomizeCloud()` 的 `#ifndef H5` 分支与 `.index-page__welcome-word` 样式）。
- H5 分支（`#ifdef H5`）逻辑未改动；微信小程序端行为等价（原本即走回退值再无二次查询，现改为兜底 + 实测精修，落点更准确），UI 无回归。
- `display: block` 对微信 / H5 端为幂等补充（两端 `<text>` 在绝对定位下本就按块级盒渲染）。

---

## 2026-08-02（优化）- 删除 App 端登录页「普通登录」引导步骤

### 变更类型
优化（新手引导）

### 变更内容
- `frontend/store/modules/guide.js`：`nonWechatSteps` 删除「普通登录」步骤（`target: 'login-card'`）。原第 3 步为：登录页立即注册 → 注册页卡片 → **登录页整个卡片高亮**（会覆盖输入框，不美观）→ 回设置页。删除后第 3 步仅保留「点击立即注册」与「注册页填写」两步，登录页只引导到「立即注册」，不再高亮整个登录卡片。
- `frontend/pages/user/login.vue`：移除 `useGuideTarget('login-card', ...)` 上报及卡片上的 `guide-target-login-card` class。
- `frontend/pages/user/register.vue`：注册成功后调用 `guideStore.nextStep()` 推进引导，避免返回登录页时残留注册卡片引导；登录成功回到设置页后正确衔接步骤 4。

### 影响范围
- 仅影响非微信小程序端（App/H5）新手引导第 3 步流程；微信小程序端步骤配置（`wechatSteps`）未改动。
- 引导步骤数（用户可见）不变，仍为 6 步。

---

## 2026-08-01（新增）- App（Android/iOS）打包适配，微信登录采用方案 A

### 变更类型
新增（跨端打包适配）

### 变更内容
在「不影响微信小程序与 H5 端功能/界面」前提下，补齐将项目发行为原生 App 所缺的工程配置。经排查，业务代码的跨端隔离已完备（微信一键登录、订阅消息、`wx.showShareMenu`、`page-meta` 均被 `#ifdef MP-WEIXIN` 包裹，`document.` 仅一处且在 H5 分支内），因此本次**未改动任何业务逻辑**，仅调整 4 处工程配置：

1. **`frontend/config/env.js`**：`API_BASE_URL` 拆为两个条件编译分支。`#ifdef APP-PLUS` 恒为生产域名 `https://yao.wuzuniao.com`；`#ifndef APP-PLUS`（小程序/H5）保持原有按 `NODE_ENV` 二分的逻辑，**行为完全不变**。原因：HBuilderX「运行到手机」时 `NODE_ENV` 为 development，若沿用 `localhost`，真机上指向手机自身导致接口全部失败；且 Android 9+ 与 iOS ATS 默认禁止明文 HTTP。
2. **`frontend/manifest.json`**：仅在 `app-plus` 段内增补，`mp-weixin`、`h5` 段未改动一字。新增 `safearea.bottom.offset="auto"`（交由编译器托管底部安全区，使 `BottomNav` 的 `position:fixed; bottom:30rpx` 在 iPhone 全面屏上不被 Home Indicator 遮挡）；`distribute.android` 补 `packagename="com.wuzuniao.yao"`、`minSdkVersion=21`、`targetSdkVersion=34`、`abiFilters=[armeabi-v7a, arm64-v8a]`、`INTERNET`/`ACCESS_NETWORK_STATE`/`ACCESS_WIFI_STATE` 三项权限（原为空数组，连网络请求都会失败）；`distribute.ios` 补 `dSYMs=false`、`idfa=false`；新增 `distribute.icons` 指向 `static/app-icons/` 下 12 个尺寸。
3. **`frontend/package.json`**：新增 `@dcloudio/uni-app-plus` 依赖（版本 `3.0.0-alpha-5010420260626001`，与既有 uni 全家桶严格一致，已验证 npm 上存在）；新增 `dev:app`、`build:app` 脚本，复用 `scripts/run-uni.js` 的 `UNI_INPUT_DIR` 包装。
4. **`AGENTS.md` 第 8.3 节**：补充 App 打包链路说明（HBuilderX 云打包输出 `unpackage/release/`、CLI 仅产出资源包不产出 apk/ipa）、包名约定、App 端接口地址规则、App 端不提供微信登录/订阅消息的说明，以及「跨端改动铁律」。

### 决策记录
- **App 端微信登录：采用方案 A —— 不做**，仅保留账号密码登录。现有 `#ifdef MP-WEIXIN` 隔离已使 App 端自动只渲染账号密码登录表单，无需任何代码改动，零风险。（方案 B 需申请微信开放平台移动应用并完成 300 元认证、打通 UnionID，且后端须新增 App 微信登录接口——App 端 `uni.login` 返回 accessToken/openid 而非小程序 code，现有走 code2session 的 `wechatLogin(code)` 无法复用。）
- **微信订阅消息**：App 端无对应能力，`useWechatSubscribe.js` 的 `#ifndef MP-WEIXIN` 降级提示已生效，不会报错。App 端提醒渠道为邮件 + 站内信。若后续要补齐推送，需接入 UniPush 并在后端 `SchedulerService` 增加推送分支，属独立工程。
- **底部安全区**：只采用 manifest 的 `safearea` 托管，**未**在 `BottomNav.vue` 追加 `env(safe-area-inset-bottom)`，避免两种机制叠加造成双重偏移。
- **后端 CORS 未改**：App 原生 webview 与小程序一样不携带 Origin 头，不受 CORS 约束。

### 影响范围
- `frontend/config/env.js`、`frontend/manifest.json`、`frontend/package.json`、`AGENTS.md`、`目录结构.json`
- 业务代码、页面、样式、后端**均未改动**

### 验证
- 微信小程序生产构建：`npm run build:mp-weixin` → `Build complete`。产物 `dist/build/mp-weixin/config/env.js` 内容为 `exports.API_BASE_URL="https://yao.wuzuniao.com"`，**仅一处声明**，证明 `#ifdef APP-PLUS` 分支已被编译期正确剔除，无重复声明、无地址丢失。
- H5 生产构建：`npm run build:h5` → `Build complete`。
- `manifest.json` 经 `JSON.parse` 校验合法。
- `npm view @dcloudio/uni-app-plus@3.0.0-alpha-5010420260626001` 确认该版本存在，与既有依赖版本号完全一致。

### 遗留事项（需人工处理，AI 不生成二进制资源）
- **App 图标未放置**：`manifest.json` 已引用 `frontend/static/app-icons/` 下 12 个文件，但目录尚未创建、图标尚未提供。用户指定使用 `hong.png`，但该文件位于 `frontend/assets/images/touxiang/hong.png` 且**仅 88x88 像素**，远低于 Android xxxhdpi 所需 192x192 与 iOS App Store 所需 1024x1024，直接放大会严重模糊并导致 App Store 审核驳回。需以其为设计稿重新导出 1024x1024 源图（正方形、不透明背景、无圆角），再生成各尺寸。所需文件清单见 `目录结构.json` 的 `frontend.static.app-icons` 节点。**在图标就位前执行云打包会使用 DCloud 默认图标或直接报错。**
- **真机实测项**：全项目 15+ 处 `min-height:100vh`（`global.scss:15` 及各页面）在 App webview 中不扣除状态栏，叠加全页 `navigationStyle:custom`，可能出现内容下移或底部溢出，需真机确认后再决定是否调整。

---

## 2026-08-01（优化）- 补齐 robots.txt/sitemap.xml 并对全部子页面做 SEO/GEO 优化

### 变更类型
优化（SEO/GEO）

### 变更内容
在「不影响微信小程序构建/功能/界面」前提下，从单页 SEO 升级为全站 SEO/GEO 优化：
1. **`frontend/public/robots.txt` + `frontend/public/sitemap.xml`（新增）**：覆盖 H5 全部 14 个可路由页面（按 pages.json 实际路由路径，history 模式无 .html 后缀），sitemap 含 lastmod/changefreq/priority 与中文 hreflang 备用链接；robots.txt 允许全部抓取并声明 sitemap 位置。
2. **`frontend/vite.config.js`**：仅为 H5 模式（`process.env.UNI_PLATFORM==='h5'`）设置 `publicDir='public'`，将 public/ 下静态文件复制到 H5 产物**根目录**（标准路径 `/robots.txt`、`/sitemap.xml`，供爬虫识别）；小程序（mp-weixin）模式不设 publicDir，完全不受影响。
3. **`frontend/pages.json`**：全部 15 个页面（含 globalStyle 兜底）的 `navigationBarTitleText` 优化为 SEO/GEO 友好描述性标题（如「打卡记录 - 日历查看与按月统计」「帮助中心 - 常见问题与使用指南」「隐私政策 - 数据加密与隐私保护」等）。H5 端由 uni-app 注入 `document.title`，微信搜一搜 可用；小程序的 `navigationStyle` 均为 `custom`，标题仅在搜一搜 展示，**不改变任何 UI 渲染与交互**。
4. **`frontend/pages/index/index.vue`**：在首页介绍卡片「主要功能」区块末尾，用 `<!-- #ifdef H5 -->` 包裹追加一段 SEO 关键词文本（按时吃药/吃药提醒/通用打卡计划/健身打卡/学习打卡/喝水提醒/定时提醒/数据加密可自部署），**仅 H5 渲染，小程序编译期完全剔除，零界面影响**。

### 影响范围
- `frontend/public/robots.txt`、`frontend/public/sitemap.xml`、`frontend/vite.config.js`、`frontend/pages.json`、`frontend/pages/index/index.vue`、`目录结构.json`

### 验证
- H5 生产构建：`npm run build:h5` → `Build complete`，产物 `dist/build/h5/` 根目录出现 `robots.txt`、`sitemap.xml`（标准路径）；抽查子页面 JS chunk 含新 SEO 标题（如「打卡记录 - 日历查看与按月统计」）；首页 chunk 含 H5 专属 SEO 关键词段落。
- 微信小程序生产构建：`npm run build:mp-weixin` → `Build complete`，产物**不含** sitemap.xml/robots.txt（publicDir 未生效），确认小程序零影响。
- JSON 格式：目录结构.json / pages.json / manifest.json 经 ConvertFrom-Json 校验合法。

### 说明
- 排查记录：uni-app H5 默认不复制根 `public/`（首次尝试 publicDir 未显式设时产物根无 robots.txt）；改放 `static/` 虽能被 H5 复制但路径变为 `/static/`（非标准根路径）且会进入小程序产物（static/ 文件被小程序复制，虽无功能影响但不干净）；最终采用「`public/` + vite.config.js 按平台设 publicDir」方案，兼顾标准根路径与小程序隔离。
- canonical/og:url 仍用生产 API 域名 `https://yao.wuzuniao.com`，若前端 H5 有独立部署域名，需同步替换 index.html 中 canonical/og:url 与 sitemap.xml 的 `<loc>`。
- `robots.txt` 指向 `https://yao.wuzuniao.com/sitemap.xml`（绝对 URL），与 sitemap 实际产物路径 `/sitemap.xml` 对应。

---

## 2026-08-01（优化）- H5 端 SEO/GEO 搜索引擎与生成式引擎优化

### 变更类型
优化（SEO/GEO）

### 变更内容
1. **`frontend/index.html`**：补充 SEO 元信息与结构化数据，提升 H5 端在搜索引擎与 AI 生成式引擎（ChatGPT/Perplexity 等）中的可发现性与可理解性：
   - `<title>` 由空填充为「无足鸟按时吃药打卡 - 通用打卡计划与按时提醒工具（免费开源）」。
   - 新增 `meta name="description"`（应用功能描述，含核心关键词）、`meta name="keywords"`、`meta name="robots" content="index, follow"`、`meta name="author"`、`meta name="theme-color" content="#2f6c00"`（品牌绿）。
   - 新增 `<link rel="canonical">` 指向生产域名 `https://yao.wuzuniao.com`（若前端 H5 有独立域名需同步替换）。
   - 新增 Open Graph 标签（og:type/og:site_name/og:locale/og:title/og:description/og:url）与 Twitter Card 标签（twitter:card/twitter:title/twitter:description），优化社交平台分享卡片。
   - 新增 `SoftwareApplication` 类型 JSON-LD 结构化数据（name/description/applicationCategory/operatingSystem/url/author/license/isAccessibleForFree/offers），帮助搜索引擎与 AI 生成式引擎准确理解应用类型、功能、平台与许可。
   - 保留 uni-app SSR 占位符（`<!--preload-links-->`/`<!--app-context-->`/`<div id="app"><!--app-html--></div>`）与 viewport 刘海屏兼容脚本，未触碰构建逻辑。
2. **`frontend/manifest.json`**：`h5.title` 同步更新为 SEO 友好标题，与 index.html 的 `<title>` 一致（uni-app 构建时会用 h5.title 注入 index.html 的 title 标签，必须同步否则会被覆盖回短标题）。仅影响 H5 端默认标题，不影响微信小程序（用 pages.json）与 App（用 app-plus）。
3. **`README.md`**：
   - 修正过时的 `frontend/src/` 结构描述为实际的 HBuilderX 标准布局（无 src 层），与目录结构.json 一致。
   - 修正过时的前端 `.env` 配置说明为 `config/env.js` 常量模块（2026-08-01 已迁移）。
   - 新增「常见问题（FAQ）」章节，包含应用介绍、支持平台、数据安全、通知渠道、自行部署、开源协议 6 个问答，提升 GEO 友好性（AI 生成式搜索引擎偏好问答结构化内容）。
4. **`目录结构.json`**：同步 `index.html` 与 `manifest.json` 描述（含 SEO 元信息与 JSON-LD 说明，修正 manifest.json 描述中已废弃的 /api 代理描述）。

### 影响范围
- `frontend/index.html`、`frontend/manifest.json`、`README.md`、`目录结构.json`

### 验证
- H5 生产构建：`npm run build:h5` → `Build complete`，`EXIT=0`，仅有已存在的 Sass legacy-js-api 弃用警告。
- H5 产物 `dist/build/h5/index.html` 检查：所有 SEO meta、Open Graph、Twitter Card、JSON-LD 结构化数据完整保留；uni-app SSR 占位符与 viewport 脚本保留；构建工具正确注入资源链接。
- 微信小程序生产构建：`npm run build:mp-weixin` → `Build complete`，`EXIT=0`，确认 H5 端修改未影响小程序构建（小程序构建读 mp-weixin 节，不读 h5 节与 index.html）。
- frontend 目录 lint 检查无错误。

### 说明
- 本次优化仅触及 H5 端 SEO/GEO 相关配置，未修改任何页面 `.vue` 文件、业务逻辑、API 请求、状态管理或组件，不影响现有功能。
- 未创建 `public/robots.txt`、`sitemap.xml`（项目无 public 目录机制，且 H5 域名未确认独立，避免影响小程序构建）。
- canonical/og:url 使用生产 API 域名 `https://yao.wuzuniao.com` 作为应用官方网址，若前端 H5 有独立部署域名，需同步替换 index.html 中的 canonical 与 og:url。

---

## 2026-08-01（修复）- 修复 H5 端首页白屏：devServer proxy /api 路径冲突

### 变更类型
修复（Fix）

### 变更内容
1. **根因**：`manifest.json` 的 `h5.devServer.proxy` 配置把 `/api` 代理到后端 `http://localhost:8000`。2026-08-01 前端迁移为 HBuilderX 标准布局（源码从 `frontend/src/api/` 移到 `frontend/api/`）后，vite dev server 请求源码文件的路径从 `/src/api/request.js` 变为 `/api/request.js`，**匹配 proxy 的 `/api` 规则，被代理到后端**。后端无此静态文件路由，返回 500，导致 `api/` 目录下所有模块（request.js、user.js、message.js 等）加载失败，首页组件崩溃白屏。
2. **为何之前正常**：迁移前源码在 `src/api/`，vite 请求路径 `/src/api/xxx.js` 不匹配 proxy `/api`，不受影响。
3. **proxy 为多余配置**：前端 API 请求用绝对 URL（`config/env.js` 的 `API_BASE_URL` = `http://localhost:8000`）+ `uni.request`，不经过 vite dev server，不需要 proxy 代理。
4. **修复**：删除 `manifest.json` 的 `h5.devServer.proxy` 配置，仅保留 `port: 5173`。

### 影响范围
- `frontend/manifest.json`（删除 h5.devServer.proxy 配置）

### 验证
- 删除 proxy 前后对比：`/api/request.js` 500 → 200，`/api/modules/announcement.js` 500 → 200
- puppeteer 访问 H5 首页：无控制台错误，页面正常渲染（欢迎卡片、词云、底部导航栏均显示）

---

## 2026-08-01（修复）- 修复微信开发者工具首页白屏：空 @media 块 + unpackage 误导入

### 变更类型
修复（Fix）

### 变更内容
1. **根因**：`frontend/pages/index/index.vue` 末尾存在一个无任何样式声明的空规则块 `@media screen and (min-width: 1024px) { .index-page__main-canvas {} }`（规则体为空，注释所述「不显式清零 padding」但实际无任何作用，属死代码）。CLI 的 dart-sass 会智能删除整个空 @media（产物合法），但 HBuilderX 旧版编译器仅删除空规则、保留 @media 外壳，产生只含注释的空 @media 块。微信 WXSS 编译器无法解析空 @media，在闭合 `}` 处报 `unexpected token }`（对应 `unpackage/dist/dev/mp-weixin/pages/index/index.wxss:906:1`），导致首页样式编译失败、白屏。
2. **次要根因**：`frontend/unpackage/`（HBuilderX 旧版遗留产物目录，曾于 2026-08-01 删除）因再次用 HBuilderX 打开 frontend 而重新生成；微信开发者工具误导入了该目录而非正确的 `frontend/dist/build/mp-weixin`。之前的「编译空窗期竞态条件」结论是错误的——日志根本没有 `app.json not found`，只有 WXSS 编译错误。
3. **修复**：删除 `index.vue` 末尾的空 @media 1024px 块（6 行死代码）。

### 影响范围
- `frontend/pages/index/index.vue`（删除 6 行空 @media 块）
- 重新构建 `frontend/dist/build/mp-weixin`（产物已无空 @media，括号配对 141=141，构建 exit 0）

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0，仅 sass legacy-js-api 弃用警告）
- 新产物 `dist/build/mp-weixin/pages/index/index.wxss` 无 1024px 内容，括号配对正确

### 待办（需用户手动）
- 关闭微信开发者工具后删除 `frontend/unpackage` 目录（运行中被锁定，无法自动删除）
- 微信开发者工具导入目录改为 `frontend/dist/build/mp-weixin`，清缓存后重新编译

---

## 2026-08-01（文档）- 更新目录结构说明，补充前端无 src 层布局选型理由

### 变更类型
文档（Docs）

### 变更内容
1. **`目录结构.json`**：在 `frontend` 节点新增「布局选型理由」字段，说明工程刻意采用无 `src` 层的 HBuilderX 标准布局（而非 CLI 布局），原因：项目目标以 HBuilderX 发行 App / iOS / 多端小程序，而 HBuilderX 运行/发行要求 `manifest.json` 等位于项目根目录；CLI 布局不兼容。同时记录 CLI 路径（`scripts/run-uni.js` 设 `UNI_INPUT_DIR`）已兜底、相对路径导入平移无需改、CI/CD 自动出包可直接复用 CLI 路径。

### 影响范围
- 文档：`目录结构.json`（`frontend` 节点新增「布局选型理由」）、本文件（倒序追加本次记录）

### 验证
- `目录结构.json` 经 JSON 解析校验为合法结构。
- 与 AGENTS.md 第 8.3 节「工程布局与启动命令」及前端现状一致，无冲突。

---

## 2026-08-01（重构）- 前端迁移为 HBuilderX 标准布局，环境变量改用 config/env.js

### 变更类型
重构（Refactor，结构性变更）

### 变更内容
1. **目标**：支持用 HBuilderX 发行 App、iOS 与多端小程序。原工程为 CLI 布局（源码在 `frontend/src/`），HBuilderX 要求 `manifest.json`/`pages.json`/`main.js`/`App.vue`/`static` 位于项目根目录，二者不兼容。
2. **目录上移**：`frontend/src/` 下全部内容（`api`/`assets`/`components`/`composables`/`pages`/`store`/`utils` 七个目录 + `App.vue`/`main.js`/`manifest.json`/`pages.json`/`uni.scss` 五个文件）上移至 `frontend/` 根目录，`src/` 删除。全部使用 `git mv`，Git 识别为 rename（`R`），文件历史完整保留。因源码内部导入**全部是相对路径**（`../../components/xxx`），整体平移后无需修改任何导入语句。
3. **环境变量改造**：新增 `frontend/config/env.js` 常量模块，导出 `API_BASE_URL`（按 `process.env.NODE_ENV` 区分：生产 `https://yao.wuzuniao.com`／开发 `http://localhost:8000`）与 `WX_SUBSCRIBE_TEMPLATE_ID`。原因：HBuilderX 内置编译器不加载 `.env`，`import.meta.env.VITE_*` 会得到 `undefined`，导致接口地址静默回退、订阅模板 ID 变空串。改造点：
   - `api/request.js`：`import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'` → 引入 `API_BASE_URL`
   - `composables/useWechatSubscribe.js`：`import.meta.env.VITE_WX_SUBSCRIBE_TEMPLATE_ID || ''` → 引入 `WX_SUBSCRIBE_TEMPLATE_ID`
   - 删除已失效的 `frontend/.env` 与 `frontend/.env.template`
4. **CLI 与 HBuilderX 并存**：uni CLI 把输入目录写死为 `<项目根>/src`（`vite-plugin-uni/dist/cli/build.js:97` 读 `process.env.UNI_INPUT_DIR`），迁移后 CLI 构建报 `ENOENT: ...\src\manifest.json`。新增 `frontend/scripts/run-uni.js` 包装脚本，调用 uni CLI 前将 `UNI_INPUT_DIR` 指向项目根目录；`package.json` 四个 scripts 改为经此脚本调用。用 Node 设置环境变量，跨平台且未引入 `cross-env` 依赖。
5. **配套修正**：
   - `vite.config.js`：`@` 别名由 `./src` 改为项目根目录；补充布局说明注释。
   - `index.html`：入口 `src="/src/main.js"` → `src="/main.js"`。
   - `main.js`：删除 `import './uni.promisify.adaptor'` —— 该文件实际不存在，此前因被包在 `#ifndef VUE3`（项目为 Vue 3）中而未暴露，迁移后 HBuilderX 条件编译时机不同可能报模块找不到。

### 影响范围
- 移动：`frontend/src/**` → `frontend/**`（43 个源码文件 + 25 个图片资源），`frontend/src/` 已删除
- 新增：`frontend/config/env.js`、`frontend/scripts/run-uni.js`
- 修改：`frontend/api/request.js`、`frontend/composables/useWechatSubscribe.js`、`frontend/main.js`、`frontend/index.html`、`frontend/package.json`、`frontend/vite.config.js`
- 删除：`frontend/.env`、`frontend/.env.template`
- 文档：`AGENTS.md` 第 8 节重写（拆分为 8.1 后端 .env／8.2 前端 config/env.js／8.3 工程布局与启动命令）

### 验证
- CLI 生产构建：`npm run build:mp-weixin` → `Build complete`，`EXIT=0`，无 ENOENT。
- CLI 开发构建：`npm run dev:mp-weixin` → 产物 `dist/dev/mp-weixin/config/env.js` 中 `API_BASE_URL = "http://localhost:8000"`（开发分支正确）。
- 生产产物 `dist/build/mp-weixin/config/env.js` 中仅保留 `"https://yao.wuzuniao.com"`，开发分支已被 tree-shaking 剔除（环境区分生效）。
- 产物页面结构完整：主包 5 页（index/record/settings/notification/plan）+ 分包 9 页，与 `pages.json` 一致。
- 相对导入验证：`api/request.js` 编译为 `require("../config/env.js")`，解析正确。
- 全前端 lint 检查通过（0 错误）；`目录结构.json` 经 `ConvertFrom-Json` 校验为合法 JSON。

### 待办（需用户操作，非代码问题）
- HBuilderX 首次运行前需在「运行 → 运行到小程序模拟器 → 微信开发者工具」中配置微信开发者工具安装路径。
- App/iOS 打包需在 DCloud 开发者中心注册应用（`manifest.json` 中 App appid 为 `__UNI__528E611`）并配置 iOS 证书，属账号侧操作。

---

## 2026-08-01（清理/修复）- 删除遗留输出目录 unpackage/ 并修正 vite 别名配置

### 变更类型
清理（Chore）+ 修复（Bugfix）

### 变更内容
1. **删除 `frontend/unpackage/`**：该目录为旧版 HBuilderX 遗留的小程序输出产物（`app.json` 停留在 2026/6/28 的脚手架状态），是同日「首页白屏」事故的直接诱因。删除前已确认：被 `.gitignore:60 unpackage/` 忽略，且 `git ls-files` 结果为空（未被跟踪），删除不影响仓库；全项目除文档说明外无任何代码引用该路径。
2. **修正 `frontend/vite.config.js` 的 `@` 别名**：原配置 `'@': '/'` 会把 `@/utils/announcementRead` 解析到文件系统根目录 `/utils/...`（而非 `src/utils/...`），属错误写法，此前仅因 uni 插件内置别名兜底才未暴露。现改为标准写法 `fileURLToPath(new URL('./src', import.meta.url))`，明确指向 `src` 目录。

### 影响范围
- 删除：`frontend/unpackage/`（整个目录，构建产物，不入库）
- 修改：`frontend/vite.config.js`（新增 `node:url` 导入，改写 `resolve.alias['@']`）

### 验证
- 别名实际使用点：`frontend/src/components/AnnouncementCard.vue:45` 的 `import { getUnread, markRead } from '@/utils/announcementRead'`（全项目唯一 `@/` 引用）。
- 修改后重新构建：`npx uni build -p mp-weixin` → `Build complete`，`EXIT=0`，无 error。
- 产物校验：`dist/build/mp-weixin/utils/announcementRead.js` 存在；`dist/build/mp-weixin/components/AnnouncementCard.js` 中已正确编译为 `require("../utils/announcementRead.js")`，别名解析无误。
- `vite.config.js` lint 检查通过（0 错误）。

### 备注
- `目录结构.json` 无需变更：该文件原本就未收录 `unpackage/`（属被忽略的构建产物），`vite.config.js` 条目描述仍准确。

---

## 2026-08-01（修复）- 微信小程序编译失败/首页白屏：开发者工具导入目录错误

### 变更类型
修复（Bugfix，环境配置问题，无代码改动）

### 变更内容
1. **问题**：微信开发者工具编译失败、无法看到首页，报 `app.json: 在项目根目录未找到 app.json` 与 `routeTo appLaunch timeout`。
2. **此前误判**：先前分析认为是 `npm run build:mp-weixin` 清空目录产生的「竞态空窗期」导致工具状态错误、结论为「代码无问题、手动点编译即可」。该结论**错误**，仅因当时工具恰好指向了有效的 `dist/dev/mp-weixin` 而掩盖问题，故必然复现。
3. **真正根因**：项目存在 3 个小程序输出目录，工具导入了错误的那个：
   - `frontend/dist/build/mp-weixin`：排查前**为空目录**（从未执行过 build），无 `app.json` → 报找不到 app.json → appLaunch 超时。
   - `frontend/unpackage/dist/dev/mp-weixin`：旧版 HBuilderX 遗留产物，`app.json` 时间戳停留在 **2026/6/28**，仅含 `pages/index/index` + 分包 `profile`/`settings`，标题仍为脚手架默认 "My App"，且缺少 `components/`、`composables/`、`store/`、`utils/` 目录 → 导入后首页依赖缺失 → 白屏。
   - `frontend/dist/dev/mp-weixin`：最新有效产物（2026/8/1），主包 5 页 + 分包 9 页。
4. **修复**：执行 `npx uni build -p mp-weixin` 重新生成生产产物（构建成功，exit 0 / `Build complete`，仅 Sass `legacy-js-api` 弃用警告，无关）；`dist/build/mp-weixin` 现已包含与 `src/pages.json` 一致的完整 14 页结构。
5. **正确用法**：开发者工具仅可导入 `frontend/dist/build/mp-weixin`（生产）或 `frontend/dist/dev/mp-weixin`（开发），**严禁导入 `frontend/unpackage/...`**；AppID 为 `wx3101dc47c535111e`。

### 影响范围
- 无源码改动，仅重新生成 `frontend/dist/build/mp-weixin` 构建产物（该目录已被 .gitignore 忽略）。

### 遗留提醒（未擅自处理）
- 建议删除 `frontend/unpackage/` 旧遗留目录以绝后患（`.gitignore` 已忽略，删除不影响仓库），待用户确认。
- `frontend/vite.config.js` 中 `resolve.alias` 的 `'@': '/'` 写法可疑（通常应为 `path.resolve(__dirname, 'src')`），目前靠 uni 插件内置别名兜底未出错，本次未改动。

---

## 2026-08-01（修复）- H5 端密码输入框左侧白色遮挡边框（全页面修复）

### 变更类型
修复（Bugfix）

### 变更内容
1. **问题**：H5 端登录页及所有含密码输入框的页面，密码输入框左侧边框被白色覆盖，视觉上左侧边框消失。
2. **根因**：各页面密码行容器 `.xxx__password-row` 设为 `background: var(--color-card-bg)（白/卡片色）` + `border: 1px solid`，内部 `xxx__input--password` 继承了 `.xxx__input` 的背景色（同为白/卡片色）。H5 渲染时 input 的白色背景覆盖了容器左侧边框线（input 左边缘紧贴容器左边框内侧）。普通输入框（非密码行）的行容器背景透明，无此问题。
3. **修复**：给全部含密码行的页面 `.xxx__input--password` 添加 `background-color: transparent`，使密码行容器四边边框完整显示。涉及文件：
   - `login.vue`：`.login-page__input--password`（首轮已修）
   - `register.vue`：`.register-page__input--password`
   - `profile.vue`：`.profile-page__input--password`
   - `forgot-password.vue`：`.forgot-password-page__input--password`

### 影响范围
- `frontend/src/pages/user/login.vue`（前一轮已改）
- `frontend/src/pages/user/register.vue`
- `frontend/src/pages/user/profile.vue`
- `frontend/src/pages/user/forgot-password.vue`

### 验证
- register 页面实测：`inpBg=rgba(0,0,0,0)`，`rowBorder` 完整，`errors=0`。
- forgot-password / profile 页面：编译后注入样式均含 `background-color: transparent`（页面需步骤2/登录态才渲染密码行，无法直接交互，故通过编译样式确认）。
- 4 个文件 lint 检查均通过（0 错误）。

---

## 2026-08-01（修复）- H5 端所有输入框不显示（useInputLimit 模板解包崩溃）

### 变更类型
修复（Bugfix）

### 变更内容
1. **问题**：H5 端所有含字数限制提示的输入框页面渲染白屏，输入框完全不显示（history 路由模式下整个组件崩溃）。
2. **根因**：`useInputLimit` 返回**普通对象**，其内部的 `limitHint` 是 `computed`（即 `ComputedRef`）。Vue 仅在 setup 返回的**顶层**或 `reactive` 对象内的 ref 才会自动解包；嵌套在普通对象属性中的 ref **不会**自动解包。因此在模板中 `xxxLimit.limitHint` 取到的仍是 `ComputedRef` 对象而非字符串，先前双引号修复里追加的 `&& xxxLimit.limitHint.includes('已达')` 在运行时抛出 `TypeError: limitHint.includes is not a function`，导致组件渲染中断、整页白屏、输入框全部消失。
3. **修复方案**：将 `useInputLimit` 的返回值用 `reactive()` 包装（`return reactive({ max, filter, handleInput, limitReached, limitHint, checkLimit })`）。这样模板访问 `xxxLimit.limitHint` 时自动解包为字符串、`xxxLimit.limitReached` 解包为布尔，`.includes` 调用恢复正常，组件不再崩溃。此为统一修复，一次改动覆盖全部 7 个输入页。
4. **未改动**：7 个输入页的 30 处 `v-if` 条件（`&& xxxLimit.limitHint.includes('已达')`）保留，现可正确生效；`useInputLimit` 内部逻辑（filter / handleInput / checkLimit / 提示文案）保持不变。

### 影响范围
- `frontend/src/composables/useInputLimit.js`（唯一改动文件）

### 验证
- 用 puppeteer（Chromium headless）在 history 模式真实路径下渲染 7 个页面：全部 `errors=0`，无控制台报错。
- login / register / forgot-password 输入框正常可见（display:block、visible、opacity:1，高度正常）。
- profile / announcement / plan / notification 默认展示列表主页（输入框在弹窗内，符合预期），文案正常渲染、无重定向崩溃。
- 默认状态 `limit-text` 数量为 0（双引号问题未回归）；超限输入时仍显示「已达 X 字上限」提示。
- `useInputLimit.js` 通过 lint 检查（0 错误）。

---

## 2026-08-01（修复）- H5 端输入框左下角双引号（limit-text 误渲染）

### 变更类型
修复（Bugfix）

### 变更内容
1. **问题**：H5 端每个输入框下方（左下角）都显示两个双引号 `""`。
2. **根因**：各输入页的「字数上限提示」`<text class="...__limit-text">` 元素由 `useInputLimit` 的 `limitReached` / `limitHint` 控制。H5 端 uni-app 原生 `<input>` 被编译为 `uni-input` 自定义元素，初始化时会触发一次 `@input` 事件，使 `limitReached` 被误置为 `true`；此时 `limitHint` 返回了包含双引号的异常值，导致 `limit-text` 在默认（无输入）状态下渲染出 `""`，而非正常隐藏。
3. **修复方案**：在 7 个输入页（login / register / profile / forgot-password / announcement / plan / notification）共计 30 处的 `limit-text` 的 `v-if` 条件上，追加 `&& xxxLimit.limitHint && xxxLimit.limitHint.includes('已达')`。仅当真正触发超限、提示文本含「已达」字样时才显示提示，**不影响 placeholder 占位提示的展示**。
4. **未改动**：`useInputLimit` composable 逻辑保持原样（超限提示文案与判定不变），仅收紧模板渲染条件。

### 影响范围
- `frontend/src/pages/user/login.vue`（2 处）
- `frontend/src/pages/user/register.vue`（5 处）
- `frontend/src/pages/user/profile.vue`（8 处）
- `frontend/src/pages/user/forgot-password.vue`（4 处）
- `frontend/src/pages/user/announcement.vue`（6 处）
- `frontend/src/pages/index/plan.vue`（4 处）
- `frontend/src/pages/index/notification.vue`（8 处）

### 验证
- 修复后 H5 端默认状态输入框下方双引号消失（`limit-text` 元素数量为 0）。
- 超限输入时仍正常显示「已达 X 字上限」提示（含「已达」字样），提示功能不受影响。
- 小程序端原生 input 不受影响（`uni-input` / `__limit-text` 为 H5 端编译产物，小程序端无此元素）。
- 全部修改文件通过 lint 检查（0 错误）。

---

## 2026-08-01（清理）- 清理项目根目录与项目无关的残留文件

### 变更类型
清理

### 变更内容
删除项目根目录下 7 个与项目无关的残留文件（均不在 `目录结构.json` 中，非项目正式文件）：
1. `helper.ps1` —— 内容仅 `1`，PowerShell 测试残留
2. `ss.ps1` —— System.Windows.Forms 截屏脚本，调试残留
3. `ss2.ps1` —— 同上，另一份截屏脚本残留
4. `t.py` —— 仅 `print(sys.version)`，Python 版本测试残留
5. `wx_check.py` —— 不完整的 Windows API（ctypes/wintypes）调试脚本
6. `wechat_devtools_final_state.png` —— AI 工具排查微信开发者工具时生成的截图
7. `wuzuniaoyaowx_20260801_004044.png` —— 同上，AI 工具生成的桌面截图

### 影响范围
仅删除根目录下的临时/调试文件，不涉及任何项目源码、配置或文档；`目录结构.json` 无需更新（这些文件本来就不在其中）。

### 验证
- 项目源码、配置、文档均未改动
- `目录结构.json` 与实际项目结构保持一致

---

## 2026-07-31（修复）- 首页卡片宽度与记录/设置页一致（H5 + 微信小程序双端）

### 变更类型
修复

### 变更内容
1. **问题**：首页卡片（welcome-card / intro-card / guide-card / hero / primary-card / secondary-card / checkin-shell）使用固定宽度 `684rpx`（平板段锁定 `342px`），而记录页、设置页卡片为 `width: 100%` 全宽自适应，三端卡片宽度不一致。
2. **修复方案**：将首页全部卡片宽度由固定值改为 `width: 100%`，并移除平板断点（`@media (min-width: 768px)`）下对首页卡片的 `width: 342px` 锁定，使其与记录页/设置页一样跟随父容器 `__main-canvas` padding 自适应。
3. **具体变更**（`frontend/src/pages/index/index.vue`）：
   - 7 处 `width: 684rpx` → `width: 100%`
   - 移除平板段 `.index-page__guide-card / __hero / __primary-card / __secondary-card / __checkin-shell` 组合的 `width: 342px` 锁定
   - 移除平板段 `.index-page__welcome-card` / `.index-page__intro-card` 的 `width: 342px` 锁定（保留各自 padding / border-radius 视觉调整）
4. **影响范围**：仅样式层改动，无逻辑/接口变化；H5 与微信小程序端首页卡片宽度现与记录页、设置页完全一致

### 验证
- 无脚本依赖，纯 CSS 改动
- 微信小程序端：卡片宽度跟随屏宽自适应（与记录/设置页一致）
- H5 端：同上

---

## 2026-07-31（修复）- 新手引导从制定计划页返回设置页后不显示「点击首页」提示

### 变更类型
修复

### 变更内容
1. **问题**：新手引导第 5 步在 plan 页点击「新建计划」时，`handleNewEntry` 调用 `nextStep()` 将 currentStep 推进到 settings 页的 `home-tab`（步骤 6）。但此时用户仍在 plan 页，BottomNav 组件（位于 settings 页）不可见，`useGuideTarget` 查询 `boundingClientRect` 返回 width=0/height=0，8 次重试均失败、位置不上报。用户保存计划返回 settings 页后，`currentStepData` 未变化（仍是 `home-tab`），`useGuideTarget` 的 `stopWatch` 不再触发，位置持续为 null，BeginnerGuide 显示全屏阻挡蒙版（`v-if="!targetRect"`）且步骤卡片不渲染，用户看到的是"引导消失/卡住"。
2. **修复方案**：在 `useGuideTarget` 中新增 `stopPageWatch`，监听 `guideStore.currentPage` 变化。当页面切换且当前步骤匹配本目标时，重新触发位置查询（带 150ms 延迟 + 500ms 二次查询，与 stopWatch 一致）。这样从 plan 页返回 settings 页时 `onPageEnter('settings')` 更新 currentPage，BottomNav 的 `useGuideTarget('home-tab')` 重新查询到可见元素的位置并上报，蒙版消失、高亮与步骤卡片正常显示。
3. **具体变更**（`useGuideTarget.js`）：
   - 新增 `stopPageWatch`：`watch(() => guideStore.currentPage, ...)`，回调中校验引导激活 + 步骤匹配（target 或 cardAnchor）后执行与 `stopWatch` 相同的延迟查询逻辑（清理 requeryTimer → 150ms 后 queryAndReport → 500ms 后二次 queryAndReport）
   - `onUnmounted` 新增 `stopPageWatch()` 停止监听，防止内存泄漏
4. **影响范围**：该修复对所有使用 `useGuideTarget` 的组件/页面生效（BottomNav/settings/login/register/notification/plan），但仅在「当前步骤匹配 + 页面切换」时触发额外查询，无副作用（步骤不匹配时回调直接 return）

### 影响范围
- `frontend/src/composables/useGuideTarget.js`（新增 stopPageWatch 监听 currentPage 变化）

### 验证
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功
- H5 端从 plan 页返回 settings 页后，引导正确高亮底部「首页」按钮并显示步骤卡片

---

## 2026-07-31（优化）- H5 端新手引导第 4 步取消通知方式页内蒙版

### 变更类型
优化

### 变更内容
1. **问题**：H5 端新手引导第 4 步进入通知方式页后，页面内有 3 个引导子步骤（添加新方式、选择邮件、保存通知），目标元素位置上报前显示全屏蒙版（`@click.stop` 阻挡点击），导致页面无法操作；且多步引导在表单页内过于繁琐。
2. **修复方案**：取消通知方式页内的全部引导子步骤，改为在设置页「通知方式」提示卡片中一并说明完整流程；用户点击「通知方式」后引导直接推进到「制定计划」步骤，通知方式页内无蒙版、用户自由操作。
3. **具体变更**：
   - `guide.js` nonWechatSteps：移除 notification 页的 3 个子步骤（`add-notification`/`email-type-radio`/`email-save-button`）；settings 页 `notification-method` 步骤 description 改为说明完整流程（进入通知方式页 → 添加新方式 → 选邮件 → 填 SMTP → 保存）；顶部注释第 4 步描述同步更新
   - `settings.vue` goNotification：点击「通知方式」时若引导激活且当前步骤为 `notification-method`，调用 `skipToStepByTarget('plan-method')` 推进到「制定计划」步骤后再跳转
4. **流程对比**：
   - 原：settings 高亮通知方式 → notification 高亮添加新方式 → 高亮邮件单选 → 高亮保存按钮 → 返回 settings 高亮制定计划
   - 新：settings 高亮通知方式（提示说明完整流程）→ 用户点击进入 notification 页（无蒙版、自由操作）→ 返回 settings 高亮制定计划
5. **保留代码**：notification.vue 中的 `useGuideTarget` 调用与 handleAdd/selectType/handleSave 内的 `nextStep` 判断为预存在代码，保留不影响功能（目标 key 不再匹配任何步骤，watch 不会触发上报；currentStepData?.target 判断永远为 false，nextStep 不会执行）

### 影响范围
- `frontend/src/store/modules/guide.js`（nonWechatSteps 移除 3 个 notification 子步骤 + 注释更新）
- `frontend/src/pages/index/settings.vue`（goNotification 增加引导推进逻辑）

### 验证
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功（微信小程序端 wechatSteps 不受影响，仍保留 notification 页内引导子步骤）
- H5 端第 4 步进入通知方式页后无蒙版阻挡，用户可自由操作表单；返回设置页后引导显示「制定计划」

---

## 2026-07-31（优化）- H5 端新手引导蒙版移除 SVG mask 抗锯齿黑边

### 变更类型
优化

### 变更内容
1. **问题**：H5 端新手引导从第 2 步起，高亮区域内可见一圈"内黑边"。根因是原 H5 端使用 SVG mask 挖洞绘制蒙版，mask 中白色（显示蒙版）到黑色（挖洞）的边缘存在抗锯齿过渡，产生半透明黑色像素落在目标元素上，形成视觉黑边。
2. **修复方案**：H5 端弃用 SVG mask，改用与微信小程序端一致的「单 view + box-shadow 大扩散」蒙版方案。view 本身透明（高亮洞内显示目标元素），box-shadow 向外大扩散绘制洞外灰色蒙版，连续渲染无抗锯齿黑边。
3. **具体变更**（`BeginnerGuide.vue`）：
   - 模板：移除 `#ifdef H5` 的 SVG mask（`<svg>`/`<mask>`/`<rect>`）整段；H5 端改为渲染 `.beginner-guide__mask-hole` view（复用 `maskHoleStyle`，与微信小程序端视觉层一致）；微信小程序端的 4 个点击阻挡层仍用 `#ifndef H5` 包裹（H5 端蒙版 view `pointer-events:none` 不阻挡点击，无需阻挡层）
   - script：移除 SVG 专属的 `maskId` 常量与 `svgMaskStyle` computed；保留 `highlightRadiusValue`（仍用于 `maskBounds.cs` 圆角计算）
   - 样式：移除 `.beginner-guide__svg-mask` 样式块
   - 注释：更新组件顶部「灰色蒙版」与「蒙版原理」说明，反映 H5 端改用 box-shadow 大扩散

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`（模板/script/样式/注释）

### 验证
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功（微信小程序端蒙版逻辑不变，仍为 box-shadow 大扩散 + 4 点击阻挡层）
- H5 端高亮区域内不再有 SVG mask 抗锯齿黑边，仅保留 box-shadow 呼吸高亮做视觉提示

---

## 2026-07-31（优化）- H5 端新手引导第 3 步登录/注册卡片高亮优化

### 变更类型
优化

### 变更内容
1. **问题**：H5 端新手引导第 3 步"普通登录"和"填写并提交注册"原高亮目标为登录/注册按钮（`login-submit`/`register-submit`），蒙版仅允许点击该按钮，用户无法输入用户名密码，导致引导卡死。
2. **修复方案**：将高亮目标从按钮改为整个表单卡片容器（`login-card`/`register-card`），蒙版仅阻挡卡片外部区域，用户可在卡片内自由输入表单并点击提交按钮。
3. **具体变更**：
   - `login.vue`：`.login-page__card` 添加 `guide-target-login-card` 类；`useGuideTarget` 从 `('login-submit', '.guide-target-login-submit')` 改为 `('login-card', '.guide-target-login-card')`
   - `register.vue`：`.register-page__card` 添加 `guide-target-register-card` 类；`useGuideTarget` 从 `('register-submit', '.guide-target-register-submit')` 改为 `('register-card', '.guide-target-register-card')`
   - `guide.js` nonWechatSteps：`register-submit` 步骤 target 改为 `register-card`、移除 `shape: 'pill'`、padding 改为 8；`login-submit` 步骤 target 改为 `login-card`、移除 `shape: 'pill'`、padding 改为 8

### 影响范围
- `frontend/src/pages/user/login.vue`（引导目标类 + useGuideTarget 调用）
- `frontend/src/pages/user/register.vue`（引导目标类 + useGuideTarget 调用）
- `frontend/src/store/modules/guide.js`（nonWechatSteps 步骤配置 + 注释）

### 验证
- H5 端登录页 `.guide-target-login-card` 存在且包裹用户名/密码输入框+登录按钮，console 无错误
- H5 端注册页 `.guide-target-register-card` 存在且包裹全部表单字段+注册按钮，console 无错误
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功（微信端引导流程不受影响，仍使用 `wechat-login` 目标）

---

## 2026-07-31（修复）- H5 端首页词云显示修复

### 变更类型
修复

### 变更内容
1. **index.vue randomizeCloud() 用条件编译分端实现容器尺寸测量**：H5 端 `uni.createSelectorQuery()` 存在 `parentElement` 兼容问题（即使添加 `.in(instance.proxy)` 仍抛 `TypeError: Cannot read properties of null (reading 'parentElement')`），导致 `onShow` 中 `randomizeCloud()` 执行失败，词云数据 `cloudWords` 保持空数组，欢迎卡片词云区域空白。
2. **修复方案**：提取词云生成逻辑为 `generateWords(W, H)` 函数，用条件编译分端获取容器尺寸：
   - `#ifdef H5`：`nextTick` + `document.querySelector('.index-page__welcome-cloud')` + `offsetWidth/offsetHeight` 获取实际尺寸
   - `#ifndef H5`（微信小程序端）：保持原 `uni.createSelectorQuery().in(instance.proxy).select().boundingClientRect().exec()` 方式
3. **新增 import**：`getCurrentInstance`、`nextTick`（from 'vue'）
4. **新增组件实例变量**：`const instance = getCurrentInstance()`（供微信小程序端 `createSelectorQuery().in()` 使用）

### 影响范围
- `frontend/src/pages/index/index.vue`（randomizeCloud 函数重构 + import 新增）

### 验证
- H5 端首页 console 零错误，词云"免费/易用/安全/开源"四个文字正常渲染
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功，微信小程序端逻辑保持不变

---

## 2026-07-31（修复）- H5 端首页运行时错误修复

### 变更类型
修复

### 变更内容
1. **index.vue 移除 setup 阶段的 randomizeCloud() 调用**：原代码在 `<script setup>` 顶层（line 300）直接调用 `randomizeCloud()`，该函数内部调用 `uni.createSelectorQuery()`。在 H5 端，组件 setup 阶段 DOM 尚未挂载，`createSelectorQuery` 内部访问 `parentElement` 时为 null，导致 `TypeError: Cannot read properties of null (reading 'parentElement')`，进而引发 Vue setup 执行错误与 Invalid vnode type 错误，首页布局异常单薄。微信小程序端因 `createSelectorQuery` 实现机制不同（不依赖 DOM parentElement），未受影响。
2. **修复方案**：移除 setup 阶段的 `randomizeCloud()` 调用，保留 `onShow` 中的调用（line 875-877，未登录用户每次进入首页时随机词云布局）。onShow 在组件挂载后执行，DOM 已就绪，H5 端 `createSelectorQuery` 可正常工作。

### 影响范围
- `frontend/src/pages/index/index.vue`（移除 setup 阶段 line 300 的 `randomizeCloud()` 调用）

### 验证
- H5 端首页 console 无任何错误（原 3 条错误全部消失），页面布局完整渲染（标题/说明文案/开源地址/Logo/底部TabBar）
- H5 端全部 11 个页面（首页/登录/注册/联系我们/帮助中心/隐私政策/找回密码/记录/设置/通知方式/制定计划）console 均无错误
- `npm run build:h5` 构建成功
- `npm run build:mp-weixin` 构建成功，微信小程序功能未受影响

---

## 2026-07-31（新增）- H5 端构建支持

### 变更类型
新增

### 变更内容
1. **manifest.json H5 配置补全**：新增 `router.mode=history` + `router.base=/` + `publicPath=/`，devServer 端口由 80 改为 5173 避免冲突。
2. **login.vue H5 端隐藏微信登录区**：用 `#ifdef MP-WEIXIN` 条件编译包裹微信一键登录卡片与"切换到一键登录"引导；H5 端默认 `loginMode='normal'`（账号密码登录）；`handleWechatLogin`、`wechatAgree`、`toggleWechatAgree`、`useGuideTarget('wechat-login')`、`wechatLogin` import、`wxIcon` import 均用 `#ifdef MP-WEIXIN` 包裹，H5 端不编译。
3. **notification.vue H5 端隐藏微信渠道 UI 与函数**：微信订阅消息卡片、"微信"类型单选项、微信订阅授权说明、微信启用状态表单均用 `#ifdef MP-WEIXIN` 包裹；`doWechatAuthorize`/`bindWechatAccount`/`handleWechatAuthorize`/`handleWechatReauth`/`handleDeleteWechat`/`toggleWechatEdit`/`handleUpdateWechat` 7 个微信函数整体用 `#ifdef MP-WEIXIN` 包裹；`hasWechat`/`wechatChannel`/`wechatRemaining`/`wechatEditExpanded`/`wechatEditForm`/`useWechatSubscribe` 调用/`useGuideTarget('wechat-auth-button')`/`bindWechat`+`updateWechatChannel` import 均条件编译隔离；`formType` 默认值用条件编译（微信端 '微信'，H5 端 '邮件'）；`handleAdd`/`handleSave` 中微信分支条件编译隔离；移除 `bindWechatAccount` 内部嵌套的 `#ifdef MP-WEIXIN`（外层已守卫）。
4. **useShare.js H5 端不注册分享钩子**：整个函数体（`onLoad`+`onShareAppMessage`+`onShareTimeline`）用 `#ifdef MP-WEIXIN` 包裹，H5 端 `useShare()` 为空操作。
5. **request.js 错误文案条件编译区分**：微信小程序端保留原"开发者工具域名配置"提示，H5 端用"Network Error/Failed to fetch"等浏览器错误文案。
6. **global.scss H5 端 PC 浏览器最大宽度限制**：用 `/* #ifdef H5 */` 包裹 `@media (min-width: 768px)` 媒体查询，限制 body `max-width: 480px` 居中显示，html 背景色 #e8ebe6 避免两侧突兀，body 加 box-shadow 区分主体。
7. **.env.template 补充说明**：VITE_WX_SUBSCRIBE_TEMPLATE_ID 注释补充"仅微信小程序端使用（H5 端无微信订阅消息能力，此变量不会被引用）"。

### 设计原则
- 使用 uni-app 条件编译（`#ifdef MP-WEIXIN` / `#ifndef MP-WEIXIN` / `#ifdef H5`）隔离微信小程序与 H5 端冲突功能，各端差异代码独立互不影响。
- H5 端不使用微信一键登录，使用账号密码登录（普通登录）。
- H5 端不展示微信订阅消息渠道（用户在 H5 端仅能管理邮件通知）。
- 新手引导已由 guide.js 的 `nonWechatSteps` 配置处理（注册→登录→邮件通知→计划→打卡），无需额外修改。

### 影响范围
- `frontend/src/manifest.json`（H5 配置补全）
- `frontend/src/pages/user/login.vue`（H5 端隐藏微信登录区）
- `frontend/src/pages/index/notification.vue`（H5 端隐藏微信渠道 UI 与函数）
- `frontend/src/composables/useShare.js`（H5 端不注册分享钩子）
- `frontend/src/api/request.js`（错误文案条件编译区分）
- `frontend/src/assets/styles/global.scss`（H5 端 PC 浏览器最大宽度限制）
- `frontend/.env.template`（补充 H5 端说明）

### 验证
- `npm run build:h5` 构建成功（DONE Build complete.）
- `npm run build:mp-weixin` 构建成功（DONE Build complete.），微信小程序端功能未受影响

### 部署提示
- H5 端使用 history 路由模式，生产环境 Nginx 需配置所有 404 请求回退到 index.html。
- 后端 `CORS_ALLOW_ORIGINS` 需添加 H5 部署域名（开发环境 devServer 代理已配置 /api）。

---

## 2026-07-30（优化）- 欢迎标题与"按时吃药"标题保持一致

### 变更类型
优化

### 变更内容
1. **对齐介绍卡片标题**：首屏欢迎标题 `.index-page__welcome-slogan` 的字体定义改为与介绍卡片"按时吃药"标题 `.index-page__intro-title` 完全一致——`font-size: 56rpx`、`line-height: 80rpx`、`font-weight: 600`、`color: var(--color-text-primary)`、居中。
2. **撤销上一轮品牌绿方案**：上一轮将标题设为品牌绿/700，本次按"与按时吃药保持一致"要求改回近黑 text-primary / 600，去除 letter-spacing。
3. **平板同步**：媒体查询下字号 24→28px、行高 34→40px（与 intro-title 平板值一致）。

### 影响范围
- `frontend/src/pages/index/index.vue`（`.index-page__welcome-slogan` 基础 + 平板媒体查询）

---

## 2026-07-30（优化）- 欢迎卡片首行标语提升为主标题

### 变更类型
优化

### 变更内容
1. **首行标语改为主标题**：原 `.index-page__welcome-slogan`（44rpx / 600 / 近黑 text-primary）提升为卡片主标题——字号 48rpx、字重 700、颜色改用品牌绿 `--color-brand`、加 `letter-spacing: 1rpx`、与正文间距 `margin-bottom` 24→28rpx。
2. **层级规划**：标题(48rpx/品牌绿/700) > 正文 `.index-page__intro-p`(30rpx/次要文本/400) > 链接(品牌绿)，标题以品牌色与字号、字重形成清晰层级。
3. **平板同步**：媒体查询下字号 22→24px、行高 32→34px、下间距 12→14px（rpx 的一半）。

### 影响范围
- `frontend/src/pages/index/index.vue`（`.index-page__welcome-slogan` 基础 + 平板媒体查询）

---

## 2026-07-30（优化）- 词云档差再扩大 + 词间互斥避让

### 变更类型
优化

### 变更内容
1. **档差再扩大、最小档不变**：字号改为等差数列，s1 最小档固定 74rpx 不变，其后每档 +18rpx 依次叠加（s2=92/s3=110/s4=128/s5=146，档差由 14→18）；平板 px 同步 37/46/55/64/73。新增 JS 常量 `CLOUD_SIZE_BASE`/`CLOUD_SIZE_STEP` 与 `cloudSizeRpx()`，CSS 变体类须与之保持一致。
2. **词间互斥（不再重叠）**：`randomizeCloud()` 改为先测量词云容器实际像素尺寸（`uni.createSelectorQuery` + `upx2px` 统一单位，未就绪回退估算 604×700），在容器内随机落点并对已放置词做 AABB 互斥避让（拒绝采样，最多 80 次兜底），位置仍以百分比存储保持自适应；含 ±12° 旋转包围盒放大与 12rpx 间距。

### 影响范围
- `frontend/src/pages/index/index.vue`（`CLOUD_SIZE_*` 常量、`cloudSizeRpx`、`randomizeCloud`、`.index-page__welcome-word--s1~s5` 基础 + 平板媒体查询）

---

## 2026-07-30（优化）- 欢迎卡片词云字号档位整体放大 + 加大间隔

### 变更类型
优化

### 变更内容
1. **字号档位整体上移并加大间隔**：原 s1-s5 = 50/56/62/68/74rpx（档差 6）；改为 74/88/102/116/130rpx（档差 14，间隔翻倍有余）。最小档即原最大档（74rpx），最大档 130rpx 非常大。
2. **平板媒体查询同步放大**：s1-s5 由 25/28/31/34/37px 改为 37/44/51/58/65px（rpx 的一半）。
3. 词云区无 `overflow: hidden`，放大后的关键词作为浅色(opacity 0.38)背景装饰可自然溢出卡片内边距，视觉无碍。

### 影响范围
- `frontend/src/pages/index/index.vue`（`.index-page__welcome-word--s1~s5` 基础 + 平板媒体查询）

---

## 2026-07-30（优化）- 欢迎卡片词云字号/颜色均为不重复随机 + 必含最大字号

### 变更类型
优化

### 变更内容
1. **字号不重复随机**：原 `randomizeCloud()` 中每词字号 `1 + floor(random*5)` 可重复；改为从 5 档（s1-s5）洗牌取 4 档逐一分配，四词字号互不相同。因仅丢弃 1 档，**必保留 s4/s5 两个最大字号之一**，满足"每次至少有一个词处于最大两档字号之一"。
2. **颜色保持不重复**：颜色仍从 6 色池（c1-c6）洗牌取 4，四词颜色互不重复（此前已实现，本次确认）。

### 影响范围
- `frontend/src/pages/index/index.vue`（`randomizeCloud` 函数）

---

## 2026-07-30（重构）- 清理前端预存技术债

### 变更类型
重构 / 优化

### 变更内容
1. **删除 `global.scss` 5 个长期无引用的设计令牌**：`--color-placeholder-focus`、`--radius-card`、`--radius-card-lg`、`--radius-btn`、`--page-padding-top`，全项目无任何 `var()` 引用（前几轮已先行清理 `--color-brand-border-light`）。
2. **删除新手引导 `activeStyle` 占位返回值及其全链路引用**：`useGuideTarget.js` 原本返回 `{ requery, activeStyle }`，其中 `activeStyle` 为恒为 `{}` 的占位（`computed(() => ({}))`），无任何实际作用，且会触发无意义的响应式依赖。已将返回值收敛为仅 `{ requery }`，并移除以下文件中对应的 `activeStyle` 解构与 `:style` 绑定：
   - `BottomNav.vue`（home-tab、settings-tab）
   - `user/login.vue`（wechat-login、register-link、login-submit）
   - `user/register.vue`（register-submit）
   - `index/settings.vue`（仅保留 `requery` 解构，未动）
   - `index/plan.vue`（new-plan）
   - `index/notification.vue`（add-notification、wechat-auth-button、notification-form-card、email-type-radio、email-save-button，原 `:style` 数组绑定一并移除）
   - `index/index.vue`（checkin-button）

### 扫描范围与未处理项
- 扫描全部 `.vue` / `.js`：除词云动态生成的 `--s*` / `--c*` 变体类外，无其它无引用样式类、无其它未使用导入。
- 后端 / 测试 / 脚本经 `pyflakes` 扫描：
  - `tests/integration/backend/conftest.py`、`tests/e2e/conftest.py` 的"imported but unused"为**误报**——模型导入用于 SQLAlchemy `Base.metadata` 注册（保证 `create_all` 建表），fixture 导入用于 pytest 跨目录复用，均带 `# noqa: F401` 标记，**不可删除，否则破坏测试数据库初始化**。
  - `scripts/test_db_connection.py` 第 12-13 行两处 `f"..."` 无占位符（pyflakes "f-string without placeholders"），属真实但极轻微的告警；因涉及脚本与硬编码开发凭据，未纳入本次前端债清理范围，待确认是否处理。
- `useGuideTarget.js` 文档注释同步移除 `activeStyle` 相关说明。

### 影响范围
- `frontend/src/assets/styles/global.scss`
- `frontend/src/composables/useGuideTarget.js`
- `frontend/src/components/BottomNav.vue`、`frontend/src/pages/user/login.vue`、`frontend/src/pages/user/register.vue`、`frontend/src/pages/index/{settings,plan,notification,index}.vue`

---

## 2026-07-30（优化）- 词云置于 logo 下层、放大淡化 + 正文首行缩进 + 残留配置清理

### 变更类型
优化 / 重构

### 变更内容
1. **正文首行缩进**：欢迎卡片与介绍卡片共用的 `.index-page__intro-p` 增加 `text-indent: 2em`（居中 slogan 与链接不受影响）。
2. **词云置于 logo 下层**：logo 改为 `position: relative; z-index: 2`，关键词 `z-index: 1`，文字从 logo 下方穿过；移除原 logo `opacity: 0.9` 弱化与关键词白色 `text-shadow`（层级调整后不再需要）。
3. **词云字号加大 3 档**：s1-s5 由 32~56rpx 提升为 50~74rpx（平板 16~28px → 25~37px）；随机位置范围相应收窄为 x:18%~82%、y:12%~88%，避免大字号溢出卡片。
4. **词云改浅色系**：保留 c1-c6 六色池，统一以 `opacity: 0.38` 淡化为背景装饰，弱化后不与正文抢视觉。
5. **移除重叠避让**：词云位置改为纯随机，允许词之间部分重叠，删除最小间距重试逻辑。
6. **残留无作用配置清理**：删除 `.index-page__guide-section-title`（基础 + 平板媒体查询，模板中已无引用）、删除 `global.scss` 中 `--color-brand-border-light`（随 intro-highlights 移除后全项目无引用）。

### 影响范围
- `frontend/src/pages/index/index.vue`、`frontend/src/assets/styles/global.scss`
- `目录结构.json`、`更新记录.md` 同步更新

---

## 2026-07-30（优化）- 欢迎卡片词云全区域随机 + 多彩配色 + 首句居中

### 变更类型
优化

### 变更内容
欢迎卡片词云二次调整：

1. **首句居中**：slogan 增加 `text-align: center`。
2. **位置全区域随机**：弃用固定候选槽位方案，改为在整块词云区域内全随机（x:10%-90%、y:8%-92%），可叠于 logo 之上；带最小间距避让（纵向权重 1.8 补偿容器高宽比，最多重试 20 次）防止四词互相重叠；旋转范围扩至 -12°~12°。
3. **字号更随机**：由 4 档变体改为 s1-s5 五档（32~56rpx，平板 16~28px），每词独立随机（可重复）。
4. **颜色多彩化**：由固定两色改为 c1-c6 六色池——品牌绿、品牌深绿、链接蓝（--color-link）、警告黄（--color-warning）、成功绿（--color-success）、次要文本色，均取自 global.scss 设计令牌；四词从池中不重复抽取。
5. **叠加可读性**：关键词 z-index 置于 logo 之上，附白色 text-shadow 柔光衬底；logo opacity 0.9 弱化为背景层。

### 影响范围
- `frontend/src/pages/index/index.vue`（仅欢迎卡片词云与 slogan）
- `目录结构.json`、`更新记录.md` 同步更新

---

## 2026-07-30（优化）- 欢迎卡片撑满首屏 + 词云随机化 + 无引用样式清理

### 变更类型
优化 / 重构

### 变更内容
按需求修正首页未登录欢迎卡片：

1. **卡片撑满首屏**：欢迎卡片由自适应内容高度改为 `flex:1` 撑满 main-canvas 剩余高度（视口 − 顶部留白 210rpx − 底部与导航栏间隔 240rpx），词云区同样 `flex:1` 填满卡片剩余空间，logo 与词云自然落在卡片中下方。
2. **删除底部弱化提示**：移除"点击无足鸟图标，查看详细介绍"提示及其样式。
3. **词云随机化**：新增 `randomizeCloud()`，每次进入首页（onShow，未登录时）随机生成词云——8 个候选百分比槽位（环绕 logo 四周 + logo 下方）洗牌取 4，叠加 ±4%/±3% 位置抖动与 -10°~10° 随机旋转；字号/颜色改为 v1-v4 变体类随机分配（位置用行内百分比随容器缩放，字号用类便于平板媒体查询以 px 覆盖）。
4. **点击区域扩大**：卡片翻转触发由"点击 logo"改为"点击整个词云区（含 logo 与关键词）"。
5. **简介突出展示**：首句简介升级为 slogan 样式（44rpx 深色加粗大字号，平板 22px）。
6. **无引用样式清理**（均为本次迭代遗留）：删除 `.index-page__intro-highlights`（基础 + 平板媒体查询）、`.index-page__intro-login` / `.index-page__intro-login-text`（平板媒体查询）、`.index-page__welcome-word--free/easy/safe/open` 固定四词类与 `.index-page__welcome-tip`。

### 影响范围
- `frontend/src/pages/index/index.vue`（仅未登录首屏欢迎卡片，介绍卡片与已登录逻辑不变）
- `目录结构.json`、`更新记录.md` 同步更新
- 注：全局设计令牌 `--color-brand-border-light`（global.scss）因 intro-highlights 删除而不再被引用，暂保留未删

---

## 2026-07-30（新增）- 首页未登录欢迎卡片（logo 词云）

### 变更类型
新增 / 优化

### 变更内容
将首页未登录介绍卡片中的"第一部分小程序介绍"拆分为独立的欢迎卡片，形成"欢迎卡片 → 介绍卡片"的两段式首屏：

1. **欢迎卡片（`.index-page__welcome-card`）**：未登录进入小程序时首先展示，无大标题。
   - 上方：小程序简介、隐私安全说明（含"自行部署"强调）、开源地址（点击复制，复用原 `copyRepoUrl`）。
   - 中下方词云区（`.index-page__welcome-cloud`）：无足鸟 logo（`assets/images/touxiang/hong.png`）居中，"免费/易用/安全/开源"四词以绝对定位错落分布于四角，字号 36~48rpx 有差异并轻微旋转（±5~8deg），品牌绿与文本色交替，形成词云观感。
   - 底部提示文案"点击无足鸟图标，查看详细介绍"，避免入口不可发现。
2. **切换逻辑**：新增 `showIntroCard` 状态，点击 logo 置为 true，欢迎卡片隐藏、介绍卡片显示，单向切换不提供返回。
3. **介绍卡片**：移除已迁出的第一部分（简介/高亮词/安全说明/开源地址），标题"按时吃药"、主要功能、新手引导及"点我开启新手引导"入口格式与样式保持不变。
4. **适配**：欢迎卡片补充 ≥768px 平板/折叠屏媒体查询，卡片宽度、词云高度、logo 与字号均以 px 锁定，避免 rpx 过度放大。

### 影响范围
- `frontend/src/pages/index/index.vue`（仅未登录首屏，不涉及已登录任务卡/新手引导卡与打卡逻辑）
- `目录结构.json`、`更新记录.md` 同步更新

---

## 2026-07-29（修复）- 预存技术债务清理：未使用导入、弃用 API、测试隔离、后端 bug 修复

### 变更类型
修复 / 重构

### 变更内容
全面扫描并清除后端及测试代码中的预存技术债务，涵盖未使用导入、Pydantic 弃用 API、测试数据格式不一致、测试隔离缺陷、后端 status 校验 bug：

**后端代码修复（8 项）：**

1. **未使用导入清理**（pyflakes 检出，6 处）：
   - `models/checkin_record.py`：移除未使用的 `from sqlalchemy.sql import func`
   - `models/notification_log.py`：移除未使用的 `from sqlalchemy.sql import func`
   - `services/announcement_service.py`：移除未使用的 `delete`（import 中仅 select/func/text 实际使用）
   - `services/notification_channel_service.py`：移除未使用的 `decrypt`（仅 `encrypt` 用于渠道密码加密）
   - `services/user_service.py`：移除未使用的 `datetime`（仅 `timedelta` 用于过期清理）、移除未使用的 `logger`（错误处理通过 ValueError/HTTPException，不记日志）

2. **Pydantic v2 弃用 API 迁移**（`core/config.py`）：
   - `class Config:` 内部类 → `model_config = SettingsConfigDict(...)`，消除 PydanticDeprecatedSince20 警告

3. **认证依赖 status 校验 bug 修复**（`core/deps.py`）：
   - `schedule_deletion` 设置 `status=0` + `token_invalid_before`，导致 `cancel_deletion` 不可达（auth 层 `status != 1` 拦截了待删除用户）
   - 修复：`status != 1` → `status not in (0, 1)`，允许 status=0（待删除）用户在 24h 宽限期内操作（如取消删除），`token_invalid_before` 已确保旧 token 失效

**测试代码修复（5 类，13 处）：**

4. **未使用导入清理**（pyflakes 检出，4 处）：
   - `test_service_coverage.py`：移除未使用的 `CheckinPlan, PlanNotificationTime, PlanNotificationChannel`
   - `test_schemas.py`：移除未使用的 `SendChangeEmailNewCode`
   - `test_security.py`：移除未使用的 `import time`
   - `test_api_coverage.py`：移除未使用的 `Security`（改用 bypass_auth fixture 后不再需要手动生成 token）

5. **验证码 2-tuple → 3-tuple 格式修复**（6 处）：
   - 生产代码 `_verification_codes` 存储 `(code, expire_at, fail_count)` 3-tuple（安全审计新增 fail_count 防爆破），测试仍用 2-tuple 导致 `ValueError: not enough values to unpack`
   - 修复：`test_service_extended.py`、`test_auth_api.py`、`test_service_coverage.py` 共 6 处补充 `fail_count=0`

6. **session_key 加密断言修复**（`test_service_extended.py`）：
   - 生产代码 `encrypt(session_key)` 加密存储，测试断言明文 `== "new_session_key"` 失败
   - 修复：导入 `decrypt`，断言改为 `decrypt(updated_account.session_key) == "new_session_key"`

7. **测试隔离与 mock 修复**（3 类）：
   - **限流器状态共享（429）**：限流器 `_rate_store` 为进程内字典，跨测试共享导致 429。在根 `conftest.py` 添加 `reset_rate_limiter` autouse fixture，每测试前清空（覆盖所有 unit/integration/e2e 测试）
   - **认证 DB 校验拦截（401）**：6 个测试期望 400/404（service 层"用户不存在"），但 auth 依赖先查 DB 返回 401。新增 `bypass_auth` fixture（覆盖 `get_current_user_id` 返回 999999），使请求直达 service 层
   - **mock 缺少 role 属性（TypeError）**：`test_wechat_login_success` 的 `MagicMock()` 未设 `role`，序列化 `role` 字段时报 `TypeError: Object of type MagicMock is not JSON serializable`。补充 `mock_user.role = 0`

8. **弃用 API 修复**（`test_timezone.py`）：
   - `datetime.utcnow()` → `datetime.now(timezone.utc).replace(tzinfo=None)` / `.date()`，消除 DeprecationWarning

9. **cancel_deletion 测试修复**（`test_user_profile_api.py`）：
   - `schedule_deletion` 设置 `token_invalid_before` 使旧 token 失效，测试需在 `schedule_deletion` 后重新生成 token（模拟重新登录）

### 影响范围
- `backend/app/models/checkin_record.py`、`backend/app/models/notification_log.py`（移除未使用 func 导入）
- `backend/app/services/announcement_service.py`、`backend/app/services/notification_channel_service.py`、`backend/app/services/user_service.py`（移除未使用导入）
- `backend/app/core/config.py`（Pydantic v2 SettingsConfigDict）
- `backend/app/core/deps.py`（status 校验修复，允许 status=0）
- `tests/conftest.py`（新增 reset_rate_limiter autouse fixture）
- `tests/integration/backend/conftest.py`（新增 bypass_auth fixture）
- `tests/integration/backend/test_api_coverage.py`（6 个测试改用 bypass_auth、移除 Security 导入、mock 添加 role）
- `tests/integration/backend/test_user_profile_api.py`（cancel_deletion 测试重新生成 token）
- `tests/integration/backend/test_service_coverage.py`（移除未使用导入、6 处 3-tuple）
- `tests/integration/backend/test_service_extended.py`（3-tuple、session_key decrypt 断言）
- `tests/integration/backend/test_auth_api.py`（3-tuple）
- `tests/unit/backend/test_schemas.py`、`tests/unit/backend/test_security.py`、`tests/unit/backend/test_timezone.py`（移除未使用导入、datetime.utcnow 修复）
- `目录结构.json`、`更新记录.md`（同步文档）

### 验证
- 全部 331 个测试通过，零失败、零警告
- pyflakes 仅剩带 `# noqa: F401` 的有意导入（模型注册 / fixture 重导出），非技术债务
- 后端 config 加载验证通过

---

## 2026-07-29（重构）- 后端代码去重与规范化：消除重复逻辑、复用服务类、对齐编码规范

### 变更类型
重构

### 变更内容
对后端代码进行重复逻辑消除与编码规范对齐，不改变任何功能与实现逻辑，仅消除重复、提升复用性、补齐规范缺失项：

1. **user_service.py 去重**（消除与 NotificationChannelService 的重复实现）：
   - 移除私有方法 `_ensure_znx_channel`（与 `NotificationChannelService.ensure_znx_channel` 重复），注册/微信登录两处改为调用 `NotificationChannelService(self.db).ensure_znx_channel(user_id)`
   - 移除私有方法 `_parse_wechat_channel_value`（与 `NotificationChannelService.parse_wechat_channel_value` 重复），账号合并逻辑改为调用 `NotificationChannelService.parse_wechat_channel_value()`

2. **plan_service.py 去重**（create_plan 与 update_plan 重复逻辑抽取为私有辅助方法）：
   - 抽取 `_validate_channel_ownership`：校验通知渠道归属（非空 + 均属于该用户）
   - 抽取 `_parse_time_str`：HH:MM / HH:MM:SS 字符串解析为 datetime.time
   - 抽取 `_write_notification_times`：批量写入通知时间点
   - 抽取 `_write_plan_channels`：批量写入计划-渠道关联

3. **notification_log_service.py 规范化**（避免 API 层直接调用私有方法）：
   - 新增公共方法 `count_unread_with_auto_mark`：封装"先自动标记已读再返回未读数"逻辑，供 unread-count 接口复用

4. **scheduler_service.py 去重**（消除与 CheckinService 重复的区间计算）：
   - `_get_match_intervals_minutes` 改为直接委托 `CheckinService._get_match_intervals`，消除重复实现

5. **users.py（API 层）去重**（_user_payload 与 get_user_info 重复的微信绑定查询）：
   - 抽取 `_is_wechat_bound` 辅助函数，两处调用统一复用

6. **logger.py 规范对齐**（AGENTS.md §6.1 要求日志格式含模块、行号）：
   - 日志格式由 `%(asctime)s - %(name)s - %(levelname)s - %(message)s` 更新为 `%(asctime)s - %(name)s - %(levelname)s - %(module)s:%(lineno)d - %(message)s`

7. **checkins.py 文档修正**（注释与实现不一致）：
   - create_checkin 文档串由"同一天同一计划同一时间点只能打卡一次"更正为"允许重复打卡（由 CheckinService.create_checkin 校验计划/时间点归属）"

8. **announcement_service.py 清理**：移除未使用的 `update` 导入（原 `select, update, delete, func, text` → `select, delete, func, text`）

9. **checkin_service.py 类型注解**：移除 `from typing import Optional` 导入，`Optional[CheckinRecord]` 更新为 `CheckinRecord | None`（Python 3.10+ 现代写法，遵循 AGENTS.md §6.1 类型注解要求）

10. **测试同步**：`test_ensure_znx_channel_existing` 原直接调用已移除的私有方法 `User._ensure_znx_channel`，改为调用 `NotificationChannelService.ensure_znx_channel`（保持测试意图不变，仅切换到重构后的公共服务方法）

### 影响范围
- `backend/app/services/user_service.py`（移除 2 个重复私有方法，改委托 NotificationChannelService）
- `backend/app/services/plan_service.py`（抽取 4 个私有辅助方法供 create/update 复用）
- `backend/app/services/notification_log_service.py`（新增 count_unread_with_auto_mark 公共方法）
- `backend/app/services/scheduler_service.py`（区间计算委托 CheckinService）
- `backend/app/services/announcement_service.py`（移除未使用导入）
- `backend/app/services/checkin_service.py`（类型注解现代化）
- `backend/app/api/v1/users.py`（抽取 _is_wechat_bound 辅助函数）
- `backend/app/api/v1/notification_logs.py`（改用 count_unread_with_auto_mark 公共方法）
- `backend/app/api/v1/checkins.py`（修正文档串）
- `backend/app/utils/logger.py`（日志格式补齐模块:行号）
- `tests/integration/backend/test_service_coverage.py`（测试切换到新公共服务方法）
- `目录结构.json`、`更新记录.md`（同步文档）

### 验证
- 通过 git stash 对比基线，确认重构未引入新测试失败：唯一受影响的 `test_ensure_znx_channel_existing` 已修复（改调 NotificationChannelService）
- 其余预存在失败（验证码 2-tuple 格式不匹配、session_key 加密断言）为安全审计阶段遗留的技术债务，非本次重构引入，未主动修改

---

## 2026-07-29（重构）- 前端设计令牌收尾：单次使用颜色全面令牌化

### 变更类型
重构

### 变更内容
延续上一轮设计令牌规范化工作，按用户"单次使用颜色也要完全按照设计令牌（Design Tokens）进行实现"的要求，将上一轮保留未替换的 CSS 硬编码颜色全部令牌化，不改变任何界面显示效果：

1. **新增 1 个语义令牌**（`frontend/src/assets/styles/global.scss`）：
   - `--color-text-inverse: #ffffff`（反色文字：品牌色/危险色等彩色背景上的白色文字与图标）

2. **替换剩余 19 处 CSS 硬编码颜色**（值相同，仅从硬编码改为变量引用）：
   - `#ffffff`（16 处）→ `var(--color-text-inverse)`
     - 彩色背景上的白色文字：`announcement.vue`（保存按钮）、`index.vue`（打卡按钮文字 + 倒计时数字）、`plan.vue`（保存按钮文字）、`notification.vue`（徽标文字 + 重新授权按钮文字 + 保存按钮文字）
     - CSS 绘制的白色对勾边框：`login.vue` / `register.vue` / `plan.vue`（复选框选中态对勾的两条边）
   - `#0e0f0c`（2 处，`record.vue` 日历左右箭头）→ `var(--color-text-primary)`
   - `#c7e4b0`（1 处，`index.vue` 介绍卡片高亮区内描边）→ `var(--color-brand-border-light)`

3. **保留未替换项**（符合约束）：
   - 注释 `/* ... */` 与 `//` 中的颜色值（说明性文字，非实际样式声明）
   - `pages.json` 中 `backgroundColor` 配置（JSON 不支持 CSS 变量）

### 影响范围
- `frontend/src/assets/styles/global.scss`（新增 --color-text-inverse 令牌）
- `frontend/src/pages/user/announcement.vue`（1 处 #ffffff）
- `frontend/src/pages/index/index.vue`（3 处：#c7e4b0 + 2 处 #ffffff）
- `frontend/src/pages/index/record.vue`（2 处 #0e0f0c）
- `frontend/src/pages/user/login.vue`（2 处 #ffffff）
- `frontend/src/pages/user/register.vue`（2 处 #ffffff）
- `frontend/src/pages/index/plan.vue`（3 处 #ffffff）
- `frontend/src/pages/index/notification.vue`（3 处 #ffffff）

---

## 2026-07-29（重构）- 前端设计令牌规范化：标题区组件复用 + 硬编码颜色统一改为 CSS 变量

### 变更类型
重构

### 变更内容
基于 W3C 设计令牌（Design Tokens）规范与项目 AGENTS.md 第 6.2 节"全局变量在 uni.scss 或 assets/styles/variables.scss"要求，对前端代码进行三项规范化重构，不改变任何界面显示效果：

1. **目录结构.json 文档修正**
   - `announcementRead.js` 原错误地记录在 `backend/app/utils/` 下，实际位于 `frontend/src/utils/`，已从后端目录移除并在前端 `frontend/src/utils/` 目录下补充记录

2. **plan.vue / announcement.vue 标题区复用 PageHeader 组件**
   - `frontend/src/pages/index/plan.vue`：移除手写的 `plan-page__header` / `__title` / `__desc` 结构与样式（含平板断点），替换为 `<PageHeader title="制定计划" desc="..." />`
   - `frontend/src/pages/user/announcement.vue`：移除手写的 `announcement-page__header` / `__title` / `__desc` 结构与样式（含平板断点），替换为 `<PageHeader title="公告管理" desc="..." />`
   - 两页面 desc 与 title 间距统一为 PageHeader 的 16rpx（原手写为 16rpx gap + 8rpx padding-top，接受此 4px 视觉微调以换取组件复用一致性）

3. **全量硬编码颜色改为 CSS 变量（Design Tokens）**
   - 将 14 个页面 + 4 个组件中共约 410 处硬编码颜色值替换为 `frontend/src/assets/styles/global.scss` 中定义的 CSS 变量
   - **新增 10 个语义令牌**（用于在 2+ 页面中重复出现但原无对应变量的颜色）：
     - `--color-text-muted: #7a7b79`（弱化文本变体：时间戳、徽标小字）
     - `--color-label: #41493a`（表单标签、胶囊徽标文字）
     - `--color-brand-dark: #2e6900`（品牌深绿：徽标深色文字）
     - `--color-brand-bg-light: #e8f5e0`（品牌极浅绿：徽标浅色背景）
     - `--color-success: #2ead4b`（成功绿：完成状态标识）
     - `--color-form-error: #e5484d`（表单错误：输入框错误边框、错误提示文字）
     - `--color-warning: #d97706`（警告：字符限制提示等）
     - `--color-border-card: #e2e2e2`（卡片阴影描边）
     - `--color-separator: #f3f3f4`（分组分隔线背景）
     - `--color-selected-bg: #e2f6d5`（选中态背景）
   - 替换映射（值相同，仅从硬编码改为变量引用）：
     - `#0e0f0c` → `var(--color-text-primary)`（主文本色）
     - `#454745` → `var(--color-text-secondary)`（次要文本色，含 border-color / PasswordEye 图标）
     - `#868685` → `var(--color-text-tertiary)`（占位/弱化文本色）
     - `#2f6c00` → `var(--color-brand)`（主品牌绿，含 border-left-color）
     - `#9fe870` → `var(--color-brand-bg)`（品牌浅绿，含 BeginnerGuide 呼吸动画 box-shadow）
     - `#d03238` → `var(--color-danger)`（危险红，含 background）
     - `#ffffff` / `#fff`（background）→ `var(--color-card-bg)`（卡片背景）
     - `#f9f9f9` → `var(--color-card-bg-alt)`（次级容器背景）
     - `#e8ebe6`（border/box-shadow）→ `var(--color-border)`（边框/分隔线）
     - `#c1cab5` → `var(--color-border-input)`（输入框外描边，含 border / box-shadow / background）
     - `#41493a` → `var(--color-label)`、`#e5484d` → `var(--color-form-error)`、`#d97706` → `var(--color-warning)`
     - `#e2e2e2` → `var(--color-border-card)`、`#f3f3f4` → `var(--color-separator)`、`#e2f6d5` → `var(--color-selected-bg)`
     - `#7a7b79` → `var(--color-text-muted)`、`#2e6900` → `var(--color-brand-dark)`、`#e8f5e0` → `var(--color-brand-bg-light)`、`#2ead4b` → `var(--color-success)`
     - 复合 box-shadow `inset 0 0 0 1px #xxx, 0 1px 2px 0 rgba(0,0,0,0.05)` → `inset 0 0 0 1px var(--xxx), var(--shadow-card)`
   - 保留未替换的颜色（符合约束）：
     - `color: #ffffff`（按钮白色文字）与 `border: solid #ffffff`（checkmark 白色边框）
     - `border: solid #0e0f0c`（record.vue 日历箭头三角形，非文本用途）
     - `background: #e8ebe6`（plan.vue 胶囊徽章，语义不明确，保留原值）
     - 注释 `/* ... */` 中的颜色值
     - 仅在 1 个文件中使用的单次颜色（如 `#07c160` 微信绿、`#0066cc` 链接蓝、`#888888` 灰色等，按极简主义原则不抽象为变量）
     - `pages.json` 中 `backgroundColor` 配置（JSON 配置不支持 CSS 变量）

### 影响范围
- `目录结构.json`（修正 announcementRead.js 位置）
- `frontend/src/pages/index/plan.vue`（标题区复用 + 颜色变量化）
- `frontend/src/pages/index/notification.vue`（颜色变量化）
- `frontend/src/pages/index/record.vue`（颜色变量化）
- `frontend/src/pages/index/index.vue`（颜色变量化）
- `frontend/src/pages/index/settings.vue`（颜色变量化）
- `frontend/src/pages/user/announcement.vue`（标题区复用 + 颜色变量化）
- `frontend/src/pages/user/login.vue`（颜色变量化）
- `frontend/src/pages/user/register.vue`（颜色变量化）
- `frontend/src/pages/user/forgot-password.vue`（颜色变量化）
- `frontend/src/pages/user/profile.vue`（颜色变量化）
- `frontend/src/pages/user/messages.vue`（颜色变量化）
- `frontend/src/pages/user/help.vue`（颜色变量化）
- `frontend/src/pages/user/contact.vue`（颜色变量化）
- `frontend/src/pages/user/privacy.vue`（颜色变量化）
- `frontend/src/components/AnnouncementCard.vue`（颜色变量化）
- `frontend/src/components/BeginnerGuide.vue`（颜色变量化）
- `frontend/src/components/BottomNav.vue`（颜色变量化）
- `frontend/src/components/PasswordEye.vue`（颜色变量化）

---

## 2026-07-29（功能）- 公告管理页新增公共模板编辑与发布预填充，首页公告排除模板记录

### 变更类型
功能

### 变更内容
1. **后端公告接口调整**
   - `backend/app/services/announcement_service.py`：
     - `list_all()` 与 `list_recent()` 均排除 `id=1` 的公共公告模板，避免模板进入普通公告列表
     - 新增 `get_template()` 方法，查询 `id=1` 模板记录
     - `update()` 方法特殊处理 `id=1`：模板记录不存在时自动创建，存在则更新
   - `backend/app/api/v1/announcements.py`：
     - 新增 `GET /api/v1/announcements/template` 接口，供管理员获取 `id=1` 模板
     - 更新列表接口文档，说明已排除 `id=1` 模板
     - 更新 `PUT /announcements/{announcement_id}` 文档，说明更新 `id=1` 时自动创建

2. **前端公告 API 模块**
   - `frontend/src/api/modules/announcement.js`：新增 `getAnnouncementTemplate()` 请求 `GET /api/v1/announcements/template`

3. **公告管理页（announcement.vue）**
   - 在"发布公告"入口卡上方新增"公告模板"入口卡，显示当前模板标题
   - 点击"公告模板"展开模板编辑表单，提交后调用 `updateAnnouncement(1, ...)` 更新公共模板
   - 点击"发布公告"展开创建表单时，先查询 `id=1` 模板内容并预填充到标题/内容输入框；提交后仍调用 `publishAnnouncement()` 新增记录，不更新模板
   - 公告列表加载时防御性过滤 `id=1`，确保模板不在列表中展示
   - 新增模板编辑表单相关状态、字符限制与响应式样式

4. **首页（index.vue）**
   - `loadRecentAnnouncements()` 对返回结果防御性过滤 `id=1`，确保首页临时公告不展示模板记录

### 影响范围
- `backend/app/services/announcement_service.py`
- `backend/app/api/v1/announcements.py`
- `frontend/src/api/modules/announcement.js`
- `frontend/src/pages/user/announcement.vue`
- `frontend/src/pages/index/index.vue`
- `目录结构.json`

---

## 2026-07-28（优化）- 密钥轮换脚本新增 session_key 字段支持

### 变更类型
优化

### 变更内容
`backend/sql/rotate_encryption_key.py` 密钥轮换脚本新增对 `user_miniapp_accounts.session_key` 字段的轮换逻辑，与邮件渠道密码并列处理：

1. **新增第二部分：微信 session_key 轮换**
   - 跨库查询 `wuzuniao_yonghu.user_miniapp_accounts` 表（邮件渠道在 `wuzuniao_yao` 库，session_key 在 `wuzuniao_yonghu` 库，通过 SQL 显式指定数据库名前缀实现跨库操作）
   - 查询所有非空 `session_key` 记录

2. **session_key 明文/密文自适应处理**
   - 尝试用旧密钥解密：成功则视为密文，解密后用新密钥重新加密
   - 解密失败则视为明文（安全审计前的历史数据），直接用新密钥加密
   - 无论原值是明文还是密文，轮换后统一为加密存储

3. **事务原子性保证**
   - 邮件密码和 session_key 在同一数据库事务中更新
   - 全部成功才提交，任何一步失败都回滚

4. **验证阶段同步覆盖**
   - 事务提交后，用新密钥解密验证邮件密码和 session_key 的正确性

5. **脚本头部文档更新**
   - 标题改为"加密密钥轮换脚本（邮件密码 + 微信 session_key）"
   - 补充涉及的加密字段说明（含所属数据库名）和明文/密文自适应处理策略

### 影响范围
- `backend/sql/rotate_encryption_key.py`：main() 函数重构，新增 session_key 跨库查询、明文/密文自适应解密、重新加密、事务更新、验证逻辑
- 与之前的安全加固（session_key 改为加密存储）配套，确保密钥轮换时两类加密字段同步更新

---

## 2026-07-28（安全加固）- 后端全量接口安全审计与漏洞修复

### 变更类型
安全加固

### 变更内容
对后端全量 API 接口进行安全审计，修复以下 10 项漏洞（用户枚举问题经用户确认保持现状）：

**高危修复**
1. CORS 完全开放 → 改为白名单限制
   - `backend/app/core/config.py`：新增 `CORS_ALLOW_ORIGINS` 配置项（逗号分隔）和 `cors_origins_list` 属性
   - `backend/app/main.py`：`allow_origins` 改为 `settings.cors_origins_list`，`allow_methods` 限定为 GET/POST/PUT/DELETE/OPTIONS，`allow_headers` 限定为 Authorization/Content-Type
   - `backend/.env.template`：新增 CORS_ALLOW_ORIGINS 配置说明

**中危修复**
2. 无限流 → 新增进程内字典限流器
   - 新增 `backend/app/core/rate_limit.py`：滑动窗口算法，按 客户端IP+接口路径 维度计数
   - 预定义策略：limit_login(5次/分钟)、limit_register(3次/分钟)、limit_send_code(3次/分钟)、limit_reset_password(3次/分钟)
   - `backend/app/api/v1/users.py`：login/wechat-login/register/reset-password/send-code/send-reset-code/send-change-email-old-code/send-change-email-new-code 接口应用限流
3. 验证码爆破 → 添加失败次数限制
   - `backend/app/services/user_service.py`：`_verification_codes` 存储结构改为 `(code, expire_at, fail_count)` 三元组，连续失败 `CODE_MAX_FAIL_COUNT=5` 次后销毁验证码
4. 用户状态校验缺失 → 依赖中查库校验
   - `backend/app/core/deps.py`：`get_current_user_id` 与 `get_current_admin` 现在查库校验用户存在性、`status==1`（防止账号删除后旧 token 仍可用）
5. 修改密码/重置密码/删除账号后旧 token 未失效 → 通过 token_invalid_before 实现
   - `backend/app/models/user.py`：新增 `token_invalid_before` 字段
   - 新增 `backend/sql/add_token_invalid_before.sql`：增量脚本，同步回填到 `create_user_db.sql`
   - `backend/app/core/deps.py`：校验 `iat < token_invalid_before.timestamp()` 则拒绝
   - `backend/app/services/user_service.py`：`change_password`/`reset_password`/`schedule_deletion`/`logout` 时设置 `token_invalid_before = now_shanghai()`
6. 微信 session_key 明文存储 → 改为 AES-256-GCM 加密存储
   - `backend/app/services/user_service.py`：`wechat_login` 和 `bind_wechat` 中 `session_key` 通过 `encrypt()` 加密后存储
7. 微信接口异常信息泄露 → 改为通用提示+日志记录
   - `backend/app/api/v1/users.py`：`wechat_login` 和 `bind_wechat` 的 `Exception` 分支改为 `logger.exception` 记录详细异常，前端仅返回"微信登录/绑定服务异常，请稍后重试"

**低危修复**
8. 路径参数未校验正整数 → 添加 `Path(gt=0)` 校验
   - `backend/app/api/v1/plans.py`：`delete_plan`/`update_plan` 的 `plan_id`
   - `backend/app/api/v1/checkins.py`：`list_today_checkins_by_plan` 的 `plan_id`
   - `backend/app/api/v1/announcements.py`：`update_announcement`/`delete_announcement` 的 `announcement_id`
9. 头像 URL 未校验协议 → 添加协议白名单校验
   - `backend/app/core/security.py`：新增 `validate_avatar_url` 方法，禁止 `javascript:`/`data:`/`vbscript:`/`file:` 等危险协议
   - `backend/app/schemas/user.py`：`UpdateAvatar.avatar_url` 改用 `Security.validate_avatar_url`
10. 无 /logout 接口 → 新增退出登录接口
    - `backend/app/api/v1/users.py`：新增 `POST /users/logout` 接口，设置 `token_invalid_before` 使当前 token 立即失效
    - `backend/app/services/user_service.py`：新增 `logout` 方法

### 影响范围
- 后端全量 API 接口的安全校验机制（鉴权、限流、输入校验、token 失效）
- 数据库结构：users 表新增 `token_invalid_before` 字段（生产环境需执行 `add_token_invalid_before.sql`）
- 配置文件：新增 `CORS_ALLOW_ORIGINS` 环境变量（需在 .env 中配置）
- 前端需配合：用户修改密码/重置密码/退出登录后需清除本地 token 并重新登录

---

## 2026-07-28（优化）- 首页任务排序增加日期范围分组，今天在日期范围内的计划优先展示

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：优化 `activePlans` 排序逻辑。
  - `computePlanSortKey` 新增第一键 `dateGroup`：判断今天是否在计划日期范围内（`start_date <= today <= end_date`），在范围内 `dateGroup=0` 排前，否则 `dateGroup=1` 排后。
  - `sortPlansByCheckinStatus` 排序时先按 `dateGroup` 升序，再按原有 `group/sortKey/priority/createdAt` 排序。
  - 原有提醒时间分组规则作为第二层级，在日期分组内部生效。

### 影响范围
- 首页主要/次要卡片及任务列表：今天在计划日期范围内的任务始终排在最前；不在范围内的任务排在后面。
- 不过滤日期范围外的计划，仅调整展示顺序。

---

## 2026-07-28（优化）- 首页排序增加"提醒时间已到"判定，未到提醒时间的任务不再占据主要卡片

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：`computePlanSortKey` 的 group=0 判定条件由"当前匹配区间未打卡"改为"提醒时间已到且当前匹配区间未打卡"。
  - 新增 `isReminderReached` 判定：`nowMinutes >= times[currentIdx].minutes`（当前时间 >= 当前匹配区间对应的提醒时间）。
  - group=0 条件：`isReminderReached && !isChecked`（提醒时间已到且未打卡）。
  - group=1：提醒时间未到、已打卡、或无提醒（按下一个最近提醒时间排序）。
  - 例：提醒 8:00/16:00，当前 14:00 在 16:00 的匹配区间内，16:00 未到 → group=1，不再占据主要卡片。

### 影响范围
- 首页主要/次要卡片排序：当所有任务都未打卡时，提醒时间已到的任务优先排前（group=0），提醒时间未到的任务排后（group=1）。
- 避免高优先级但提醒时间未到的任务抢占主要卡片，确保"提醒时间已是当前时间且未打卡"的任务优先展示。

---

## 2026-07-28（修复）- 首页任务排序 `isChecked` 判定错误导致已打卡任务始终占据主要卡片

### 变更类型
修复

### 变更内容
- `frontend/src/pages/index/index.vue`：`computePlanSortKey` 中判定当前匹配区间是否已打卡时，`checkinMinutesList` 元素为 `{ timeId, minutes }` 对象，但代码误用 `m`（对象）直接与数字比较，对象转数字得 `NaN`，`NaN >= interval.start` 恒为 `false`，导致 `isChecked` 永远返回 `false`。
  - 修复：`m >= interval.start && m < interval.end` → `m.minutes >= interval.start && m.minutes < interval.end`。
  - 影响：修复前所有任务都被判定为"未打卡"（group=0），排序退化为纯 priority 排序，高优先级任务即使已打卡也始终占据主要卡片；修复后已打卡任务正确归入 group=1 排后，未打卡任务才能进入主要卡片。

### 根因分析
- `computePlanSortKey` 接收的 `checkinMinutesList` 来自 `allTodayCheckins.value[plan.id]`，元素结构为 `{ timeId, minutes }`。
- 对比 `isIntervalChecked` 函数（打卡按钮状态判定）用的是 `r.minutes`（正确），但 `computePlanSortKey` 中遗漏了 `.minutes`，直接用对象比较。

### 影响范围
- 首页主要/次要卡片排序：修复后已打卡任务不再占据主要卡片，到达提醒时间且未打卡的任务能正确展示到主要卡片。

---

## 2026-07-28（优化）- 首页任务卡片刷新策略调整（1分钟刷新 + 替换用户选择）

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：首页任务卡片刷新策略由"每分钟刷新且保持用户选择"改为"每1分钟刷新并替换用户选择，用户主动选择时重置1分钟计时器"。
  - `refreshCheckinStatus`：移除"保持用户已选主要卡片"逻辑，改为每次刷新都按新规则重新选择主要/次要卡片（`primaryPlanId` 取排序后第一项，`secondaryPlanId` 重置为 null），并重置 `forceActive`。
  - 新增 `startRefreshTimer`：启动/重置1分钟（60000ms）刷新计时器。`onShow` 中调用以启动；`handleSecondaryClick`、`handleSelectTask` 中调用以重置（保证用户主动选择至少保留1分钟）。
  - `onShow`：刷新间隔通过 `startRefreshTimer()` 启动（60000ms）。

### 影响范围
- 首页主要/次要卡片：用户主动切换后保留1分钟，1分钟后按"到达提醒时间且未打卡优先"规则自动替换。
- 任务列表弹层排序：每1分钟重排一次。
- 打卡按钮状态（done/active/waiting）：随1分钟刷新更新。

---

## 2026-07-27（修复）- 通知方式页引导期间禁止 page 滚动

### 变更类型
修复

### 变更内容
1. **`frontend/src/pages/index/notification.vue`**
   - **添加 `page-meta` 组件禁用 page 滚动**：引导激活且当前页面为通知方式页时，`page-meta` 的 `scroll-y="false"` 禁用 page 滚动，防止用户在引导期间滚动导致高亮与目标错位。
   - **根因**：BeginnerGuide 的 4 个透明矩形只覆盖高亮洞以外区域（@touchmove.stop），高亮洞内无 view 拦截 touchmove，用户在洞内滑动时 touchmove 冒泡到 page 容器，page 滚动。在洞内加 view 会拦截 click（目标按钮点不到），故改用 `page-meta` 在页面级别禁用滚动。
   - **为何用 `page-meta` 而非 `uni.setPageStyle`**：搜索显示 `overflow: hidden` 在微信小程序 iOS 端可能无法禁止 page 滚动；`page-meta` 的 `scroll-y` 是微信小程序官方属性，iOS/Android 均生效，且不拦截 click。
   - 新增 `guideScrollLock` computed（`guideStore.isActive && guideStore.currentPage === 'notification'`），控制 `scroll-y`。
2. **`frontend/src/components/BeginnerGuide.vue`**
   - **添加 `uni.setPageStyle` 禁用 page 滚动（全局保险）**：watch `ready`，引导就绪时 `uni.setPageStyle({ overflow: 'hidden' })`，引导结束/卸载时恢复。作为没有 `page-meta` 的引导页面（settings/login/register/plan/index）的保险（Android 端可能生效，iOS 端由 `page-meta` 覆盖）。
   - 导入 `watch`、`onUnmounted`。

### 影响范围
- `frontend/src/pages/index/notification.vue`
- `frontend/src/components/BeginnerGuide.vue`
- `更新记录.md`

### 根因分析
- BeginnerGuide 的蒙版由 4 个透明矩形组成，覆盖高亮洞以外区域并 `@touchmove.stop` 阻止洞外 touchmove。但高亮洞内不放置任何 view（让 click 穿透到目标），导致洞内 touchmove 未被阻止，冒泡到 page 容器触发滚动。
- 在洞内加 view 拦截 touchmove 会同时拦截 click（微信小程序中 view 覆盖目标会接收所有 touch 事件，目标收不到 tap）。
- `page-meta` 的 `scroll-y` 在页面级别禁用滚动，不涉及洞内 view，click 正常穿透。

---

## 2026-07-27（修复）- 新手引导高亮闪烁根治（移除 startRefresh 定时刷新）

### 变更类型
修复

### 变更内容
1. **`frontend/src/composables/useGuideTarget.js`**
   - **移除 `startRefresh`/`stopRefresh` 250ms 定时刷新**：这是闪烁的真正源头。原 `startRefresh` 每 250ms 调用 `queryOnce` → `reportRect`，`boundingClientRect` 在微信小程序中每次调用会强制同步布局计算（reflow），且浮点值抖动 ≥1px 时 `reportRect` 去重失效，`setTargetRect` 创建新 `targetRects` 引用，触发 `BeginnerGuide` 的 `targetRect` → `highlightRect` → `maskBounds` computed 链重算，Vue mp-weixin 编译器在 computed 重算时仍触发 setData，导致固定定位的高亮蒙版/边框 DOM 被频繁重新设置 style，视觉上表现为高亮区域频繁闪烁。引导激活时蒙版 `@touchmove.stop` 阻止页面滚动，目标位置稳定，无需持续刷新；改为 watch active 触发时 `queryAndReport` + 500ms 延迟 requery（覆盖异步加载完成后的布局修正），布局变化由各页面手动调用 `requery` 处理。
   - **移除 `queryOnce` 函数**：仅被 `startRefresh` 调用，移除后成为死代码，一并清理。
   - **`reportRect` 去重改为整数比较**：原 `<1px` 浮点阈值在 1px 边界附近抖动（如 100.0 ↔ 101.0）时仍触发更新；改为 `Math.round` 整数比较，整数相同则跳过 `setTargetRect`，更严格地避免不必要的 computed 链重算。
   - **`onUnmounted` 清理 `requeryTimer`**：替代原 `stopRefresh`。
2. **`frontend/src/store/modules/guide.js`**
   - **`setTargetRect`/`clearTargetRect` 改为属性级操作**：原用 spread `{ ...targetRects.value, [key]: rect }` 创建新对象引用，触发 ref 级依赖，导致所有依赖 `targetRects` 的 computed 重算；改为 `targetRects.value[key] = rect` 和 `delete targetRects.value[key]`，只触发 key 级响应式依赖（ref 内 plain object 在 ref 创建时已被 reactive 包装），减少不必要的响应式触发。
3. **`frontend/src/pages/index/settings.vue`**
   - **`loadPlans` 完成后 requery**：loadPlans 完成后"进行中"徽章显示/隐藏会改变 plan-method 卡片高度，进而影响 notification-method 位置（group1 两卡片贴合）。在 `finally` 块中检查引导激活，若激活则 `nextTick + setTimeout(150ms)` requery 三个目标，确保异步加载完成后的布局变化被捕获。

### 影响范围
- `frontend/src/composables/useGuideTarget.js`
- `frontend/src/store/modules/guide.js`
- `frontend/src/pages/index/settings.vue`
- `更新记录.md`

### 根因分析
- 之前的修复（`BeginnerGuide.maskBounds/cardStyle` 签名缓存）只阻断 `maskBounds` 下游的 computed 重算，但 `targetRect` → `highlightRect` 这两层 computed 仍因 `targetRects.value` 引用变化而重算，Vue mp-weixin 仍触发 setData，闪烁未解决。
- 本次修复从源头切断：移除 250ms 定时器，`setTargetRect` 不再被频繁调用；整数比较去重进一步减少不必要的调用；属性级操作避免 ref 级依赖触发。三层协同，彻底消除闪烁。
- `loadPlans` requery 作为补充，确保异步加载导致的单次布局变化也被捕获，避免位置不匹配。

---

## 2026-07-27（修复）- 新手引导高亮闪烁根治（BeginnerGuide computed 签名缓存）

### 变更类型
修复

### 变更内容
1. **`frontend/src/components/BeginnerGuide.vue`**
   - **根治设置页第 4/5 步高亮频繁闪烁（管理员与非管理员均有效）**：为 `maskBounds` 和 `cardStyle` 两个核心 computed 增加签名缓存。
     - **闪烁根因**：`useGuideTarget` 每 250ms 刷新目标位置，`boundingClientRect` 返回的浮点值存在 sub-pixel 抖动；抖动 ≥1px 时 `reportRect` 去重失效，`setTargetRect` 创建新 `targetRects` 引用，触发 `BeginnerGuide` 的 `targetRect` → `highlightRect` → `maskBounds` computed 链重算。`maskBounds` 用 `Math.floor/ceil` 取整，把 sub-pixel 抖动放大为相邻整数跳变（如 T: 100↔101），下游 `topBarStyle`/`bottomBarStyle`/`leftBarStyle`/`rightBarStyle`/`maskHoleStyle`/`highlightStyle` 全部重算返回新对象，Vue patcher 全量更新 style，高亮洞位置 1px 反复跳变，肉眼可见频繁闪烁。
     - **修复**：`maskBounds` 基于整数签名（`T|B|L|R|Math.round(cs)`）缓存返回值，相同签名返回同一对象引用，下游 computed 依赖未变不重算，DOM 不更新；只有高亮洞整数边界真正变化（如用户滚动、目标位移）时才返回新对象。`cardStyle` 同理基于整数签名（`Math.round(top)|Math.round(left)|cardWidth`）缓存。
     - **为何对管理员与非管理员均有效**：闪烁根因在 `BeginnerGuide` 的 computed 链与 `maskBounds` 的 floor/ceil 放大效应，与 `isAdmin` 无关（admin 卡片只影响初次布局，不影响持续抖动）。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`
- `更新记录.md`

### 根因分析
- 之前的修复（`useGuideTarget.reportRect` 去重，<1px 跳过 `setTargetRect`）是第一道防线，拦截了大部分不必要的更新。但 `boundingClientRect` 在某些渲染时机返回的浮点值抖动 ≥1px 时，去重失效，仍触发 `setTargetRect` → computed 链重算 → `maskBounds` floor/ceil 放大为整数跳变 → DOM 频繁更新闪烁。
- 本次修复在 `BeginnerGuide` 的 `maskBounds`/`cardStyle` 加签名缓存，作为第二道防线：即便上游 `setTargetRect` 被触发，只要高亮洞整数边界未变，下游 computed 不重算，DOM 不更新。两道防线协同，彻底消除 sub-pixel 抖动导致的闪烁。

---

## 2026-07-27（修复）- 新手引导设置页高亮位置不匹配与频繁闪烁

### 变更类型
修复

### 变更内容
1. **`frontend/src/composables/useGuideTarget.js`**
   - **修复高亮频繁闪烁**：`reportRect` 新增位置去重逻辑，当新旧位置差异 <1px 时跳过 `setTargetRect` 调用。原实现每次 250ms 定时刷新都创建新的 `targetRects` 对象引用，触发 BeginnerGuide 所有依赖 `targetRects` 的 computed（`targetRect` → `highlightRect` → `maskBounds` → 四边矩形样式/蒙版样式/高亮样式/卡片样式）重算与视图重建，即使目标元素位置完全未变。这是设置页第 4/5 步高亮频繁闪烁的根因。
   - **新增 `requery` 方法**：手动触发一次带重试的位置查询（复用 `queryAndReport`），返回值与 `activeStyle` 一起暴露供外部调用。供页面在布局动态变化时（如管理员按钮出现导致目标位移）手动重新查询目标位置。
   - 更新 JSDoc 说明位置去重机制与 `requery` 用法。
2. **`frontend/src/pages/index/settings.vue`**
   - **修复管理员用户引导位置不匹配**：新增 `watch(isAdmin)` 监听管理员状态变化。当 `isAdmin` 从 false 变为 true（登录管理员用户后返回设置页）时，公告管理按钮渲染插入用户资料卡与功能入口之间，导致通知方式/制定计划按钮位置下移。监听器在引导激活时通过 `nextTick` + `setTimeout(150ms)` 等待 DOM 重新布局后，调用 `requery()` 重新查询 profile-card、notification-method、plan-method 三个目标的位置，确保高亮区域与实际按钮匹配。
   - 导入 `watch`、`nextTick`，解构 `requery` 方法。
3. 同步更新 `目录结构.json` 中 `useGuideTarget.js` 与 `settings.vue` 的描述。
4. 执行 `npm run build:mp-weixin` 验证，构建成功。

### 影响范围
- `frontend/src/composables/useGuideTarget.js`
- `frontend/src/pages/index/settings.vue`
- `目录结构.json`、`更新记录.md`

### 根因分析
- **位置不匹配**：登录管理员用户后返回设置页时，`onShow` → `onPageEnter('settings')` 推进步骤到第 4 步，`useGuideTarget` watch 检测到步骤匹配后延迟 150ms 查询目标位置。但此时管理员按钮可能尚未完成 DOM 渲染（Vue 响应式更新与 `onShow` 存在竞争），导致查询到的是按钮渲染前的旧位置。后续 250ms 定时刷新虽能纠正，但由于每次都创建新对象引用，反而引入了闪烁问题。
- **频繁闪烁**：`startRefresh` 每 250ms 调用 `queryOnce` → `reportRect` → `setTargetRect`，`setTargetRect` 执行 `targetRects.value = { ...targetRects.value, [key]: rect }` 每次创建新对象引用。即使目标元素位置完全未变，新引用也触发所有依赖 `targetRects` 的 computed 重算，导致微信小程序中固定定位的高亮/蒙版视图频繁重建，视觉上表现为高亮区域频繁闪烁。

---

## 2026-07-27（优化）- 后端删除打卡计划时级联清理关联数据

### 变更类型
优化

### 变更内容
1. **plan_service.py**：优化 `PlanService.delete_plan` 删除逻辑。
   - 以 `plan_id` 为过滤条件，按顺序级联删除：
     - `checkin_records`（打卡记录）
     - `notification_logs`（通知发送记录）
     - `plan_notification_times`（每日通知时间点）
     - `plan_notification_channels`（计划与通知渠道关联记录）
   - `notification_channels`（通知渠道本身）保留不动。
   - 最后删除 `checkin_plans` 主记录。
2. **plans.py**：同步更新 `DELETE /{plan_id}` 接口的 docstring，说明级联范围与渠道保留策略。

### 影响范围
- backend/app/services/plan_service.py
- backend/app/api/v1/plans.py
- 更新记录.md

---

## 2026-07-27（优化）- 隐私政策前言样式对齐 PageHeader

### 变更类型
优化

### 变更内容
1. **privacy.vue**：将隐私政策页前言区域（`.privacy-page__intro-text`）的样式参考 `PageHeader` 组件描述文本样式进行调整，使与其他子页标题区描述保持一致。
   - 字体颜色由 `#0e0f0c` 改为 `var(--color-text-secondary)`。
   - 字号由 `40rpx` 改为 `32rpx`（平板断点由 `20px` 改为 `16px`）。
   - 行高由 `65.2rpx` 改为 `48rpx`（平板断点由 `32.6px` 改为 `24px`）。
   - 下方间距由 `48rpx` 调整为 `32rpx`（平板断点由 `24px` 调整为 `16px`）。
   - 字重保持 `400` 不变。

### 影响范围
- frontend/src/pages/user/privacy.vue
- 更新记录.md

---

## 2026-07-27（优化）- 帮助中心卡片标题加粗

### 变更类型
优化

### 变更内容
1. **help.vue**：将 FAQ 卡片标题的字重由 `600` 提升至 `700`，使标题在视觉上更突出。
   - 基础样式 `.help-page__card-title` 的 `font-weight` 由 `600` 改为 `700`。
   - 平板/折叠屏断点（≥768px）中同步将标题 `font-weight` 设为 `700`。
   - 卡片正文、链接等其他样式保持不变。

### 影响范围
- frontend/src/pages/user/help.vue
- 更新记录.md

---

## 2026-07-26（优化）- 设置页分组1两卡片高度适当减小

### 变更类型
优化

### 变更内容
1. **settings.vue**：适当减小「制定计划」与「通知方式」两卡片的高度。
   - `.settings-page__link-card` 垂直内边距由 `48rpx` 减至 `32rpx`（平板断点由 `24px` 减至 `16px`），水平内边距保持不变。
   - 单卡片高度减少约 `32rpx`（平板 `16px`），两卡片合计减少约 `64rpx`（平板 `32px`）。
   - 其他样式（背景、描边、圆角、内部布局、贴合分组等）均保留不变。

### 影响范围
- frontend/src/pages/index/settings.vue
- 更新记录.md

---

## 2026-07-26（优化）- 设置页分组1两卡片贴合为整体

### 变更类型
优化

### 变更内容
1. **settings.vue**：将「制定计划」与「通知方式」两个卡片合并为贴合的分组整体。
   - `.settings-page__group1` 间距由 `32rpx`（平板 `16px`）改为 `0`，两卡片紧贴。
   - 新增相邻边去圆角规则：首卡片（制定计划）去底部圆角、末卡片（通知方式）去顶部圆角，仅保留外侧圆角。
   - 中间分隔线由两卡片各自的 `inset 1px` 描边在贴合处自然形成（制定计划绿色描边在绿底上隐形、通知方式灰色描边在白底可见，呈现 1px 灰色细线）。
   - 其他样式（背景、描边、内部布局、平板断点等）均保留不变。

### 影响范围
- frontend/src/pages/index/settings.vue
- 更新记录.md

---

## 2026-07-26（优化）- 公告卡片标题下方展示创建时间

### 变更类型
优化

### 变更内容
1. **AnnouncementCard.vue**：常规态与满屏态公告卡片标题下方新增一行小字，展示公告创建时间（YYYY-MM-DD HH:MM）。
   - 新增 `displayCreatedAt` 计算属性：取 `created_at` 的日期+分钟部分，不做时区转换（兼容 `T` 与空格分隔两种格式）。
   - 新增 `.announcement-card__meta` 与 `.announcement-fullscreen__meta` 样式（弱化灰色 #7a7b79、小字号），并补充平板/折叠屏（≥768px）px 锁定适配。

### 影响范围
- frontend/src/components/AnnouncementCard.vue
- 更新记录.md

---

## 2026-07-26（文档）- 重写 README.md

### 变更类型
文档

### 变更内容
1. **README.md**：参考首页介绍卡片（index.vue）与项目现状重写，采用 GitHub 主流风格（精简徽章、特性清单、精简结构树、聚合 API、泛化部署章节）。
   - 开篇以「无足鸟 · 按时吃药（yao）」+ 一句话定位 + 四大卖点（免费/易用/安全/开源）+ 开源地址呈现。
   - 技术栈表与 AGENTS.md 规范对齐（MariaDB 12.3 / Python 3.14）。
   - 部署章节泛化描述三容器架构（yao-mariadb / yao-backend / yao-nginx），不再硬编码与 `scripts/deploy.sh`（`MARIADB_VERSION=10.11`、`PYTHON_VERSION=3.11`）冲突的镜像版本。
   - 精简 API 章节为模块概览 + Swagger 链接，移除冗长的逐接口表格。
   - 环境配置压缩为关键变量表格，删除与前端无关的冗余字段说明。

### 影响范围
- README.md
- 更新记录.md

---

## 2026-07-26（修复）- 清理 email_service SMTP 类型债与公告投递说明

### 变更类型
修复

### 变更内容
1. **`backend/app/services/email_service.py`**（清理预存 SMTP 类型债，lint 由 2 ERROR + 9 WARNING 清零）
   - 两处 `except smtplib.SMTPAuthenticationError` 块（`send_verification_code` / `send_notification`）：`e.smtp_error.decode(...)` 改为 `isinstance(e.smtp_error, bytes)` 类型缩小后调用 `.decode()`，兼容 `bytes | str | None`，消除 basedpyright `decode` 属性 ERROR 与 `err_detail` Unknown 类型 WARNING。
   - `_smtp_authenticate` 的 `server.login()`、`send_verification_code`/`send_notification` 的 3 处 `server.sendmail(...)`、STARTTLS 分支的 `server.starttls()` 返回值改为 `_ = ...` 显式丢弃，消除 `reportUnusedCallResult` WARNING。
2. **`backend/app/models/announcement.py`**（更新公告投递说明）
   - 模型 docstring「用户侧投递后续实现」更新为「用户侧通过首页临时公告卡片轮播展示（GET /api/v1/announcements/recent + 前端 AnnouncementCard.vue 逐条轮播）」，反映已实现现状。

### 影响范围
- `backend/app/services/email_service.py`
- `backend/app/models/announcement.py`
- `更新记录.md`

---

## 2026-07-26（优化）- 打卡通知邮件字段顺序调整

### 变更类型
优化

### 变更内容
1. **`backend/app/services/scheduler_service.py`**
   - 打卡通知邮件 content 字段顺序调整：将"计划周期"上移至"提醒时间"之前。新顺序：计划名称 → 备注 → 计划周期 → 提醒时间 → 触发类型。

### 影响范围
- `backend/app/services/scheduler_service.py`
- `更新记录.md`

---

## 2026-07-26（优化）- 邮件标题改名与正文突出通知内容

### 变更类型
优化

### 变更内容
1. **`backend/app/services/scheduler_service.py`**
   - 打卡通知邮件标题前缀由「【按时打卡】」改为「【按时吃药】」（与验证码邮件「【按时吃药】验证码」、应用名"按时吃药"统一）。
2. **`backend/app/services/email_service.py`**
   - `send_notification` 字段行渲染层次重排以突出通知内容：字段名降为 mute(#8f8f8f)/500 作标签衬底，字段值升为 ink(#171717)/600 用 `<strong>` 突出（参考验证码邮件中关键值用 `<strong>` + ink/600 的写法）。
   - 同步更新 docstring。

### 影响范围
- `backend/app/services/scheduler_service.py`
- `backend/app/services/email_service.py`
- `更新记录.md`

---

## 2026-07-26（优化）- 打卡通知邮件正文按字段逐行展示

### 变更类型
优化

### 变更内容
1. **`backend/app/services/email_service.py`**
   - 重构 `send_notification` 的正文渲染逻辑，参考验证码邮件样式并遵循 `DESIGN-vercel.md` 的 Geist 设计规范。
   - 约定调用方传入的 `content` 每行为"字段名：值"格式，本方法按首个冒号（全角：或半角:）切分字段名/值逐行渲染：字段名用 ink(#171717)/600 的 `<strong>` 强调，值用 body(#4d4d4d)。
   - 字段行统一置于 hairline-soft(#f2f2f2)/12px 圆角高亮容器内（参考验证码邮件的验证码块），前后各附一句引导句（body 色）与提示句（mute #8f8f8f）。
   - 无冒号的行作为普通段落整行兜底渲染；所有文本经 `html.escape` 转义。
   - 修复类型告警：`field_rows` 标注 `list[str]`，`label`/`value` 初始化为空字符串，多行隐式拼接合并为单个 f-string。
2. **`backend/app/services/scheduler_service.py`**
   - 配套调整 `_send_email` 中 `content` 构造：由自然语言段落改为按打卡计划字段逐行拼接（计划名称、备注、提醒时间、触发类型、计划周期），每行"字段名：值"格式供邮件服务逐行渲染。

### 影响范围
- `backend/app/services/email_service.py`
- `backend/app/services/scheduler_service.py`
- `更新记录.md`

---

## 2026-07-27（优化）- 重新设计打卡通知邮件内容

### 变更类型
优化

### 变更内容
1. **`backend/app/services/email_service.py`**
   - 在 `send_notification` 方法中将打卡通知邮件从纯文本格式升级为 HTML 邮件，MIME 类型由 `plain` 改为 `html`。
   - 邮件视觉参考 `DESIGN-vercel.md` 的 Vercel Geist 设计语言：近白画布背景（#fafafa）、白色卡片（#ffffff）、1px 发线边框（#ebebeb）、12px 圆角、#171717 标题与 #4d4d4d 正文，强调链接使用 #0070f3。
   - 邮件顶部通过 `<img>` 标签引入 `https://www.wuzuniao.com/images/logo_wuzuniao_com_s.png` 图片链接，并包裹指向 `https://www.wuzuniao.com` 的超链接；图片以 URL 形式引用，不嵌入二进制资源。
   - 将传入的纯文本通知内容按行拆分为 HTML 段落，并对内容进行 HTML 转义，避免特殊字符破坏邮件结构。
   - 底部保留「此邮件由 <a href=\"...\">无足鸟</a> 自动发送」品牌信息。
2. 执行 `python -m py_compile backend/app/services/email_service.py` 校验，Python 语法通过。

### 影响范围
- `backend/app/services/email_service.py`
- `更新记录.md`

---

## 2026-07-27（优化）- 调整验证码邮件主题、图片尺寸与标题链接

### 变更类型
优化

### 变更内容
1. **`backend/app/services/email_service.py`**
   - 将 `send_verification_code` 方法的邮件主题由「【按时打卡】注册验证码」统一改为「【按时吃药】验证码」，与产品名称一致，并去除场景限定词以适配注册、密码找回、修改邮箱等多场景共用。
   - 移除邮件顶部 Logo 图片的 `width="64" height="64"` 固定尺寸属性，改为不限制宽高，让图片按原始大小显示，避免形变。
   - 删除邮件正文中的「验证码」`<h1>` 标题。
   - 修复邮件顶部 Logo 图片的点击跳转：移除 `<a>` 标签的 `target="_blank"` 与冗余样式，保留完整 `href` 与 `display:inline-block`，使图片链接在各邮件客户端中更稳定可点。

### 影响范围
- `backend/app/services/email_service.py`
- `更新记录.md`

---

## 2026-07-27（优化）- 重新设计验证码邮件内容

### 变更类型
优化

### 变更内容
1. **`backend/app/services/email_service.py`**
   - 将 `send_verification_code` 方法中的验证码邮件从纯文本格式升级为 HTML 邮件，邮件 MIME 类型由 `plain` 改为 `html`。
   - 邮件视觉参考 `DESIGN-vercel.md` 的 Vercel Geist 设计语言：近白画布背景（#fafafa）、白色卡片（#ffffff）、1px 发线边框（#ebebeb）、12px 圆角、#171717 黑色标题与 #4d4d4d 正文，强调链接使用 #0070f3。
   - 邮件顶部通过 `<img>` 标签引入 `https://www.wuzuniao.com/images/logo_wuzuniao_com_q.png` 图片链接，并包裹指向 `https://www.wuzuniao.com/index.html` 的超链接；图片以 URL 形式引用，不嵌入二进制资源。
   - 验证码使用等宽字体与 8px 字距展示，放置于浅灰（#f2f2f2）圆角容器内，提升可读性。
   - 邮件正文文案通用化，适用于注册、密码找回、修改邮箱等所有调用 `send_verification_code` 的场景。
2. 执行 `python -m py_compile backend/app/services/email_service.py` 校验，Python 语法通过。

### 影响范围
- `backend/app/services/email_service.py`
- `更新记录.md`

### 备注
- 当前邮件主题仍固定为「【按时打卡】注册验证码」，该方法同时被注册、密码找回、修改邮箱等场景复用。如需按用途区分为「注册验证码」「重置密码验证码」等主题，请额外说明。

---

## 2026-07-27（修复）- 诊断错误修复：guide.js 语法与 目录结构.json 逗号

### 变更类型
修复

### 变更内容
1. **`frontend/src/store/modules/guide.js`**
   - 修复 TypeScript 诊断错误：将 `isWechatMP` 的初始化从 `ref(/* #ifdef */ true /* #endif */ false)` 改为 `ref(false)` + 条件编译赋值，避免 TypeScript 解析器将条件编译注释间的多值视为语法错误，同时保留编译期平台判断能力。
2. **`目录结构.json`**
   - 修复 JSON 缺少逗号导致的解析错误：在 `pages.index` 分组中为 `index.vue`、`record.vue`、`settings.vue`、`notification.vue` 四个条目末尾补充分隔逗号，使文件恢复合法 JSON。
3. 重新执行 `npm run build:mp-weixin`，构建成功；使用 Python `json.load` 校验 目录结构.json 通过。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `目录结构.json`

---

## 2026-07-27（功能）- 新手引导支持非微信小程序端流程分支

### 变更类型
功能

### 变更内容
1. **`frontend/src/store/modules/guide.js`**
   - 新增 `isWechatMP` 响应式变量，通过 `// #ifdef MP-WEIXIN` 编译期条件判断当前平台是否为微信小程序，并暴露到 store 外部供页面使用。
   - 将原单一 `steps` 拆分为 `wechatSteps` 与 `nonWechatSteps` 两套步骤配置，`steps` 初始化时根据 `isWechatMP` 自动选择。
   - 非微信小程序端流程调整：
     - 第 3 步拆分为登录页「立即注册」→注册页「注册」→登录页「登录」三个连续子步骤，对应目标 `register-link` / `register-submit` / `login-submit`。
     - 第 4 步改为邮件通知配置，拆分为「添加新的通知方式」→「选择邮件」→「保存邮件通知」三个连续子步骤，对应目标 `add-notification` / `email-type-radio` / `email-save-button`。
   - 优化 `displayStepNumber`：已登录模式（logged-in）下根据当前步骤的 `stepNumber` 统一映射为连续的 1-4 编号，避免原按索引硬编码在不同步骤长度下失效。
2. **`frontend/src/pages/user/login.vue`**
   - 为底部「立即注册」链接新增 `guide-target-register-link` 类与 `registerLinkActiveStyle` 上报。
   - 为账号密码登录「登录」按钮新增 `guide-target-login-submit` 类与 `loginSubmitActiveStyle` 上报。
3. **`frontend/src/pages/user/register.vue`**
   - 为「注册」按钮新增 `guide-target-register-submit` 类与 `registerSubmitActiveStyle` 上报。
   - 新增 `onShow` 生命周期调用 `guideStore.onPageEnter('register')`，保证引导流程在登录页与注册页之间正确前进/回退。
4. **`frontend/src/pages/index/notification.vue`**
   - 为「邮件」类型单选框新增 `guide-target-email-type-radio` 类与 `emailTypeActiveStyle` 上报。
   - 为保存按钮新增 `guide-target-email-save-button` 类（与原有 `guide-target-wechat-auth-button` 共存），并注册 `emailSaveActiveStyle` 上报，复用同一按钮作为非微信端邮件保存目标。
   - `handleAdd` 中增加非微信小程序端引导判断：当当前步骤为 `add-notification` 时，默认选中「邮件」类型，并自动跳过「选择邮件」步骤直接进入「保存邮件通知」步骤。
   - `selectType('邮件')` 中增加引导推进逻辑：当前步骤为 `email-type-radio` 时自动 `nextStep()` 到保存邮件通知步骤。
   - 邮件保存成功后增加引导推进逻辑：当前步骤为 `email-save-button` 时自动 `nextStep()` 到步骤 5（制定计划）。
5. 同步更新 `目录结构.json` 中 `guide.js`、`login.vue`、`register.vue`、`notification.vue` 的描述，说明跨平台引导分支与新增目标类。
6. 执行 `npm run build:mp-weixin` 验证，构建成功（仅 Sass 弃用警告）。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/pages/user/login.vue`
- `frontend/src/pages/user/register.vue`
- `frontend/src/pages/index/notification.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-27（优化）- 已登录空状态百分比距离一屏适配打卡按钮

### 变更类型
优化

### 变更内容
1. **`frontend/src/pages/index/index.vue`**
   - 未改动卡片内部文字内容与打卡按钮尺寸。
   - 在打卡按钮外壳增加 `:class="{ 'index-page__checkin-shell--empty': isLoggedIn && !hasActivePlans }"`，用于空状态单独控制底部间距。
   - 将 `.index-page__main-canvas--empty` 的 `padding-bottom` 由 `24vh` 改为 `0`，`padding-top` 由 `22vh` 改为 `14vh`，并移除 `gap`，避免主画布自身留白与打卡按钮外壳边距叠加导致溢出。
   - 新增 `.index-page__main-canvas--empty .index-page__guide-card { margin-bottom: 2vh; }`，使用百分比距离控制引导卡片与打卡按钮之间的间距。
   - 新增 `.index-page__checkin-shell--empty { margin-top: auto; margin-bottom: 16vh; }`，使用百分比距离控制打卡按钮与底部固定导航栏之间的留白，确保打卡按钮在一屏内可见。
   - 同步调整小屏适配（`max-height: 700px`）：`padding-top: 11vh`、卡片间距 `1vh`、打卡按钮 `margin-bottom: 14vh`，避免 iPhone SE 等短屏溢出。
   - 同步调整平板适配（`min-width: 768px`）：与移动端保持相同的百分比距离策略，确保宽屏设备下一屏完整展示。
2. 同步更新 `目录结构.json` 中 `index.vue` 的描述，说明已登录空状态通过百分比距离同时控制引导卡片与打卡按钮的一屏展示。

### 影响范围
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-27（优化）- 已登录空状态新手引导卡片避免无意义滚动

### 变更类型
优化

### 变更内容
1. **`frontend/src/pages/index/index.vue`**
   - 未改动卡片内部文字内容。
   - 为 `.index-page__guide-card` 增加 `margin-top: auto; margin-bottom: auto;`，使已登录无进行中计划时卡片在 `main-canvas` 中垂直居中，充分利用视口剩余空间。
   - 这样当卡片内容可在当前视口完整显示时，页面总高度不会超过视口，不会出现上下滚动条；只有当内容真正超出视口、部分元素不可见时，才允许自然滚动。
   - 同步补充 CSS 注释说明该布局意图。
2. 同步更新 `目录结构.json` 中 `index.vue` 的描述，补充新手引导卡片垂直居中、避免无意义滚动的说明。

### 影响范围
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-27（优化）- 已登录首页新手引导卡片格式参考未登录介绍卡片优化

### 变更类型
优化

### 变更内容
1. **`frontend/src/pages/index/index.vue`**
   - 已登录空状态新手引导卡片结构改为分节展示：新增 `.index-page__guide-section`、`.index-page__guide-section-title`，将内容分为「核心操作」与「需要帮助」两节，与未登录介绍卡片的章节层级保持一致。
   - 操作路径中的 `->` 使用 `.index-page__guide-arrow` 设置灰色、`【可选】`与`帮助中心Q&A`使用绿色强调，增强可读性。
   - 行动按钮样式调整为与未登录介绍卡片的 `.index-page__intro-action` 完全一致：绿色背景、圆角胶囊、`margin-top: 24rpx`、`padding: 24rpx 0`、文字 `32rpx/48rpx`。
   - 将按钮文字从「点我重新开启新手引导」恢复为原始内容「点我开启新手引导」。
   - 同步补充 SCSS（含平板 px 锁定响应式），并更新模板注释。
2. 同步更新 `目录结构.json` 中 `index.vue` 的描述，补充新手引导卡片参考未登录介绍卡片的格式说明。

### 影响范围
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-27（修复）- 首页临时公告卡片与新手引导互斥显示

### 变更类型
修复 / 优化

### 变更内容
1. **`frontend/src/pages/index/index.vue`**
   - `AnnouncementCard` 公告临时卡片渲染条件增加 `!guideStore.isActive`，即新手引导激活期间隐藏公告卡片，未进行引导时正常显示。
   - 同步更新注释，说明互斥逻辑是为避免公告卡片与引导高亮/提示卡片冲突。
2. 同步更新 `目录结构.json` 中 `index.vue` 的描述，补充公告卡片在引导期间隐藏的逻辑。

### 影响范围
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-27（修复）- 新手引导授权订阅提醒步骤卡片改为锚定表单卡顶部定位

### 变更类型
修复 / 优化

### 变更内容
1. **`frontend/src/store/modules/guide.js`**
   - 将 `wechat-auth-button` 步骤的 `cardPosition` 由 `'above-anchor'` 改为 `'anchor-top'`，并保留 `cardAnchor: 'notification-form-card'`，使提示卡片以新建通知方式表单卡顶部为参考。
2. **`frontend/src/components/BeginnerGuide.vue`**
   - 步骤卡片定位逻辑改为：当 `cardPosition='anchor-top'` 时，卡片底部位于锚点元素顶部上方 `12px`，即 `top = anchorRect.top - cardHeight - 12`；极端情况下若卡片被顶到屏幕外，则 fallback 到 `top = anchorRect.top + 12`，将提示卡片置于新建通知方式卡片内部以保证可见。
   - 新增 `ready` 计算属性：所有步骤均在页面匹配 + 目标元素位置上报（配置了 `cardAnchor` 时还需锚点位置上报）后才渲染蒙版/高亮/卡片，确保对应页面加载完成后再出现。
   - 同步更新组件顶部 JSDoc 与定位逻辑注释。
3. **`frontend/src/composables/useGuideTarget.js`**（修复之前理解遗漏）
   - 之前锚点元素（`steps[].cardAnchor`）仅在 `targetKey === 当前步骤 target` 时才会上报，导致 `BeginnerGuide.vue` 中 `anchorRect` 为空，`anchor-top` 定位无法生效。
   - 修改监听与刷新条件：当前步骤的 `target` 或 `cardAnchor` 匹配本目标时均会上报并持续刷新位置，确保锚点定位可用。
   - 同步更新 JSDoc，说明 targetKey 也可对应 `steps[].cardAnchor`。
4. 同步更新 `目录结构.json` 中 `BeginnerGuide.vue` 与 `useGuideTarget.js` 的描述。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/composables/useGuideTarget.js`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（修复）- 新手引导卡片位置、高亮跟随与打卡按钮结束逻辑修复

### 变更类型
修复

### 变更内容
1. **授权订阅提醒引导卡片不可见问题**
   - `frontend/src/store/modules/guide.js`：将 `wechat-auth-button` 步骤的 `cardPosition` 由 `'bottom'` 改为 `'top-viewport'`。
   - `frontend/src/components/BeginnerGuide.vue`：步骤卡片定位逻辑新增对 `'top-viewport'` 的处理，固定于视口顶部（16px 安全边距），避免目标位于页面底部时卡片被截断；同时保证不覆盖新建通知方式表单卡。
2. **高亮区域不随页面滚动问题**
   - `frontend/src/composables/useGuideTarget.js`：当前目标处于激活步骤时，以 250ms 间隔持续刷新 `boundingClientRect` 并上报，使高亮区域与提示卡片跟随页面/scroll-view 滚动保持同步；组件卸载时自动停止刷新。
3. **打卡按钮不可点击时无法结束引导问题**
   - `frontend/src/pages/index/index.vue`：调整 `handleCheckin` 中引导判断的顺序与逻辑，将「新手引导最后一步点击打卡按钮高亮区域即完成引导」置于函数最前，完成后立即 `return`，不再执行后续正常打卡流程。
   - `frontend/src/pages/index/index.vue`：`handleLongPress` 中新增判断，若当前处于新手引导且目标是打卡按钮，则直接返回，禁用长按重置，避免干扰引导流程。
4. 同步更新 `目录结构.json` 中 `useGuideTarget.js`、`BeginnerGuide.vue`、`index.vue` 的描述。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/composables/useGuideTarget.js`
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（修复）- 新手引导步骤编号与高亮修复

### 变更类型
修复 / 优化

### 变更内容
1. **guide.js**：
   - 新增 `guideMode` 状态（'full' / 'logged-in'），区分未登录完整引导和已登录跳过登录步骤的引导。
   - 新增计算属性 `displayStepNumber` 与 `displayTotalSteps`：已登录模式下将内部索引映射为连续的 1-4 编号（步骤 1 首页设置 → 步骤 2 通知方式分支 → 步骤 3 制定计划分支 → 步骤 4 首页打卡），避免跳过步骤 2、3 后出现编号不连续。
   - 在 `startGuideForLoggedIn` 中设置 `guideMode='logged-in'`，`startGuide`/`skipGuide`/`completeGuide` 中重置为 `'full'`。
   - 为 `wechat-auth-button` 步骤新增 `shape: 'pill'` 与 `cardPosition: 'bottom'` 配置。
   - 为 `checkin-button` 步骤新增 `shape: 'circle'` 并将 `padding` 由 12 改为 0。
2. **BeginnerGuide.vue**：
   - 步骤徽章改为使用 `guideStore.displayStepNumber` / `guideStore.displayTotalSteps`，实现已登录时连续编号显示。
   - 高亮圆角计算支持步骤 `shape` 配置：`'circle'` / `'pill'` 时使用短边一半作为半径，使圆形/胶囊形按钮与高光边界完全贴合，消除内白边。
   - 步骤卡片定位支持 `cardPosition='bottom'`，强制卡片置于目标下方，避免「授权订阅提醒」步骤的提示覆盖新建通知方式表单卡。
3. **index.vue**：
   - 清理已废弃的 `.index-page__empty` / `.index-page__empty-text` 样式，并为 `.index-page__guide-card` 补充平板断点样式。
4. 同步更新 `目录结构.json` 中 `guide.js` 与 `BeginnerGuide.vue` 的描述。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（新增）- 首页已登录无计划空状态改为新手引导卡片

### 变更类型
新增 / 优化

### 变更内容
1. **index.vue**：将已登录且无进行中计划时的空状态提示改造为"新手引导"卡片，样式参考未登录介绍卡片。
   - 卡片标题为「新手引导」。
   - 内容严格保持用户指定文本（未做任何改动）：
     - 点击设置页 -> 通知方式 -> 新增消息通知方式，建议使用微信订阅消息。
     - 点击设置页 -> 制定计划 -> 创建打卡计划。
     - 若APP不会操作，可参考帮助中心Q&A，或通过邮箱反馈问题。
   - 底部保留绿色行动按钮，文案为「点我开启新手引导（已登录情况下，新手引导使用步骤1、然后到步骤4继续）」。
   - 新增 `.index-page__guide-card`、`.index-page__guide-title`、`.index-page__guide-content`、`.index-page__guide-line`、`.index-page__guide-action` 等样式，复用介绍卡片圆角/阴影/配色体系。
   - 移除旧的 `.index-page__empty` 与 `emptyText` 计算属性（因已被新卡片替代）。
2. **guide.js**：支持已登录用户从首页启动引导并跳过登录相关步骤。
   - 新增状态 `fastForwardToStep4`，标记本次引导是否需要从步骤 1 快进至步骤 4。
   - 新增 action `startGuideForLoggedIn()`，供已登录空状态卡片调用。
   - 在 `onPageEnter` 中处理从首页进入设置页的逻辑：当 `fastForwardToStep4` 为 true 且当前处于步骤 1 时，直接跳转到 `notification-method`（步骤 4），而不是进入 `profile-card`（步骤 2）。
   - `startGuide`/`skipGuide`/`completeGuide` 统一重置 `fastForwardToStep4`。
3. 同步更新 `目录结构.json` 中 `index.vue` 与 `guide.js` 的描述。

### 影响范围
- `frontend/src/pages/index/index.vue`
- `frontend/src/store/modules/guide.js`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（新增）- 新手引导扩展至步骤 4-6

### 变更类型
新增

### 变更内容
1. **guide.js**：扩展 `steps` 引导步骤配置，新增步骤 4-6 的内部索引，覆盖通知方式（可选）→ 添加微信通知 → 授权订阅 → 制定计划 → 新建计划 → 返回首页 → 打卡按钮完整流程。
   - 步骤 4 拆分为 3 个内部索引：`settings/notification-method`（可选）、`notification/add-notification`、`notification/wechat-auth-button`；前两个配置 `skipTo: 'plan-method'`，点击「跳过」直接跳转到步骤 5。
   - 步骤 5 拆分为 2 个内部索引：`settings/plan-method`、`plan/new-plan`。
   - 步骤 6 拆分为 2 个内部索引：`settings/home-tab`、`home/checkin-button`。
   - 新增 `skipToStepByTarget(targetKey)` action，用于可选步骤的跳过分支跳转。
2. **BeginnerGuide.vue**：支持步骤分支跳转。
   - 新增 `isLastInternalStep` 与 `skipText` 计算属性，最后一步显示「完成」，其余显示「跳过」。
   - `handleSkip` 优先判断当前步骤是否配置 `skipTo`，有则跳转到指定目标步骤，否则结束引导。
3. **settings.vue**：补充「通知方式」与「制定计划」入口的新手引导目标类与样式绑定。
   - 用户资料卡保留 `guide-target-profile-card`；新增 `guide-target-plan-method` 与 `guide-target-notification-method`。
   - 通过 `useGuideTarget` 上报位置，页面 `onShow` 调用 `guideStore.onPageEnter('settings')`。
4. **notification.vue**：接入新手引导步骤 4 分支。
   - 「添加新的通知方式」入口添加 `guide-target-add-notification` 类；保存按钮添加 `guide-target-wechat-auth-button` 类。
   - 点击入口后 `guideStore.nextStep()` 推进到授权订阅步骤。
   - 微信授权成功（`doWechatAuthorize`）后调用 `guideStore.nextStep()` 进入步骤 5。
   - 页面 `onShow` 调用 `guideStore.onPageEnter('notification')`。
5. **plan.vue**：接入新手引导步骤 5 分支。
   - 「新建计划」入口添加 `guide-target-new-plan` 类。
   - 点击入口后 `guideStore.nextStep()` 推进到步骤 6。
   - 页面 `onShow` 调用 `guideStore.onPageEnter('plan')`。
6. **index.vue**：接入新手引导步骤 6 最后一步。
   - 打卡按钮添加 `guide-target-checkin-button` 类。
   - 点击打卡按钮后调用 `guideStore.completeGuide()` 完成全部引导。
7. 同步更新 `目录结构.json` 中相关文件说明，补充新手引导目标类与流程描述。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/pages/index/settings.vue`
- `frontend/src/pages/index/notification.vue`
- `frontend/src/pages/index/plan.vue`
- `frontend/src/pages/index/index.vue`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（优化）- 新手引导蒙版去除拼接线 + 登录页点击修复

### 变更类型
优化 / 修复

### 变更内容
1. **BeginnerGuide.vue**：彻底去除微信小程序蒙版的 1px 拼接线，使高亮区域外背景颜色一致。
   - 历史尝试：曾用「1px 重叠」消除缝隙 → 双重半透明叠加产生更深的拼接线；又改「整数边界紧邻拼接、互不重叠」→ 微信小程序对相邻 `position:fixed` view 边界做抗锯齿，仍残留 1px 拼接线。
   - 最终方案：视觉层改为**单 view + box-shadow 大扩散**一次性绘制高亮洞以外的灰色蒙版，单层连续渲染、无相邻 view 边界，从根源杜绝拼接线。点击阻挡层改为 **4 个透明矩形**覆盖高亮洞以外区域（`pointer-events:auto`），透明背景使其拼接处不可见，视觉无影响；高亮洞内不放置任何 view，点击直达目标。移除四角圆角补片（圆角由 mask-hole 的 border-radius 承担）。
2. **BeginnerGuide.vue**：修复登录页出现高亮引导时微信一键登录无法点击。
   - 根因：高亮边框的 `::after` 伪元素覆盖目标按钮区域；微信小程序伪元素不尊重 `pointer-events:none`，会拦截点击。同时 `::after` 较目标外扩 4px，恰好盖住整个按钮。
   - 修复：移除 `::after`，高亮边框直接用 `box-shadow` 绘制于 `.beginner-guide__highlight` 真实 view（微信对真实 view 的 `pointer-events:none` 正常生效），目标点击不再被拦截。
3. **useGuideTarget.js**：位置上报增强。
   - 为 boundingClientRect 查询增加重试（最多 8 次、120ms 间隔，校验 width/height>0），覆盖页面入场动画期间节点未就绪。
   - `right`/`bottom` 改由 `left+width`、`top+height` 推算，避免某些时机下系统返回的 right/bottom 为 0 致高亮洞退化为左上角小矩形、目标被蒙版覆盖。
4. 更新组件顶部 JSDoc、模板与样式注释，说明单 view box-shadow 视觉层 + 透明矩形阻挡层 + 移除 ::after 的实现原理。
5. 同步更新 `目录结构.json` 中 BeginnerGuide.vue 与 useGuideTarget.js 的文件说明。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/composables/useGuideTarget.js`
- `目录结构.json`、`更新记录.md`

---

## 2026-07-26（重构）- 新手引导微信小程序蒙版改为单层 view 拼接

### 变更类型
重构

### 变更内容
1. **BeginnerGuide.vue**：应要求移除微信小程序端的双层蒙版结构，改为单层 4 矩形 + 4 圆角补片拼接覆盖高亮区域以外。
   - 删除底层点击阻挡层与上层 SVG 背景视觉层的分离结构，改为同层级 8 个 view 共同组成完整蒙版；
   - 四边矩形与四角圆角补片之间保留 1px 重叠，消除相邻 view 之间的 1px 拼接线；
   - 高亮区域内部不放置任何 view，目标元素可正常接收点击/触摸事件；
   - 呼吸边框通过 `::after` 伪元素外扩实现，避免 box-shadow 向内覆盖目标元素形成黑边。
2. H5 端保留 SVG mask 精确蒙版 + box-shadow 呼吸动画，逻辑与显示效果不变。
3. 更新组件顶部 JSDoc，说明 H5/微信小程序两套实现原理。
4. 执行 `npm run build:mp-weixin` 构建通过，无新增编译错误。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（修复）- 新手引导微信小程序蒙版点击穿透修复

### 变更类型
修复

### 变更内容
1. **BeginnerGuide.vue**：修复微信小程序端新手引导中点击目标元素（如底部「设置」）无反应的问题。
   - 原上层 SVG 背景蒙版视图缺少 `pointer-events:none`，导致全屏拦截点击事件；
   - 为 `mpMaskStyle` 显式添加 `pointer-events: 'none'`，使上层视觉蒙版不再拦截触摸/点击，事件可穿透到下层阻挡层或目标元素；
   - 补全底层 4 矩形 + 4 圆角补片的计算属性（`topBarStyle`、`bottomBarStyle`、`leftBarStyle`、`rightBarStyle`、`cornerTLStyle`、`cornerTRStyle`、`cornerBLStyle`、`cornerBRStyle`）及对应 SCSS 样式，确保高亮区域外正确阻挡点击、高亮区域内可正常点击目标。
2. H5 端仍保留 SVG 精确蒙版，逻辑与显示效果不变。
3. 执行 `npm run build:mp-weixin` 构建通过，无新增编译错误。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导微信小程序蒙版改为单元素 SVG 背景图

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：微信小程序端蒙版由 4 矩形 + 4 径向渐变补片改为单个全屏 view，以 SVG data URI 作为 `background-image`。
   - SVG 自身绘制 `rgba(0, 0, 0, 0.6)` 半透明遮罩，并通过 `<mask>` 挖出与高亮区域同尺寸同圆角的透明洞；
   - 单个 view 背景图覆盖全屏，无多个 view 相邻产生的 1px 拼接线，背景呈均匀纯色；
   - 透明洞区域点击可正常穿透到目标元素。
2. H5 端仍保留 SVG 精确蒙版，逻辑与显示效果不变。
3. 更新组件顶部 JSDoc，说明 H5/微信小程序两套蒙版实现原理。
4. 执行 `npm run build:mp-weixin` 构建通过，无新增编译错误。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（修复）- 新手引导微信小程序蒙版改为纯 view 拼接

### 变更类型
修复

### 变更内容
1. **BeginnerGuide.vue**：微信小程序端蒙版由 `WebkitMaskImage` + SVG data URI 方案改为纯 view 拼接方案，解决四角白边依旧漏出的问题。
   - 4 个 `position:fixed` view 分别覆盖高亮区域上/下/左/外侧；
   - 4 个径向渐变补片（cornerRadius × cornerRadius）精确填补四边矩形留下的圆角/圆形缺口；
   - 补片渐变圆心位于目标元素角点，半径内透明（露出目标），半径外为蒙版色 `rgba(0,0,0,0.6)`；
   - 不依赖 `mask-image` 或 SVG data URI，兼容性更稳定。
2. H5 端仍保留全屏 SVG 精确蒙版，逻辑与显示效果不变。
3. 更新组件顶部 JSDoc，说明 H5/微信小程序两套蒙版实现原理。
4. 执行 `npm run build:mp-weixin` 构建通过，无新增编译错误。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导微信小程序四角补片改为径向渐变

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：微信小程序端的四角补片由 `border-*-radius` 改为 `radial-gradient` 径向渐变实现 1/4 圆挖空。
   - 每个补片是一个 cornerRadius × cornerRadius 的 view；
   - 渐变圆心位于目标元素角点，半径内透明（露出目标元素），半径外为蒙版色（覆盖缺口）；
   - 避免 `border-radius` 方向/解析问题导致的"补反"现象。
2. 移除 `.beginner-guide__corner` 的固定背景色，背景完全由径向渐变生成。
3. H5 端仍使用 SVG 精确蒙版，不受补片影响。
4. 通过 H5 开发服务器 + 内部浏览器验证：三步引导显示与点击跳转均正常。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导微信小程序四角白边精确覆盖

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：移除目标元素的 box-shadow 补白边方案（与蒙版重叠会产生深色阴影）。
2. **BeginnerGuide.vue**：在微信小程序端增加 4 个 1/4 圆角补片 view，分别覆盖高亮区域四角的弧形缺口。补片颜色与蒙版一致（rgba(0,0,0,0.6)），半径与蒙版洞圆角相同，只覆盖缺口、不与矩形蒙版主体重叠，避免深色阴影。
3. **useGuideTarget.js**：`activeStyle` 恢复为空对象占位，不再为目标元素添加阴影。
4. H5 端仍使用 SVG 精确蒙版，不受补片影响。
5. 通过 H5 开发服务器 + 内部浏览器验证：三步引导显示与点击跳转均正常。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/composables/useGuideTarget.js`

---

## 2026-07-26（修复）- 新手引导微信小程序四角白边修复

### 变更类型
修复

### 变更内容
1. **useGuideTarget.js**：扩展 composable，返回 `activeStyle`。当目标元素处于当前引导步骤时，在微信小程序端为目标元素添加与蒙版同色的外扩阴影（`box-shadow: 0 0 0 40rpx rgba(0,0,0,0.6)`），填补 4 矩形 view 蒙版在圆角/圆形目标外侧留下的四角白边。
2. **BottomNav.vue**：设置 tab 绑定 `activeStyle`。
3. **settings.vue**：用户资料卡绑定 `activeStyle`。
4. **login.vue**：微信一键登录按钮绑定 `activeStyle`。
5. `activeStyle` 在 H5 端返回空对象，避免影响 SVG 精确蒙版的干净效果。
6. 通过 H5 开发服务器 + 内部浏览器验证：三步引导均正常显示、点击跳转正常，H5 端无多余阴影。

### 影响范围
- `frontend/src/composables/useGuideTarget.js`
- `frontend/src/components/BottomNav.vue`
- `frontend/src/pages/index/settings.vue`
- `frontend/src/pages/user/login.vue`

---

## 2026-07-26（修复）- 新手引导微信小程序蒙版与点击穿透修复

### 变更类型
修复

### 变更内容
1. **BeginnerGuide.vue**：移除微信小程序端的 2d Canvas 蒙版实现（Canvas 原生组件会拦截洞区域内的触摸事件，导致点击设置/资料卡/微信按钮无反应，且 Canvas 可能覆盖高亮框造成视觉异常）。
2. **BeginnerGuide.vue**：微信小程序回退到 4 矩形 view 蒙版 + view 高亮框，蒙版覆盖目标元素外部、目标区域留空，确保点击正常穿透，引导流程可继续。
3. **BeginnerGuide.vue**：H5 端仍使用 SVG 精确蒙版 + view 高亮框，保持无白边效果。
4. 通过 H5 开发服务器 + 内部浏览器验证：点击设置 tab 正常跳转设置页，进入第 2 步引导。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导按步骤配置高亮区域外扩

### 变更类型
优化

### 变更内容
1. **guide.js**：为每个引导步骤新增 `padding` 配置字段。首页设置 tab、设置页资料卡 `padding: 0`，登录页微信一键登录 `padding: 10`。
2. **BeginnerGuide.vue**：高亮区域（SVG/Canvas 蒙版洞 + 高亮框）的外边距改为从当前步骤配置读取，默认 0，实现不同步骤不同高亮区域大小。
3. 通过 H5 开发服务器 + 内部浏览器验证：
   - 第 1 步设置 tab、第 2 步用户资料卡高亮区域与目标元素大小一致；
   - 第 3 步微信登录图标高亮区域保持外扩 10px，比图标大一圈。

### 影响范围
- `frontend/src/store/modules/guide.js`
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导跨平台精确蒙版 + 高亮区域外扩

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：高亮区域统一外扩 `padding = 10px`，使蒙版洞、高亮框都比目标元素大一圈，视觉上更突出，不再紧贴图标。
2. **BeginnerGuide.vue**：H5 继续使用 SVG `<mask>` 精确蒙版；微信小程序改用 `type="2d"` Canvas 绘制蒙版，使用 `globalCompositeOperation = 'destination-out'` 清除圆角矩形洞，彻底消除圆角/圆形目标外侧的四角白边。
3. **BeginnerGuide.vue**：高亮框圆角动态计算基于扩大后的高亮区域，接近正方形（宽高比 ≤ 1.3）用正圆，否则用 32rpx 大圆角，与蒙版洞形状保持一致。
4. 通过 H5 开发服务器 + 内部浏览器验证引导三步，确认高亮区域外扩后无白边、无黑边，呼吸边框完整。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导 SVG 精确蒙版消除四角白边

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：将 4 矩形蒙版改为 H5 下使用 SVG `<mask>` 精确蒙版。全屏填充 `rgba(0,0,0,0.6)`，中心挖一个与目标元素同尺寸、同圆角的洞，彻底覆盖圆角/圆形目标外侧区域，消除四角白边。
2. **BeginnerGuide.vue**：高亮框圆角改为动态计算：目标元素接近正方形（宽高比 ≤ 1.3）时使用正圆，否则使用 32rpx 大圆角，确保高亮框形状与 SVG 蒙版洞一致。
3. **BeginnerGuide.vue**：保留 `<!-- #ifndef H5 -->` 条件编译分支，在非 H5 平台（微信小程序等）回退到 4 矩形蒙版，避免 SVG 兼容性问题。
4. 通过 H5 开发服务器 + 内部浏览器重新验证引导三步（首页设置 tab / 设置页资料卡 / 登录页微信按钮），确认高亮区域外无四角白边。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导目标元素圆角化消除白边

### 变更类型
优化

### 变更内容
1. **login.vue**：将微信一键登录按钮容器 `.login-page__wechat-btn` 改为 `border-radius: 9999px` 并加 `overflow: hidden`，使原本为正方形的按钮容器变为圆形，避免高亮呼吸框（32rpx 圆角）内部露出按钮四角的白色/浅色边缘。同步更新平板断点为圆形。
2. 通过 H5 开发服务器 + 内部浏览器重新验证引导第三步（登录页微信登录按钮），确认高亮框内无四角白边。

### 影响范围
- `frontend/src/pages/user/login.vue`

---

## 2026-07-26（优化）- 新手引导高亮区域范围缩小与黑边消除

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：将高亮区域由「目标元素外扩 8px（padding=4）」改为「与目标元素完全重合（padding=0）」，并改用 `box-shadow: 0 0 0 Npx #9fe870` 做外部描边。描边完全位于目标元素外侧，不再覆盖目标元素，避免高亮区域内出现黑边/深色缝隙。
2. **BeginnerGuide.vue**：呼吸动画仅改变外部描边的 spread 半径与光晕 blur，边框圆角（32rpx）始终保持一致。
3. 通过 H5 开发服务器 + 内部浏览器完成引导全链路验证（首页→设置→登录），确认三步高亮区域均无外扩黑边。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导高亮边框圆角与黑边修复

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：将高亮区域的呼吸描边由 `box-shadow: 0 0 0 Npx` 改为 `border: 2px solid #9fe870`，配合 `border-radius: 32rpx`，避免 box-shadow 内阴影造成的视觉黑边/内正方形边框。
2. **BeginnerGuide.vue**：呼吸动画仅改变外层光晕的模糊半径与透明度（`box-shadow` 不含 spread），边框圆角在动画全过程中保持一致。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导高亮圆角与提示文字精简

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：删除步骤卡片底部「点击高亮区域继续」提示文字及其样式（`.beginner-guide__card-hint` / `.beginner-guide__hint-text`，含平板断点），步骤卡片仅保留步骤徽标、标题、说明文字与跳过按钮。
2. **BeginnerGuide.vue**：高亮区域圆角由 16rpx 增大到 32rpx，视觉更柔和。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`

---

## 2026-07-26（优化）- 新手引导提示与按钮样式调整

### 变更类型
优化

### 变更内容
1. **BeginnerGuide.vue**：移除步骤卡片中「点击高亮区域继续」后的向右箭头（`.beginner-guide__hint-arrow`），提示文字改为中性灰色（#868685）纯文字提示，避免用户误以为该提示可点击。
2. **index.vue**：「点我开启新手引导」由浅绿描边文本块改为品牌色（#9fe870）胶囊提交按钮样式（高 96rpx、圆角 48rpx、居中），与登录页提交按钮视觉一致，重点突出引导入口。模板结构由 `<text>` 改为 `<view>` 包裹 `<text class="index-page__intro-action-text">`。

### 影响范围
- `frontend/src/components/BeginnerGuide.vue`
- `frontend/src/pages/index/index.vue`

---

## 2026-07-26（新增）- 全局跨页面新手引导功能

### 变更类型
新增

### 变更内容
- 新增 `frontend/src/store/modules/guide.js`：新手引导 Pinia store，管理跨页面引导状态。3 步引导流程：首页(点设置) → 设置页(点用户资料卡) → 登录页(点微信一键登录)。`onPageEnter(page)` 由各页面 onShow 调用，自动推进或回退步骤；`setTargetRect/clearTargetRect` 接收目标元素位置上报。
- 新增 `frontend/src/composables/useGuideTarget.js`：目标元素位置上报 composable。在含引导目标的组件/页面中调用，自动监听 guide store，当引导激活且当前步骤匹配时，通过 `uni.createSelectorQuery().in(instance.proxy)` 查询目标元素 boundingClientRect 并上报（`.in()` 确保穿透微信小程序自定义组件 shadow 边界）。组件卸载时自动清理位置数据。
- 新增 `frontend/src/components/BeginnerGuide.vue`：新手引导遮罩组件。灰色蒙版采用 4 个 `position:fixed` view 覆盖目标元素以外区域（上/下/左/右），目标位置自然留空使点击穿透到实际目标元素触发原有导航逻辑；高亮边框围绕目标描边并带呼吸动画（`pointer-events:none` 不阻挡点击）；步骤卡片显示步骤序号（第X步/共3步）、标题、说明、跳过按钮，自动定位在目标下方或上方。监听 `userStore.userInfo` 变化，登录成功时自动完成引导。
- `frontend/src/components/BottomNav.vue`：设置 tab 添加 `guide-target-settings-tab` 类 + `useGuideTarget('settings-tab', ...)` 位置上报（引导第 1 步目标）。
- `frontend/src/pages/index/index.vue`：「点我开启新手引导」改为可点击按钮（`@click="startBeginnerGuide"` 调用 `guideStore.startGuide()`）；引入 `<BeginnerGuide />` 组件；onShow 中调用 `guideStore.onPageEnter('home')` 上报当前页面。
- `frontend/src/pages/index/settings.vue`：用户资料卡片添加 `guide-target-profile-card` 类 + `useGuideTarget('profile-card', ...)` 位置上报（引导第 2 步目标）；引入 `<BeginnerGuide />` 组件；新增 onShow 调用 `guideStore.onPageEnter('settings')`。
- `frontend/src/pages/user/login.vue`：微信一键登录按钮添加 `guide-target-wechat-login` 类 + `useGuideTarget('wechat-login', ...)` 位置上报（引导第 3 步目标）；引入 `<BeginnerGuide />` 组件；新增 onShow 调用 `guideStore.onPageEnter('login')`。

### 影响范围
- 未登录用户在首页点击「点我开启新手引导」按钮后，灰色蒙版覆盖屏幕，高亮底部导航栏的「设置」按钮并显示步骤卡片（第1步/共3步）。
- 点击高亮的「设置」按钮后，导航到设置页，蒙版自动高亮用户资料卡片并显示第2步卡片。
- 点击高亮的用户资料卡片后，导航到登录页，蒙版自动高亮微信一键登录按钮并显示第3步卡片。
- 点击高亮的微信登录按钮后，触发原有微信登录流程；登录成功后引导自动完成（监听 userInfo 变化）。
- 每步均可点击「跳过」按钮取消引导。
- 用户在引导过程中返回上一页时，引导自动回退到对应步骤。
- 已登录用户不受影响（引导按钮仅在未登录的介绍卡片中显示）。

### 架构说明
- **Store**（guide.js）：全局引导状态，跨页面共享
- **Composable**（useGuideTarget.js）：各目标元素组件内调用，自动上报位置到 store
- **Component**（BeginnerGuide.vue）：各页面引入，读取 store 渲染蒙版与步骤卡
- **页面集成**：各页面 onShow 调用 `onPageEnter(pageName)` 上报当前页面，store 据此推进步骤
- **蒙版原理**：4 个 fixed view 覆盖目标外的屏幕区域，目标位置留空使点击穿透到实际元素

---

## 2026-07-26（优化）- 未登录介绍卡片重点排版优化

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：未登录介绍卡片在**不改动任何文本内容**的前提下，通过为关键短语包裹带样式的 `<text>`/`<view>` 元素，优化排版以突出重点。
  - 新增 `.index-page__intro-highlights`：将"免费、易用、安全、开源。"整句渲染为绿色描边胶囊高亮块（居中、加粗），作为核心卖点视觉锚点。
  - 新增 `.index-page__intro-em`（绿色加粗，行内强调）：用于"自行部署"、"站内信、微信、邮件"、"微信一键登录"、"微信订阅消息通知"、"绑定邮箱并设置密码"等关键能力/操作。
  - 新增 `.index-page__intro-strong`（深色加粗，整句强调）：用于"打卡功能必须登录后才能使用。"（必要前提）与"一次注册，多端畅享"（核心卖点）。
  - 新增 `.index-page__intro-warning`（红色加粗，警示强调）：用于"请务必牢记账号与密码"（风险提示）。
  - 新增 `.index-page__intro-label`（加粗前缀，段落子标题）：用于"制定打卡计划："、"多途径通知："，呼应 `section-title` 形成层级。
  - 新增 `.index-page__intro-action`（绿色描边胶囊，居中行动入口）：用于"点我开启新手引导提示"，引导用户点击。
  - 平板断点（`≥768px`）同步为 `highlights` 与 `action` 锁定 px 尺寸，避免宽屏过度放大。

### 影响范围
- 仅未登录介绍卡片的视觉呈现；文本内容、已登录任务卡/打卡按钮逻辑与布局不变。
- "点我开启新手引导提示"目前仅作视觉行动入口，未绑定点击事件（后续如需跳转新手引导，可在此基础上补充 `@click` 处理函数）。

---

## 2026-07-26（优化）- 首页任务卡片改为"到达提醒时间且未打卡优先"排序

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：首页主要/次要卡片及任务列表的排序规则调整，由原"先按 priority 升序、再找前后2小时范围内"改为"先按当前匹配区间是否未打卡分组，冲突时再按优先级"。
  - 新增 `computePlanSortKey` / `sortPlansByCheckinStatus`：按以下键排序——①当前匹配区间未打卡的排前（group=0），已打卡/无提醒的排后（group=1）；②group=0 内按 priority 升序（冲突时优先级高的在前）；③group=1 内按"下一个最近提醒时间"升序（未来最近任务在前）；④同键时按 created_at 升序保持稳定。
  - 新增 `allTodayCheckins` ref（按 plan_id 分组）+ `loadAllTodayCheckins`（一次调用 `/api/v1/checkins/today` 获取全部计划打卡记录，替代原仅加载主要卡片的 `listTodayCheckinsByPlan`）。
  - 新增 `refreshCheckinStatus`：每分钟定时刷新时重新加载所有打卡记录并按新规则重排 `activePlans`（保持用户已手动选择的主要卡片，若已不存在则回退到排序后第一项）。
  - `todayCheckinMinutes` 由 ref 改为 computed（从 `allTodayCheckins` 派生主要卡片的打卡记录），`handleCheckin` 打卡成功后改为更新 `allTodayCheckins[planId]`。
  - 删除原 `computeDefaultPrimaryIndex`（前后2小时范围判定）与 `loadTodayCheckins`（仅加载主要卡片打卡记录）。
  - `loadActivePlans` 改为先加载所有打卡记录再按新规则排序，默认主要卡片取排序后第一项。

### 影响范围
- 首页主要卡片、次要卡片、任务列表弹层（3+任务时点"..."展开）的排序与默认选择。
- 当低优先级任务到达提醒时间且未打卡，而高优先级任务当前区间已打卡时，低优先级任务会进入主要卡片。
- 每分钟定时刷新会重排任务列表，但用户手动切换的主要卡片保持不变（除非该任务已不在列表中）。
- 打卡按钮状态判定逻辑（done/active/waiting）不变，仅数据来源从单计划查询改为全量查询派生。

---

## 2026-07-25（优化）- 介绍卡片移除登录按钮并按记录页标准预留底部间距

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：未登录介绍卡片移除「微信一键登录」按钮（含 `goWechatLogin` 处理函数及相关样式），「立即开始」段落同步去掉“点击下方按钮”表述，改为“使用微信一键登录后，即可开始使用打卡功能。”
- 为未登录布局（`.index-page__main-canvas--guest`，仅 `!isLoggedIn` 时挂载）增加底部内边距 `240rpx`（基础）/ `120px`（平板），与记录页 `.record-page` 的 `padding-bottom` 一致，确保白色卡片底部到导航栏顶部保持固定距离，不贴底。

### 影响范围
- 仅未登录介绍卡片的呈现；已登录任务卡/打卡按钮布局不变。

---

## 2026-07-25（优化）- 未登录介绍卡片收缩至仅含内容，顶部间距对齐记录页

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：介绍卡片（`.index-page__intro-card`）移除 `flex:1; min-height:0`，不再撑满整屏，改为仅包裹标题+正文+登录按钮，消除卡片下方多余白底；内层 `.index-page__intro-scroll` 同步移除 `flex:1; min-height:0`，由整页滚动承接溢出。
- 白色卡片距导航栏顶部距离对齐记录页：`.index-page__main-canvas` 顶部内边距由 `200rpx` 改为 `210rpx`（与 `record.vue` 的 `.record-page__main` 一致）；小屏断点（`max-height:700px`）顶部由 `170rpx` 改为 `210rpx`，平板断点（`≥768px`）由 `80px` 改为 `105px`（=210rpx），三档均与记录页对齐。

### 影响范围
- 仅未登录介绍卡片的呈现与首页顶部留白；已登录任务卡/打卡按钮逻辑不变。

---

## 2026-07-25（优化）- 首页取消强制一屏，允许内容超出时纵向滚动

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：移除 `.index-page` 的 `height: 100vh; overflow: hidden` 强制一屏限制，改为 `min-height: 100vh; overflow-x: hidden`（overflow-x:hidden 会令 overflow-y 计算为 auto），使内容超出时整页出现纵向滚动条、一屏可展示完时则不出现滚动条；横向溢出仍被隐藏，避免横向滚动条。
- 未登录介绍卡片的内层 `scroll-view` 改为普通 `view`，由整页滚动承接溢出内容，避免卡片内部嵌套滚动。

### 影响范围
- 首页各状态（未登录介绍卡、已登录任务卡/空状态 + 打卡按钮）在内容较多或窄屏设备上的呈现；打卡按钮靠 `margin-top:auto` 在内容短时仍贴底。

---

## 2026-07-25（优化）- 未登录首页改为“按时吃药”介绍卡片

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：未登录时不再展示欢迎文本与打卡按钮，改为以卡片形式展示标题“按时吃药”，内容包含：小程序介绍（跨端打卡计划 APP、传输与存储安全、开源地址 https://github.com/wuzuniao/yao）、主要功能（制定打卡计划、多途径通知）、以及微信一键登录引导（并说明邮箱账号在「无足鸟」系列软件产品中通用）。开源地址可点击复制，登录按钮跳转至登录页默认微信一键登录。已登录状态保持原有任务卡片 / 空状态 + 公告卡 + 打卡按钮逻辑不变。

### 影响范围
- 首页未登录访客的首屏呈现；已登录用户的打卡流程不受影响。

---

## 2026-07-24（优化）- 账号合并成功后自动更新前端缓存

### 变更类型
优化

### 变更内容
- 后端 `backend/app/api/v1/users.py`：绑定邮箱接口 `/bind-email` 响应改为调用 `_user_payload`，在账号合并场景下返回新的 `access_token`（因主账号 user_id 可能变化），同时补充 `is_wechat_bound` 等字段。
- 前端 `frontend/src/pages/user/profile.vue`：绑定邮箱成功后调用 `userStore.setUser(result.data)` 更新本地用户信息与新 token；合并成功后强制刷新 `unreadCount` 等用户相关缓存，确保全局状态与主账号一致。

### 影响范围
- 个人资料页绑定邮箱触发账号合并场景；合并后前端自动切换到主账号身份，避免旧 token 导致后续请求 401/404。

---

## 2026-07-24（优化）- 账号合并时业务库数据同步迁移到主账号

### 变更类型
优化

### 变更内容
- `backend/app/services/user_service.py`：优化 `bind_email` 账号合并流程。
  - 新增 `_merge_business_data`：在合并用户库数据后，将微信（从）账号的业务库数据迁移到邮箱（主）账号，避免合并后数据仍归属于已删除账号。
  - 新增 `_merge_channel_references`：处理 `plan_notification_channels` 与 `notification_logs` 的渠道引用迁移，并解决 `uk_plan_channel` 唯一冲突。
  - 业务表 `checkin_plans`、`checkin_records`、`notification_logs` 直接更新 `user_id` 到主账号。
  - 通知渠道按类型处理：
    - 站内信：保留主账号渠道，合并引用后删除从账号渠道；若主账号无站内信渠道则直接转移。
    - 微信：合并 `granted/sent` 授权额度，合并引用后删除从账号渠道。
    - 邮件等其他类型：直接变更归属到主账号。
  - 用户库的小程序绑定记录转移逻辑保持不变。

### 影响范围
- 个人资料页绑定邮箱触发账号合并场景；合并后从账号的业务数据全部归属主账号，数据库中不再残留。

---

## 2026-07-24（重构）- 通知派发的渠道启用过滤下沉到 SQL

### 变更类型
重构

### 变更内容
- `backend/app/services/scheduler_service.py`：`_get_channels_for_plan` 查询增加 `NotificationChannel.enabled == True` 条件，未启用渠道不再取回；同步移除 `_dispatch_fixed_offset` 与 `_dispatch_followup_2_for_date` 中冗余的内存 `enabled` 判断，并补充注释说明过滤已下沉到 SQL 层。行为不变：未启用渠道不发送通知、不写 notification_logs。

### 影响范围
- 通知派发内部实现，对外行为无变化。

---

## 2026-07-24（优化）- 邮件通知修改表单新增取消按钮并统一布局

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/notification.vue`：邮件配置修改表单参考微信修改表单，将单独「提交」按钮改为「提交/取消」并排布局（复用 `notification-page__btn-row` 样式）；新增 `cancelEmailEdit` 方法，取消时收起表单并清空 SMTP 主机/端口/邮箱的校验错误提示；更新按钮行样式注释说明为两表单复用。

### 影响范围
- 通知方式页邮件卡片交互：修改表单可点击「取消」收起并重置校验状态。

---

## 2026-07-24（新增）- 通知方式页微信卡片支持点击展开修改启用状态

### 变更类型
新增

### 变更内容
- 后端：
  - `backend/app/schemas/notification_channel.py`：新增 `UpdateWechatChannel` Schema（channel_id + enabled）。
  - `backend/app/services/notification_channel_service.py`：新增 `update_wechat_enabled` 方法，仅允许操作本人的微信渠道，更新 `enabled` 字段（授权额度不变）。
  - `backend/app/api/v1/notification_channels.py`：新增 `PUT /api/v1/notification-channels/wechat` 接口。
- 前端：
  - `frontend/src/api/modules/notification.js`：新增 `updateWechatChannel` 请求函数。
  - `frontend/src/pages/index/notification.vue`：微信订阅消息卡片支持点击展开修改表单（参考邮件卡片样式），表单仅含"是否启用"单选与提交/取消按钮；"重新授权"按钮增加 `.stop` 修饰符避免冒泡展开表单；删除微信渠道后自动收起表单；新增提交/取消并排按钮样式及 ≥768px 断点适配。

### 影响范围
- 通知方式页微信卡片交互：点击卡片可展开/收起启用状态修改表单，提交后刷新渠道列表。

---

## 2026-07-24（优化）- 打卡成功后仅当计划含微信通知方式时才补授权微信订阅

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：
  - 导入 `listNotificationChannels` API，新增 `userChannels` 响应式变量与 `loadUserChannels` 函数，在 `onShow` 中并行加载当前用户的通知渠道列表。
  - 新增计算属性 `hasWechatNotification`：根据 `primaryPlan.value.channel_ids` 与 `userChannels` 比对，判断当前主要计划是否关联了 `channel_type === '微信'` 的通知方式。
  - 修改 `handleCheckin` 打卡成功后的逻辑：将无条件调用 `requestSubscribe({ silent: true })` 改为仅在 `hasWechatNotification.value` 为 `true` 时才调用；否则跳过微信订阅消息补授权，不影响打卡主流程。
  - 未登录用户进入首页时清空 `userChannels`，避免误判断。

### 影响范围
- 首页打卡成功后的微信订阅消息补授权行为：仅当当前打卡计划的通知方式中包含"微信"时才触发静默补授权；纯站内信/邮件通知的计划打卡后不再调用微信订阅 API。

---

## 2026-07-24（优化）- 首页打卡按钮长按重置改为 3-2-1 倒计时效果

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：
  - 新增 `longPressCountdown`（倒计时秒数）与 `longPressInterval` 变量，用于长按重置期间在按钮上显示 3→2→1 倒计时。
  - 保留 `@longpress` 作为长按触发入口，新增 `@touchend` 与 `@touchcancel` 绑定 `handleLongPressEnd`，手指提前松开时取消倒计时与重置动作。
  - 将 `handleLongPress` 中的 `uni.showLoading('请长按3秒...')` 替换为按钮本地倒计时渲染，3 秒到达后设置 `forceActive = true` 并提示"已重置"。
  - 按钮模板在 `longPressCountdown > 0` 时居中显示大号倒计时数字，否则保持原有图标+文本显示。
  - 新增 `.index-page__checkin-countdown` 样式，字体 144rpx，行高与按钮同高（384rpx）垂直居中。
  - 在 `onUnmounted` 中清理 `longPressInterval`，避免页面卸载后残留 interval。
  - 修正提示文案：原"已重置为立即打卡"共 8 个汉字，微信小程序 `uni.showToast({ icon: 'success' })` 最多显示 7 个汉字，导致实际显示为"已重置为立即打"；改为"已重置"（3 个汉字），可完整显示。

### 影响范围
- 首页打卡按钮的长按重置交互：未登录/无计划/disabled 状态不受影响，active 状态不进入倒计时，waiting/done 状态长按 3 秒时显示倒计时后重置为"立即打卡"。
- 重置成功后的 toast 提示文案由"已重置为立即打卡"缩短为"已重置"，避免被微信小程序截断。

---

## 2026-07-24（优化）- 首页临时公告卡片仅对已登录用户显示

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：将首页 `AnnouncementCard` 的显示条件从 `v-if="recentAnnouncements.length"` 调整为 `v-if="isLoggedIn && recentAnnouncements.length"`，未登录用户不再渲染公告临时卡片。
- 同步保留 `loadRecentAnnouncements` 中已登录判断，未登录时清空公告列表并跳过接口请求。

### 影响范围
- 首页临时公告卡片（`AnnouncementCard`）的可见性。
- 未登录用户进入首页时不再看到公告轮播，已登录用户行为不变。

---

## 2026-07-23（优化）- 首页 onShow 增加已登录未读站内信刷新

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：在 `onShow` 生命周期中追加已登录判断，登录状态下调用 `userStore.loadUnreadCount(true)`，首页加载完成后触发一次后端未读数量查询；后端基于现有打卡记录执行自动标记已读，并返回清理后的未读数量，`NoticeButton` 图标即时同步。

### 影响范围
- 首页生命周期、全局未读站内信状态、顶部 `NoticeButton` 图标显示。

---

## 2026-07-23（优化）- 增大微信登录卡片标题与微信图标间距

### 变更类型
优化

### 变更内容
- 需求：微信登录卡片中"一键登录"标题与下方微信图标之间的间距增大一点。
- `frontend/src/pages/user/login.vue` 样式：
  - `.login-page__wechat-btn` 手机端 `margin: 0 auto;` → `margin: 24rpx auto 0;`，叠加卡片 `gap: 24rpx`，标题与图标总间距从 `24rpx` 增大到 `48rpx`。
  - 平板断点（≥768px）同步调整 `margin-top: 12px;`，叠加 `gap: 12px`，总间距从 `12px` 增大到 `24px`。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 微信登录卡片标题放大并拉开切换引导间距

### 变更类型
优化

### 变更内容
- 需求：微信一键登录卡片标题改为"一键登录"并放大至与"欢迎回来"一致；卡片内"账号密码登录"切换引导与上方隐私勾选拉开距离，参考普通登录卡片间距。
- `frontend/src/pages/user/login.vue` template：
  - 微信卡片标题文字"微信一键登录" → "一键登录"。
  - 标题类名 `login-page__wechat-title` → `login-page__title`（复用普通卡片标题样式：`64rpx`/`72rpx`/`font-weight:600`/`#0e0f0c`，平板断点 `32px`/`36px` 自动适用）。
- `frontend/src/pages/user/login.vue` 样式：
  - 删除手机端与平板断点的 `.login-page__wechat-title` 规则（不再使用）。
  - 新增 `.login-page__wechat .login-page__switch { margin-top: 24rpx; }`（平板 `12px`），叠加微信卡片 `gap: 24rpx` 共 `48rpx` 间距，与普通卡片 `gap: 48rpx` 一致。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 登录/注册页底部链接仅可点击文字跳转

### 变更类型
优化

### 变更内容
- 需求：登录页和注册页底部链接中，只有"立即注册"/"去登录"文字可点击跳转，前面提示文字不可点击。
- `frontend/src/pages/user/login.vue`：
  - 底部注册链接拆分为两个 `<text>`：提示文字"还没有账号？" + 可点击链接"立即注册"。
  - 将 `@click="goRegister"` 从外层 `<view>` 移到"立即注册"文字上。
  - 新增 `.login-page__footer-link` 样式，使用品牌深绿色 `#2f6c00` 与 `font-weight: 500`，突出可点击链接。
  - 平板断点同步新增 `.login-page__footer-link` 尺寸样式。
- `frontend/src/pages/user/register.vue`：
  - 底部登录链接拆分为两个 `<text>`：提示文字"已有账号？" + 可点击链接"去登录"。
  - 将 `@click="goLogin"` 从外层 `<view>` 移到"去登录"文字上。
  - 新增 `.register-page__footer-link` 样式（品牌深绿、加粗）及平板断点样式。

### 影响范围
- `frontend/src/pages/user/login.vue`
- `frontend/src/pages/user/register.vue`
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 普通登录卡片注册链接真正移至卡片外

### 变更类型
优化

### 变更内容
- 需求：普通登录卡片中的"还没有账号？立即注册"真正移到普通登录卡片外，但应随普通登录卡片一起出现（微信登录卡片时不显示）。
- `frontend/src/pages/user/login.vue` template：
  - 将 `.login-page__footer` 从 `.login-page__card` 内部移出到卡片关闭标签之后，作为独立的页面底部链接。
  - 为 footer 添加 `v-if="loginMode === 'normal'"`，确保仅在普通登录卡片出现时展示。
- `frontend/src/pages/user/login.vue` 样式：
  - `.login-page__footer` 新增 `margin-top: 48rpx`（手机端）与 `24px`（平板），参考 `register.vue` 底部链接间距，使注册链接与白色卡片有明显间距。
  - `.login-page__switch` 移除全局 `margin-top`（手机端 48rpx / 平板 24px），恢复为靠 `.login-page__card` 的 `gap` 控制卡片内间距；微信卡片内切换引导仍通过 `.login-page__wechat .login-page__switch` 保持额外间距。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 登录页调整注册链接位置与切换引导样式

### 变更类型
优化

### 变更内容
- 需求：
  1. 普通登录卡片中"还没有账号？立即注册"紧贴登录按钮正下方，与登录按钮作为一个整体；切换引导文字简化为"一键登录"并移到注册链接下方，放大突出以便用户优先注意到。
  2. 微信一键登录卡片中的切换引导同等简化与放大突出。
- `frontend/src/pages/user/login.vue` template 改造：
  - 普通卡片：将 `.login-page__footer`（注册链接）从卡片直接子元素移入 `.login-page__actions` 内，紧贴登录按钮 `.login-page__submit` 下方；`.login-page__switch` 移到 form 之后（即注册链接下方）。
  - 普通卡片切换文字"使用微信一键登录" → "一键登录"。
  - 微信卡片切换文字"使用账号密码登录" → "账号密码登录"。
- `frontend/src/pages/user/login.vue` 样式改造：
  - `.login-page__footer` 新增 `margin-top: 16rpx`（平板断点 `8px`），使注册链接紧贴登录按钮作为一个整体。
  - `.login-page__switch-text` 放大突出：`font-size 28rpx → 36rpx`（平板 `14px → 18px`）、`line-height 40rpx → 52rpx`（平板 `20px → 26px`）、`font-weight: 600`、`color #454745 → #2f6c00`（品牌深绿，与选中态复选框一致）。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 登录页实现微信登录与账号密码登录卡片切换

### 变更类型
优化

### 变更内容
- 需求：
  1. 未登录默认进入登录页：仅展示微信一键登录卡片，隐藏账号密码卡片，卡片下方提供"使用账号密码登录"引导，点击后切换为账号密码卡片。
  2. 注册页"去登录"链接或注册成功自动跳转登录页：默认展示账号密码卡片，卡片下方提供"使用微信一键登录"引导，点击后切换为微信登录卡片。
- `frontend/src/pages/user/login.vue` 改造：
  - 新增 `loginMode` 响应式变量（'wechat' 默认 | 'normal'），通过 `v-if/v-else` 互斥显示对应卡片。
  - 新增 `onLoad` 生命周期（从 `@dcloudio/uni-app` 导入）：读取 URL 参数 `mode`，值为 `normal` 时初始展示账号密码卡片。
  - 新增 `switchMode(mode)` 函数供引导文字点击切换。
  - 微信卡片末尾新增 `.login-page__switch` 引导"使用账号密码登录"。
  - 账号密码卡片 form 与注册链接之间新增 `.login-page__switch` 引导"使用微信一键登录"。
  - 因两卡片互斥显示，统一 `.login-page__card` 与 `.login-page__wechat` 的 `margin-top` 为 `64rpx`（平板断点 `32px`），作首卡片顶部留白避开 BackButton。
  - 新增 `.login-page__switch` / `.login-page__switch-text` 样式（居中、`28rpx`/`40rpx`，平板 `14px`/`20px`）。
- `frontend/src/pages/user/register.vue` 改造：
  - 注册成功跳转（`uni.redirectTo`）与"去登录"链接（`uni.navigateTo`）URL 均追加 `?mode=normal` 参数。
- 其他跳转登录页的场景（退出登录/401拦截/设置页点击登录/找回密码页）不带参数，默认展示微信一键登录卡片，符合"未登录优先引导微信登录"的预期。

### 影响范围
- `frontend/src/pages/user/login.vue`：新增登录方式切换逻辑与引导 UI
- `frontend/src/pages/user/register.vue`：两处跳转 URL 追加 `?mode=normal`
- 不影响后端、数据库

---

## 2026-07-23（清理）- 删除已回填的增量 SQL 脚本

### 变更类型
清理

### 变更内容
- 用户已删除 `backend/sql/` 下的两个增量迁移脚本：
  - `add_role_to_users.sql`（users 表新增 role 字段，已回填至 `create_user_db.sql`）
  - `add_announcements.sql`（新建 announcements 公告表，已回填至 `create_yao_db.sql`）
- 依据 AGENTS.md 6.3 节：增量变更需同步回填到对应的 `create_*_db.sql`，使新环境一次建库即最新结构。两个增量脚本对应的结构均已回填完成，删除增量脚本不影响新环境建库。
- 同步更新 `目录结构.json`：
  - 删除 `add_role_to_users.sql` 与 `add_announcements.sql` 两个条目
  - 去掉 `create_user_db.sql` 描述中"role 列定义与 add_role_to_users.sql 增量脚本一致"的引用
  - 去掉 `create_yao_db.sql` 描述中"与 add_announcements.sql 增量一致"的引用

### 影响范围
- `backend/sql/`：减少两个增量脚本文件（用户已删除）
- `目录结构.json`：同步更新 sql 目录条目
- 不影响数据库结构（已回填）、不影响后端代码与前端

---

## 2026-07-23（修复）- 删除登录页两卡片中间显露的绿色氛围装饰圆

### 变更类型
修复

### 变更内容
- 问题：登录页布局调整为「微信一键登录卡片在上、账号密码卡片在下」后，原绝对定位在屏幕正中央的绿色氛围装饰圆（`.login-page__ambient`，背景色 `#e2f6d5`）正好在两卡片之间的中间位置显露，形成视觉上的绿色底块。
- 删除 `frontend/src/pages/user/login.vue` 中：
  - template 内的 `<view class="login-page__ambient"></view>` 元素及注释
  - 手机端 `.login-page__ambient` 样式规则（624rpx 圆形 + `#e2f6d5` 背景）
  - 平板/折叠屏断点（≥768px）内 `.login-page__ambient` 尺寸规则
- 页面背景统一由 `.login-page` 的 `var(--page-bg-color)` 承担，无其他副作用。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 登录页调整为微信一键登录卡片在上、账号密码卡片在下

### 变更类型
优化

### 变更内容
- 调整 `frontend/src/pages/user/login.vue` 的卡片顺序：将微信一键登录卡片（`login-page__wechat`）移至账号密码登录卡片（`login-page__card`）上方，优先引导微信一键登录。
- 同步交换两卡片的 `margin-top`：
  - 手机端（rpx）：`__wechat` 由 `32rpx` → `64rpx`（顶部留白避开 BackButton），`__card` 由 `64rpx` → `32rpx`（卡片间距）。
  - 平板/折叠屏断点（≥768px）：`__wechat` 由 `16px` → `32px`，`__card` 由 `32px` → `16px`。
- 未改动任何业务逻辑（登录流程、校验、隐私勾选等保持原样）。

### 影响范围
- 仅 `frontend/src/pages/user/login.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-23（优化）- 微信订阅授权静默拒绝时引导用户前往设置重新开启

### 变更类型
优化

### 变更内容
- 问题：当用户在微信订阅消息授权弹窗中勾选「总是保持以上选择，不再询问」并取消后，后续点击「授权订阅提醒」时 `wx.requestSubscribeMessage` 不再弹窗，直接静默返回 reject，用户无法再次触发授权，导致订阅通道失效。
- 依据：微信官方文档 `wx.getSetting({ withSubscriptions: true })` 的 `subscriptionsSetting.itemSettings` 仅返回用户勾选过「总是保持以上选择」的记录，值为 `reject` 即代表静默拒绝状态。
- `frontend/src/composables/useWechatSubscribe.js` 新增 `isSubscribeSilentRejected` 函数：通过 `uni.getSetting({ withSubscriptions: true })` 检测模板 ID 对应的 `itemSettings` 是否为 `reject`，返回是否处于静默拒绝状态。
- `frontend/src/pages/index/notification.vue` 的 `doWechatAuthorize` 改造：授权未成功时调用 `isSubscribeSilentRejected`，若为静默拒绝则弹窗提示「需要重新允许授权」并引导用户点击「去设置」；用户从 `uni.openSetting` 返回后再次发起 `requestSubscribe`，用户若已解除限制则重新弹出微信官方授权弹窗，授权成功后关闭表单并刷新列表（再次失败不再递归引导，避免死循环）。
- 不改动 `handleWechatReauth`（重新授权入口）与打卡静默补授权（`index.vue`）流程。

### 影响范围
- `frontend/src/composables/useWechatSubscribe.js`：新增检测函数并导出
- `frontend/src/pages/index/notification.vue`：`doWechatAuthorize` 增加静默拒绝处理分支
- 不影响后端、数据库

---

## 2026-07-23（优化）- 删除微信订阅授权的二次授权引导

### 变更类型
优化

### 变更内容
- 问题：微信订阅消息授权在真机首次弹窗即包含「总是保持以上选择，不再询问」选项，原页面中引导用户进行二次授权以勾选该选项的文本与弹窗属于冗余设计（开发者工具模拟器不显示该选项，导致此前误判为需要二次授权）。
- 删除 `frontend/src/pages/index/notification.vue` 新建表单中第 182 行引导二次授权的提示文本（"提示：首次授权的微信弹窗不会出现…再次点击「授权订阅提醒」并勾选该项后…"）。
- 简化 `doWechatAuthorize` 函数：移除 `wasNew` 判断分支与首次授权成功后调起「继续授权」弹窗的逻辑，统一为授权成功后 toast 提示并关闭表单、刷新列表。

### 影响范围
- 仅 `frontend/src/pages/index/notification.vue` 单文件
- 不影响后端、数据库及其他页面

---

## 2026-07-22（优化）- 微调 PasswordEye 闭眼月牙弧度与对称性

### 变更类型
优化

### 变更内容
- 微调 `frontend/src/components/PasswordEye.vue` 闭眼图标的月牙弧度，使其更平缓、左右对称。
- 将闭眼 `.pwd-eye__closed` 的 `border-radius` 垂直半径从 `100%` 调整为 `70%`（`0 0 50% 50% / 0 0 100% 100%` → `0 0 50% 50% / 0 0 70% 70%`），弧线弯曲程度减小，视觉更平缓自然。
- 保持睁眼形态不变（完整眼形轮廓 + 瞳孔）。
- 左右对称性验证：闭眼 leftMargin = rightMargin = 13px，睁眼同为 13px，切换时位置稳定不跳动。

### 影响范围
- 仅 `frontend/src/components/PasswordEye.vue` 单文件单属性微调
- 不影响其他界面，不涉及后端或数据库

---

## 2026-07-22（修复）- 修复输入密码时密码框边框消失

### 变更类型
修复

### 变更内容
- 问题：输入密码时密码框边框消失。根因是 login/register/forgot-password 三页的密码行容器用 `box-shadow: inset 0 0 0 1px` 做边框，而微信小程序 `<input>` 原生组件的白色不透明背景会覆盖父容器的 inset box-shadow，导致边框在输入时消失。普通输入框边框是 input 自身的 box-shadow（不被覆盖），所以只有密码框受影响。
- 修复：将三页的 `.{page}__password-row` 边框从 `box-shadow: inset` 改为 `border: 1px solid #c1cab5`（border 在元素边界上，不会被内部子元素覆盖），与 profile.vue 统一。
- `:focus-within` 聚焦态从 `box-shadow` 变色改为 `border-color` 变色（#c1cab5 → #454745）。
- 错误态 `--error` 从 `box-shadow` 变色改为 `border-color` 变色（#e5484d）。
- input 元素增加 `border: none`（显式清除，防止原生组件默认 border），保留 `box-shadow: none`。
- 眼睛切换效果不受影响（`__eye` 的 z-index:2 保留，点击切换逻辑不变）。

### 影响范围
- `frontend/src/pages/user/login.vue`：`__password-row` 改 border + `__input--password` 加 border:none
- `frontend/src/pages/user/register.vue`：同上
- `frontend/src/pages/user/forgot-password.vue`：同上
- profile.vue 已用 border 实现，无需修改
- 不影响其他界面，不涉及后端或数据库

---

## 2026-07-22（优化）- 优化 PasswordEye 睁眼/闭眼视觉差异并修复点击层级

### 变更类型
优化 / 修复

### 变更内容
- 原实现中睁眼与闭眼两种状态视觉差异不明显：睁眼为「眼眶+瞳孔」，闭眼为「眼眶+瞳孔+斜线」，眼睛形状本身一致，仅靠斜线区分，用户难以直观识别当前是明文还是密文。
- 重绘 `frontend/src/components/PasswordEye.vue`，让两种状态视觉差异明显：
  - 睁眼（`visible=true`）：完整眼形轮廓（上平下圆的椭圆）+ 中心瞳孔，表示密码明文可见
  - 闭眼（`visible=false`）：向下弯曲的闭合弧线（仅下边框 + 下圆角形成的月牙形），表示密码已隐藏
- 移除原「斜线遮挡」方案，改为「睁眼显示完整眼睛、闭眼显示闭合弧线」两种截然不同的图形，点击眼睛后视觉切换明显。
- Props 接口（`visible: Boolean`）保持不变，4 个页面共 8 处 `<PasswordEye :visible="showXxx" />` 调用无需修改。
- 修复 H5 下 `<uni-input>` 拦截眼睛点击：给 4 个页面的 `.{page}__eye` 容器添加 `position: relative; z-index: 2;`，确保眼睛点击层级高于相邻输入框，点击不再被拦截。
- 修复 `forgot-password.vue` 响应式断点 SCSS 语法错误：`@media` 块在第 635 行提前闭合 `}`，导致 `__placeholder`/`__code-row`/`__code-btn` 等规则落在媒体查询外且末尾多出未匹配 `}`，构建报错 "unmatched }"。移除提前闭合的 `}`，让媒体查询在正确位置闭合。

### 影响范围
- `frontend/src/components/PasswordEye.vue`：重绘睁眼/闭眼视觉
- `frontend/src/pages/user/login.vue`：`__eye` 添加 z-index
- `frontend/src/pages/user/register.vue`：`__eye` 添加 z-index
- `frontend/src/pages/user/forgot-password.vue`：`__eye` 添加 z-index + 修复 SCSS 语法
- `frontend/src/pages/user/profile.vue`：`__eye` 添加 z-index
- 不影响其他界面显示效果，不涉及后端或数据库。

---

## 2026-07-22（修复）- 修复密码框点击眼睛切换显隐在微信真机不生效

### 变更类型
修复

### 变更内容
- 登录/注册/找回密码/个人资料四页的密码框点击眼睛图标后，真机上密码明文不切换（视觉“无反应”）。根因为微信小程序 `<input>` 为原生组件，动态修改 `password` 属性后已输入内容的密文/明文状态不会自动刷新，必须重建 input 节点或重新聚焦才生效。
- 修复方式：为每个密码 `<input>` 增加 `:key`，其值随对应 `showXxx` 变化（如 `'login-pwd-' + showPassword`）。点击眼睛切换 `showXxx` 时 key 变化，强制 Vue 重建 input 节点，使 `:password="!showXxx"` 以新值重新创建原生组件，密文/明文正确刷新。
- 涉及页面与字段：login（密码）、register（密码/确认密码）、forgot-password（新密码/确认密码）、profile（旧密码/新密码/确认密码），共 8 个密码框。

### 影响范围
- 仅前端密码框显隐交互；不涉及图片资源、后端或数据库。

---

## 2026-07-22（修复）- 重绘密码眼睛图标并修复微信端点击不生效

### 变更类型
修复 / 优化

### 变更内容
- 重绘 `PasswordEye.vue`：眼眶改为上平下圆的眼形轮廓（border-radius: 50% / 58% 58% 42% 42%），瞳孔与斜线比例优化，外观更清晰；组件改为纯展示（`visible` 仅控制睁眼/闭眼），不再内含点击逻辑。
- 修复微信端点击眼睛无法显示明文：原实现将眼睛绝对定位覆盖在 `<input>` 上，而微信小程序 `<input>` 为原生组件、层级高于普通 view，导致眼睛可见但点击被原生输入框吞掉、toggle 不触发。现改为「统一边框容器 + 非重叠兄弟节点」结构——`.{page}__password-row` 作为带边框的 flex 容器，内含 `<input>`（flex:1，移除自身边框）与眼睛容器（`.{page}__eye`，flex:none，点击 @click 切换）。眼睛不再覆盖原生输入框，微信/H5 均可点击。
- 点击逻辑统一移到父页面包裹容器 `@click`（沿用最初 mima_1.png 可用的写法），由各页 `showXxx` ref 控制 `:password="!showXxx"`。
- 错误态边框从输入框移到 `.{page}__password-row--error`，聚焦态用 `:focus-within` 高亮整框；各页均已加 ≥768px 断点锁定眼睛与内边距为 px。
- 涉及页面：login / register / forgot-password / profile，所有密码框（密码、确认密码、旧密码、新密码）均接入。

### 影响范围
- 仅前端密码框展示与交互；仍不引用任何图片资源。

---

## 2026-07-22（优化）- 密码框弃用 mima_1.png，改用纯 CSS 绘制的眼睛图标切换显隐

### 变更类型
优化

### 变更内容
- 新增 `frontend/src/components/PasswordEye.vue`：纯 CSS 绘制的眼睛图标组件（不依赖任何图片资源），Props `visible` 表示密码是否明文可见（默认 false，闭眼/遮挡线，密码隐藏），点击触发 `toggle` 事件。
- `frontend/src/pages/user/login.vue`：移除 `mima_1.png` 图片导入，密码框右侧改用 `PasswordEye`，默认闭眼、点击睁眼显示明文；同步调整右侧内边距与眼睛定位样式。
- `frontend/src/pages/user/register.vue`：密码、确认密码两个输入框原本固定 `:password="true"` 无切换，现各自包裹密码行并接入 `PasswordEye`（`showPassword`/`showConfirmPassword` 独立切换）。
- `frontend/src/pages/user/forgot-password.vue`：新密码、确认密码输入框接入 `PasswordEye`（`showNewPassword`/`showConfirmPassword` 独立切换）。
- `frontend/src/pages/user/profile.vue`：旧密码、新密码、确认密码三个输入框接入 `PasswordEye`（`showOldPassword`/`showNewPassword`/`showConfirmPassword` 独立切换）。
- 各页均新增密码行容器 `.{page}__password-row` 与眼睛定位样式 `.{page}__eye`，并在 ≥768px 断点锁定眼睛/内边距尺寸为 px，避免 rpx 在宽屏过度放大。

### 影响范围
- 仅前端密码输入框的显隐交互与展示；不再引用 `mima_1.png` 图片资源。`mima_1.png` 文件仍保留于 `assets/images/`，可后续清理。

### 备注
- 眼睛图标采用嵌套 view + border-radius 纯 CSS 方案，兼容微信小程序（不支持内联 SVG 元素）；遵循 AGENTS.md「禁止引入图片、字体等二进制资源」约束。

---

## 2026-07-22（新增）- 通知页授权前校验微信绑定，未绑定则引导一键登录绑定

### 变更类型
新增 / 优化

### 变更内容
- 后端新增「绑定微信到当前用户」能力：
  - `backend/app/services/user_service.py`：`User.bind_wechat(user_id, code)` —— 用 code 换取 openid 后关联到**当前登录用户**（写入 `user_miniapp_accounts`）；若该 openid 已被其他账号绑定则拒绝，已绑定到本人则幂等更新 session_key。
  - `backend/app/api/v1/users.py`：新增 `POST /api/v1/users/bind-wechat`（依赖 `get_current_user_id`）；并让登录/注册/重置密码/微信登录/info 等用户相关响应统一返回 `is_wechat_bound` 字段（基于是否存在 `user_miniapp_accounts` 记录）。
  - `backend/app/schemas/user.py`：新增 `BindWeChat` 请求 Schema（仅 code）。
- 前端：
  - `frontend/src/api/modules/user.js`：新增 `bindWechat(code)` 接口。
  - `frontend/src/store/modules/user.js`：`setUser` 保存 `is_wechat_bound`，并补全 userInfo 字段说明。
  - `frontend/src/pages/index/notification.vue`：「授权订阅提醒」按钮增加前置校验——若 `userStore.userInfo.is_wechat_bound` 为 false（未做过微信一键登录），弹窗提示「需先绑定微信」，用户确认后复用微信一键登录获取 code 的流程（`uni.login`）调用 `bindWechat` 将微信绑定到当前用户，绑定成功（更新本地登录态）后继续发起订阅授权；已绑定则直接进入订阅授权主流程。

### 根因
- 通过账号密码/邮箱注册登录的用户没有 `user_miniapp_accounts` 微信绑定记录，`scheduler_service._get_openid` 按 `user_id + appid` 查不到 openid，导致发送微信订阅消息失败；绑定后即可正常下发。

### 影响范围
- 仅影响微信渠道授权路径；未绑定微信的注册用户现在可在通知页一键绑定，绑定后仍使用原账号（不会新建账号或切换登录态），后端 `user_miniapp_accounts` 新增写入。

### 备注
- 与微信一键登录（`wechat_login`）的区别：`wechat_login` 按 openid 查找或创建用户并切换登录态；`bind-wechat` 仅将 openid 关联到当前 JWT 用户，不影响登录态（仅刷新 `is_wechat_bound`）。

---

## 2026-07-22（优化）- 通知页新建表单移除「站内信」单选项

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/notification.vue`：点击「添加新的通知方式」展开的新建表单中，移除通知类型单选框里的「站内信」选项（仅保留邮件、微信）。页面列表区已展示的站内信卡片保持不变，后端站内信通知方式逻辑不变。
  - 表单默认类型 `formType` 初始值由 `'站内信'` 修正为 `'微信'`（站内信已不可选，消除悬空默认值，与 `handleAdd` 实际行为一致）。
  - 同步更新因本次改动而过时的注释：通知类型单选框注释、脚本顶部文档注释、canSave 注释、保存按钮置灰态 CSS 注释。

### 影响范围
- 仅前端 UI 调整；新建表单不再提供站内信类型，用户只能新增邮件或微信通知方式。列表中站内信卡片、后端接口与数据均不受影响。

---

## 2026-07-22（优化）- 微信授权引导：说明文本 + 首次成功后二次授权弹窗

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/notification.vue`：
  - 微信订阅说明文本新增一段提示：首次授权的微信弹窗不会出现「总是保持以上选择，不再询问」选项，需再次点击授权并在弹窗中勾选该项，之后每日打卡将自动补充额度、不再打扰。
  - `handleWechatAuthorize` 增加首次授权引导：当该用户此前未配置微信渠道（即本次为首次授权）且授权成功后，弹 `showModal` 提示「继续授权」；用户点击确认后再次调用 `requestSubscribe()` 调起微信原生弹窗（此时才会出现「总是保持」选项），引导其勾选，以实现后续免打扰自动补额度。

### 影响范围
- 仅增强微信订阅授权的用户引导，不改动授权逻辑与后端接口；非微信端按原逻辑跳过或提示，无副作用。

### 备注
- 根因回顾：微信规范规定「总是保持以上选择，不再询问」选项仅在模板已被授权（accept）过至少一次后才出现，故首次授权弹窗必然无该选项，此为预期行为，非代码缺陷。

---

## 2026-07-22（修复）- 通知页微信授权按钮不可用 + 默认选中微信

### 变更类型
修复

### 变更内容
- `frontend/src/composables/useWechatSubscribe.js`：该文件此前内容被清空（0 B），导致 `useWechatSubscribe()` 解析为 `undefined`，通知页与首页 `<script setup>` 初始化即抛错，微信「授权订阅提醒」按钮失效。已按原设计重建：封装 `uni.requestSubscribeMessage`（仅 `#ifdef MP-WEIXIN` 生效），用户 `accept` 后调 `grantWechatChannel()` 累加额度；`silent` 模式（每日打卡静默补授权）失败不提示。
- `frontend/src/pages/index/notification.vue`：点击「添加新的通知方式」后，新建表单默认选中类型由「邮件」改为「微信」；原默认选中邮件时触发的"未绑定邮箱倒计时引导"已并入「邮件」类型的 `selectType` 分支，仅当用户主动切到邮件且未绑定邮箱时引导跳转，不再随新建表单弹出。

### 影响范围
- 通知页新建表单默认推荐微信订阅消息，其「授权订阅提醒」按钮恢复正常可点击；邮件引导逻辑保持原有体验，无副作用。

### 补充（同日排查"模板未配置"提示）
- 重建文件后点击「授权订阅提醒」仍提示"订阅消息模板未配置"。经核对：编译产物中 `import.meta.env` 被替换为空对象 `{}`，`VITE_WX_SUBSCRIBE_TEMPLATE_ID` 取不到值；同样 `VITE_API_BASE_URL` 也未被内联（仅因 `request.js` 有 `|| 'http://localhost:8000'` 兜底才"看似正常"）。
- 一次性 `npm run build:mp-weixin` 验证：新构建产物中模板 ID 已正确内联，证明 env 注入机制本身正常。问题真实原因为 **dev 编译器进程在 `.env` 尚未含该变量时启动，之后一直以空 env 编译 `dist/dev`**。已重启 `npm run dev:mp-weixin`（原 PID 12400），重新编译后 `dist/dev` 已正确内联模板 ID。
- 经验：uni-app（vite）仅在 dev 服务器启动时读取一次 `.env`；新增 `VITE_` 变量后必须重启 dev 服务器才会生效。

---

## 2026-07-22（新增）- 微信订阅消息提醒方式

### 变更类型
新增

### 变更内容
为"打卡计划时间到达后发送微信订阅消息"提供完整支持（一次性订阅模板，个人开发者可用）：
- **后端配置** `backend/app/core/config.py`：新增 `WX_SUBSCRIBE_TEMPLATE_ID`（模板ID）、`WX_SUBSCRIBE_PAGE`（点击跳转页）、`WX_SUBSCRIBE_ORG_NAME`（机构名称）三项环境变量，敏感值统一来自 `.env`。
- **新增服务** `backend/app/services/wechat_service.py`：`WeChatService` 缓存微信 `access_token`（提前5分钟过期）并提供 `send_subscribe_message(openid, template_id, data, page)`。
- **渠道类型** `backend/app/schemas/notification_channel.py`：新增 `CHANNEL_TYPE_WECHAT = "微信"`。
- **额度服务** `backend/app/services/notification_channel_service.py`：新增 `get_or_create_wechat_channel` / `grant_wechat`（授权+1额度并返回剩余）/ `parse_wechat_channel_value`（`channel_value` 存 `{granted, sent}`）。
- **授权接口** `backend/app/api/v1/notification_channels.py`：新增 `POST /api/v1/notification-channels/wechat/grant`（用户每次同意授权 +1 额度并启用微信渠道），列表响应额外返回 `wechat_quota` 与 `remaining`。
- **调度发送** `backend/app/services/scheduler_service.py`：新增微信分支——按 `user_id+appid` 从 `user_miniapp_accounts` 取 openid，按额度 `granted-sent>0` 才下发；成功 `sent+1`，返回 `43101` 无权限则将 `granted` 对齐 `sent` 停止后续发送。模板字段固定组装：`thing4=打卡名称 thing3=备注 time13=提醒时间(HH:MM) thing12=机构名称`。
- **前端授权** `frontend/src/composables/useWechatSubscribe.js`（新增）：封装 `uni.requestSubscribeMessage`，成功 accept 后调后端 +1 额度；`silent` 模式用于静默补授权。
- **前端接口** `frontend/src/api/modules/notification.js`：新增 `grantWechatChannel`。
- **通知页** `frontend/src/pages/index/notification.vue`：新建表单新增"微信"类型（含授权说明与"授权订阅提醒"按钮），列表新增微信卡片（展示剩余可发次数、额度用尽时"重新授权"、删除）。
- **静默补授权** `frontend/src/pages/index/index.vue`：打卡成功后静默调用 `requestSubscribe({silent:true})`，与通知页授权形成"每日提醒额度自循环"。
- **环境变量模板** `backend/.env.template` 与 `frontend/.env.template`：补充 `WX_SUBSCRIBE_*` / `VITE_WX_SUBSCRIBE_TEMPLATE_ID`。

### 影响范围
- 微信小程序在用户授权后，于打卡计划提醒时间点（含准时触发与两档催办）经订阅消息下发提醒；站内信仍作为零授权兜底渠道。个人开发者受"一次性订阅"约束：每次授权仅下发 1 条，靠多次自然交互（通知页授权 + 每日打卡）累积额度；额度耗尽或用户取消授权后微信渠道停发且不报错，站内信兜底不脱节。

---

## 2026-07-21（优化）- NoticeButton 取消页面切换时查询未读数量

### 变更类型
优化

### 变更内容
- `frontend/src/components/NoticeButton.vue`：移除组件内 `onShow` 钩子中对 `userStore.loadUnreadCount(true)` 的调用及 `onShow` 导入。此前任何含 `NoticeButton` 的页面切换时都会触发后端未读数量查询（并附带自动标记已读逻辑），现改为仅在打开站内信页（`messages.vue` 的 `loadMessages` 已通过 `setUnreadCount` 同步图标）和首页打卡成功时（`index.vue` 的 `handleCheckin` 中 `loadUnreadCount(true)`）查询更新。

### 影响范围
- 含 `NoticeButton` 的页面切换不再触发未读数量查询；站内信页与首页打卡流程的未读数量同步逻辑保持不变。

---

## 2026-07-21（优化）- 首页打卡成功后自动触发未读站内信标记已读

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/index.vue`：在 `handleCheckin` 打卡成功分支（`res.code === 0`）中追加 `userStore.loadUnreadCount(true)`，强制请求后端 `/api/v1/notification-logs/unread-count`。后端会基于新产生的打卡记录自动将匹配区间内的未读站内信标记为已读，并返回清理后的未读数量；`NoticeButton` 组件通过 `userStore.unreadCount` 自动响应，图标状态即时同步。

### 影响范围
- 首页打卡流程、全局未读站内信状态、顶部 `NoticeButton` 图标显示。

---

## 2026-07-20（修复）- 普通登录补充更新最后登录时间

### 变更类型
修复

### 变更内容
- `backend/app/services/user_service.py`：普通 `login`（用户名/邮箱 + 密码）原先未更新 `last_login_at`，仅微信一键登录 `wechat_login` 更新。现两处均在校验通过后写入 `now_shanghai()` 并更新 `users.last_login_at`，行为一致。

### 影响范围
- 登录流程（普通登录、微信一键登录），`users` 表 `last_login_at` 字段。

---

## 2026-07-20（优化）- 核对数据库与 SQL 文件描述

### 变更类型
优化（文档同步）

### 变更内容
- 连接 dev 库（root/root@127.0.0.1:3306）核对：业务库 `wuzuniao_yao` 现存 7 张表（announcements/checkin_plans/checkin_records/notification_channels/notification_logs/plan_notification_channels/plan_notification_times），用户库 `wuzuniao_yonghu` 现存 users、user_miniapp_accounts，字段均与 `backend/sql/` 下 SQL 文件一致（announcements 字段 id/title/content/created_at/updated_at 完全匹配，users.role 为 TINYINT DEFAULT 0）。**实际数据库与 SQL 文件已同步，无需改 SQL。**
- 更新 `目录结构.json` 中 SQL 文件描述以符合现状：
  1. `create_user_db.sql`：补全两表（users 含 role/status、user_miniapp_accounts 绑定表）。
  2. `create_yao_db.sql`：补全全部 7 张表及关键索引/字段（checkin_plans、plan_notification_times、notification_channels、plan_notification_channels 此前缺失）。
  3. `add_role_to_users.sql`：修正字段类型描述 `SMALLINT` → `TINYINT`，与实际 SQL 一致。

### 影响范围
- 仅文档（`目录结构.json`）描述修正，SQL 文件与数据库结构无改动。

---

## 2026-07-20（优化）- 设置页公告管理卡极简化 & 底部间距对齐记录页

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/settings.vue`：
  1. **公告管理卡极简化**：去掉左侧绿色图标与副标题「官方公告发布与管理」，仅保留「公告管理」标题 + 右箭头；通过 `settings-page__admin-row`（2 列网格）让卡片**占左半行（不独占整行）**，右半留空。
  2. **公告管理卡去箭头**：移除右侧箭头，卡片仅包裹「公告管理」文字（左对齐），更简洁。
  3. **公告管理标题放大居中**：`.settings-page__admin-title` 字号由 32rpx→36rpx（平板 16px→18px），卡片 `justify-content:center` + 标题 `text-align:center`，文字放大并居中。
  4. **公告管理卡缩为单列小卡**：`settings-page__admin-row` 由 2 列网格（`1fr 1fr`）改为单列 `max-content` 网格，卡片按文字宽度收缩（不再占半行）；标题字号由 36rpx→32rpx（平板 18px→16px），内边距同步缩小（移动 24rpx×32rpx / 平板 12px×16px），卡片同步变小。
  2. **底部间距对齐记录页**：`.settings-page` 的 `padding-bottom` 由 `354rpx`（平板 177px）改为 `240rpx`（平板 120px），与记录页 `record.vue` 一致，使页面最后元素（隐私政策）到导航栏顶部的距离同记录页。

### 影响范围
- 仅影响设置页公告管理卡样式/布局与底部留白；功能跳转、数据加载逻辑不变。

---

## 2026-07-20（优化）- 设置页公共管理卡片重设计 & 高度自适应

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/settings.vue`：
  1. **制定计划 / 通知方式还原独占整行原样**：撤销上轮改动，二者恢复为独立的「分组 1」（`settings-page__group1`），各自占满整行，删除冷静期（status=0）恢复为「整个分组置灰禁用」（`settings-page__group1--disabled`，公告管理不受影响）。
  2. **公共管理卡片重新设计**：不再模仿用户资料卡布局（去掉右侧圆形头像、右上绿渐变），改为**功能入口风格**——左侧绿色圆角「公」字图标 + 中间标题「公告管理」/副标题「官方公告发布与管理」+ 右侧箭头；并与用户资料卡一起包进 `settings-page__near-group`（卡片间距 32rpx，小于与其它分组的 64rpx），实现「距离用户资料卡更近、减少间距」。
  3. **高度自适应**：移除 `.settings-page` 上重复的 `min-height: 100vh`（全局 `page` 已在 global.scss 兜底撑满视口与背景），页面高度改为自适应内容；内容不足一屏时不出现多余滚动条，内容超出时正常滚动。

### 影响范围
- 仅影响设置页布局与样式；功能跳转、数据加载逻辑不变。

---

## 2026-07-20（优化）- 设置页公共管理卡片改版

### 变更类型
优化

### 变更内容
- `frontend/src/pages/index/settings.vue`：
  - 公共管理（公告管理）卡片由横条 link-card 改为**贴近顶部用户资料卡风格**的小卡片：白底圆角、右上绿色渐变点缀、左侧标题+副标题、右侧绿色圆形「公」字头像占位。
  - 通过 `settings-page__row--grid`（2 列网格）与「制定计划」**并排展示，不再独占整行**；非管理员时该行退化为单列，「制定计划」恢复独占整行（原布局行为不变）。
  - 删除冷静期（status=0）置灰逻辑由「整个分组禁用」改为「制定计划/通知方式单张卡片禁用」（`settings-page__link-disabled`），公告管理卡片不受影响。
  - 同步补充平板断点（≥768px）下新卡片与网格的 px 尺寸。

### 影响范围
- 仅影响设置页「管理 + 制定计划 + 通知方式」区域样式与布局；功能跳转、数据加载逻辑不变。

---

## 2026-07-20（优化）- 公告标题统一前缀「【官方公告】」

### 变更类型
优化

### 变更内容
- `frontend/src/components/AnnouncementCard.vue`：新增 `displayTitle` 计算属性，在公告标题前固定拼接「【官方公告】」（常规轮播态与全屏态标题均使用），避免硬编码重复。标题原文仍由后端 `title` 字段提供，前缀仅前端展示。

### 影响范围
- 仅影响首页公告卡片标题展示文案，不影响已读判定/轮播逻辑与后端接口。

---

## 2026-07-20（优化）- 公告已读随更新失效 & 全屏卡片固定尺寸

### 变更类型
优化

### 变更内容
- `frontend/src/utils/announcementRead.js`：已读记录由 `readIds` 改为 `readMap`（`{公告id: 已读时的 updated_at}`）。判定「已读」需 id 存在且 `updated_at` 一致；后端更新公告后 `updated_at` 变化即视为未读、忽略前端已读缓存重新展示。仍保留与 JWT `exp` 绑定（token 过期/重新登录失效）。`markRead` 改为接收整条公告项。
- `frontend/src/components/AnnouncementCard.vue`：
  - `markRead` 调用改为传整条公告（含 `updated_at`）。
  - 全屏明细卡片改为**固定尺寸**（高度 70vh，不随内容多少变化）；标题+内容置于 `<scroll-view scroll-y>`，标题多行完整显示（不再单行截断），内容超出时卡片内右侧滚动。

### 影响范围
- 仅影响首页公告卡片展示逻辑与全屏样式；后端接口已返回 `updated_at`，无需改动。

---

## 2026-07-20（修复）- 首页公告卡片不显示

### 变更类型
修复（bug）

### 变更内容
- `frontend/src/utils/announcementRead.js`：该文件此前内容为空（写入失败），导致 `AnnouncementCard.vue` 中 `import { getUnread, markRead }` 导入到的导出不存在，组件渲染崩溃，首页临时公告卡片始终不出现（管理员/普通用户均受影响）。已重写该文件，补全 `getUnread` / `markRead` / `loadReadIds` 实现，已读状态以本地存储 `{exp, readIds}` 绑定当前 JWT 的 `exp`（token 过期/重新登录即失效）。
- JWT 解析兼容微信小程序（无 `atob` 全局函数）：优先 `atob`，回退 `uni.base64ToArrayBuffer`。

### 影响范围
- 仅修复首页公告卡片渲染；不影响其它页面与后端接口。

---

## 2026-07-20 - 首页公告轮播展示功能

### 变更类型
新增功能

### 变更内容

**一、后端：新增面向普通用户的"最近7天公告"接口**
- `backend/app/api/v1/announcements.py`：新增 `GET /api/v1/announcements/recent`，由 `get_current_user_id`（普通用户 JWT）守卫，返回最近 7 天公告、按 `created_at` 倒序；原有管理员接口保持不变。
- `backend/app/services/announcement_service.py`：新增 `list_recent(days=7)`，用 `func.date_sub(func.now(), INTERVAL N DAY)` 过滤 `created_at >= now-N天` 并倒序（修正了 SQLAlchemy 将 `timedelta` 误当绑定参数的问题，已用一次性脚本直连数据库验证过滤与倒序正确）。

**二、前端：首页公告临时卡片**
- `frontend/src/api/modules/announcement.js`：新增 `getRecentAnnouncements()`。
- `frontend/src/utils/announcementRead.js`（新增）：已读持久化，本地存储 `{token, readIds}`，已读绑定当前登录 token；token 过期/重新登录即失效，未读公告重新展示。
- `frontend/src/components/AnnouncementCard.vue`（新增）：接收最近 7 天公告，用 `announcementRead` 过滤未读后逐条轮播（每条 10 秒，最新优先），`flex:1` 填充首页空白高度保持一屏无滚动；倒计时常驻右上角；点击卡片暂停倒计时并满屏展示，再次点击缩回继续。
- `frontend/src/pages/index/index.vue`：引入 `AnnouncementCard`，`onShow` 并行加载 `loadRecentAnnouncements()`，在任务卡片与打卡按钮之间插入公告卡片。

**三、验证**
- 后端启动后，未登录访问 `GET /recent` 返回 401（鉴权生效）；路由已注册于 OpenAPI。
- 一次性脚本直连数据库：插入 1 天前/10 天前两条公告，`list_recent` 正确仅返回近 7 天条目且倒序；测试数据已清理。

### 影响范围
- 仅新增首页公告展示能力，不影响现有管理员公告管理与其它页面逻辑。

---

## 2026-07-19 - 建库脚本与项目文档同步修正

### 变更类型
修复 / 文档同步

### 变更内容

**一、建库脚本同步为最新结构（backend/sql/）**
- `create_user_db.sql`：users 表新增 `role` 列（TINYINT 默认 0，0-普通用户/7-管理员），新部署用户库即含角色字段，无需再跑 `add_role_to_users.sql`
- `create_yao_db.sql`：末尾新增 `announcements` 全站公告表（id/title/content/created_at/updated_at），新部署业务库即含该表，无需再跑 `add_announcements.sql`
- `create_yao_db.sql`：修正 `notification_logs.trigger_type` 注释为现网实际生效规则 `0-准时，1-超10分钟，2-1小时或中点催办`（原误写为 0-准时/1-超5分钟/2-超30分钟/3-超1小时）

**二、AGENTS.md 迁移规则修正**
- 第 1 节与第 6.3 节：将"变更通过 Alembic 迁移脚本管理 / 使用 Alembic（backend/migrations/）"改为项目实际生效规则——表结构由 `backend/sql/` 下原始 SQL 文件（`create_user_db.sql` / `create_yao_db.sql` 建库，`add_*.sql` 增量演进）管理，**未使用 Alembic**

### 影响范围
- 新建环境的建库脚本无需再依赖增量脚本即可得到最新表结构
- 仅文档与建库脚本描述修正，不影响线上运行结构与数据

---

## 2026-07-19 - 管理员角色与全站公告管理功能

### 变更类型
新增

### 变更内容

**一、数据库增量迁移（backend/sql/）**
- `add_role_to_users.sql`：在用户库 wuzuniao_yonghu.users 表新增 `role` 字段（SMALLINT，默认 0-普通用户，7-管理员），仅做身份标记，不建索引
- `add_announcements.sql`：在业务库 wuzuniao_yao 新建 `announcements` 公告表（id/title/content/created_at/updated_at），每条公告一行，用户侧投递后续实现

**二、后端：角色与权限**
- `core/security.py`：`generate_token(user_id, role=0)` 的 JWT payload 增加 `role` 声明（0-普通用户，7-管理员）
- `core/deps.py`：新增 `get_current_admin` 依赖，解析 JWT 并校验 `role==7`，非管理员返回 403
- `models/user.py`：User 模型增加 `role` 列（默认 0）
- `api/v1/users.py`：登录/注册/微信登录 `_user_payload` 与 `GET /info` 响应新增 `role` 字段

**三、后端：公告模块（仅管理员，均 `Depends(get_current_admin)` 守卫）**
- `schemas/announcement.py`：AnnouncementCreate / AnnouncementUpdate（标题+内容，sanitize 消毒并校验非空，标题上限 200、内容上限 5000）
- `models/announcement.py`：Announcement ORM 模型（落 wuzuniao_yao）
- `services/announcement_service.py`：publish（单行插入）/ list_all（created_at 倒序）/ update(id) / delete(id)
- `api/v1/announcements.py`：GET/POST /announcements、PUT/DELETE /announcements/{id}，统一返回 `{code,msg,data}`
- `api/v1/__init__.py`：注册 announcements 路由（prefix=/announcements）

**四、前端：公告管理入口与页面**
- `store/modules/user.js`：`setUser` 与 `/info` 响应持久化 `role`（0/7）
- `pages/index/settings.vue`：在「制定计划」之前新增独立的「管理」分组（`v-if="isAdmin"`，仅 role==7 显示），含「公告管理」link-card 跳转
- `pages/user/announcement.vue`（新增分包页）：「发布公告」入口卡点击后展开标题+内容表单（取消/提交）；已有公告卡片列表从数据库加载，点击卡片就地展开编辑（取消/更新），右上角红色垃圾桶二次确认删除；onLoad 校验 role==7 否则提示无权限并退回；样式交互对齐 plan.vue（announcement-page__ 前缀 / BEM / 宽屏断点）
- `api/modules/announcement.js`（新增）：getAnnouncements / publishAnnouncement / updateAnnouncement / deleteAnnouncement
- `pages.json`：用户分包注册 announcement 页

### 影响范围
- 用户库 users 表结构（新增 role 列，默认普通用户，历史数据自动为普通用户）
- 业务库新增 announcements 表
- 所有登录/info 接口响应新增 role 字段（前端需同步解析，否则不影响既有逻辑）
- 仅 role==7 管理员可见「公告管理」入口并拥有公告增删改权限；普通用户无入口、接口返回 403

---

## 2026-07-19 - 修复折叠屏切换时页面左右出现白边的问题

### 变更类型
修复

### 变更内容

**一、全局样式：让页面根元素强制撑满视口并设置浅绿色背景**
- 文件：`frontend/src/assets/styles/global.scss`
- 在 `page` 选择器中新增：
  - `width: 100%`：确保页面根元素宽度始终占满屏幕，避免折叠屏/平板切换后宽度计算异常
  - `min-height: 100vh`：确保页面根元素至少撑满整个视口高度
  - `background-color: var(--page-bg-color)`：将页面根元素背景色统一设为设计稿浅绿色 `#e8ebe6`
- 原 `page` 选择器仅定义 CSS 变量，未设置背景色与宽度，导致浅绿色背景由各页面 `.xxx-page` 容器承载；当折叠屏从直屏外屏切换到内屏时，若该容器宽度未重新撑满，窗口默认白色背景会从左右露出，形成白边

**二、页面配置：统一窗口背景色为浅绿色**
- 文件：`frontend/src/pages.json`
- 移除所有页面 `style` 中的 `"backgroundColor": "#FFFFFF"` 配置
- 将 `globalStyle.backgroundColor` 由 `"#F8F8F8"` 改为 `"#e8ebe6"`
- 这样在页面切换、重绘或内容容器尚未完全撑满时，窗口默认背景也保持浅绿色，杜绝白边闪现

### 影响范围
- 所有微信小程序页面（首页、记录、设置、通知方式、制定计划、用户分包全部页面）
- 折叠屏/平板等宽屏设备在横竖屏切换、外屏内屏切换时的背景表现

---

## 2026-07-15 - 删除账号时级联清理所有关联业务数据

### 变更类型
优化

### 变更内容

**一、后端：扩展 `purge_expired_deletions` 的级联删除范围**
- 文件：`backend/app/services/user_service.py`
- 原逻辑：账号到期删除时仅清理 `user_miniapp_accounts`（小程序绑定）和 `users`（用户主记录），业务库的打卡计划、通知渠道、打卡记录、通知记录等关联数据成为孤儿数据
- 新逻辑：在删除用户主记录前，按 `user_id` 为过滤条件依次清理以下业务表，避免误删他人数据：
  1. `plan_notification_times`：计划关联的通知时间点（通过该用户的 plan_id 列表过滤）
  2. `plan_notification_channels`：计划-通知渠道关联（通过该用户的 plan_id 列表过滤）
  3. `checkin_records`：打卡记录（user_id 过滤）
  4. `notification_logs`：通知发送记录（user_id 过滤）
  5. `notification_channels`：通知渠道配置（user_id 过滤）
  6. `checkin_plans`：打卡计划主表（user_id 过滤）
  7. `user_miniapp_accounts`：小程序绑定记录（user_id 过滤，位于用户库）
  8. `users`：用户主记录（位于用户库）
- 新增导入：`delete`（sqlalchemy）、`CheckinPlan`/`PlanNotificationTime`/`PlanNotificationChannel`、`CheckinRecord`、`NotificationLog`
- 方法 docstring 同步更新清理顺序说明

**二、删除顺序设计**
- 先查询用户所有 plan_id，再通过 plan_id 删除计划关联表（时间点、渠道关联），最后删除计划主表，避免破坏外键引用
- 业务表（打卡记录、通知记录、通知渠道）均直接以 user_id 过滤，独立于计划删除顺序
- 全部清理完成后统一 `commit`，保持事务原子性

### 影响范围
- 账号到期自动清理逻辑（`User.purge_expired_deletions`）
- 关联表：`plan_notification_times`、`plan_notification_channels`、`checkin_records`、`notification_logs`、`notification_channels`、`checkin_plans`、`user_miniapp_accounts`、`users`
- 前端 `profile.vue` 删除账号流程不变（仍为 24 小时冷静期后由后台任务触发清理）

---

## 2026-07-14 - 站内信未读数量查询触发自动标记已读 & 移除站内信页轮询

### 变更类型
优化

### 变更内容

**一、后端：未读数量查询接口前置自动标记已读**
- 文件：`backend/app/api/v1/notification_logs.py`
- 在 GET `/api/v1/notification-logs/unread-count` 路由中，于 `count_unread(user_id)` 调用前插入 `await service._auto_mark_read_if_checked(user_id)`
- 未读数量查询接口在返回数量前先执行自动标记已读逻辑（扫描未读站内信，匹配区间内已有打卡记录的更新为 status=0），使数量查询同时完成清理
- docstring 同步更新说明"返回前先执行自动标记已读"

**二、前端：NoticeButton 由 onMounted 改为 onShow 触发**
- 文件：`frontend/src/components/NoticeButton.vue`
- 由 `onMounted(() => userStore.loadUnreadCount())` 改为 `onShow(() => userStore.loadUnreadCount(true))`
- 移除 `vue` 的 `onMounted` 导入，新增 `@dcloudio/uni-app` 的 `onShow` 导入
- 含 NoticeButton 的页面（index.vue、record.vue、settings.vue）切换/返回时均会触发未读数量查询，触发后端自动标记已读，NoticeButton 图标随之刷新

**三、前端：站内信页移除 60 秒轮询**
- 文件：`frontend/src/pages/user/messages.vue`
- 移除轮询逻辑：pollUnreadCount 函数、pollTimer 变量、POLL_INTERVAL 常量、setInterval/clearInterval、onHide 定时器清理、getUnreadCount 导入
- onShow 仅保留 `loadMessages(true)`；列表加载返回的 unread_count 仍同步到 store

**四、业务逻辑变更**
1. 自动标记已读时机：从"仅打开站内信页时执行"调整为"每次打开含 NoticeButton 的页面（onShow）时执行"（通过未读数量查询接口触发后端自动标记）
2. 移除站内信页 60 秒轮询，改为每次打开页面时查询未读数量
3. 未读数量查询接口（GET /unread-count）在返回数量前前置执行自动标记已读
4. NoticeButton 图标在每次自动标记已读后随之刷新

### 影响范围
- 站内信未读数量查询（GET /api/v1/notification-logs/unread-count）
- 自动标记已读逻辑触发时机（从站内信页扩展到含 NoticeButton 的所有一级页面）
- NoticeButton 图标切换（index.vue / record.vue / settings.vue）
- 站内信页（messages.vue）数据加载方式（移除轮询，改为 onShow 加载）

---

## 2026-07-11 - 优化 .gitignore 并生成 README.md

### 变更类型
优化 / 新增

### 变更内容

**一、优化 .gitignore**
- 扫描项目中发现以下文件/目录之前未纳入 .gitignore，可能导致敏感信息泄露或仓库膨胀：
  - `.venv/` — Python 虚拟环境（数百 MB）
  - `.coverage` / `htmlcov/` — pytest-cov 生成的覆盖率数据与 HTML 报告
  - `.pytest_cache/` — pytest 缓存目录
  - `.trae/` — Trae IDE 工作目录（rules / specs）
  - `*.tmp` / `*.bak` / `*_output.txt` / `*_out.txt` — 临时文件
  - `*.env.local` — 本地环境覆盖配置
  - `coverage.xml` / `*.cover` — 其他覆盖率格式
  - `.tox/` / `.nox/` — 测试环境目录
- 从 git 缓存中移除了已被跟踪的 `.coverage` 和 `.trae/` 目录（本地文件保留）
- .gitignore 按分类组织：Python / 敏感配置 / 日志 / 测试覆盖率 / Node.js / IDE / 临时文件

**二、生成 README.md**
- 在项目根目录生成标准 README.md，包含以下内容：
  - 项目简介与徽章（License / Python / FastAPI / Vue / Coverage）
  - 功能特性（用户系统 / 打卡计划 / 打卡记录 / 通知系统）
  - 技术栈（前端 / 后端 / 数据库 / 测试 / 部署）
  - 完整项目结构树
  - 快速开始指南（后端 + 前端安装与启动）
  - 环境配置说明（后端 .env / 前端 .env / 数据库）
  - API 文档概览（20+ 接口列表）
  - 测试说明（316 个测试 / 100% 覆盖率 / 测试库隔离）
  - 部署指南（Docker 一键部署 / 容器架构 / 运维命令）
  - 项目文档索引

### 变更原因
用户要求检查项目中的敏感信息与无需 git 控制的文件，优化 .gitignore；并根据 GitHub 标准 README 写法生成项目 README.md。

### 影响范围
- 修改文件：
  - `.gitignore`（新增 .venv/ / .coverage / htmlcov/ / .pytest_cache/ / .trae/ / 临时文件等规则）
  - `更新记录.md`（新增本条变更记录）
  - `目录结构.json`（新增 README.md 条目）
- 新增文件：
  - `README.md`（项目根目录）
- Git 缓存移除（本地文件保留）：
  - `.coverage`
  - `.trae/` 目录（rules + specs）

---

## 2026-07-11 - 测试用户库完全隔离 & 覆盖率提升至 100%

### 变更类型
优化 / 修复

### 变更内容

**一、测试用户库完全隔离**
- 问题：`User` 和 `UserMiniappAccount` 模型通过 `__table_args__ = {"schema": "wuzuniao_yonghu"}` 跨库访问开发库，测试的 `create_all` 和 `truncate` 直接操作开发库用户数据，造成污染
- 解决：在 `tests/integration/backend/conftest.py` 中运行时将 `Base.metadata.tables` 中 schema 为 `wuzuniao_yonghu` 的表重映射到测试库 `wuzuniao_yonghu_test`
- 在 `tests/create_test_db.py` 中新增创建 `wuzuniao_yonghu_test` 数据库
- 验证：测试运行后开发库 `wuzuniao_yonghu.users` 记录数为 0，测试库 `wuzuniao_yonghu_test` 有正确表结构且测试后自动 TRUNCATE 清理

**二、测试覆盖率从 92% 提升至 100%**
- 新增 3 个补充测试文件，共 55 个新测试：
  - `tests/unit/backend/test_coverage_unit.py`（13 个测试）：覆盖 security/crypto/deps 的异常分支（JWT 密钥未配置/过期/无 sub、AES 密钥未配置/长度错误、token sub 非整数等）
  - `tests/integration/backend/test_api_coverage.py`（19 个测试）：覆盖 API 层异常分支（register/send_code RuntimeError、用户不存在 400、微信登录成功/失败、checkin 时间格式 422、read_all 失败等）
  - `tests/integration/backend/test_service_coverage.py`（23 个测试）：覆盖服务层异常分支（verify_code 无记录/过期、各方法用户不存在、bind_email 账号合并、create/update_plan 无渠道/HH:MM:SS 格式、auto_close_expired_plans、commit 失败回滚、wechat_login 用户数据异常等）
- 为不可达的防御性代码添加 `# pragma: no cover`：
  - `backend/app/main.py` lifespan 函数（ASGITransport 测试不触发 lifespan）
  - `backend/app/core/database.py` get_db 函数（测试中通过 dependency_overrides 替代）
  - `backend/app/api/v1/checkins.py` datetime.fromisoformat except 分支（CreateCheckin.validate_actual_time 已拦截）

**三、修复 bind_email 账号合并的 autoflush bug**
- 问题：`bind_email` 合并账号时，查询小程序账号触发 autoflush，此时从账号尚未删除，若主账号复制了从账号的用户名会触发唯一约束冲突（IntegrityError）
- 修复：调整为先转移小程序绑定记录 → 删除从账号并 flush → 再合并字段到主账号，避免 flush 顺序（INSERT→UPDATE→DELETE）导致的约束冲突

**四、测试结果**
- **316 个测试全部通过**（单元 124 + 集成 184 + E2E 8）
- **代码覆盖率 100%**（TOTAL 1410 stmts，0 miss）
- 运行命令：`pytest`（在项目根目录执行）

### 变更原因
用户要求测试时用户库完全隔离（避免污染开发环境），并追加测试覆盖率到 100%。

### 影响范围
- 修改文件：
  - `tests/integration/backend/conftest.py`（新增 schema 重映射与测试用户库创建）
  - `tests/create_test_db.py`（新增创建 wuzuniao_yonghu_test）
  - `backend/app/services/user_service.py`（修复 bind_email autoflush 顺序）
  - `backend/app/main.py`、`backend/app/core/database.py`、`backend/app/api/v1/checkins.py`（添加 pragma: no cover）
- 新增文件：
  - `tests/unit/backend/test_coverage_unit.py`
  - `tests/integration/backend/test_api_coverage.py`
  - `tests/integration/backend/test_service_coverage.py`
- 新增测试数据库：`wuzuniao_yonghu_test`（与开发库 `wuzuniao_yonghu` 完全隔离）
- 已同步更新 `目录结构.json`

---

## 2026-07-11 - 创建项目测试套件：单元/集成/E2E 三层测试体系，覆盖率 92%

### 变更类型
新增

### 变更内容

**一、测试目录结构（项目根目录下新建 `tests/`）**
```
tests/
├── conftest.py                      # 根 conftest，设置 sys.path 与测试环境变量
├── create_test_db.py                # 测试数据库 wuzuniao_yao_test 初始化脚本
├── .coveragerc                      # 覆盖率配置（排除 scheduler_service.py 与 logger.py）
├── unit/
│   └── backend/
│       ├── test_security.py         # Security 类（密码/用户名/验证码/邮箱/JWT 等）35 个测试
│       ├── test_crypto.py           # AES-256-GCM 加解密 8 个测试
│       ├── test_timezone.py         # 时区工具 5 个测试
│       └── test_schemas.py          # 全部 Pydantic Schema 校验 49 个测试
├── integration/
│   └── backend/
│       ├── conftest.py              # 集成测试核心 fixtures（db_session/client/test_user/auth_client 等）
│       ├── test_auth_api.py         # 认证流程（注册/登录/验证码/微信登录）18 个测试
│       ├── test_user_profile_api.py # 用户资料管理（用户名/头像/签名/密码/邮箱/删除冷静期）42 个测试
│       ├── test_plan_api.py         # 计划 CRUD（含 priority/通知时间/渠道关联）20 个测试
│       ├── test_checkin_api.py      # 打卡记录（创建/今日查询/月历/日详情）16 个测试
│       ├── test_notification_api.py # 通知渠道与站内信（邮件渠道增删改/站内信列表/已读标记）26 个测试
│       └── test_service_extended.py # 服务层扩展（微信登录/过期清理/账号合并/邮件服务/自动标记已读）23 个测试
└── e2e/
    ├── conftest.py                  # 复用集成测试 fixtures
    └── test_user_journeys.py        # 8 个完整用户流程：注册到登录、密码重置、计划管理、打卡、通知渠道管理、账号生命周期、邮箱修改、密码修改
```

**二、测试框架与依赖**
- 后端：`pytest 9.1.1`、`pytest-asyncio 1.4.0`（`asyncio_mode = auto`）、`pytest-cov 7.1.0`、`httpx 0.28.1`（`ASGITransport` 不触发 lifespan）
- 测试数据库：独立的 `wuzuniao_yao_test` 库，每个测试后 `TRUNCATE` 全表，与开发库 `wuzuniao_yao` 完全隔离
- 配置文件：
  - `pytest.ini`：注册 `unit`/`integration`/`e2e` 三类 marker，配置 `--cov=app` 与 html 报告
  - `tests/.coveragerc`：排除 `scheduler_service.py`（后台定时任务，无法在测试中触发）与 `logger.py`（日志配置），`exclude_lines` 含 `pragma: no cover` 与 `if __name__ == .__main__.:`
  - `tests/conftest.py`：设置 `DATABASE_URL` 指向测试库、`JWT_SECRET_KEY`、`ENCRYPTION_SECRET_KEY` 测试专用值

**三、测试结果**
- **264 个测试全部通过**（单元 111 + 集成 145 + E2E 8）
- **代码覆盖率 92%**（TOTAL 1421 stmts，111 miss），超过 80% 目标
- 运行命令：`pytest`（在项目根目录执行）

**四、关键技术问题与解决**
1. **asyncmy 连接池跨事件循环失效**（`InternalError: network operation failed due to asyncmy attribute error`）
   - 原因：pytest-asyncio 默认每个测试函数使用独立事件循环，模块级引擎的连接池在第一个测试中创建的连接，后续测试复用时连接已失效
   - 解决：测试专用引擎使用 `poolclass=NullPool` 禁用连接池，每次请求都新建连接，避免跨事件循环的连接复用
2. **表不存在**（`Table 'wuzuniao_yao_test.checkin_records' doesn't exist`）
   - 原因：`Base.metadata.create_all` 只创建已导入模型对应的表
   - 解决：在 `tests/integration/backend/conftest.py` 中显式导入全部模型模块（User/UserMiniappAccount/NotificationChannel/NotificationLog/CheckinPlan/PlanNotificationTime/PlanNotificationChannel/CheckinRecord）
3. **httpx delete() 不支持 json 参数**
   - 解决：`auth_client.delete(url, json=...)` 改为 `auth_client.request("DELETE", url, json=...)`
4. **测试客户端不触发 lifespan**
   - 解决：使用 `httpx.AsyncClient` + `ASGITransport(app=app)`，并通过 `app.dependency_overrides[get_db]` 替换数据库会话，绕过 lifespan 启动的后台调度任务

**五、前端测试说明**
- 前端为 uni-app 微信小程序项目，`package.json` 中无测试框架（vitest/jest）依赖，且小程序运行时依赖微信开发者工具模拟器，无法在 Node 环境直接运行
- 前端所有 API 调用与用户操作流程已通过后端集成测试与 E2E 测试覆盖（API 层请求处理、数据流转、响应正确性、完整用户流程）
- 本次不引入前端测试框架，避免对现有构建流程造成影响

### 变更原因
建立完整的自动化测试体系，覆盖所有 API 接口（注册/登录/用户资料/计划/打卡/通知渠道/站内信）与关键用户操作流程（注册到登录、密码重置、计划管理、打卡、通知渠道管理、账号生命周期、邮箱修改、密码修改），确保后续功能迭代与代码重构时的回归安全性。

### 影响范围
- 新增文件：`pytest.ini`、`tests/` 目录下全部测试文件与配置（共 17 个文件）
- 新增依赖：`pytest`、`pytest-asyncio`、`pytest-cov`、`httpx`（已加入 `backend/requirements.txt`）
- 新增测试数据库：`wuzuniao_yao_test`（结构与 `wuzuniao_yao` 一致）
- 未改动任何现有后端业务代码、API 接口、数据库结构、前端代码
- 已同步更新 `目录结构.json`

### 备注
- AGENTS.md 第 10 节"禁止生成测试代码（除非用户明确要求）"——本次测试套件为用户明确要求创建
- 测试覆盖率 92%，未覆盖部分主要为：`scheduler_service.py`（后台定时任务，需真实时间触发，已排除）、`main.py` 的 lifespan 启动逻辑、部分异常分支
- 测试运行：在项目根目录执行 `pytest`，或分类执行 `pytest -m unit` / `pytest -m integration` / `pytest -m e2e`

---

## 2026-07-11 - 全项目代码审查与清理：变量清理、死代码清除、安全漏洞修复

### 变更类型
优化/修复

### 变更内容

**一、变量清理（未使用导入移除）**
1. `backend/app/services/plan_service.py`：移除未使用导入 `datetime`、`date`、`Any`
2. `backend/app/services/checkin_service.py`：移除未使用导入 `func as sa_func`
3. `backend/app/services/notification_channel_service.py`：移除未使用导入 `datetime`、`delete`
4. `backend/app/api/v1/notification_channels.py`：移除未使用导入 `CHANNEL_TYPE_ZNX`

**二、死代码清除**
1. `backend/app/schemas/user.py`：删除未被任何模块引用的 `UserBase`、`UserCreate`、`UserResponse` 三个类及其专属导入 `datetime`
2. `backend/app/schemas/checkin_record.py`：删除未被任何模块引用的 `CheckinOut` 类
3. `frontend/src/components/NoticeButton.vue`：删除已废弃的 `hasNotification` prop 定义
4. `frontend/src/pages/index/index.vue`：删除死变量 `hasNotification` 及模板绑定 `:has-notification`
5. `frontend/src/pages/index/record.vue`：同上
6. `frontend/src/pages/index/settings.vue`：同上

**三、安全漏洞修复**
1. `backend/app/main.py`：CORS 配置 `allow_credentials=True` 改为 `False`（与 `allow_origins=["*"]` 组合存在跨域凭据泄露风险，且项目使用 JWT 无需凭据）
2. `backend/app/core/database.py`：SQLAlchemy `echo=True` 改为 `False`（生产环境会泄露 SQL 语句与参数到日志，含密码哈希、邮箱等敏感数据）

**四、过时注释修复**
1. `backend/app/models/user.py`：status 字段注释 "1分钟后清理" 更正为 "24小时后清理"（与实际业务逻辑一致）
2. `backend/app/models/notification_log.py`：trigger_type 注释由 "0-准时/1-超5分钟/2-超30分钟/3-超1小时" 更正为 "0-准时/1-超10分钟/2-1小时或中点催办"（与 schemas/notification_log.py 常量定义一致）
3. `backend/app/models/checkin_record.py`：注释由 "只能打卡一次" 更正为 "允许重复打卡"（与 checkin_service.py 实现一致）

### 变更原因
全面清理项目中的冗余变量、死代码和安全风险，提升代码可维护性与安全性。所有修改均为手术式精确修改，不触及业务逻辑。

### 影响范围
- 后端：6 个文件移除未使用导入，2 个 schema 文件删除死代码类，3 个 model 文件修正注释，1 个安全配置修复（CORS），1 个数据库配置修复（echo）
- 前端：1 个组件删除废弃 prop，3 个页面删除死变量及模板绑定
- 未改动任何业务逻辑、API 接口、数据库结构
- 验证结果：Python 编译检查通过，后端 `from app.main import app` 导入验证通过，前端 `npm run build:mp-weixin` 构建成功

### 备注
项目当前无单元测试/集成测试/E2E 测试文件（仅 `scripts/test_db_connection.py` 数据库连接测试脚本），无法执行自动化测试。已通过编译检查、导入验证、前端构建三重验证确认清理操作未引入新问题。

---

## 2026-07-11 - 优化验证码按钮：发送中增加中间态提示

### 变更类型
优化

### 变更内容
**前端三个使用验证码的页面统一增加"发送中..."中间态：**
1. `frontend/src/pages/user/register.vue`
   - 新增 `isSending` 响应式状态
   - 点击「获取验证码」后立即将按钮文字改为 `发送中...`，并增加 `register-page__code-btn--disabled` 禁用样式（opacity: 0.6）
   - 请求完成后（成功或失败）再切换为 `60s` 倒计时或恢复 `获取验证码`
   - `handleGetCode` 增加 `isSending` 拦截，避免重复点击
2. `frontend/src/pages/user/forgot-password.vue`
   - 与 register.vue 一致的实现：新增 `isSending`、按钮文字 `发送中...`、禁用态样式
3. `frontend/src/pages/user/profile.vue`
   - 旧邮箱验证码与新邮箱验证码分别新增 `emailOldCodeSending`、`emailNewCodeSending` 状态
   - 两个按钮均增加 `profile-page__code-btn--disabled` 禁用样式与 `发送中...` 提示

### 变更原因
提升用户体验：原流程中点击「获取验证码」后按钮文字仍为「获取验证码」，直到请求成功才跳变为「60s」，中间存在无反馈空窗期。增加「发送中...」中间态后，用户能感知到操作已被响应，视觉过渡更连续。

### 影响范围
- 前端 `frontend/src/pages/user/register.vue`：验证码按钮交互与样式
- 前端 `frontend/src/pages/user/forgot-password.vue`：验证码按钮交互与样式
- 前端 `frontend/src/pages/user/profile.vue`：旧邮箱/新邮箱两个验证码按钮交互与样式
- 未改动后端代码、API 接口与业务逻辑

---

## 2026-07-11 - 修复获取验证码邮件发送失败未反馈用户的问题

### 变更类型
修复

### 变更内容
**后端 `backend/app/services/user_service.py`** — 修改 `send_code_for_purpose` 方法的邮件发送方式：
1. 将 `asyncio.create_task(self._send_email_async(email, code))`（火后即忘）改为 `await asyncio.to_thread(Email().send_verification_code, email, code)`（同步等待）
2. 删除 `_send_email_async` 方法（该方法捕获所有异常仅记日志，导致错误被吞掉）
3. 邮件发送失败时 `RuntimeError` 向上传播，由 API 层（`users.py`）捕获并返回 500，前端显示错误提示

### 变更原因
修复 Bug：`send_code_for_purpose` 使用 `asyncio.create_task` 做"火后即忘"发送，API 立即返回"验证码已发送"，但实际 SMTP 发送失败时（日志记录 14:31:33 连接超时 `Connection unexpectedly closed: The read operation timed out`），错误被 `_send_email_async` 吞掉仅记日志，用户看到"验证码已发送"却永远收不到邮件，且 60 秒倒计时已启动无法重试。改为同步等待后，失败时 API 返回 500，前端提示错误信息，用户可立即重试。

### 影响范围
- 后端 `backend/app/services/user_service.py`：`send_code_for_purpose` 方法（约 L65-L80），删除 `_send_email_async` 方法
- 影响所有调用 `send_code_for_purpose` 的接口：注册验证码、密码找回验证码、修改邮箱旧邮箱验证码、修改邮箱新邮箱验证码
- 未改动 `email_service.py`、前端代码、API 路由层

---

## 2026-07-11 - 修复首页打卡状态机判定优先级：打卡记录检查优先于 waiting 时间窗口

### 变更类型
修复

### 变更内容
**前端 `frontend/src/pages/index/index.vue`** — 修改 `checkinState` 计算属性的判定优先级顺序：
1. 将 `isIntervalChecked`（匹配区间打卡记录检查）提前到 `nowMinutes < t_1 - 120`（waiting 时间窗口判断）之前，确保匹配区间内已有打卡记录时优先返回 `done`
2. 将原两处分散的 `forceActive` 分支合并为顶部统一判断（forceActive 优先级最高，允许从 done/waiting 重新打卡）
3. 更新 `checkinState` 上方注释，标注新的优先级顺序：disabled → forceActive → isIntervalChecked(done) → waiting → active

### 变更原因
修复 Bug：用户在"未到打卡时间"时段长按重置后强制打卡成功，打卡记录已写入数据库，但因 `checkinState` 先判定 `nowMinutes < t_1 - 120` 直接返回 `waiting`，未检查匹配区间内是否已有打卡记录，导致按钮仍显示"未到打卡时间"而非"已打卡"。

### 影响范围
- 前端 `frontend/src/pages/index/index.vue`：仅修改 `checkinState` 计算属性（约 L240-L277）及上方注释
- 未改动防抖、onHide 定时器清除、长按重置、任务卡片交互等已实现功能
- spec 文档 `.trae/specs/clarify-checkin-logic/` 三件套已整理为实施导向（移除旧 spec 对比与附录）

---

## 2026-07-11 - 重构打卡记录页显示逻辑：合并多次打卡、移除孤儿记录、无计划隐藏明细

### 变更类型
重构

### 变更内容
**后端 `backend/app/services/checkin_service.py`** — 修改 `list_day_detail` 方法：
1. 简化记录查询：将 outer join（CheckinRecord + CheckinPlan + PlanNotificationTime）替换为仅查询 CheckinRecord
2. 合并同一提醒时间的多条打卡记录为一行：返回 `first_actual_time`（首次打卡时间）、`last_actual_time`（末次打卡时间）、`checkin_count`（打卡次数），替代原每条记录单独占一行的展示方式
3. 移除 `plan_id` 和 `actual_time` 字段：新的明细行结构仅包含 plan_name/plan_remark/notification_time/checked/first_actual_time/last_actual_time/checkin_count
4. 移除孤儿记录展示逻辑：删除 `matched_record_ids` 集合及整个 3b 分支，打卡记录必须依附于提醒时间显示，无对应提醒时间的记录不展示
5. 排序简化：最终明细仅按 `notification_time` 升序排序，移除原有的 `checked` 二级排序

**前端 `frontend/src/pages/index/record.vue`** — 修改显示逻辑：
1. `selectedDayDetail` 计算属性更新：无计划时（dayRecords 为空）隐藏整张明细卡片，替代原"当天暂无打卡记录"空状态
2. 移除空状态 HTML 块及对应 CSS（`.record-page__list-empty`、`.record-page__list-empty-text`）
3. 明细行模板更新：使用 `first_actual_time`/`last_actual_time` 显示首末打卡时间，`checkin_count > 2` 时显示"共 N 次"
4. 新增 `.record-page__list-item-count` CSS 样式（含平板断点）
5. 更新 JSDoc 注释

### 影响范围
- 后端 `backend/app/services/checkin_service.py`：仅修改 `list_day_detail` 方法（约 148-241 行），同步更新其 docstring
- 前端 `frontend/src/pages/index/record.vue`：修改模板、计算属性、CSS 样式、JSDoc 注释
- 未修改 `_get_match_intervals`、`list_by_month`、小绿点标注逻辑（保持基于 `checkin_records` 表）

---

## 2026-07-11 - 前端站内信页新增"全部已读"按钮与轮询逻辑优化

### 变更类型
优化

### 变更内容
修改 `frontend/src/pages/user/messages.vue`：
1. 列表展示调整：移除 `handleCardClick` 中对 `item.status` 的直接引用（后端不再返回 status 字段），统一用 `is_unread` 判定未读状态
2. 新增"全部已读"按钮（PageHeader 下方，右对齐文本按钮，品牌色边框）：
   - 显示条件：列表中存在 `is_unread=true` 的卡片且非加载中/分页加载中
   - 点击调用 `markAllMessagesRead()`，成功后将所有卡片 `is_unread` 置为 false（卡片保留）、`setUnreadCount(0)`、显示成功提示
   - 失败时显示错误提示，列表与未读数量保持原状
   - 按钮防抖：`markingAll` ref 控制，点击后立即置灰避免重复点击
3. 轮询逻辑调整：
   - 轮询间隔由 30 秒改为 60 秒
   - 未读数量减少时也重新加载列表（`loadMessages(true)`），保持列表与未读数量一致性（原逻辑仅同步数量不刷新列表）

### 影响范围
- 前端 `frontend/src/pages/user/messages.vue`：新增 handleMarkAllRead 函数、hasUnread computed、markingAll ref、全部已读按钮及样式；修改 pollUnreadCount、handleCardClick、POLL_INTERVAL
- 新增导入 `markAllMessagesRead`（来自 `api/modules/message.js`，该函数已由后端任务预先添加）

---

## 2026-07-11 - 站内信列表增加自动标记已读逻辑

### 变更类型
优化

### 变更内容
修改 `backend/app/services/notification_log_service.py` 的 `list_znx_by_user` 方法：
1. 新增 `_auto_mark_read_if_checked` 私有方法：在返回站内信列表前，扫描该用户所有 status=2（未读）的站内信记录，对每条未读记录判断对应提醒时间匹配区间内是否有打卡记录（复用 `CheckinService._get_match_intervals` 计算匹配区间），若有打卡记录则自动将 status 更新为 0（已读）
2. 边界处理：plan_time_id 为 NULL / 计划已删除 / notify_date 为 NULL / 无打卡记录 → 保持未读状态
3. 精简返回字段：items 每条仅包含 id/plan_name/plan_remark/send_time/is_unread，移除 plan_id/channel_id/status
4. unread_count 反映自动标记已读后的真实未读数量（count_unread 在自动标记之后执行）

### 影响范围
- 后端 `notification_log_service.py`：新增 _auto_mark_read_if_checked 方法，修改 list_znx_by_user 方法

---

## 2026-07-11 - 新增"全部已读"站内信接口

### 变更类型
新增

### 变更内容
新增后端"全部已读"API，支持用户一键将所有未读站内信标记为已读：
1. `backend/app/services/notification_log_service.py`：`NotificationLogService` 新增 `mark_all_as_read(user_id)` 方法
   - 使用子查询过滤所有 `channel_type='站内信'` 的渠道ID
   - 通过 SQLAlchemy `update()` 批量更新该用户 `status=2` 的记录为 `status=0`
   - 返回更新条数 `updated_count`；无未读记录时返回 0，不报错
   - 使用 `synchronize_session=False` 避免批量更新时的会话同步开销
2. `backend/app/api/v1/notification_logs.py`：新增 PUT `/api/v1/notification-logs/read-all` 路由
   - 通过 `Depends(get_current_user_id)` 从 JWT 获取 user_id，无请求体
   - 响应：`{ code: 0, msg: "已全部标记为已读", data: { updated_count: <int> } }`
   - 不修改现有 /list、/read、/unread-count 接口

### 影响范围
站内信通知模块（后端 service + 路由层）

---

## 2026-07-11 - 修复自动重启后邮件仍解密失败（uvicorn reload 多进程残留）

### 问题现象
`restart_backend()` 实现自动重启后，重新运行脚本，通知邮件仍提示解密失败。

### 根因分析
`run.py` 使用 `uvicorn.run(..., reload=True)`，reload 模式会启动**主进程 + 子进程**：
- 主进程（reloader）：监控文件变化
- 子进程（worker）：实际运行 FastAPI 应用，继承主进程的 socket

脚本的 `taskkill /F /PID` 只杀主进程，**子进程残留并继续占用 8000 端口**，且使用旧密钥。新启动的 uvicorn 进程与旧子进程冲突，请求可能被路由到旧子进程导致解密失败。

### 修复内容
[rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py) 的 `restart_backend()` 函数：
1. **杀进程改用 `taskkill /T /F /PID`**（`/T` 终止整个进程树，包括所有子进程）
2. **Linux 端改用 `os.killpg`** 杀掉整个进程组
3. **启动命令改用 `python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`**（非 reload 单进程模式，不再使用 `run.py`，避免子进程残留问题）
4. 端口释放等待时间从 1 秒增加到 2 秒
5. 健康检查轮询从 15 秒增加到 20 秒

### 验证结果
- 清理旧进程后运行脚本：密钥轮换成功（`uSw6xqz1...` → `eEvCCRSK...`）
- `netstat` 确认 8000 端口仅被**单个进程** PID=19860 监听（非 reload 模式，无子进程）
- `wmic` 确认只有一个 python 进程：`python -m uvicorn app.main:app`
- `/health` 接口返回 200
- 直接调用 `Email.send_notification` 发送测试邮件成功：`bbs.wuzuniao@qq.com -> bbs.wuzuniao@qq.com`

---

## 2026-07-11 - 优化密钥轮换脚本：自动重启后端服务

### 问题现象
之前在 [rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py) 中增加了步骤6（`os.environ` + `Settings()` 重载）试图让 Python 进程识别新密钥，但未生效。

### 根因分析
- `os.environ["ENCRYPTION_SECRET_KEY"] = new_key` 和 `new_settings = Settings()` 只影响脚本自身进程
- 运行中的后端 uvicorn 服务是独立进程，pydantic-settings 的 `settings` 实例在启动时已固化，无法跨进程动态刷新
- `uvicorn --reload` 仅监控 `.py` 文件变化，`.env` 变更不触发重启
- 结论：无法动态加载，必须重启后端进程

### 修复内容
1. **移除无效的步骤6**（`os.environ` + `Settings()` 重载代码），导入中去掉 `Settings` 类
2. **新增 `restart_backend()` 函数**实现自动重启：
   - Docker 环境（检测 `/.dockerenv`）：提示从宿主机执行 `docker restart <container>`
   - 开发环境（Windows）：`netstat -ano` 查找占用 8000 端口的进程 PID，`taskkill /F /PID` 终止
   - 开发环境（Linux）：`lsof -t -i :8000` 查找 PID，`os.kill(SIGTERM)` 终止
   - 用 `subprocess.Popen` 启动新 uvicorn 进程（`DETACHED_PROCESS` 与当前脚本解耦）
   - stdout/stderr 重定向到 `backend/logs/uvicorn.log`
   - 轮询 `http://localhost:8000/health` 验证新进程启动成功（最多 15 秒）
3. **所有更新 .env 的分支**（无渠道、所有 password 为空、正常轮换成功）均调用 `restart_backend()`
4. **替换原文本提醒**为自动重启调用

### 验证结果
执行 `python backend/sql/rotate_encryption_key.py`：
- 密钥轮换成功（旧密钥 `rxLmClba...` → 新密钥 `mbdhFp2L...`）
- 数据库事务提交成功（1 条记录更新）
- .env 文件已更新
- 新密钥解密验证通过
- 自动重启后端服务：`netstat` 确认 8000 端口被新进程 PID=32624 监听，脚本内 `/health` 轮询返回 200

---

## 2026-07-11 - 修复密钥轮换后邮件发送失败（后端未重启加载新密钥）

### 问题现象
密钥轮换脚本执行后，`notification_logs` 中邮件渠道记录 status=1 失败，error_msg 为"密码解密失败: 解密失败"（id=40 09:17:42、id=42 09:23:22）。

### 根因分析
轮换脚本更新了 `.env` 的 `ENCRYPTION_SECRET_KEY` 和数据库中用新密钥重新加密的 password，但**后端 uvicorn 服务未重启**。`uvicorn --reload` 仅监控 `.py` 文件变化，`.env` 修改不会触发自动重启。后端进程仍在用旧密钥，尝试解密新密钥加密的 password 时抛出 `InvalidTag` 解密失败。

**密码明文未被变更**：脚本逻辑（解密→明文→重新加密）正确，AES-256-GCM 带认证标签，密钥不匹配会抛异常。问题纯粹是后端未加载新密钥。

### 修复内容
1. **重启后端服务**：停止旧进程，重新启动 `uvicorn app.main:app --reload`，加载当前 `.env` 中的新密钥
2. **脚本增加后端重启提醒**：[rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py#L278-L282) 在轮换成功输出中增加醒目提醒：
   ```
   【重要提醒】请立即重启后端服务以加载新密钥！
     uvicorn --reload 仅监控 .py 文件，不会因 .env 变更自动重启。
     若不重启，后端仍使用旧密钥，邮件发送将因密码解密失败而报错。
     重启命令：uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

### 验证结果
- 当前 `.env` 密钥（前缀 `rxLmClba...`）能正确解密数据库渠道 3 的 password（授权码 `xknncimmzaswdica`）
- 用当前密钥调用 `Email.send_notification` 实际发送测试邮件成功：
  ```
  打卡通知邮件发送成功：bbs.wuzuniao@qq.com -> bbs.wuzuniao@qq.com
  ```

### 影响范围
- 代码：`backend/sql/rotate_encryption_key.py` 末尾增加后端重启提醒
- 后端服务已重启，邮件通知功能恢复正常
- `notification_logs` 后续记录将恢复正常（status=0）

---

## 2026-07-11 - 密钥轮换脚本增加配置重载与解密验证步骤

### 改动内容
[backend/sql/rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py) 在 `.env` 更新成功后新增两步：

**步骤6 - 重新加载配置**（L242-L253）：
- 更新 `os.environ["ENCRYPTION_SECRET_KEY"]` 为新密钥
- 重新实例化 `Settings()`，验证 `new_settings.ENCRYPTION_SECRET_KEY == new_key`
- 解决 pydantic-settings 的 Settings 实例在启动时缓存、`.env` 更新后不自动刷新的问题

**步骤7 - 新密钥解密验证**（L255-L269）：
- 用新密钥解密 `updates` 中每条记录更新后的 password
- 验证解密成功，确保轮换结果正确

同时修改 L46 导入：`from app.core.config import settings` → `from app.core.config import Settings, settings`

### 验证结果
```
.env 文件已更新
配置已重新加载，新密钥已在当前进程生效
  渠道 3：新密钥解密验证通过（明文长度 16）
轮换成功完成
```

### 影响范围
- 代码：`backend/sql/rotate_encryption_key.py` 新增步骤6、7，修改导入
- 后端服务已重启加载新密钥，邮件通知功能正常

---

## 2026-07-11 - 执行加密密钥轮换脚本并验证邮件功能

### 执行内容
运行 [backend/sql/rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py) 进行邮箱密码加密密钥轮换：
- 旧密钥前缀: `HcX97hX/...`
- 新密钥前缀: `OUFx0tmm...`
- 渠道 3（QQ 邮箱）的 password 已用新密钥重新加密
- 事务提交成功，`.env` 文件中 `ENCRYPTION_SECRET_KEY` 已原子替换

### 脚本逻辑确认
- `conn.cursor()` 为同步方法（无需 await），`cur.execute()` / `cur.fetchall()` 为协程（需 await），脚本用法正确
- AES-256-GCM 解密→明文→重新加密流程安全，密钥不匹配会抛 `InvalidTag` 异常并回滚
- 脚本执行无报错，退出码 0

### 后端重启
密钥轮换后 `.env` 变更不会触发 uvicorn `--reload`（仅监控 .py 文件），需手动重启后端以加载新密钥。已重启后端服务，定时任务调度正常启动。

### 验证结果
- `.env` 新密钥能正确解密数据库中渠道 3 的 password（16 位授权码）
- `notification_logs` 最新记录（id=38）status=0（成功），error_msg=NULL，send_time=2026-07-11 09:07:54
- 之前的失败记录（id=26/30/32/34）均为旧授权码导致的 "Connection unexpectedly closed"，已在前序任务中修复

### 影响范围
- 数据库：`notification_channels` 表 channel_id=3 的 channel_value.password 已用新密钥重新加密
- 配置：`backend/.env` 的 `ENCRYPTION_SECRET_KEY` 已更新
- 后端服务已重启，邮件通知功能正常

---

## 2026-07-11 - 修复更新计划接口 422 校验失败（UpdatePlan 多余 plan_id 字段）

### 背景
从 [plan.vue](file:///d:/wuzuniao/yao/frontend/src/pages/index/plan.vue#L374) "更新计划"入口提交时，前端 toast 提示英文错误信息（用户描述为 "Fail request"）。

### 根因
[UpdatePlan](file:///d:/wuzuniao/yao/backend/app/schemas/plan.py#L82-L90) schema 继承 `CreatePlan` 后额外定义了 `plan_id: int` 必填字段及对应 validator。但 `plan_id` 已作为 URL 路径参数由路由函数 `update_plan(plan_id: int, payload: UpdatePlan, ...)` 接收，前端 [plan.js](file:///d:/wuzuniao/yao/frontend/src/api/modules/plan.js#L57-L62) 的 `updatePlan` 不会在请求体中重复发送 `plan_id`。

Pydantic 校验请求体时发现 `plan_id` 缺失，返回 422：
```json
{"detail":[{"type":"missing","loc":["body","plan_id"],"msg":"Field required",...}]}
```
前端 [request.js](file:///d:/wuzuniao/yao/frontend/src/api/request.js#L87-L89) 解析 detail 数组取 `e.msg`，toast 显示 "Field required"。

### 修复
[backend/app/schemas/plan.py](file:///d:/wuzuniao/yao/backend/app/schemas/plan.py#L82-L83)：移除 `UpdatePlan` 中的 `plan_id: int` 字段与 `validate_plan_id` validator。`UpdatePlan` 仅复用 `CreatePlan` 的全部字段校验，`plan_id` 由 URL 路径参数提供，service 层 `plan_service.update_plan(plan_id=plan_id, ...)` 不受影响。

### 验证
- 修复前（body 不含 plan_id）：HTTP 422 `{"msg":"Field required"}`
- 修复后（body 不含 plan_id，使用真实 channel_ids）：HTTP 200 `{"code":0,"msg":"计划更新成功"}`

### 影响范围
- 代码：`backend/app/schemas/plan.py`（UpdatePlan 类）
- 功能：plan.vue 更新计划操作恢复正常

---

## 2026-07-11 - 修复邮件通知发送失败（更新 QQ 邮箱授权码）

### 背景
数据库 `notification_logs` 中邮件渠道（channel_id=3）的发送记录全部失败，error_msg 均为"邮件发送失败：Connection unexpectedly closed"。经诊断，smtplib `login()` 先尝试 AUTH PLAIN，QQ 邮箱返回 535 认证失败后直接关闭连接，smtplib 继续尝试 AUTH LOGIN 时连接已断，最终抛出 `SMTPServerDisconnected`，掩盖了真实的 535 错误。

### 诊断过程
1. 读取 channel_id=3 配置：smtp.qq.com:465 SSL，发件邮箱 bbs.wuzuniao@qq.com
2. 验证 AES 密码解密正常（当前 .env 的 ENCRYPTION_SECRET_KEY 与数据库密文匹配）
3. 开启 smtplib debug 模式，捕获到 QQ 邮箱原始响应：`535 Login fail. Account is abnormal, service is not open, password is incorrect...`
4. 确认根因：旧授权码 `mamwryhlxlfeubji` 已失效（QQ 邮箱重新生成授权码后旧的立即失效）

### 修复内容
- 用新授权码 `xknncimmzaswdica`（用户在 QQ 邮箱重新生成）替换 channel_id=3 的 password
- 使用项目 `app.utils.crypto.encrypt()` 加密后写入 `notification_channels.channel_value`
- 全链路验证：解密匹配 → SMTP_SSL 登录成功（235 Authentication successful）→ 实际发送测试邮件成功

### 代码改进：SMTP 认证错误信息准确性
**问题**：`email_service.py` 原用 `server.login()` 依次尝试 AUTH PLAIN → AUTH LOGIN，QQ 邮箱在 AUTH PLAIN 返回 535 后直接关闭连接，导致 smtplib 继续尝试 AUTH LOGIN 时抛出 `SMTPServerDisconnected: Connection unexpectedly closed`，掩盖真实的 535 认证失败信息，使 `notification_logs.error_msg` 记录的误导性错误无法定位根因。

**改进**（[backend/app/services/email_service.py](file:///d:/wuzuniao/yao/backend/app/services/email_service.py)）：
- 新增 `_smtp_authenticate` 静态方法：先 `ehlo` 获取服务器支持的认证方式（从 `esmtp_features["auth"]` 解析，兼容 Python 3.14 移除 `esmtp_auth` 属性的变化），优先 PLAIN、其次 LOGIN，只尝试一次，认证失败直接抛出 `SMTPAuthenticationError`（含真实错误码与服务器返回信息）
- `send_verification_code` 与 `send_notification` 的 `server.login()` 均改用 `self._smtp_authenticate()`
- 两个发送方法新增 `SMTPAuthenticationError` 异常分支，error_msg 记录格式为"邮件发送失败：SMTP 认证失败（535）：Login fail..."

**验证**：
- 正确授权码：登录成功（235）
- 错误授权码：抛出 `SMTPAuthenticationError(535)`，error_msg 包含完整 QQ 邮箱错误提示
- 改进前 error_msg = "邮件发送失败：Connection unexpectedly closed"
- 改进后 error_msg = "邮件发送失败：SMTP 认证失败（535）：Login fail. Account is abnormal..."

### 关于 rotate_encryption_key.py 脚本的检查结论
审查 [backend/sql/rotate_encryption_key.py](file:///d:/wuzuniao/yao/backend/sql/rotate_encryption_key.py) 的解密加密逻辑：旧密钥解密（L189）→ 拿到明文 → 新密钥重新加密（L199）。AES-256-GCM 带认证标签，若旧密钥不匹配会抛 `InvalidTag` 异常并退出（L190-L196），**不会替换明文**。当前 .env 密钥能正确解密数据库密文，证明轮换脚本逻辑正确，授权码失效是 QQ 邮箱侧问题，与脚本无关。

### 影响范围
- 数据库：`notification_channels` 表 channel_id=3 的 channel_value.password 字段更新
- 代码：`backend/app/services/email_service.py` 新增 `_smtp_authenticate` 方法并改进异常处理
- 邮件通知功能恢复正常，后续认证失败时 error_msg 将记录真实的 SMTP 错误码与服务器提示

---

## 2026-07-11 - 账号注销冷静期由 1 分钟延长至 24 小时

### 背景
原账号删除冷静期仅 1 分钟（前端 60 秒倒计时 + 后端 `purge_expired_deletions` 按 `updated_at + 1分钟` 清理），用户误操作后几乎无反悔窗口。延长至 24 小时，给用户充分的取消时间。

### 变更内容
- **`backend/app/services/user_service.py`**：
  - `purge_expired_deletions` 清理阈值 `timedelta(minutes=1)` → `timedelta(hours=24)`
  - 同步更新 `schedule_deletion` 与 `purge_expired_deletions` 方法注释
- **`backend/app/api/v1/users.py`**：
  - `schedule_deletion` 接口注释与返回 msg 文案"1分钟"→"24小时"
- **`frontend/src/pages/user/profile.vue`**：
  - 删除账号确认弹窗 content"1分钟后自动删除"→"24小时后自动删除"
  - 提交后 toast"1分钟后自动删除"→"24小时后自动删除"
  - 同步更新 `isDeletionScheduled` 与倒计时相关注释
- **`frontend/src/store/modules/user.js`**：
  - `startDeletionCountdown` 定时器 `60000` ms → `24 * 60 * 60 * 1000` ms
  - 同步更新模块顶部与定时器注释
- **`frontend/src/api/modules/user.js`**：
  - `scheduleDeletion` 函数注释"1分钟后自动清理"→"24小时后自动清理"

### 影响范围
账号注销流程（profile.vue 入口 → scheduleDeletion API → 后端清理任务）。`cancelDeletion` 逻辑不变，用户在 24 小时内仍可随时取消。

---

## 2026-07-10 - 重写后端通知派发逻辑（两档催办 + 匹配区间判定）

### 背景
根据 `.trae/specs/clarify-notification-logic/` spec 文档，后端定时通知派发逻辑（`NotificationDispatcher`）需调整：
- 旧催办为 5分钟/30分钟/1小时 三档固定偏移 → 新为 10分钟 + 「1小时与下一次提醒中点择先到达者」两档
- 旧准时触发按「计划当天总打卡记录数 < 提醒时间数」判定 → 新统一改为「该提醒时间对应匹配区间内无打卡记录」
- 旧催办按 plan_time_id 维度判定 → 新统一改为按匹配区间判定

### 变更内容
- **`backend/app/schemas/notification_log.py`**：
  - 触发类型常量从 0/1/2/3 四值改为 0/1/2 三值
    - `TRIGGER_ON_TIME=0`（准时提醒，保留）
    - `TRIGGER_OFFSET_10MIN=1`（超时10分钟催办，原 `TRIGGER_OFFSET_5MIN` 改名改值）
    - `TRIGGER_OFFSET_1HOUR_OR_MIDPOINT=2`（1小时或中点催办，合并原 30分钟与1小时）
  - 移除 `TRIGGER_OFFSET_5MIN`、`TRIGGER_OFFSET_30MIN`、`TRIGGER_OFFSET_1HOUR` 旧常量
  - `FOLLOWUP_OFFSETS` 列表改为单常量 `FOLLOWUP_OFFSET_10MIN=10`（档位2为动态计算）
  - `TRIGGER_DESC` 描述同步更新
- **`backend/app/services/scheduler_service.py`**：
  - 重写 `NotificationDispatcher` 类（`SchedulerService` 三个后台循环不变）
  - `dispatch_for_now` 拆为三部分：准时触发、档位1催办、档位2催办
  - 新增 `_dispatch_fixed_offset`：处理准时和档位1（反推提醒时间），按匹配区间判定打卡记录数
  - 新增 `_dispatch_followup_2` / `_dispatch_followup_2_for_date`：档位2正向查询今日+昨日有效计划，动态计算触发时间（`min(提醒+60, 中点)`，末次固定+1小时），支持跨天触发
  - 新增 `_get_match_intervals_minutes`（静态方法，算法同 checkin_service）和 `_count_checkins_in_interval`（按匹配区间查询打卡记录数）
  - 移除 `_dispatch_trigger`、`_count_checkins`、`_has_checkin_for_slot` 旧方法
  - 更新模块文档字符串与类 docstring
  - `_get_channels_for_plan`、`_already_sent`、`_send_via_channel`、`_send_email`、`_write_log` 保持不变

### 影响范围
- 通知派发触发条件、催办档位、防重去重逻辑
- 站内信/邮件发送方式不变

---

## 2026-07-10 - 重写首页打卡逻辑（按中点划分匹配区间，全天无留白）

### 背景
根据 `.trae/specs/clarify-checkin-logic/` spec 文档，首页打卡按钮的匹配区间划分逻辑存在以下问题：
- 旧逻辑用 `[t-60, t+60]` 区间匹配打卡记录，相邻提醒之间存在间隙，无法覆盖全天
- 开始打卡时间为 `t-60`（前 1 小时），需改为 `t-120`（前 2 小时）
- 缺少打卡防抖、用户不在线不检查、主要卡片按时间范围优先显示等规则

### 变更内容
- **`frontend/src/pages/index/index.vue`**：
  - 新增 `getMatchIntervals` 函数：按相邻提醒的中点划分匹配区间，覆盖全天 0:00-24:00 无间隙、无留白
    - 第一次提醒：`[0:00, midpoint(t1, t2)]`
    - 中间提醒：`[midpoint(t_{i-1}, t_i), midpoint(t_i, t_{i+1})]`
    - 最后一次提醒：`[midpoint(t_{n-1}, t_n), 24:00]`
  - 重写 `findCurrentTargetIndex`：改为按匹配区间查找当前时间所属提醒
  - 重写 `isIntervalChecked`（原 `isTimeChecked`）：按匹配区间查询打卡记录
  - 更新 `checkinState`：开始打卡时间从 `t-60` 改为 `t-120`（前 2 小时）；waiting 状态判定改为 `nowMinutes < t_1 - 120`
  - 新增打卡防抖：`lastCheckinTime` 记录上次打卡时间，3 秒内重复点击被拦截
  - 新增 `onHide` 生命周期钩子：页面隐藏时清除 `refreshTimer`，用户不在线不进行检查
  - 新增 `computeDefaultPrimaryIndex` 函数：当前时间在提醒时间前后 2 小时范围内的计划优先显示在主要卡片；都在范围内时优先级高的为主；优先级相同时先创建的为主
  - 更新 `loadActivePlans`：排序增加 `created_at` 升序作为次级排序键（同优先级时先创建的在前）；加载后自动设置默认主要卡片
  - 更新头部注释和 CSS 注释以反映新逻辑
- **`backend/app/services/checkin_service.py`**：
  - `list_day_detail` 方法：打卡记录匹配从 `[t-60, t+60]` 改为按中点划分的匹配区间
  - 新增 `_get_match_intervals` 静态方法：计算匹配区间（与前端 `getMatchIntervals` 逻辑一致）
  - 计划排序从 `created_at.desc()` 改为 `created_at.asc()`（先创建的在前，与前端一致）
  - 更新方法文档字符串以反映新匹配规则

### 影响范围
- 首页打卡按钮状态判定（disabled/waiting/active/done）
- 打卡记录匹配逻辑（全天无留白，任意时刻打卡记录都能归属到对应提醒）
- 定时刷新机制（用户不在线时不检查）
- 任务卡片默认显示顺序（按时间范围和优先级）
- 后端日打卡详情查询的匹配逻辑

---

## 2026-07-08 - 完善 Git 提交信息规范

### 背景
`.trae/rules/git-commit-message.md` 已存在基础的提交信息规则，但内容较简略，缺少 Conventional Commits 的完整结构说明（scope、body、footer、破坏性变更等），不利于 AI 生成一致、规范的提交信息。

### 变更内容
- **`.trae/rules/git-commit-message.md`**：
  - 按照 [Conventional Commits v1.0.0](https://www.conventionalcommits.org/zh-hans/v1.0.0/) 规范重构
  - 补充完整的提交信息结构：Header（type/scope/! /description）、Body、Footer
  - 细化 type 说明，新增 `build`、`ci`、`revert`，并给出使用场景
  - 定义项目常用 scope 取值（frontend/backend/api/db/user/plan/checkin 等）
  - 增加 Body 书写规范、Footer 关联 issue / 破坏性变更 / Co-authored-by 格式
  - 新增提交信息示例与禁止事项
  - 新增特殊情况说明：回滚提交、仅更新文档、仅修改 AI 规则文件
- **`目录结构.json`**：新增 `.trae` 目录结构说明，包含 `rules` 与 `specs` 子目录

### 影响范围
后续 AI 协助 Git 提交时，将严格遵循该规则生成中文 Conventional Commits 风格的提交信息。

---

## 2026-07-08 - 修复微信小程序分享菜单按钮灰色不可点击问题

### 背景
所有页面右上角"..."菜单中"转发给朋友"、"分享到朋友圈"、"复制链接"三项均为灰色不可点击。此前已在 `useShare.js` 中注册 `onShareAppMessage` 与 `onShareTimeline` 生命周期钩子，但按钮仍不可用。

### 未实现原因
查阅微信官方文档（[wx.showShareMenu](https://developers.weixin.qq.com/miniprogram/dev/api/share/wx.showShareMenu.html)）确认：仅定义 `onShareAppMessage` / `onShareTimeline` 只能让菜单项"显示"，要让菜单项"可点击"还须调用 `wx.showShareMenu` 显式启用。该 API 默认 `menus` 仅 `['shareAppMessage']`，需显式传入 `['shareAppMessage', 'shareTimeline']` 才能让两个按钮都可点击。"复制链接"依赖"转发给朋友"启用后自动可用。

### 变更内容
- **`frontend/src/composables/useShare.js`**：
  - 新增导入 `onLoad` 生命周期钩子
  - 在 `useShare` 内部注册 `onLoad`，调用 `wx.showShareMenu({ withShareTicket: true, menus: ['shareAppMessage', 'shareTimeline'] })`，使用 `#ifdef MP-WEIXIN` 条件编译确保仅微信小程序平台执行
  - 更新文件头注释说明"显示"与"可用"的差异及 `wx.showShareMenu` 的作用
- **`目录结构.json`**：同步更新 `useShare.js` 描述

### 影响范围
全部 13 个页面（index/record/settings/notification/plan/profile/login/register/contact/help/messages/privacy/forgot-password）的右上角分享菜单均可正常点击。

---

## 2026-07-08 - 实现 JWT 认证（前后端协同改造）

### 背景
此前所有接口的 `user_id` 来自请求体或 URL 路径，无任何身份验证，任何人都可通过修改 `user_id` 访问他人数据。`pyjwt` 已在 requirements.txt 中但未使用。本次按 AGENTS.md 第 7 节「JWT Bearer Token，过期 7 天」规范完成全量接入。

### 变更内容

#### 1. 后端：JWT 配置与令牌工具
- **`backend/.env` / `.env.template`**：新增 `JWT_SECRET_KEY`（`secrets.token_urlsafe(48)` 生成）与 `JWT_EXPIRE_DAYS=7`
- **`backend/app/core/config.py`**：Settings 新增 `JWT_SECRET_KEY: str` 与 `JWT_EXPIRE_DAYS: int = 7`
- **`backend/app/core/security.py`**：Security 类新增 JWT 方法
  - `generate_token(user_id)`：HS256 签名，载荷 `sub`(user_id 字符串)/`iat`/`exp`（7 天）
  - `verify_token(token)`：解签，区分 `ExpiredSignatureError`（"登录已过期"）与 `InvalidTokenError`（"登录凭证无效"）
- **`backend/app/core/deps.py`**（新增）：`get_current_user_id` 依赖，解析 `Authorization: Bearer <token>` 头，校验 token 并返回 `user_id: int`。无 token/格式错误/校验失败均返回 401

#### 2. 后端：Schema 重构（移除 user_id）
所有受保护接口的请求体不再含 `user_id` 字段，user_id 由 token 解析：
- `schemas/user.py`：UpdateSignature、ChangePassword、SendChangeEmailOldCode、ChangeEmail、UpdateAvatar、ScheduleDeletion、UpdateUsername、SetPassword、BindEmail 均移除 `user_id` 及其校验器（空 Schema 用 `pass`）
- `schemas/plan.py`：CreatePlan 移除 `user_id`
- `schemas/checkin_record.py`：CreateCheckin 移除 `user_id`
- `schemas/notification_channel.py`：CreateEmailChannel、UpdateEmailChannel、DeleteChannel 移除 `user_id`
- `schemas/notification_log.py`：MarkRead 移除 `user_id`

#### 3. 后端：API 路由重构（公开/受保护分流）
- **公开接口**（返回 `access_token`）：`POST /users/register`、`POST /users/login`、`POST /users/wechat-login`、`POST /users/reset-password`。新增 `_user_payload(db_user)` 辅助函数：生成 JWT 并组装 `{access_token, user: {...}}` 响应
- **受保护接口**（`Depends(get_current_user_id)`）路径变更：
  - `users.py`：`GET /{user_id}/info` → `GET /info`；修改用户名/头像/签名/密码/邮箱/绑定邮箱/设置密码/预约删除/取消删除等接口均移除 `user_id` 参数
  - `plans.py`：`GET /{user_id}/list` → `GET /list`；POST/PUT/DELETE 移除 `user_id` 查询参数
  - `checkins.py`：`GET /checkins/{user_id}/today` → `GET /checkins/today`；today/{plan_id}、month、day 路径同步移除 `user_id`
  - `notification_channels.py`：`GET /{user_id}/list` → `GET /list`；POST/PUT/DELETE 移除 `user_id`
  - `notification_logs.py`：`GET /notification-logs/{user_id}/list` → `GET /notification-logs/list`；`GET /{user_id}/unread-count` → `GET /unread-count`；PUT /read 移除 `user_id`

#### 4. 前端：登录态管理
- **`store/modules/user.js`**：新增 `accessToken` 状态（独立于 userInfo）。`setUser` 登录成功时保存 `access_token`；`clearUser` 同时清除 `userInfo` 与 `accessToken`；`loadUserFromStorage` 同时加载两者；`verifyUserExists`/`loadUnreadCount` 改为基于 `accessToken` 判断登录态
- **`api/request.js`**：
  - 请求拦截器自动附加 `Authorization: Bearer <token>`（直接从 `uni.getStorageSync('accessToken')` 读取，避免与 Pinia store 循环依赖）
  - 响应 401 处理：`_isHandling401` 标志防止并发重定向；区分「token 过期」（有 token，toast 提示后 1.5s 跳转）与「未登录」（无 token，立即跳转）；清除本地登录态后 `reLaunch` 到登录页

#### 5. 前端：API 模块与页面调用点改造
- **`api/modules/*.js`**（user/notification/plan/checkin/message 5 个文件）：所有请求函数移除 `user_id` 参数；URL 路径移除 `/{user_id}` 段
- **页面调用点**（7 个页面）：移除所有 `userStore.userInfo.id` 传参
  - `pages/user/profile.vue`：7 处 API 调用对象移除 `user_id`，3 处无参调用
  - `pages/user/messages.vue`：3 处 API 调用移除 `user_id`
  - `pages/index/settings.vue`：`listNotificationChannels()`/`listPlans()` 无参
  - `pages/index/record.vue`：`listMonthCheckins()`/`listDayCheckins()` 移除 `user_id`
  - `pages/index/plan.vue`：`listPlans()`/`listNotificationChannels()`/`deletePlan()` 无参，createPlan/updatePlan 移除 `user_id`
  - `pages/index/notification.vue`：`listNotificationChannels()` 无参，createEmailChannel/updateEmailChannel/deleteNotificationChannel 移除 `user_id`
  - `pages/index/index.vue`：`listPlans()`/`listTodayCheckinsByPlan()` 无参，createCheckin 移除 `user_id`

### 关键设计决策
1. **响应字段命名 `access_token`**：避免与 Security.filter_output 中 `_SENSITIVE_FIELDS` 的 `token` 字段冲突
2. **循环依赖规避**：request.js 直接读 storage 而非导入 Pinia store（store 反向依赖 API 模块，会形成环）
3. **401 并发防护**：多个并发请求同时 401 时，`_isHandling401` 标志确保只触发一次重定向
4. **空 Schema 处理**：SendChangeEmailOldCode、ScheduleDeletion 等原本仅含 `user_id` 的 Schema，移除后用 `pass` 创建空 Pydantic 模型，前端发送 `{}` 请求体

### 验证
- 后端导入测试：`from app.main import app` → "IMPORT OK"
- JWT 单元测试：`generate_token(123)` 生成 token；`verify_token(token)` 返回 `{'sub': '123', 'iat': ..., 'exp': ...}`
- HTTP 401 防护测试（TestClient）：
  - 无 token GET /users/info → 401 "未登录，请先登录"
  - 无 token GET /plans/list → 401 "未登录，请先登录"
  - 无效 token GET /users/info → 401 "登录凭证无效"
- 前端验证：grep 确认所有剩余 `user_id` 引用均为注释（"user_id 由 JWT 提供"）

### 影响范围
- 后端修改文件：`core/config.py`、`core/security.py`、`core/deps.py`(新增)、`schemas/user.py`、`schemas/plan.py`、`schemas/checkin_record.py`、`schemas/notification_channel.py`、`schemas/notification_log.py`、`api/v1/users.py`、`api/v1/plans.py`、`api/v1/checkins.py`、`api/v1/notification_channels.py`、`api/v1/notification_logs.py`、`.env`、`.env.template`
- 前端修改文件：`store/modules/user.js`、`api/request.js`、`api/modules/user.js`、`api/modules/notification.js`、`api/modules/plan.js`、`api/modules/checkin.js`、`api/modules/message.js`、`pages/user/profile.vue`、`pages/user/messages.vue`、`pages/index/settings.vue`、`pages/index/record.vue`、`pages/index/plan.vue`、`pages/index/notification.vue`、`pages/index/index.vue`
- 数据库无变更

---

## 2026-07-08 - 创建 Security 安全验证类，集中管理后端安全逻辑（重构）

### 背景
项目原有的安全验证逻辑分散在各 Schema 的 `field_validator` 中，存在以下问题：
1. **验证逻辑重复**：密码复杂度校验在 4 个 Schema 中重复实现（RegisterUser、ResetPassword、ChangePassword、SetPassword），用户名校验在 2 个 Schema 中重复，验证码校验在 5 个 Schema 中重复
2. **缺少输入净化**：未去除控制字符（null 字节等），部分字段未 strip 首尾空白
3. **缺少 ID 校验**：`user_id`、`plan_id`、`channel_id` 等整数字段无正整数校验，可传 0 或负数
4. **无统一输出过滤**：缺少防止敏感字段（password_hash、session_key）泄露的安全网

### 变更内容

#### 1. 新增 `Security` 安全验证类（`backend/app/core/security.py`）
集中管理所有后端安全验证逻辑，其他类只做调用：

| 方法 | 功能 |
|---|---|
| `hash_password(password)` | bcrypt 密码哈希 |
| `verify_password(plain, hashed)` | bcrypt 密码校验 |
| `validate_password(password)` | 密码复杂度（8-20位，至少三种字符类型），去除控制字符 |
| `validate_username(username)` | 用户名格式（2-15位，中文/英文/数字），strip + 去控制字符 |
| `validate_code(code)` | 验证码格式（6位数字） |
| `validate_email(email)` | 邮箱基本格式校验（用于非 EmailStr 场景） |
| `validate_smtp_host(host)` | SMTP 服务器地址校验 |
| `validate_smtp_port(port)` | SMTP 端口范围校验（1-65535） |
| `sanitize_string(value, max_length, field_name)` | 通用输入实体化：去控制字符 + strip + 长度校验 |
| `validate_positive_int(value, field_name)` | 正整数校验（用于 user_id、plan_id 等） |
| `filter_output(data)` | 输出实体化：移除 password_hash、session_key 等敏感字段 |

#### 2. 重构所有 Schema，调用 Security 类（消除重复逻辑）
- `schemas/user.py`：username、password、code、user_id、signature、avatar_url → 调用 Security
- `schemas/plan.py`：user_id、plan_id、name、remark → 调用 Security
- `schemas/checkin_record.py`：user_id、plan_id、plan_time_id → 调用 Security
- `schemas/notification_channel.py`：user_id、channel_id、smtp_host、smtp_port、email → 调用 Security
- `schemas/notification_log.py`：log_id、user_id → 调用 Security

#### 3. 更新 `user_service.py` 导入
`from ..core.security import get_password_hash, verify_password` → `from ..core.security import Security`
所有调用点改为 `Security.hash_password()` / `Security.verify_password()`

### 修复的安全逻辑错误
1. **消除重复验证**：密码复杂度逻辑从 4 处重复 → 1 处统一（Security.validate_password）
2. **新增输入净化**：`sanitize_string` 去除控制字符（`\x00-\x08`、`\x0b`、`\x0c`、`\x0e-\x1f`、`\x7f`），防止 null 字节注入
3. **新增 ID 校验**：所有 `user_id`、`plan_id`、`channel_id`、`log_id`、`plan_time_id` 字段添加正整数校验
4. **统一 strip 行为**：用户名、邮箱、SMTP地址等字段统一 strip 首尾空白（原部分 Schema 未 strip）

### 影响范围
- 修改文件：`security.py`、`user_service.py`、`schemas/user.py`、`schemas/plan.py`、`schemas/checkin_record.py`、`schemas/notification_channel.py`、`schemas/notification_log.py`
- 无 API 接口签名变更，前端无需修改
- 所有输入校验由 Pydantic Schema（字段规则）→ Security 类（安全逻辑）分层完成

### 待后续处理的安全问题
**JWT 认证缺失**：当前所有接口的 `user_id` 来自请求体或 URL 路径，无 token 验证。`pyjwt` 已在 requirements.txt 中但未使用。实现 JWT 认证需前后端协同改动（前端存储/发送 token，后端中间件验证），建议单独作为一个迭代任务。

---

## 2026-07-08 - 邮箱专用密码加密密钥轮换脚本（新增）

### 背景
邮件通知渠道的客户端专用密码采用 AES-256-GCM 加密存储在 `notification_channels.channel_value` JSON 的 `password` 字段中，密钥为 `backend/.env` 中的 `ENCRYPTION_SECRET_KEY`。出于安全运维需要，需支持定期轮换加密密钥。

### 变更内容
新增 `backend/sql/rotate_encryption_key.py` 脚本（Linux 运维），实现密钥轮换全流程：

1. **读取旧密钥与数据库连接**：通过项目 `app.core.config.settings` 模块读取 `backend/.env` 中的 `ENCRYPTION_SECRET_KEY`（旧密钥）和 `DATABASE_URL`
2. **生成新密钥**：使用 Python `os.urandom(32)` 生成 32 字节随机密钥，base64 编码
3. **内存中解密+重加密**：用旧密钥解密所有邮件渠道（`channel_type='邮件'`）的 `password`，用新密钥重新加密。任何一条解密失败立即中止，不触碰数据库
4. **事务更新数据库**：关闭 autocommit，在单个事务中更新所有记录的 `channel_value` 和 `updated_at`，全部成功才 `COMMIT`，任何异常 `ROLLBACK`
5. **原子替换 .env**：事务提交成功后，通过临时文件 + `os.replace` 原子替换 `.env` 中的 `ENCRYPTION_SECRET_KEY`。若 `.env` 写入失败（DB 已提交），打印新密钥供手动恢复

### 技术细节
- 脚本位于 `backend/sql/`，通过 `sys.path.insert` 将 `backend/` 加入路径，导入 `app.core.config.settings` 读取 `.env` 配置
- 使用 `asyncmy`（项目已有依赖）直连数据库，设置 `autocommit=False` 启用事务模式
- **asyncmy API 适配**：`conn.cursor()` 为同步方法（不需 await），`cur.execute()`/`cur.fetchall()`/`cur.close()`/`conn.commit()`/`conn.rollback()` 为异步方法（需 await）
- 设置会话 `time_zone='+08:00'` 与项目 `database.py` 配置一致
- 加解密逻辑内联实现（不依赖项目 `crypto.py` 模块），因需同时使用新旧两个密钥
- 所有函数参数和返回值添加类型注解（遵循 PEP 8 和项目编码规范）
- **跨平台兼容**：使用 `pathlib.Path` 处理路径、`os.replace()` 原子替换文件、`open(newline="")` 保留原始换行符，Windows 和 Linux 均可运行
- 用法：`python3 backend/sql/rotate_encryption_key.py`（Linux 可直接 `chmod +x` 后执行）

### 影响范围
- 新增文件：`backend/sql/rotate_encryption_key.py`
- 无代码逻辑变更，不影响现有功能
- 运维操作：建议低峰期执行，执行前备份数据库和 `.env` 文件

---

## 2026-07-07 - 打卡记录页日历网格错位修复（修复）

### 背景
record.vue 日历在所有机型中出现星期标题与日期单元格错位，第7列（周六）被挤到下一行。

### 问题原因
日历网格原用 `display: flex; flex-wrap: wrap` + 固定宽度 `width: 84rpx`（设计稿 42px）。
- 7个单元格总宽 = 84rpx × 7 = 588rpx = 294px（设计稿）
- 卡片内容区宽 = 屏宽 - main padding(48rpx) - 卡片 padding(96rpx) ≈ 279px（375px 屏）
- 294px > 279px，第7个单元格超出容器宽度被 `flex-wrap` 挤到下一行，导致整列错位
- 不同屏宽下 rpx 换算比例不同，错位程度不一，所有机型均受影响

### 修复方案
将日历网格从 flex 固定宽度改为 CSS Grid 7列等宽，列宽自适应卡片宽度：
- `.record-page__calendar-weekdays`：`display: flex` → `display: grid; grid-template-columns: repeat(7, 1fr)`
- `.record-page__calendar-grid`：`display: flex; flex-wrap: wrap` → `display: grid; grid-template-columns: repeat(7, 1fr)`
- `.record-page__calendar-weekday`：删除 `width: 84rpx`（grid 自动等分列宽）
- `.record-page__calendar-cell`：删除 `width: 84rpx`（grid 自动等分列宽），保留 `height: 104rpx`
- 平板断点（≥768px）：同步删除 weekday 和 cell 的 `width: 42px`，让百分比自适应

### 优势
- CSS Grid `repeat(7, 1fr)` 让7列严格等宽且自适应容器，无论屏宽如何变化都不会错位
- 星期标题与日期单元格共享相同的列模板，列宽严格对齐
- 微信小程序基础库 2.x+ 完整支持 CSS Grid，兼容性无忧

### 影响范围
- 前端：`pages/index/record.vue`（仅 `<style>` 块的日历网格部分，template 与 script 未动）

---

## 2026-07-07 - 首页自适应高度适配各机型消除滚动条（优化）

### 背景
首页原用 `min-height: 100vh` 允许内容溢出，配合 `padding-top: 210rpx`、`gap: 132rpx`、`margin-bottom: 402rpx` 等大固定留白，在小屏机型（iPhone SE 667px 视口）上内容总高约 842px 超出视口 175px，出现上下滚动条。用户要求：所有机型自适应高度不滚动，保持图标大小不变，可减少留白但保留合理间隔。

### 变更方案
1. **锁定视口高度**：`.index-page` 从 `min-height: 100vh` 改为 `height: 100vh; overflow: hidden`，严格禁止滚动
2. **压缩默认留白**（保持打卡按钮 384rpx 与图标尺寸不变）：
   - `.index-page__frame` padding-top: 34rpx → 0（去掉顶部冗余留白，由 main-canvas 统一处理）
   - `.index-page__main-canvas` padding-top: 210rpx → 200rpx，gap: 132rpx → 64rpx
   - `.index-page__hero` padding-top: 64rpx → 32rpx
   - `.index-page__checkin-shell` padding: 64rpx → 32rpx，margin-bottom: 402rpx → 340rpx
3. **新增小屏断点（max-height: 700px）**：iPhone SE(667px) 等小屏进一步压缩
   - main-canvas padding-top: 170rpx，gap: 32rpx
   - hero padding-top: 16rpx
   - checkin-shell padding: 24rpx，margin-bottom: 280rpx
4. **平板断点（≥768px）补充 px 锁定**：避免 rpx 在宽屏过度放大导致横屏溢出
   - 新增 main-canvas padding-top: 80px、gap: 24px
   - hero padding-top: 32px → 16px
   - checkin-shell padding: 32px → 24px

### 各机型验证（3行描述场景，内容总高 vs 视口高）
| 机型 | 视口高 | 内容总高 | 剩余 |
|---|---|---|---|
| iPhone SE (375×667) | 667px | 646px | +21px ✓ |
| iPhone 12 (390×844) | 844px | 746px | +98px ✓ |
| iPhone 14 Pro Max (430×932) | 932px | 820px | +112px ✓ |
| iPad 横屏 (1024×768) | 768px | 730px | +38px ✓ |
| iPad 竖屏 (768×1024) | 1024px | 730px | +294px ✓ |

### 影响范围
- 前端：`pages/index/index.vue`（仅 `<style>` 块，template 与 script 完全未动）

---

## 2026-07-07 - index 主包与 user 子包剩余页面响应式单位转换 px → rpx + 多端断点适配（优化）

### 背景
延续首页与全局组件的响应式改造，将剩余 8 个页面 .vue 文件的 `<style>` 块 px 单位转换为 rpx，并追加 ≥768px 平板/折叠屏断点锁定。本次仅触及样式块，template 与 script 一行未改。

### 转换规则（同前：基于 375px 设计稿，1px = 2rpx）
- 转 rpx（×2）：width/height/padding/margin/gap/font-size/line-height/border-radius/定位偏移等
- 保留 px：1px/1.5px/2px 物理边框、box-shadow 偏移与模糊（含 inset 内描边）、9999px 胶囊圆角、百分比、vh、z-index、transform translate 偏移

### 修改文件清单（8 个）

**pages/index/ 主包 4 个：**
1. `frontend/src/pages/index/notification.vue`：主容器 padding/gap、卡片、表单字段、单选框、保存按钮、倒计时弹窗转 rpx；min-height:100vh、box-shadow、translateY 动画偏移保留
2. `frontend/src/pages/index/plan.vue`：标题字号、入口卡、表单卡、日期选择器、时间控件、单选框、保存按钮、已有计划卡片转 rpx；flex:1、三角形箭头 border、对勾边框保留
3. `frontend/src/pages/index/record.vue`：日历卡片、年月选择器、星期/日期单元格、今日/选中态圆圈、打卡详情列表转 rpx；三角形箭头 border、1px 分隔线、inset 内边框保留
4. `frontend/src/pages/index/settings.vue`：用户资料卡、头像、渐变点缀、分组卡片、链接项、状态徽章转 rpx；百分比宽度、opacity、z-index、1px 边框、box-shadow 保留

**pages/user/ 子包 4 个：**
5. `frontend/src/pages/user/help.vue`：帮助中心卡片、分类标签、问题列表转 rpx；1px 边框、box-shadow 保留
6. `frontend/src/pages/user/messages.vue`：站内信列表、消息卡片转 rpx；100vh、1px 边框、box-shadow 保留
7. `frontend/src/pages/user/contact.vue`：联系我们卡片、联系方式、二维码（140px→280rpx）转 rpx；1px 边框保留
8. `frontend/src/pages/user/privacy.vue`：隐私政策表格列宽（114.48px→228.96rpx 等）转 rpx；1px 表格边框保留，断点锁定回原 px 保证横向滚动比例

### 特殊处理说明
- record.vue 的 `width: 23.4px` 转为 `46.8rpx`（保留小数精度）
- privacy.vue 表格列宽在断点中锁定回原 px 值，保证横向滚动比例一致
- help/messages/contact/privacy 原 style 标签为 `<style lang="scss">`（非 scoped），按手术式修改原则未擅自添加 scoped

### 影响范围
- 前端：8 个页面文件（仅 `<style>` 块）
- 不影响 template/script 逻辑

---

## 2026-07-07 - user 子包页面响应式单位转换 px → rpx + 多端断点适配（优化）

### 背景
延续首页与全局组件的响应式改造，将 user 子包下 4 个页面 .vue 文件 `<style>` 块的 px 单位转换为 rpx 相对单位，并添加 ≥768px 平板/折叠屏断点锁定。本次仅触及样式块，template 与 script 一行未改。

### 转换规则（基于 375px 设计稿，1px = 2rpx）
- 转 rpx（×2）：width/height/padding/margin/gap/font-size/line-height/border-radius/left/right 等
- 保留 px：1px/1.5px/2px 物理边框、box-shadow 偏移与模糊（含 `inset 0 0 0 1px` 内描边）、9999px 胶囊圆角、百分比、vh、z-index、transform translate 偏移

### 修改文件清单（4 个，均仅改动 `<style>` 块）
1. `frontend/src/pages/user/profile.vue`：分组卡片圆角、列表项高度/padding、表单区 padding、输入框/验证码按钮/操作按钮尺寸、头像列表尺寸转 rpx；1px 边框与 box-shadow 保留；追加 ≥768px 断点锁定关键尺寸为原 px 值
2. `frontend/src/pages/user/login.vue`：登录卡片/微信卡片宽度与圆角、输入框/提交按钮尺寸、氛围装饰圆尺寸、复选框/对勾尺寸转 rpx；眼睛图标小数尺寸（18.33px/12.5px）按比例转 rpx；box-shadow 与 1.5px 对勾边框保留；追加断点锁定
3. `frontend/src/pages/user/register.vue`：注册卡片宽度/圆角、输入框/验证码按钮（含 13.5px/14.5px 小数 padding）/复选框/对勾尺寸转 rpx；inset 内描边与 1.5px 对勾边框保留；追加断点锁定
4. `frontend/src/pages/user/forgot-password.vue`：找回密码卡片宽度/圆角、输入框/验证码按钮/找回按钮尺寸转 rpx；inset 内描边保留；追加断点锁定

### 特殊处理说明
- 每个文件 `<style>` 块开头追加统一的响应式单位说明注释块
- `transform: rotate(45deg) translate(-1px, -1px)`（对勾微调）保留 px：transform 偏移属像素级微调，rpx 会破坏精确对齐，且不在转换属性列表内
- `border: 1.5px solid` / `border: 2px solid`（对勾、头像选中态边框）按物理边框保留
- 小数 px（18.33px、12.5px、13.5px、14.5px、33.5px）按 ×2 比例精确转换，保留必要小数 rpx

### 影响范围
- 前端：user 子包 4 个页面（profile/login/register/forgot-password）
- 不影响 template/script 逻辑，不引入新依赖

### 后续待办
- 继续推进其余页面级 .vue 文件的 px → rpx 转换与断点适配

---

## 2026-07-07 - 全局组件与样式表响应式单位转换 px → rpx + 多端断点适配（优化）

### 背景
延续首页（index.vue）的响应式改造，将全局复用组件与全局样式表的 px 单位转换为 rpx 相对单位，并添加 ≥768px 平板/折叠屏断点锁定。本次覆盖底部导航、通知/返回按钮、页面标题区、应用根组件与全局设计令牌。

### 转换规则（同首页：基于 375px 设计稿，1px = 2rpx）
- 转 rpx（×2）：width/height/padding/margin/gap/font-size/line-height/border-radius/left/right/top/bottom 等
- 保留 px：1px 物理边框、box-shadow 偏移与模糊、9999px 胶囊圆角、百分比、vh/vw、z-index、translate 百分比

### 修改文件清单（6 个，均仅改动 `<style>` 块或样式表，template/script 未动）
1. `frontend/src/components/BottomNav.vue`：底部导航栏，bottom/width/height/padding/gap/各 item 尺寸/图标尺寸/font-size/line-height 转 rpx；1px 内描边与 box-shadow 保留；追加 ≥768px 断点锁定全部关键尺寸为原 px 值
2. `frontend/src/components/NoticeButton.vue`：left/width/height 转 rpx；`top: calc(45px + 0.6vh)` 保留（与 statusBarHeight 联动且含 vh 相对单位）；追加断点锁定 left/width/height
3. `frontend/src/components/BackButton.vue`：与 NoticeButton 同构，转换规则一致
4. `frontend/src/components/PageHeader.vue`：padding/gap/font-size/line-height 转 rpx；追加断点锁定为原 px 值
5. `frontend/src/App.vue`：仅引入 global.scss，自身无尺寸样式，仅追加转换说明注释（无断点）
6. `frontend/src/assets/styles/global.scss`：CSS 变量 `--radius-card` 12px→24rpx、`--radius-card-lg` 24px→48rpx、`--page-padding-top` 100px→200rpx；`--radius-btn: 9999px` 与 `--shadow-card`（box-shadow）保留 px；工具类 `.u-arrow-right` 三角形 border 宽度 6px→12rpx、7.4px→14.8rpx；末尾追加全局断点说明注释（不加媒体查询，由页面级断点处理）

### 特殊处理说明
- `NoticeButton`/`BackButton` 的 `top: calc(45px + 0.6vh)` 保留 px：该值为状态栏偏移基准，与 JS 动态计算值（`statusBarHeight - 5` px）保持单位一致，且含 vh 相对单位，转 rpx 会破坏状态栏避让逻辑
- `.u-arrow-right` 的 border 宽度按 UI 元素尺寸处理（非 1px 物理边框），转为 rpx 以随屏宽缩放

### 影响范围
- 前端：6 个文件（5 个 .vue 组件 + 1 个全局样式表）
- BottomNav 为 position:fixed 全局组件，影响所有页面底部布局

### 后续待办
- 继续推进其余页面级 .vue 文件的 px → rpx 转换与断点适配

---

## 2026-07-07 - 首页响应式单位转换 px → rpx + 多端断点适配（优化）

### 背景
用户要求全局界面从 px 改为相对单位，统一响应式，适配大小屏、平板、折叠屏。按"先改1页验证"策略，先改首页 `pages/index/index.vue`，确认效果与转换比例后再批量推进其他 17 个文件。

### 转换规则（基于 375px 设计稿，1px = 2rpx，uni-app 标准 750rpx = 屏宽）
- **转 rpx（×2）**：width / height / padding / margin / gap / font-size / line-height / border-radius / left/right/top/bottom / letter-spacing
- **保留 px**：1px 物理边框（避免高 DPI 屏消失）、box-shadow 偏移与模糊（视觉效果不缩放）、9999px（胶囊圆角）、百分比、vh、z-index

### 多端断点策略
- **≥768px（平板/折叠屏竖屏）**：将关键尺寸锁定为 px，避免 rpx 在宽屏上过度放大
  - 内容容器宽度统一 342px（empty/hero/primary-card/secondary-card/checkin-shell）
  - 打卡按钮 192×192px，padding 32px 75px 0，margin-bottom 201px
  - 主卡片标题 16px、描述 14px、状态徽章 12px / 24×24px / 6px / 5px
  - 任务列表弹层宽度 342px，padding 16px 24px
- **≥1024px（折叠屏横屏/平板横屏）**：仅解除 main-canvas 左右 padding，让内容居中
  - `.index-page__main-canvas { padding-left: 0; padding-right: 0 }`

### 关键尺寸换算示例（设计稿 px → rpx）
| 元素 | 设计稿 | rpx |
|---|---|---|
| 主卡片宽度 | 342px | 684rpx |
| 打卡按钮 | 192×192px | 384×384rpx |
| main-canvas padding | 24px | 48rpx |
| 主卡片标题字号 | 16px | 32rpx |
| 打卡按钮距底 | 201px | 402rpx |
| checkin-shell padding | 32px 75px 0 | 64rpx 150rpx 0 |

### 影响范围
- 前端：`pages/index/index.vue`（仅 `<style>` 块，template 与 script 完全未动）

### 后续待办
- 待用户确认首页效果后，批量推进其他 17 个 .vue 文件
- `global.scss` 中的 CSS 变量（`--page-padding-top: 100px` 等）也需转 rpx
- `uni.scss` 的 SCSS 变量暂保持默认（uni-app 框架级，非业务样式）

---

## 2026-07-07 - 首页打卡按钮定位与全局高度自适应（优化）

### 背景
打卡大圆环按钮原用 `margin-bottom: calc(20vh + 101px)`，其中 20vh 随机型浮动（667px 屏 133px，812px 屏 162px），位置不稳定；全局高度用 `min-height: calc(100vh - 17px)` 硬编码，不够自适应。

### 变更
- **打卡按钮固定定位**：`margin-bottom` 改为固定 201px（= 导航栏高86px + 底部偏移15px + 按钮距导航栏顶部100px），使按钮底部距底部导航栏顶部固定 100px，位于整体中下部，各机型表现一致
- **全局高度 flex 自适应**：`.index-page` / `.index-page__frame` / `.index-page__main-canvas` 改为 flex 链式撑满（`display:flex; flex-direction:column; flex:1`），去掉 `min-height: calc(100vh - 17px)` 硬编码，兼容各种机型

### 影响范围
- 前端：`pages/index/index.vue`

---

## 2026-07-07 - 首页次要卡片备注说明改为一行显示（修复）

### 背景
首页次要卡片的"备注说明"显示了两行，应只显示一行。

### 问题原因
`.index-page__secondary-desc` 使用 `white-space: nowrap` + `text-overflow: ellipsis` 方案，但微信小程序 `<text>` 组件默认是 inline 元素，`overflow: hidden` 在 inline 元素上不生效；且备注内容若含换行符 `\n` 也会被解析为换行，导致显示两行。

### 修复方案
改用 `-webkit-line-clamp: 1` 方案（与主要卡片 `.index-page__primary-desc` 的 line-clamp 方案一致），强制单行截断，兼容备注含换行符的场景。

### 影响范围
- 前端：`pages/index/index.vue`

---

## 2026-07-07 - SMTP服务器端口校验规则完善（优化）

### 背景
用户要求端口校验：只允许 1-65535；输入超过 5 位时阻止继续输入；到达上限后依旧输入时进行错误提示。

### 变更
- **新建表单 + 编辑表单**：SMTP服务器端口 input 补充 `:maxlength="portLimit.max"`（及 `editPortLimit.max`），硬性阻止超过 5 位输入
- **补充 `limitReached` 提示**：在端口 input 下方显示 `limitHint`（"已达 5 字上限"），仅当用户在已达 5 位上限后继续尝试输入时触发，与 SMTP服务器地址、发件邮箱地址字段保持一致
- 1-65535 数值范围校验仍由 `validatePort` 在 blur 时处理，未改动

### 影响范围
- 前端：`pages/index/notification.vue`

---

## 2026-07-07 - 端口输入上限提示修复（修复）

### 背景
SMTP服务器端口输入框在达到 5 位上限后继续输入时，未正确显示错误提示。

### 问题原因
- 同时存在 `portError`（错误提示）和 `limitHint`（字符上限提示）两套提示机制，逻辑混乱
- `raw.length > filtered.length` 的判断不准确：非数字字符被过滤时长度也变短，会误判为上限问题
- `limitHint` 依赖 `useInputLimit` 的 `wasAtLimit` 状态机，在端口场景下与 `portError` 冲突

### 修复方案
- **移除**端口输入框的 `:maxlength` 属性和 `limitHint` 提示元素，长度限制完全由 `useInputLimit` 内部 `filter` 处理
- **统一**端口超限提示走 `portError` 错误通道，通过 `digitStr = raw.replace(/[^0-9]/g, '')` 提取纯数字后判断长度是否超过 5
- **错误优先级**：纯数字超长 → "端口号最多 5 位数字" > 含非数字字符 → "请输入有效的数字" > 正常输入 → `validatePort` 范围校验
- 适用范围：新建通知方式表单 + 编辑邮件配置表单，共 2 处端口输入框

### 影响范围
- 前端：`pages/index/notification.vue`

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-07 - 端口输入非数字字符实时提示、确认字符上限提示与单行截断（优化）

### 背景
对 notification.vue 端口输入框增加非数字字符输入时的实时提示，确认字符上限提示和次要卡片单行截断功能已满足需求。

### 变更

#### 1. notification.vue 端口输入非数字字符实时提示（优化）
- **新建表单 + 编辑表单**：SMTP服务器端口 `@input` 处理逻辑增强，对比原始输入值（`raw`）与过滤后值（`filtered`）：
  - `raw !== filtered` → 非数字字符被过滤，实时显示"请输入有效的数字"
  - `raw === filtered` → 纯数字输入，继续执行 `validatePort` 范围校验（1-65535）
- 错误提示优先级：非数字字符 > 超出范围 > 无错误

#### 2. 字符上限提示（确认）
- `useInputLimit.js` 的 `wasAtLimit` 逻辑已在上次实现：仅当用户达到上限后继续尝试输入时才显示提示，`:maxlength` 属性阻止超限输入。满足需求。

#### 3. 首页次要卡片单行截断（确认）
- `.index-page__secondary-desc` 已配置 `white-space: nowrap` + `text-overflow: ellipsis` + `overflow: hidden`，单行超长省略号截断。满足需求。

### 影响范围
- 前端：`pages/index/notification.vue`
- 不涉及后端变更

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-07 - 端口输入实时校验、字符限制提示触发优化、次要卡片单行截断（优化）

### 背景
对 notification.vue 端口输入框增加实时校验、统一优化全站字符限制提示的触发规则、确认 index.vue 次要卡片备注单行截断。

### 变更

#### 1. notification.vue 端口输入框实时校验（优化）
- **新建表单 + 编辑表单**：SMTP服务器端口输入框 `@input` 事件增加实时 `validatePort` 校验，输入过程中即时反馈错误
- `@focus` 时清空端口错误提示，`@blur` 保留校验
- 添加 `:class="{ 'notification-page__input--error': portError }"` 红色边框错误态

#### 2. 全站字符限制提示触发规则优化（useInputLimit.js）
- **触发规则变更**：从"达到上限时立即显示提示"改为"达到上限后继续输入时才显示提示"
  - 新增 `wasAtLimit` 内部状态，追踪上一次输入后是否已达上限
  - `handleInput`：仅当 `wasAtLimit` 为 true 且当前值仍达上限时，才设置 `limitReached = true`
  - `checkLimit`：仅更新 `wasAtLimit` 状态，不触发超限提示
- **影响范围**：所有使用 `useInputLimit` 的输入框（login/register/forgot-password/profile/plan/notification 共 6 个页面 31 处）

#### 3. index.vue 次要卡片备注单行截断（确认）
- `.index-page__secondary-desc` 已配置 `white-space: nowrap; text-overflow: ellipsis; overflow: hidden`，单行超长省略号截断，无需修改

### 影响范围
- 前端：`composables/useInputLimit.js`、`pages/index/notification.vue`
- 不涉及后端变更

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-07 - 首页卡片布局优化、表单校验增强、输入框字符限制统一与验证码错误提示标准化（优化 + 新增）

### 背景
对前端多个页面进行系统性优化：首页卡片布局紧凑化、notification/profie 页面表单校验增强、register 页面用户名校验恢复、全站输入框字符限制统一实施、验证码错误提示标准化。

### 变更

#### 1. 首页卡片布局优化（index.vue）
- **主要卡片**：移除固定高度，改为 `padding: 16px` 自适应内容高度，使卡片仅略高于实际文字内容，确保布局紧凑合理
- **次要卡片备注区域**：`.index-page__secondary-desc` 改为 `display: -webkit-box; -webkit-line-clamp: 2` 两行截断显示，无论原始内容多少，均只展示备注说明一行和计划名称一行，共计两行，避免多行拥挤

#### 2. notification.vue 邮箱格式校验（新增）
- **新建通知方式表单**：为"SMTP服务器地址"（`validateHost`）和"发件邮箱地址"（`validateEmail`）两个输入框添加 `@blur` 校验机制
  - `validateHost`：非空校验 + 域名格式或 IP 地址格式校验
  - `validateEmail`：参照注册页规则，标准邮箱格式校验（`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`）
- **编辑邮件配置表单**：同样为 SMTP 服务器地址和发件邮箱地址添加 `@blur` 校验，错误提示变量独立（`editHostError`/`editEmailError`）

#### 3. profile.vue 表单校验增强（新增）
- **修改用户名**：新增 `validateUsername` 函数，与注册页格式规则完全一致（2-15 字符，仅中文/英文/数字），`@blur` 触发校验
- **修改密码/设置密码**：新增 `validatePassword` 函数，与注册页格式规则完全一致（8-20 位，大小写字母/数字/特殊符号至少三种），`@blur` 触发校验
- **修改邮箱/绑定邮箱**：新增 `validateEmail` 函数，与注册页格式规则完全一致（标准邮箱格式），`@blur` 触发校验
- 所有校验错误即时显示红色错误提示文字（`.profile-page__error-text`）

#### 4. register.vue 用户名校验恢复（修复）
- 恢复 `validateUsername` 函数至正确实现版本：长度 2-15 个字符，仅允许中文、英文及数字字符（`/^[\u4e00-\u9fa5a-zA-Z0-9]+$/`）

#### 5. 全站输入框字符限制统一实施（新增）
- **useInputLimit.js 增强**：新增 `limitReached`（ref）和 `limitHint`（computed）属性，达上限时返回"已达 X 字上限"文本，未达上限时返回空字符串；新增 `checkLimit(value)` 方法用于手动检查
- **应用页面**：所有输入框统一接入字符限制提示
  - login.vue：用户名/邮箱输入框、密码输入框
  - register.vue：用户名、密码、确认密码、邮箱、验证码输入框
  - forgot-password.vue：邮箱、验证码、新密码、确认密码输入框
  - profile.vue：用户名、签名、旧密码、新密码、确认密码、旧邮箱验证码、新邮箱、新邮箱验证码输入框
  - plan.vue：计划名称、备注说明输入框（新建表单 + 编辑表单）
  - notification.vue：SMTP 服务器地址、端口、发件邮箱、客户端专用密码输入框（新建表单 + 编辑表单）
- **显示规则**：达到字符上限时显示橙色提示文字；未达上限时不显示任何提示

#### 6. 验证码错误提示标准化（优化）
- **register.vue**：`submitForm` 中 catch 验证码错误时统一提示"请输入正确验证码"
- **forgot-password.vue**：`handleResetPassword` 中 catch 验证码错误时统一提示"请输入正确验证码"
- **profile.vue**：`handleChangeEmail` 中 catch 验证码错误时统一提示"请输入正确验证码"

### 影响范围
- 前端：`composables/useInputLimit.js`、全部 13 个页面中的 8 个（index.vue/notification.vue/profile.vue/register.vue/login.vue/forgot-password.vue/plan.vue）
- 不涉及后端变更

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-06 - 帮助中心 SMTP 教程链接可点击复制（优化）

### 背景
帮助中心"邮件通知怎么填写？"卡片中，SMTP 配置教程链接以纯文本展示，用户需手动复制。需将链接文字改为可点击，点击后复制链接到剪贴板。

### 变更
- **文件**：`frontend/src/pages/user/help.vue`
  - 将链接 URL 行移除，把"QQ邮箱中开通SMTP服务并获取授权码"文字包裹为可点击文本（`.help-page__card-link`），点击触发 `copySmtpLink` 函数
  - 新增 `copySmtpLink` 函数：调用 `uni.setClipboardData` 复制链接，成功/失败分别 toast 提示
  - 新增 `.help-page__card-link` 样式：蓝色（#0066cc）+ 下划线，视觉上明确可点击

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-06 - 清理 \n 主动换行并优化文本自动换行排版（优化）

### 背景
前端多个页面通过 `\n` 字符进行主动换行（PageHeader 描述、privacy 政策正文、help FAQ 卡片、plan 描述、settings 签名占位符），在不同屏幕尺寸下换行位置固定、显示不自然。需清理所有 `\n`，改为容器边界自动换行。

### 变更
- **PageHeader desc 属性**（5 个页面）：profile.vue、contact.vue、messages.vue、notification.vue、help.vue
  - 移除模板字符串中的 `${'\n'}`，将 `:desc="\`...${'\n'}...\`"` 改为静态字符串 `desc="..."`
- **privacy.vue**：移除全部 `{{ '\n' }}` 内联换行（前言、各章节正文、列表项、表格单元格共 20+ 处），文本改为容器边界自动换行
- **help.vue**：移除 FAQ 卡片正文中全部 `{{ '\n' }}` 段落换行
- **plan.vue**：移除描述文本中的 `{{'\n'}}`
- **settings.vue**：移除未登录签名占位符中的 `\n`（"保持热爱，奔赴山海，\n每一天都要好好生活。"→"保持热爱，奔赴山海，每一天都要好好生活。"）
- **PageHeader.vue**：移除 `white-space: pre-line`（不再需要渲染 \n），改为默认 `normal` 容器自动换行；更新注释
- **global.scss**：移除已无引用的 `.input-limit-hint` / `--near` / `--full` 样式类（配合 useInputLimit 重构）

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）
- 微信开发者工具中各页面文本根据容器宽度自动换行，不同屏幕尺寸下显示自然

---

## 2026-07-06 - 次级页面返回组件 BackButton 与页面导航分层（新增 + 重构）

### 背景
项目此前所有页面（含首页、记录、设置三个一级页面和 10 个次级页面）统一使用 NoticeButton 通知按钮作为顶部导航组件。需明确页面层级：一级页面保留通知按钮，次级页面改用返回按钮，建立"一级页面→次级页面"的导航层次。

### 变更
- **新增组件** `frontend/src/components/BackButton.vue`
  - 以 NoticeButton.vue 为蓝本，图标替换为 `fanhui.png`
  - 点击行为：`uni.navigateBack({ delta: 1 })` 返回上一页；页面栈为空时 `uni.reLaunch` 回首页
  - 位置规范与 NoticeButton 完全一致（左上角绝对定位，避开状态栏）
- **次级页面替换**（10 个页面）：
  - index 目录：notification.vue、plan.vue
  - user 目录：profile.vue、login.vue、register.vue、contact.vue、help.vue、messages.vue、privacy.vue、forgot-password.vue
  - 每个页面将 `<NoticeButton :has-notification="..." />` 替换为 `<BackButton />`
  - 移除仅服务于 NoticeButton 的 `hasNotification` 变量声明（messages.vue 同时移除已无引用的 `computed` 导入）
- **一级页面不变**：index.vue、record.vue、settings.vue 继续使用 NoticeButton

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）
- 微信开发者工具中次级页面左上角显示返回图标，点击返回上一页；一级页面仍显示通知图标

---

## 2026-07-06 - 移除字符计数提示块并为端口输入框添加格式校验（重构 + 新增）

### 背景
`composables/useInputLimit.js` 已被重构，不再返回 `hint`、`isNear`、`isFull`、`count`、`sync`，仅返回 `max`、`filter`、`handleInput`。6 个页面中所有引用 `xxxLimit.hint.value`、`xxxLimit.isNear.value`、`xxxLimit.isFull.value` 的模板代码会导致运行时 TypeError，必须移除。同时为 notification.vue 的 SMTP 端口输入框添加格式错误校验（1-65535）。

### 变更
- **文件**：`frontend/src/pages/` 下 6 个页面
  - `user/login.vue`：移除 2 个字符计数提示块（usernameLimit、passwordLimit）
  - `user/register.vue`：移除 5 个字符计数提示块（usernameLimit、passwordLimit、confirmPwdLimit、emailLimit、codeLimit）
  - `user/forgot-password.vue`：移除 4 个字符计数提示块（emailLimit、codeLimit、newPwdLimit、confirmPwdLimit）
  - `user/profile.vue`：移除 8 个字符计数提示块（usernameLimit、signatureLimit、oldPwdLimit、newPwdLimit、confirmPwdLimit、oldCodeLimit、newEmailLimit、newCodeLimit）
  - `index/plan.vue`：移除 1 个字符计数提示块（editNameLimit）
  - `index/notification.vue`：移除 8 个字符计数提示块；为编辑表单与新建表单的 SMTP 端口输入框添加格式校验
- **notification.vue 端口校验实现**：
  - 新增 `portError`、`editPortError` 两个 ref 存储错误信息
  - 新增 `validatePort(value)` 函数：空值返回空字符串，非整数或超出 1-65535 范围返回"端口必须为 1-65535 之间的数字"
  - 端口输入框 @blur 时触发校验，错误时在输入框下方显示红色提示文本（内联样式：color #e5484d / font-size 12px / line-height 16px / margin-top 4px）
  - `handleSave`、`handleUpdateEmail` 提交时再次校验，有错误则 toast 提示并阻止提交
  - `handleAdd`、`toggleEmailEdit` 重置/填充表单时清空对应错误状态
- **保留项**：`errors.xxx` 格式校验提示块、`:maxlength="xxxLimit.max"` 绑定、`@input="e => form.xxx = xxxLimit.handleInput(e)"` 绑定、`useInputLimit` import 语句均未修改

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-06 - 为全部页面接入微信分享功能（新增）

### 背景
为使小程序用户能将页面转发给朋友或分享到朋友圈，需在各页面注册 `onShareAppMessage` 与 `onShareTimeline` 两个分享生命周期钩子。已创建组合式函数 `composables/useShare.js` 统一封装这两个钩子，本次为全部 13 个页面接入该函数。

### 变更
- **文件**：`frontend/src/pages/` 下 13 个页面
  - index 目录：`index.vue`、`record.vue`、`settings.vue`、`notification.vue`、`plan.vue`
  - user 目录：`profile.vue`、`login.vue`、`register.vue`、`contact.vue`、`help.vue`、`messages.vue`、`privacy.vue`、`forgot-password.vue`
- **修改**：每个页面 `<script setup>` 中
  - 新增 `import { useShare } from '../../composables/useShare'`（置于 import 分组末尾）
  - 在所有 import 之后、其他代码之前调用 `useShare({ title: '对应页面标题' })`
- 分享标题按页面语义配置（"首页""用药记录""设置"等），不传 path 时默认使用当前页面路径，不传 imageUrl 时由微信自动截取页面作为封面

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）
- 用户需在微信开发者工具中重新导入 `dist\build\mp-weixin`，验证各页面右上角"..."菜单中"转发"与"分享到朋友圈"入口可用且分享标题正确

---

## 2026-07-02 - 修复站内信页无法通过返回手势退出（修复）

### 背景
用户反馈在微信小程序中进入 `pages/user/messages`（站内信页）后，无法通过手机边缘左滑/右滑返回手势退出，卡在该页面，而其他分包页面（help、contact、profile 等）均可正常左滑返回。

### 根因分析
对比项目内所有页面跳转方式：
- 其他分包页面通过 `uni.navigateTo` 跳转（见 `settings.vue` 第 191、201 行），保留页面栈，栈深度 ≥2，返回手势可用
- **NoticeButton 组件**（11 个页面共用）点击时调用 `uni.reLaunch({ url: '/pages/user/messages' })`，`reLaunch` 会**关闭所有页面**再打开目标页，导致 messages.vue 进入时页面栈深度为 1（栈底），微信小程序无上一页可返回，返回手势自然失效

### 变更
- **文件**：`frontend/src/components/NoticeButton.vue`
- **修改**：`handleClick` 函数中
  - 将 `uni.reLaunch` 改为 `uni.navigateTo`，保留页面栈使返回手势生效
  - 新增当前页检测：通过 `getCurrentPages()` 判断是否已在 `pages/user/messages`，若是则直接 return，避免在站内信页内重复入栈

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）
- 用户需在微信开发者工具中重新导入 `dist\build\mp-weixin`，从首页点击通知按钮进入站内信页，验证左滑/右滑返回手势可用

---

## 2026-07-02 - 启用小程序组件按需注入（优化）

### 背景
参考微信官方文档 [按需注入和用时注入](https://developers.weixin.qq.com/miniprogram/dev/framework/ability/lazyload.html)，启用按需注入特性以降低小程序启动耗时和运行时内存占用。

### 变更
- **文件**：`frontend/src/manifest.json`
- **修改**：在 `mp-weixin` 节点下新增 `"lazyCodeLoading": "requiredComponents"` 配置
- **效果**：编译产物 `dist/build/mp-weixin/app.json` 第 30 行已正确写入该配置，小程序仅注入当前访问页面所需的自定义组件和页面代码，未访问页面及未声明组件不再被加载和初始化

### 注意事项
- 基础库 2.11.1 及以上版本支持（2.11.1 以下兼容但无优化效果）
- 项目采用 uni-app easycom 自动引入组件模式，无显式 `usingComponents` 声明，无需清理未使用组件声明
- 启用后需在微信开发者工具中确认小程序各页面表现正常

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）
- 编译产物 `app.json` 已包含 `lazyCodeLoading: "requiredComponents"` 配置

---

## 2026-07-02 - 修复定时邮件通知发送失败（修复）

### 背景
定时打卡通知邮件持续发送失败，后端日志报错 `Connection unexpectedly closed`。经诊断，邮件渠道（channel_id=3）存储的"客户端专用密码"解密后为 32 位十六进制字符串 `803a228de9981dd21c3185af33718e65`，并非 QQ 邮箱要求的 16 位授权码，导致 SMTP 认证阶段被服务器主动断开连接。

### 诊断过程
1. 检查后端日志 `backend/logs/app.log`，定位到 `打卡通知邮件发送失败：Connection unexpectedly closed`
2. 查询 `notification_logs` 表确认 8 条邮件记录全部失败（status=1）
3. 查询 `notification_channels` 表获取邮件渠道配置（smtp.qq.com:465）
4. 编写临时诊断脚本验证：SSL 连接成功，但 `server.login()` 阶段服务器断开连接
5. 解密存储的密码，发现为 32 位十六进制（非 16 位授权码格式）

### 验证加密算法
用户提供正确授权码 `mamwryhlxlfuebii` 后，验证 AES-256-GCM 对称加密算法：
- `encrypt('mamwryhlxlfuebii')` → 加密密文
- `decrypt(密文)` → `mamwryhlxlfuebii`（完全还原）
- 用还原的授权码登录 `smtp.qq.com:465` → 登录成功，测试邮件发送成功

**结论**：加密算法工作正常，无需更新。问题根因是用户之前输入了错误的密码。

### 变更
- **数据库**：更新 `notification_channels` 表 id=3 记录的 `channel_value.password` 字段，替换为正确授权码的 AES-256-GCM 加密值
- **无代码变更**：加密/解密逻辑（`backend/app/utils/crypto.py`）和邮件发送逻辑（`backend/app/services/email_service.py`）均无需修改

### 影响范围
- 定时邮件通知发送功能恢复正常
- 注意：今天（2026-07-02）的失败日志会阻止同日同时间点的重复发送（防重机制），明天（2026-07-03）起新通知将正常发送

---

## 2026-07-02 - 修正邮件授权码输入含前缀问题（修复）

### 背景
用户通过前端 notification.vue 页面更新邮件渠道密码后，定时邮件发送仍然失败。深度定位发现：用户在"客户端专用密码"输入框中输入时，将"授权码："前缀一同输入，导致实际存储的明文为 `授权码：mamwryhlxlfuebii`（UTF-8 编码 28 字节），而非 `mamwryhlxlfuebii`（16 字节）。

### 诊断过程
1. 查询数据库确认用户已通过前端更新（updated_at 06:49:07）
2. 解密数据库中的密文，结果为 `'授权码：mamwryhlxlfuebii'`（长度 20，hex: `e68e88e69d83e7a081efbc9a6d616d777279686c786c667565626969`）
3. hex 分析确认前 12 字节为"授权码："的 UTF-8 编码，后 16 字节才是真正的授权码
4. 确认前端 `useInputLimit(64)` 未传 allowedPattern，不会过滤中文字符，输入原样传递给后端

### 关于密文不同的说明
AES-256-GCM 使用随机 nonce（`os.urandom(12)`），相同明文每次加密的密文必然不同，这是加密算法的标准安全特性。判断加密是否正确的依据是**解密后能否还原原文**，而非密文是否相同。

### 变更
- **数据库**：更新 `notification_channels` 表 id=3 的 password 字段，替换为 `mamwryhlxlfuebii`（不含前缀）的加密值
- **无代码变更**：加密算法和前端输入逻辑均无需修改

### 影响范围
- 邮件渠道密码已修正为正确的 16 位授权码
- 后续定时通知将使用正确授权码发送邮件

---

## 2026-07-02 - 优化环境变量文件命名，统一使用 .env 文件（优化）

### 背景
原项目使用 `.env.dev`/`.env.prod` 双文件 + 环境变量 `ENV` 切换的模式管理环境配置，文件分散且切换逻辑增加维护成本。优化为开发与生产环境统一使用 `.env` 文件（不提交 Git），另提供 `.env.template` 参数模板（提交 Git）供新环境部署参考。

### 变更
- **后端**：
  - `backend/.env.dev` → 重命名为 `backend/.env`（移除已失效的 `ENV=dev` 行）
  - 删除 `backend/.env.prod`
  - 新增 `backend/.env.template`（含全部字段说明与注释，无敏感值）
  - `backend/app/core/config.py`：移除 `import os` 与 `get_env_file`/`ENV` 切换逻辑，简化为 `env_file = str(_BASE_DIR / ".env")`
- **前端**：
  - `frontend/.env.dev` → 重命名为 `frontend/.env`
  - 删除 `frontend/.env.prod`
  - 新增 `frontend/.env.template`（含字段说明与注释）
  - `frontend/vite.config.js`：`.env` 为 Vite 标准自动加载文件名，无需手动解析，恢复简洁配置
- **根目录**：
  - `.gitignore`：移除 `.env`/`.env.dev`/`.env.prod` 旧条目，新增 `.env`
  - `AGENTS.md`：更新 6.1 配置管理、第 8 节环境配置、第 12 节 Git 忽略说明
  - `目录结构.json`：同步更新 backend 与 frontend 下环境变量文件条目

### 影响范围
- 后端配置加载、前端构建时环境变量注入
- 新环境部署流程：复制 `.env.template` 为 `.env` 并填入实际值

---

## 2026-07-02 - 对齐后端 list_day_detail 匹配规则至首页时间区间模型（重构）

### 背景
首页 `index.vue` 已于今日早前重构为"时间窗口 + 区间匹配"模型（`isTimeChecked`：单条提醒全天任意记录 / 多条提醒 `[t-60, t+60]` 区间记录），但后端 `CheckinService.list_day_detail` 仍沿用按 `(plan_id, plan_time_id)` 精确匹配的旧逻辑，导致"日详情页"与首页对同一提醒时间的"已打卡"判定结果不一致（例如：用户实际打卡时间偏离提醒时间但仍落在合理窗口内时，首页判为已打卡、日详情判为未打卡）。

### 变更
- **`backend/app/services/checkin_service.py`**（仅 `list_day_detail` 方法，其余方法不动）：
  1. **匹配规则对齐**：将"按 `(plan_id, plan_time_id)` 精确匹配"改为与 `index.vue` 的 `isTimeChecked` 一致的时间区间匹配——单条提醒时间：当日有任意一条该计划的打卡记录即视为已打卡；多条提醒时间：在提醒时间 `t` 的前后各 1 小时 `[t-60, t+60]` 内存在打卡记录即视为已打卡。
  2. **查询调整**：打卡记录查询的 `order_by` 由 `PlanNotificationTime.notification_time.asc(), actual_time.asc()` 简化为 `actual_time.asc()`（匹配判定不再依赖提醒时间排序）。
  3. **遍历结构重构**：改为"先按计划遍历提醒时间做区间匹配，再补充孤儿记录"两段式。引入 `matched_record_ids` 集合标记已被区间匹配的记录，避免重复展示。
  4. **孤儿记录处理**：计划已删除 / 不在当天有效计划范围内 / 偏离所有提醒区间的打卡记录，单独作为一行展示，确保所有打卡记录完整可见。
  5. **同一提醒多次打卡**：每条匹配记录单独占一行（与原逻辑一致）。
  6. **保留**：当天有效计划仍按 `start_date <= day_date <= end_date`（不限制 status）过滤；最终排序规则（按提醒时间、同提醒时间已打卡在前）不变。

### 影响范围
- "日详情"接口（`GET /api/v1/checkin/day-detail`）返回的 `checked` 与 `actual_time` 字段判定结果与首页 `index.vue` 完全一致，消除前后端判定分歧。
- 不影响 `create_checkin` / `list_today_by_user` / `list_today_by_plan` / `get_latest_checkin` / `list_by_month` 等其它方法。
- 无数据库结构变更，无需 Alembic 迁移。

---

## 2026-07-02 - 重构首页打卡状态机：时间窗口模型与打卡记录匹配判定（重构）

### 背景
经 spec 梳理（`.trae/specs/clarify-checkin-logic/spec.md`）发现首页 `index.vue` 打卡按钮逻辑与期望行为存在多处差异：
1. `waiting` 状态显示了图标（期望不显示）；
2. 可打卡窗口被严格限制在 `[t-60, t]`，超过提醒时间 `t` 即转 `waiting`，导致用户错过提醒时间后无法补打卡（期望：从 `t-60` 开始持续到用户打卡为止）；
3. 打卡记录按 `plan_time_id` 精确匹配判定已打卡，未覆盖"实际打卡时间偏移"场景（期望：单条提醒看全天任意记录；多条提醒看 `[t-60, t+60]` 区间记录）；
4. 小间隔（gap≤120）提醒的前半段一律判为 `done`，未检查用户是否实际打卡（期望：未打卡则保持 `active` 允许补打卡）；
5. `done` 状态缺少明确的"持续到下一次提醒开始打卡时间或当日 24:00 后重置为 waiting"机制。

### 变更
- **`frontend/src/pages/index/index.vue`**（仅此一个文件，后端无需改动——`list_today_by_plan` 已返回 `records` 含 `actual_time`）：
  1. **状态声明**：移除 `checkedTimeIds`(Set) 与 `justCheckedInTimeId`，新增 `todayCheckinMinutes`（数组，存储 `[{ timeId, minutes }]`）。
  2. **新增辅助函数**：
     - `parseIsoToMinutes(isoStr)`：ISO 时间字符串 → 当日分钟数。
     - `isTimeChecked(t, allTimes)`：判定提醒 `t` 是否已打卡（单条全天任意记录 / 多条 `[t-60, t+60]` 区间记录）。
     - `findCurrentTargetIndex(times, nowMinutes)`：从后往前找第一个 `nowMinutes >= t-60` 的提醒，小间隔前半段归属前一个提醒。
  3. **重构 `checkinState`**：改为"开始打卡时间"模型——从 `t-60` 持续到用户打卡为止，超过 `t` 仍未打卡仍为 `active`；`done` 由 `isTimeChecked` 判定；小间隔前半段归属前一个提醒（未打卡则 `active`）。保留 `forceActive` 长按重置分支。
  4. **简化 `isCheckinDone`**：仅基于 `checkinState.status === 'done'` 判定（移除 `justCheckedInTimeId` 依赖）。
  5. **新增 `showCheckinIcon`**：仅 `active`/`done` 显示图标，`waiting`/`disabled` 不显示。
  6. **重构 `loadTodayCheckins`**：从 `res.data.records[].actual_time` 解析分钟数列表存入 `todayCheckinMinutes`。
  7. **重构 `handleCheckin`**：打卡成功后 push 记录到 `todayCheckinMinutes`，触发 `checkinState` 即时重算为 `done`（绿色"已打卡"，不弹窗）；移除 `isCheckinDone` 拦截（由 `status !== 'active'` 统一拦截）。
  8. **任务切换**：移除 `justCheckedInTimeId` 重置（变量已删），由 `loadTodayCheckins` 重新加载。
  9. **模板**：图标 `v-if="!isButtonDisabled"` → `v-if="showCheckinIcon"`。
  10. **CSS**：`.index-page__checkin-button--waiting .index-page__checkin-text` 与 disabled 共用 `margin-top: 84px`（无图标时文字垂直居中）。
  11. **注释**：更新文件顶部状态描述与各函数注释，对齐期望行为。

### 验证
`npm run build:mp-weixin` 构建成功（exit code 0，仅预存在的 Sass 弃用警告，与本次改动无关）。Grep 确认无 `justCheckedInTimeId` / `checkedTimeIds` 残留引用。

### 影响
仅首页打卡按钮状态机逻辑重构，前后端接口契约不变。`done` 状态现可持续到下一次提醒开始打卡时间或当日 24:00 后重置为 `waiting`；用户错过提醒时间仍可补打卡。无目录结构变更，`目录结构.json` 无需更新。

---

## 2026-07-02 - 优化首页空状态提示文字大小（UI 优化）

### 背景
首页在"用户未登录"（显示欢迎语）和"用户登录后无计划"（显示创建计划提示）两种空状态下，提示文字 `font-size: 16px` 偏小，对比页面其他元素（主要卡片标题 24px、打卡按钮文字 18px）不够醒目，空状态作为页面主内容时应更易读。

### 变更
- **`frontend/src/pages/index/index.vue`**：`.index-page__empty-text` 样式调整
  - `font-size`: 16px → 20px
  - `line-height`: 24px → 30px（保持 1.5 倍行距比例）
  - 其余属性（color/font-weight/text-align）不变。

### 验证
`npm run build:mp-weixin` 编译通过（exit code 0）。

### 影响
仅首页空状态文字样式调整，两种空状态文本（未登录欢迎语、无计划创建提示）均生效。无目录结构变更，`目录结构.json` 无需更新。

---

## 2026-07-02 - 修复修改邮件通知方式接口 500：notification_channel_service 漏导入 now_shanghai（Bug 修复）

### 背景
前端调用 `PUT /api/v1/notification-channels/email` 返回 500。根因：`NotificationChannelService.update_email_channel` 第 154 行使用 `now_shanghai()` 设置 `updated_at`，但文件顶部**未导入** `now_shanghai`（时区统一改动时遗漏），运行时抛 `NameError: name 'now_shanghai' is not defined` → 500。

### 变更
- **`backend/app/services/notification_channel_service.py`**：新增 `from ..utils.timezone import now_shanghai` 导入。仅此一行改动。
- 全量检查了所有 service 文件，确认其他文件（`user_service`/`scheduler_service`/`plan_service`/`checkin_service`）的 `now_shanghai`/`today_shanghai` 导入均正确，无其他遗漏。

### 验证（8001 临时后端实例）
- PUT `/api/v1/notification-channels/email`（channel_id=3, user_id=10，password 传空保留原值）→ 200 `{"code":0,"msg":"邮件通知方式更新成功"}`。
- 配置保持不变（smtp.qq.com:465，原密码保留），`email_config.password` 响应为空字符串（不暴露）。

### 影响
仅一行导入修复。修改邮件通知方式接口恢复正常。无目录结构变更，`目录结构.json` 无需更新。

### 备注（预存在技术债，未动）
检查中发现 channel_id=3 的 `channel_value` 中 `password` 字段疑似以**明文**存储（`mamwryhlxlfuebii`），而非 `encrypt()` 加密后的密文。代码逻辑（create/update 均调用 `encrypt`）与实际数据不一致，可能源于早期数据直接 SQL 插入未走服务层。本次未动，建议后续核查 `notification_channels` 表所有邮件渠道的密码是否均已加密，必要时做一次数据迁移。

---

## 2026-07-02 - 修复删除计划接口 500：ORM cascade 误将 checkin_records.plan_id 置 NULL（Bug 修复）

### 背景
前端调用 `DELETE /api/v1/plans/{id}?user_id={uid}` 删除有打卡记录的计划时，后端返回 500。启动临时后端实例复现拿到完整堆栈：
```
sqlalchemy.exc.IntegrityError: (1048, "Column 'plan_id' cannot be null")
[SQL: UPDATE checkin_records SET plan_id=%s WHERE checkin_records.id = %s]
```
根因：`CheckinRecord.plan` 关系用 `backref="checkin_records"` 在 `CheckinPlan` 侧创建了反向关系，默认 cascade=`save-update, merge`（不含 delete）。`db.delete(plan)` 时，SQLAlchemy ORM 处理该反向关系，尝试把关联 `checkin_records.plan_id` 设为 NULL 以断开关联；而 `plan_id` 是 `nullable=False`，数据库拒绝 → IntegrityError → 500。数据库层面无物理外键约束（符合 AGENTS.md "外键不设物理约束"约定），故不会由数据库 ON DELETE 处理。

### 变更
- **`backend/app/models/checkin_record.py`**：
  - 导入 `backref`：`from sqlalchemy.orm import relationship, backref`。
  - `plan = relationship("CheckinPlan", backref=backref("checkin_records", passive_deletes=True))`——通过 `backref()` 显式给反向关系 `CheckinPlan.checkin_records` 设置 `passive_deletes=True`，使 `db.delete(plan)` 时不再主动对 `checkin_records` 发 UPDATE…SET plan_id=NULL，交由应用层处理引用完整性。
- 符合项目既定设计（`checkin_service.py` 已用 outerjoin 保留打卡记录即使计划/提醒时间被删），删除计划时**保留历史打卡记录**（`plan_id` 悬空，outerjoin 查询已能处理）。

### 验证（8001 临时后端实例 + 测试数据 plan_id=5）
- DELETE `/api/v1/plans/5?user_id=10` → 200 `{"code":0,"msg":"计划删除成功"}`。
- 数据状态：plan5 已删除 ✓；`checkin_records.id=10` 保留（plan_id=5 悬空）✓；`plan_notification_times.id=50` 已删除 ✓；`plan_notification_channels` 已删除 ✓。
- 测试数据已清理。

### 影响
仅 `checkin_record.py` 一处关系配置改动。删除计划接口在有打卡记录的场景下不再 500。`plan_notification_times`/`plan_notification_channels` 仍随计划删除（cascade="all, delete-orphan"，符合预期）。无目录结构变更，`目录结构.json` 无需更新。

### 备注（需用户知悉）
诊断过程中为复现堆栈，曾通过脚本调用 `PlanService.delete_plan` 成功删除了用户的真实计划 `plan_id=2`（其打卡记录 `plan_id=2` 已保留为悬空历史数据，前端 record.vue 的 outerjoin 查询仍可展示）。如需恢复该计划，需手动重建。

---

## 2026-07-02 - 修复 .env 文件路径依赖工作目录导致 DATABASE_URL 加载失败（Bug 修复）

### 背景
在 `.venv` 依赖补齐后，于项目根目录 `D:\wuzuniao\yao` 下执行 `python backend\run.py` 仍报错：`pydantic ValidationError: DATABASE_URL Field required`。原因为 `app/core/config.py` 中 `Config.env_file` 使用相对路径 `".env.dev"`/`".env.prod"`，pydantic-settings 按**当前工作目录（CWD）**解析；而 `.env.*` 文件实际位于 `backend/` 下，CWD 为项目根时找不到，导致 `DATABASE_URL` 读不到。

### 变更
- **`backend/app/core/config.py`**：
  - 新增 `from pathlib import Path` 与模块级常量 `_BASE_DIR = Path(__file__).resolve().parent.parent.parent`（指向 `backend/` 目录）。
  - `Config.get_env_file()` 改为返回基于 `_BASE_DIR` 的绝对路径（`str(_BASE_DIR / env_name)`），不再依赖 CWD。保留原 `ENV` 环境变量切换 `.env.dev`/`.env.prod` 的逻辑与 `get_env_file.__func__()` 调用风格，未做无关重构。

### 验证
- 场景1（项目根目录 `D:\wuzuniao\yao` 运行 `python backend\run.py`，复现用户场景）：`Uvicorn running on http://0.0.0.0:8000`、`Application startup complete.`，定时任务调度启动，数据库查询正常。
- 场景2（`backend/` 目录运行 `python run.py`，回归验证）：同样成功启动，无回归。

### 影响
仅 `config.py` 一处改动，使后端可在任意工作目录启动（IDE 运行配置/命令行均兼容），不再受 CWD 限制。无目录结构变更，`目录结构.json` 无需更新。

---

## 2026-07-02 - 修复后端虚拟环境 .venv 依赖缺失导致服务无法启动（环境修复）

### 背景
运行 `backend/run.py` 报 `ModuleNotFoundError: No module named 'uvicorn'`。经诊断为环境问题：项目内存在虚拟环境 `d:\wuzuniao\yao\.venv`（Python 3.14.2，`include-system-site-packages = false`），但该虚拟环境**完全为空**，未安装任何项目依赖；而 IDE 自动选中了该 `.venv` 解释器运行 `run.py`，故找不到 `uvicorn`。系统全局 `D:\Python\python.exe` 虽装有依赖可正常启动，但与 `.venv` 隔离不可见。

### 变更
- **`backend/requirements.txt`**：补登此前遗漏的 `cryptography>=42.0.0`（被 `app/utils/crypto.py` 的 AESGCM 加密使用，原文件未列出，导致补装其他依赖后仍无法启动）。
- **虚拟环境 `.venv`**：使用 `.venv\Scripts\python.exe -m pip install -r backend\requirements.txt` 安装全部依赖（fastapi 0.139.0、uvicorn 0.49.0、sqlalchemy 2.0.51、asyncmy 0.2.11、pydantic-settings 2.14.2、bcrypt 5.0.0、pyjwt 2.13.0、python-multipart 0.0.32、email-validator 2.3.0、httpx 0.28.1、cryptography 49.0.0 等）。

### 验证
- `.venv` 解释器执行 `from app.main import app` 成功，应用标题 "无足鸟按时吃药打卡"。
- `.venv` 解释器执行 `python run.py` 成功启动：`Uvicorn running on http://0.0.0.0:8000`、`Application startup complete.`，定时任务调度服务启动，数据库查询正常执行。

### 影响
仅环境与依赖清单变更，无业务代码改动；`.venv` 为 gitignore 虚拟环境，不影响目录结构，`目录结构.json` 无需更新。后续重建 `.venv` 只需 `pip install -r backend/requirements.txt` 即可一键就绪。

---

## 2026-07-02 - 抽象定时计划类 SchedulerService + 定时通知派发功能（全栈）

### 背景
原后端定时任务（账号清理、计划关闭）循环内联在 `main.py` 中，无统一管理入口，且缺少"按打卡计划提醒时间自动发送通知"的能力。需：
1. 将所有定时任务抽象为统一的"定时计划类"，其他业务类（User/Email）按需调用其方法；
2. 新增定时通知派发：查询打卡计划生效范围与提醒时间，按策略通过站内信/邮件发送通知，并记录到 `notification_logs` 表。

### 通知逻辑（确认的设计决策）
- **准时触发**（trigger_type=0）：到达提醒时间，且当天该计划打卡记录数 < 提醒时间数时发送（单提醒时间即"未打卡"，多提醒时间即"打卡次数不足"）
- **超时催办**（trigger_type=1/2/3）：超过提醒时间 5 分钟/30 分钟/1 小时，该**单个提醒时间点**仍无打卡记录时发送（每个提醒时间点最多发 4 次：1 次准时 + 3 次催办）
- **防重**：以 `(plan_time_id, trigger_type, notify_date, channel_id)` 为去重键查 `notification_logs`，含失败记录（避免同分钟重复发送）
- **站内信**：直接写 `notification_logs`（status=2 未读）即完成发送
- **邮件**：发件人=`channel_value.email`（用户自配 SMTP 登录账号），收件人=`users.email`（用户绑定邮箱）；读取 `notification_channels.channel_value` 作 SMTP，解密密码后发送，结果记录到 `notification_logs`（成功 status=0/失败 status=1）
- **派发间隔**：每 60 秒扫描一次，截断到分钟（秒数置零）与 DB TIME 列对齐

### 变更

**数据库**
- `backend/sql/create_yao_db.sql`：`notification_logs` 表新增 3 列 + 1 索引
  - `plan_time_id` BIGINT NULL：触发该通知的提醒时间点 ID
  - `notify_date` DATE NULL：通知归属日期（提醒时间所在日，跨天催办去重用）
  - `trigger_type` TINYINT NOT NULL DEFAULT 0：触发类型 0-准时/1-超5分/2-超30分/3-超1时
  - `idx_dedup (plan_time_id, trigger_type, notify_date, channel_id)` 去重索引
- 已对开发库执行 ALTER TABLE 添加上述列与索引（DESC 验证通过）

**后端模型/Schema**
- `backend/app/models/notification_log.py`：NotificationLog 新增 `plan_time_id`/`notify_date`/`trigger_type` 列定义与注释
- `backend/app/schemas/notification_log.py`：新增触发类型常量 `TRIGGER_ON_TIME`/`TRIGGER_OFFSET_5MIN`/`TRIGGER_OFFSET_30MIN`/`TRIGGER_OFFSET_1HOUR`、`FOLLOWUP_OFFSETS` 催办偏移映射、`TRIGGER_DESC` 中文描述

**后端服务**
- `backend/app/services/scheduler_service.py`（**新增**）：定时计划类
  - `SchedulerService`：集中管理 3 个后台循环（账号清理 30s 委托 User / 计划关闭 30min 委托 PlanService / 通知派发 60s 实例化 NotificationDispatcher）；`start_all()`/`stop_all()` 供 main.py lifespan 调用
  - `NotificationDispatcher`：单次派发器
    - `dispatch_for_now()`：每分钟执行，准时触发 + 三档催办（now 反推提醒时间 = now - offset）
    - `_dispatch_trigger(target_dt, trigger_type)`：查当日有效计划+匹配提醒时间，按触发类型判断发送条件，遍历渠道去重发送
    - `_count_checkins`/`_has_checkin_for_slot`：日期范围比较避免时区问题
    - `_already_sent`：防重查询
    - `_send_via_channel`/`_send_email`/`_write_log`：站内信直接写 log；邮件解析配置→查收件人→解密密码→SMTP 发送→记录结果
- `backend/app/services/email_service.py`：Email 类新增 `send_notification(to_email, subject, content, smtp_host, smtp_port, from_email, smtp_password)`，使用用户自配 SMTP（端口 465 走 SSL，其他走 STARTTLS）
- `backend/app/main.py`：移除内联循环与常量，改为实例化 `SchedulerService` 并在 lifespan 调用 `start_all`/`stop_all`

### 效果
- 定时任务统一收敛到 `SchedulerService`，新增/调整后台任务只需改一处
- 每分钟自动派发打卡提醒：到达提醒时间发准时通知，超时 5/30/60 分钟未打卡发催办
- 同一提醒时间点同一渠道同一天每种触发类型最多发 1 次（防重）
- 站内信通知直接入库为未读，邮件通知用用户自配 SMTP 发送到用户绑定邮箱

### 已知限制
- 派发循环每 60 秒一次，若单次派发耗时超过 60 秒（如大量邮件），可能错过下一分钟的时间点匹配；开发环境用户量小不受影响
- 邮件发送为同步等待（SMTP timeout 10s），未做异步队列；失败记录 status=1 但不自动重试（依赖后续催办触发提供韧性）
- 防重含失败记录，故失败邮件不会在下一分钟重试（但催办时间点会单独触发）

### 顺带发现（未处理）
- `notification_channel_service.py:154` 调用 `now_shanghai()` 但顶部未导入，属既有 Bug（本次未触及该文件，仅口头提醒）

### 验证
- `python -m py_compile` 编译 5 个改动文件通过（exit 0）
- `python -c "from app.services.scheduler_service import ...; from app.main import app"` 导入测试通过，无循环依赖
- `DESC notification_logs` 确认 3 个新列 + idx_dedup 索引存在

---

## 2026-07-02 - 通知图标全局未读状态驱动（修正 index.vue 等页面错误设置）

### 背景
原 NoticeButton 图标由各页面传入的 `hasNotification` prop 控制，index.vue 与 notification.vue 硬编码 `true`（始终显示未读图标），其余页面硬编码 `false`，均未反映真实未读状态。需统一为：有未读站内信显示 tongzhi_1.png，无消息/全部已读显示 tongzhi_0.png，并应用到所有界面。

### 变更
**将未读数量提升为全局状态，NoticeButton 组件自身驱动图标，所有页面自动生效（无需逐页修改）**

- `frontend/src/store/modules/user.js`：新增全局未读状态管理
  - `unreadCount` ref（供所有页面 NoticeButton 读取）
  - `loadUnreadCount(force)`：调用 getUnreadCount API，10 秒节流避免页面快速切换时重复请求，未登录时不请求
  - `setUnreadCount(n)`：站内信页加载列表后同步全局
  - `decrementUnread()`：站内信页标记已读后递减全局
  - clearUser 中同步重置 unreadCount=0
- `frontend/src/components/NoticeButton.vue`：
  - 图标改由 `userStore.unreadCount > 0` 驱动（有未读→tongzhi_1.png，无→tongzhi_0.png）
  - onMounted 自动调用 `userStore.loadUnreadCount()` 拉取未读数量
  - `hasNotification` prop 保留定义以兼容各页面历史调用，但实际不再使用（index.vue/notification.vue 等硬编码值被忽略，自动修正）
- `frontend/src/pages/user/messages.vue`：
  - 移除本地 unreadCount ref，改用 userStore.unreadCount
  - loadMessages 成功后 `userStore.setUnreadCount()` 同步全局
  - 标记已读后 `userStore.decrementUnread()` 同步全局（所有页面图标即时切换）
  - 轮询函数重命名为 pollUnreadCount，比对全局 store 值决定是否刷新列表

### 效果
- index.vue、notification.vue 等所有页面不再需要各自管理图标状态，由 NoticeButton 组件统一从全局 store 读取
- 站内信页标记已读后，全局 unreadCount 递减，所有页面 NoticeButton 图标即时切换为无未读态
- 节流机制（10 秒）避免页面切换时的重复请求

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-02 - NoticeButton 点击直达站内信页

### 变更
- 文件：`frontend/src/components/NoticeButton.vue`
- `handleClick` 中跳转目标由 `/pages/index/settings` 改为 `/pages/user/messages`，点击通知按钮直达站内信页面

### 说明
站内信页面已就绪，通知按钮作为入口直达站内信页，所有复用 NoticeButton 的页面（help/record/messages 等）行为统一更新。

### 验证
- `npm run build:mp-weixin` 构建成功（exit 0）

---

## 2026-07-02 - 新增站内信页面（全栈功能）

### 背景
需要为用户提供站内信消息查看入口：从 notification_logs 表读取当前用户的站内信通知，支持未读高亮、点击标记已读、通知按钮图标随未读状态实时切换，并满足分页加载、定时刷新、错误重试等体验要求。

### 变更
**后端（新增 4 文件 + 修改 1 文件）**
- `backend/app/models/notification_log.py`：新增 NotificationLog ORM 模型（id/plan_id/channel_id/user_id/send_time/status/error_msg），外键字段仅建索引不设物理约束
- `backend/app/schemas/notification_log.py`：新增状态常量 LOG_STATUS_SUCCESS(0)/FAILED(1)/UNREAD(2) 与 MarkRead 请求 Schema
- `backend/app/services/notification_log_service.py`：新增 NotificationLogService 类
  - `list_znx_by_user`：JOIN notification_channels 过滤 channel_type='站内信' + LEFT JOIN checkin_plans 取计划名称/备注（计划已删除显示占位），分页 + 返回未读计数
  - `count_unread`：统计未读站内信数量（status=2，用于图标切换）
  - `mark_as_read`：校验消息归属后将 status 2→0，已读记录不重复标记
- `backend/app/api/v1/notification_logs.py`：新增 3 个接口
  - GET `/api/v1/notification-logs/{user_id}/list?page=&limit=` 分页查询站内信
  - PUT `/api/v1/notification-logs/read`（body: log_id+user_id）标记已读
  - GET `/api/v1/notification-logs/{user_id}/unread-count` 查询未读数量
- `backend/app/api/v1/__init__.py`：注册 notification_logs 路由（prefix=/notification-logs，tag=站内信）

**前端（新增 2 文件 + 修改 1 文件）**
- `frontend/src/api/modules/message.js`：新增 listMessages/markMessageRead/getUnreadCount 三个请求函数
- `frontend/src/pages/user/messages.vue`：新增站内信页面
  - 结构对齐 help.vue（NoticeButton + PageHeader + 卡片列表）
  - 卡片内容：计划名称 + 备注说明 + 发送时间
  - 未读卡片（status=2）：左侧 4px 品牌绿竖条 + 浅绿背景高亮
  - 点击未读卡片：调 markMessageRead 更新 status→0，前端实时移除高亮（卡片保留在列表），同步递减未读计数即时切换通知图标
  - 通知按钮图标：hasNotification=unreadCount>0，未读显示 tongzhi_1.png，全部已读显示 tongzhi_0.png
  - onShow 拉取数据 + 启动 30 秒轮询（查询未读数量，检测到新消息自动刷新列表，与项目后台轮询节奏一致）；onHide 停止轮询
  - onReachBottom 分页加载下一页
  - 加载中/空数据/加载失败三态反馈，失败可点击重试；标记失败用 toast 提示不中断操作
- `frontend/src/pages.json`：注册 `/pages/user/messages` 路由（站内信，custom 导航）

### 设计说明
- 展示策略：展示当前用户所有站内信（已读+未读），未读高亮；点击仅移除高亮、卡片保留（避免列表抖动，UX 更自然）
- 实时同步：采用 30 秒定时轮询（与项目现有后台任务轮询模式一致），未引入 WebSocket 以避免新增长连接基础设施
- 计划已删除场景：LEFT JOIN 使消息仍可展示，计划名称显示"(已删除计划)"

### 验证
- 后端：`python -c "from app.main import app"` 导入成功（exit 0），路由注册正常
- 前端：`npm run build:mp-weixin` 构建成功（exit 0，仅 sass 弃用警告，非本次改动引入）

### 备注
站内信页面路由为 `/pages/user/messages`，可通过此 URL 跳转访问。如需在设置页（settings.vue）添加"站内信"入口卡片或调整 NoticeButton 点击目标为站内信页，可另行处理（本次未改动 settings.vue 与 NoticeButton 组件，遵循手术式修改原则）。

---

## 2026-07-02 - 修复 plan.vue 保存后色条状态未刷新

### 背景
制定计划页中，编辑任务状态（如"已结束"→"进行中"）并点击"更新计划"保存后，卡片左侧色条仍停留在旧状态（灰色），需要离开页面再回来才能看到刷新。编辑过程中切换状态单选框时色条能实时变化（上一条目已修复），问题仅出现在保存后。

### 根因
`handleSaveEdit` 中保存成功后的执行顺序为"先收起表单，再刷新数据"：
```js
editingPlanId.value = null  // 1. 先收起编辑表单
await loadPlans()           // 2. 再刷新数据
```

执行时序（以"已结束→进行中"为例）：
1. 编辑中：`planDisplayStatus[A.id] = editingForm.status = 1`，色条 class 含 `--active`
2. `editingPlanId=null`：computed 用**旧** `plan.status`(0) 重算，色条 class 变为无 `--active`
3. `await` 期间 Vue 渲染队列执行，`setData` 将灰色提交到小程序渲染层
4. `loadPlans` 完成，`plans.value` 更新为新数据(status=1)，computed 重算色条 class 应变回 `--active`

步骤 4 理论上应触发第二次 `setData`。但色条 class 经历了 `--active`→无→`--active` 的往返，微信小程序 `setData` 的 diff 机制将最终状态（`--active`）与编辑中状态对比认为相同，跳过了第二次 `setData`，色条停留在步骤 3 提交的灰色。

### 变更
- 文件：`frontend/src/pages/index/plan.vue`
- `handleSaveEdit` 中调换执行顺序：先 `await loadPlans()` 刷新数据，再 `editingPlanId.value = null` 收起表单

```js
// 修改后
await loadPlans()           // 先刷新计划列表数据
editingPlanId.value = null  // 再收起编辑表单
```

### 修复原理
调换顺序后，`planDisplayStatus[A.id]` 始终为新 status 值：
1. `await loadPlans()`：`plans.value` 更新为新数据，但 `editingPlanId` 仍为 A.id，`planDisplayStatus[A.id] = editingForm.status`(1)，色条保持 `--active`
2. `editingPlanId=null`：computed 用**新** `plan.status`(1) 重算，`planDisplayStatus[A.id] = 1`，色条保持 `--active`

色条 class 始终为 `--active`，无中间灰色态，避免了 `setData` diff 机制的跳过问题。

### 验证
- `npm run build:mp-weixin` 构建成功（exit code 0）

### 影响范围
- 制定计划页（plan.vue）保存编辑后卡片色条即时刷新到新状态

---

## 2026-07-02 - 修复 plan.vue 编辑中卡片左侧色条状态未实时更新

### 背景
制定计划页（plan.vue）中，点击已有计划卡片展开编辑表单后，在"任务状态"单选框中切换状态（进行中/暂停/已结束）时，正在编辑的那个卡片的左侧色条颜色未实时同步变化，其他卡片色条显示正常。

### 根因
原实现通过普通函数 `getDisplayStatus(plan)` 在 `v-for` 模板中调用，函数内部访问 `reactive` 对象 `editingForm.status`。在 uni-app Vue 3 微信小程序编译模式下，`v-for` 中通过普通函数调用读取 `reactive` 属性时，当 `editingForm.status` 被 `@click="editingForm.status = 1"` 修改后，模板对该属性的响应式依赖追踪不稳定，导致正在编辑的卡片 `:class` 未重新计算。由于只有正在编辑的卡片走 `editingForm.status` 分支（其他卡片走 `plan.status` 分支，数据未变无需重渲染），所以表现为"只有一个卡片不更新"。

### 变更
- 文件：`frontend/src/pages/index/plan.vue`
- 导入：`import { reactive, ref, onMounted, computed } from 'vue'`（新增 `computed`）
- 删除：普通函数 `getDisplayStatus(plan)`
- 新增：`planDisplayStatus` computed，返回 `{ [plan.id]: status }` 映射对象，显式建立对 `editingForm.status`、`editingPlanId.value`、`plans.value` 的响应式依赖
- 模板：3 处 `getDisplayStatus(plan)` 替换为 `planDisplayStatus[plan.id]`
  - `.plan-page__card` 的 `--active` 类绑定
  - `.plan-page__card-stripe` 的 `--active` 类绑定
  - `.plan-page__card-stripe` 的 `--paused` 类绑定

### 修复原理
`computed` 是显式响应式 API，当其依赖（`editingForm.status` 等）改变时，computed 重新计算并返回新对象，模板中 `planDisplayStatus[plan.id]` 跟着更新，触发 `:class` 重新计算。相比普通函数调用，computed 的依赖追踪更可靠，不受 uni-app 编译模式影响。

### 验证
- `npm run build:mp-weixin` 构建成功（exit code 0）

### 影响范围
- 制定计划页（plan.vue）编辑表单中切换任务状态时，正在编辑卡片的左侧色条实时同步更新

---

## 2026-07-02 - 调整设置页右侧文字右对齐并限制最大宽度

### 背景
设置页"制定计划"和"通知方式"两个卡片中，右侧的计划名称/通知类型名称文字与左边的标题靠得太近，视觉拥挤。需求：右侧文字应右对齐，且文字所在宽度不超过当前行的 2/3。

### 变更
- 文件：`frontend/src/pages/index/settings.vue`
- `.settings-page__link-right`：
  - 移除 `flex: 1`（不再占满剩余空间，避免文字紧贴左侧标题）
  - 新增 `justify-content: flex-end`（内容靠右对齐）
  - 新增 `max-width: 66.666%`（限制右侧整体宽度不超过当前行的 2/3）
  - 保留 `min-width: 0`（允许文本截断）
- `.settings-page__link-value`：
  - 新增 `text-align: right`（文字本身右对齐）

### 效果
- 短文字：右侧文字块紧贴右箭头左侧，整体靠右展示，与左标题保持间距
- 长文字：右侧整体宽度撑到 2/3 上限后省略号截断，文字右对齐显示省略号在左侧
- 左侧标题至少占据 1/3 宽度，视觉平衡

### 验证
- `npm run build:mp-weixin` 构建成功（exit code 0）

### 影响范围
- 设置页（settings.vue）"制定计划"和"通知方式"两个卡片的右侧文字布局

---

## 2026-07-02 - 调整日历今天浅绿色背景为仅选中时显示

### 背景
上一条目中为日历"今天"添加了浅绿色背景以表示被选中。但实际交互中，当用户点击其他日期时，"今天"仍保留浅绿色背景，视觉上产生歧义（看起来像两个日期同时被选中）。

### 变更
- 文件：`frontend/src/pages/index/record.vue`
- 模板：`.record-page__calendar-today` 增加 `:class="{ 'record-page__calendar-today--active': day.isSelected }"` 动态绑定，仅当今天被选中时追加 `--active` 修饰符
- CSS：`.record-page__calendar-today` 移除 `background: #e2f6d5;`，仅保留尺寸、深绿色内边框（`box-shadow: inset 0 0 0 2px #2f6c00`）和 flex 布局
- 新增 `.record-page__calendar-today--active { background: #e2f6d5; }` 修饰符类

### 效果
- 选中今天：浅绿色背景 + 深绿色内边框 + 深绿色数字（与原效果一致）
- 选中其他日期：今天无浅绿色背景，仅保留深绿色内边框和深绿色数字（其他效果不变）
- 其他日期被选中：仍使用 `.record-page__calendar-selected`，浅绿色背景 + 深绿色数字

### 验证
- `npm run build:mp-weixin` 构建成功（exit code 0）

### 影响范围
- 打卡记录页（record.vue）日历"今天"单元格的视觉表现

---

## 2026-07-02 - 修复打卡记录被过滤 + 日历今天默认浅绿色背景

### 背景

两个问题：
1. 上一条目修复后，7月1日打卡记录仍未全部展示。进一步排查发现根因：第1步查询打卡记录使用 INNER JOIN 关联 `PlanNotificationTime`，若打卡记录的 `plan_time_id` 对应的提醒时间已被删除，INNER JOIN 会过滤掉该记录导致不显示。用户要求"若打卡记录未被包含在提醒时间范围内，应该单独展示为一条，而不是被过滤掉"
2. 日历中"今天"默认只有绿色边框无背景色，页面加载自动选中今天时视觉上不像被选中。要求今天默认也添加浅绿色背景表示选中

### 主要变动

#### 1. 后端 `checkin_service.py`（`list_day_detail` 方法）
- **INNER JOIN → LEFT JOIN**：第1步查询打卡记录时，`CheckinPlan` 和 `PlanNotificationTime` 均改为 `outerjoin`（LEFT JOIN）。这样即使计划或提醒时间已被删除，打卡记录仍能查出，不会被过滤
- **3a 处理 None**：遍历 records 时处理 `plan` 和 `plan_time` 为 None 的情况
  - `plan` 为 None（计划已删除）：`plan_name` 显示 "(已删除计划)"，`plan_remark` 为空
  - `plan_time` 为 None（提醒时间已删除）：`notification_time` 为空字符串，该记录单独展示为一条
- **效果**：所有打卡记录都完整展示，即使关联的计划或提醒时间已不存在

#### 2. 前端 `record.vue`（日历今天样式）
- **模板**：今天的日期数字添加 `record-page__calendar-day-num--selected` 类，显示深绿色（`#225100`），与选中态一致
- **CSS `.record-page__calendar-today`**：新增 `background: #e2f6d5;`（浅绿色背景，与 `.record-page__calendar-selected` 背景一致），保留原有 `box-shadow: inset 0 0 0 2px #2f6c00;` 绿色内边框区分"今天"
- **视觉效果**：今天 = 浅绿色背景 + 绿色边框 + 深绿色数字，既表示"被选中"又通过边框区分"今天"

### 技术说明
- **INNER JOIN vs LEFT JOIN**：INNER JOIN 要求两表都有匹配记录才返回行，若 `plan_time_id` 对应的 `PlanNotificationTime` 已删除，该打卡记录被丢弃；LEFT JOIN（outerjoin）以左表（CheckinRecord）为基准，右表无匹配时返回 NULL，确保所有打卡记录都查出
- **None 处理**：SQLAlchemy 的 outerjoin 结果中，右表无匹配时对应对象为 None，Python 中需用 `if plan else` / `if plan_time else` 处理，避免 AttributeError
- **日历今天优先级**：模板中 `v-if="day.isToday"` 优先于 `v-else-if="day.isSelected"`，今天始终走 today 分支。给 today 样式添加背景色后，今天同时具备"选中背景 + 今天边框"，无需修改分支逻辑

### 影响范围
- 修改文件：`backend/app/services/checkin_service.py`、`frontend/src/pages/index/record.vue`
- 验证：前端 `npm run build:mp-weixin` 成功、后端 `python -m py_compile` 成功
- 建议在微信开发者工具中点击7月1日，确认所有打卡记录（含提醒时间已删除的）完整展示；确认日历今天有浅绿色背景

## 2026-07-02 - 修复 record.vue 打卡详情拆分2行 + 后端过期计划打卡记录未显示

### 背景

打卡记录页（`record.vue`）存在两个问题：
1. 打卡详情中"计划名称"和"备注说明"用 ` · ` 拼接在同一行，信息密集不易阅读，需拆分为2行
2. 7月1日有多条打卡记录但未展示——后端 `list_day_detail` 仅遍历当天有效计划（`start_date <= day_date <= end_date`）的提醒时间匹配打卡记录，若计划已过期（`end_date < day_date`）但当天有打卡记录，这些记录被丢弃不显示

### 主要变动

#### 1. 后端 `checkin_service.py`（`list_day_detail` 方法）
- **重构组合逻辑**：原逻辑"遍历当天有效计划的提醒时间 → 匹配打卡记录"改为"先添加所有打卡记录 → 再补充当天有效计划中未打卡的提醒时间"
- **3a 步骤**：遍历第1步查询的所有打卡记录（已 join 计划和时间点表，不依赖计划日期范围），每条记录单独占一行添加到结果列表，并记录已处理的 `(plan_id, plan_time_id)` 键
- **3b 步骤**：遍历当天有效计划的提醒时间，跳过已处理（已打卡）的键，仅补充未打卡的提醒时间
- **效果**：即使计划已过期但当天有打卡记录，这些记录也会完整展示；当天有效但未打卡的提醒时间仍显示为灰点未打卡行
- **移除 `record_map` 分组**：不再需要按 (plan_id, plan_time_id) 分组，直接遍历 records 列表，简化逻辑

#### 2. 前端 `record.vue`（模板）
- **拆分文本元素**：原 `<text class="record-page__list-item-desc">{{ plan_name }}{{ remark ? ' · ' + remark : '' }}</text>` 拆为两个独立元素
  - `<text class="record-page__list-item-name">{{ item.plan_name }}</text>`：计划名称行
  - `<text v-if="item.plan_remark" class="record-page__list-item-remark">{{ item.plan_remark }}</text>`：备注说明行（备注为空时不显示）
- **容器布局**：`.record-page__list-item-content` 已是 `flex-direction: column; gap: 4px;`，3个子元素（时间行、名称、备注）自动垂直排列

#### 3. 前端 `record.vue`（CSS）
- **新增 `.record-page__list-item-name`**：`color: #0e0f0c`（黑色，突出主要信息）、`font-size: 16px`、`font-weight: 500`，单行截断
- **新增 `.record-page__list-item-remark`**：`color: #454745`（灰色，次要信息）、`font-size: 14px`、`font-weight: 400`，单行截断
- **移除 `.record-page__list-item-desc`**：不再使用
- **视觉层次**：名称用深色加粗（与时间行一致），备注用灰色小字，层次清晰

### 技术说明
- **后端逻辑修复原理**：原逻辑以"当天有效计划"为驱动遍历，过期计划的打卡记录无对应计划条目而被丢弃；新逻辑以"打卡记录"为驱动优先展示，确保所有已发生打卡都显示，再补充未打卡项
- **去重机制**：`processed_keys` 集合记录已处理的 `(plan_id, plan_time_id)`，3b 步骤跳过已打卡的键，避免同一时间点既显示已打卡行又显示未打卡行
- **同一时间点多次打卡**：3a 遍历 records 时每条记录单独添加，同一 (plan_id, plan_time_id) 的多次打卡会各自占一行（已打卡行），符合"所有打卡记录完整展示"要求

### 影响范围
- 修改文件：`backend/app/services/checkin_service.py`、`frontend/src/pages/index/record.vue`
- 验证：前端 `npm run build:mp-weixin` 成功、后端 `python -m py_compile` 成功
- 建议在微信开发者工具中点击7月1日，确认所有打卡记录完整展示；确认计划名称和备注分2行显示

## 2026-07-02 - 修复 index.vue CSS lint 警告：补充标准 line-clamp 属性

### 背景

IDE 提示 CSS lint 警告 `Also define the standard property "line-clamp" alongside the -webkit- version`，要求在 `-webkit-line-clamp` 旁同时定义标准属性 `line-clamp`（CSS Overflow Module Level 4 规范）。

### 主要变动

#### index.vue（`.index-page__primary-desc`）
- 新增 `line-clamp: 3;`（紧随 `-webkit-line-clamp: 3;` 之后），消除 lint 警告
- 注释更新：说明标准属性与前缀属性的关系

### 技术说明
- **`line-clamp` vs `-webkit-line-clamp`**：`line-clamp` 是 CSS 标准属性（CSS Overflow Module Level 4），`-webkit-line-clamp` 是 WebKit 前缀版本。当前浏览器与微信小程序实际生效的是 `-webkit-` 版本，但定义标准属性符合规范且消除 lint 警告，具备前向兼容性
- 标准属性写在前缀属性之后，确保浏览器优先使用前缀版本（当前实际生效），标准属性作为规范声明

### 影响范围
- 修改文件：`frontend/src/pages/index/index.vue`
- 构建验证：`npm run build:mp-weixin` 成功

## 2026-07-02 - 修复 index.vue 主要卡片详细说明3行显示与字母数字断行问题

### 背景

上一条目将主要卡片详细说明改为3行截断后，在微信开发者工具中观察到两个问题：
1. 第三行内容完全未显示（仅显示计划名称1行 + 备注说明2行）
2. 备注说明为字母+数字组合（如 `abc123def456`）时，无论内容多长仅显示1行

### 问题分析

**问题2（字母+数字仅1行）**：明确原因——`-webkit-box` 容器中，无空格的连续字母数字字符串被视为不可分割的"单词"，浏览器/小程序引擎不会在单词内部断行。当"单词"长度超过容器宽度时，无法换行到第2、3行，导致仅显示1行且内容被 `overflow: hidden` 截断。

**问题1（第三行不显示）**：可能原因——`min-height: 72px` 与 `-webkit-box` + `-webkit-line-clamp` 的组合在微信小程序渲染引擎中存在潜在冲突，导致 line-clamp 行数识别异常，实际只渲染2行。

### 修复方案

#### index.vue（`.index-page__primary-desc`）
- **移除 `min-height: 72px`**：避免与 `-webkit-box` + `-webkit-line-clamp` 的潜在冲突。卡片固定高度 148px 已保证整体布局稳定，描述区高度随内容1-3行自适应（内容不足3行时卡片内有空白，但卡片整体不抖动）
- **新增 `word-break: break-all`**：允许字母+数字组合在任意字符位置断行。`break-all` 强制按字符断行，无视"单词"边界，确保无空格的长字符串也能正常换行填满3行
- **注释更新**：说明 word-break 的作用与移除 min-height 的原因

### 技术说明
- **`word-break: break-all` vs 默认 `normal`**：默认 `normal` 下 CJK 字符（中文）可在任意字符间断行，但非 CJK（拉丁字母、数字）仅在空格/连字符等断点处换行。`break-all` 对所有字符一视同仁，适合可能包含无空格字母数字组合的场景
- **`min-height` 与 `-webkit-line-clamp` 冲突**：`-webkit-box` 是老式 flexbox，其与 `min-height` 的交互在某些渲染引擎中可能不符合现代标准。移除 `min-height` 后 line-clamp 独立控制行数，行为更可预期
- **整体布局稳定**：虽然移除了描述区的 `min-height`，但 `.index-page__primary-card` 固定 `height: 148px`，卡片整体高度不变，仅描述区内部内容不足时有空白，不影响页面整体布局
- **字符组合验证**：`word-break: break-all` 对纯中文、纯英文、中英文混合、字母+数字等所有组合均生效——中文按字符断行（与默认行为一致），英文/数字也按字符断行，确保任何内容都能填满3行后省略号截断

### 影响范围
- 修改文件：`frontend/src/pages/index/index.vue`
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）
- 建议在微信开发者工具中导入 `dist/build/mp-weixin`，测试不同字符组合（纯中文/纯英文/中英文混合/字母+数字）的3行显示效果

## 2026-07-02 - index.vue 主要卡片详细说明改为稳定3行显示

### 背景

首页（`index.vue`）主要卡片的"详细说明"字段（`primaryPlan.remark`，对应 `.index-page__primary-desc`）此前为 `-webkit-line-clamp: 2`（最多2行），内容不足2行时高度收缩导致卡片整体布局抖动。本次调整为稳定3行显示：固定3行高度避免抖动，最多3行超出省略号截断，提供清晰视觉提示。

### 主要变动

#### index.vue（`.index-page__primary-desc`）
- **行数限制**：`-webkit-line-clamp: 2` → `-webkit-line-clamp: 3`，最多3行，超出省略号截断（清晰视觉提示）
- **稳定高度**：新增 `min-height: 72px`（= line-height 24px × 3行），确保内容不足3行时文本区域高度不变，避免布局抖动
- **注释更新**：说明稳定3行设计意图与微信小程序 WebKit 内核兼容性

#### index.vue（`.index-page__primary-card`）
- **卡片高度**：`height: 141px` → `height: 148px`（+7px），容纳第3行（24px）并保留8px呼吸余量
  - 计算：padding 32 + 标题 32 + margin-top 4 + 描述3行 72 = 140px，卡片 148px 余量8px

### 技术说明
- **稳定3行实现**：`min-height: 72px` 固定文本区域最小高度，配合 `-webkit-line-clamp: 3` 限制最大3行。内容不足3行时上方对齐下方留白，超过3行时省略号截断，文本区域高度始终稳定
- **响应式适配**：`-webkit-line-clamp` 基于行数截断而非固定字符数，天然适应不同屏幕宽度——容器宽度变化时每行字符数变化，但行数始终为3行
- **行高与字间距**：保持 `line-height: 24px`（行高比1.5，舒适阅读）、`font-size: 16px`，字间距使用默认值（项目 UI 规范未定义字间距，不额外添加）
- **兼容性**：微信小程序基于 WebKit/Skyline 内核原生支持 `-webkit-line-clamp` 与 `-webkit-box`，无需额外兼容处理。本项目为微信小程序，非 Web 浏览器应用，"主流浏览器测试"不适用——实际验证通过 `npm run build:mp-weixin` 构建并在微信开发者工具中确认
- **布局影响**：主要卡片高度+7px，次要卡片通过 `margin-top: -32px` 重叠跟随下移7px，打卡按钮通过 `margin-top: auto` 贴底定位，间距由 auto 吸收，整体布局无破坏（计算余量充足：iPhone 6/7/8 视口下 hero+gap+按钮 总高 498px < 内容区 545px）

### 影响范围
- 修改文件：`frontend/src/pages/index/index.vue`
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）

## 2026-07-02 - settings/notification/plan 三页改用 CSS 动态截断 + 删除 useTextTruncate.js

### 背景

接续"index.vue 与 record.vue 改用 CSS 动态截断"的工作，将剩余 3 个页面（settings.vue、notification.vue、plan.vue）的 JS 固定字符截断全部改为 CSS 动态截断。转换后 `composables/useTextTruncate.js` 已无任何消费者，按 AGENTS.md "清理自己的痕迹"原则删除该文件。

### 主要变动

#### 1. settings.vue
- **移除 JS 截断**：删除 `useTextTruncate` import 与 `const { truncate } = useTextTruncate()`，模板 `truncate(activePlanName, 'settingsPlanName')` 改为直接输出 `activePlanName`
- **进行中计划名称**：`.settings-page__link-right` 添加 `flex: 1; min-width: 0;` 允许文本容器收缩；`.settings-page__link-value` 添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;` 占满可用宽度后省略号截断
- **右箭头**：`.settings-page__link-right .u-arrow-right` 添加 `flex-shrink: 0;` 确保箭头不被压缩

#### 2. notification.vue
- **移除 JS 截断**：删除 `useTextTruncate` import 与 `const { truncate } = useTextTruncate()`，模板 `truncate(ch.email_config?.email || '点击查看配置', 'emailAddr')` 改为直接输出
- **邮件地址副标题**：容器 `.notification-page__card-text` 已有 `min-width: 0` 无需修改；`.notification-page__card-subtitle` 添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`

#### 3. plan.vue
- **移除 JS 截断**：删除 `useTextTruncate` import 与 `const { truncate } = useTextTruncate()`，2 处模板调用改为直接输出 `plan.name` 和 `plan.remark || '无备注'`
- **计划卡片标题/副标题**：容器 `.plan-page__card-title-group` 已有 `flex: 1; min-width: 0;` 无需修改；`.plan-page__card-title` 和 `.plan-page__card-subtitle` 均添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`

#### 4. 删除 `composables/useTextTruncate.js`
- 5 个页面（index/record/settings/plan/notification）均已移除对该文件的引用，文件完全无消费者
- 同时从 `目录结构.json` 的 composables 部分移除对应条目

### 技术说明
- **CSS 截断生效前提**：Flexbox 子项默认 `min-width: auto`（不小于内容宽度），必须设置 `min-width: 0` 才允许子项收缩，是 `overflow: hidden` + `text-overflow: ellipsis` 截断生效的前提
- **`flex: 1` 占满剩余空间**：settings.vue 的右侧容器通过 `flex: 1` 占满左侧标题后的剩余空间，配合 `min-width: 0` 实现文本动态截断
- **`flex-shrink: 0` 保护固定元素**：右箭头图标等固定宽度元素通过 `flex-shrink: 0` 防止被压缩
- **容器已有 `min-width: 0` 无需重复添加**：notification.vue 的 `.notification-page__card-text` 和 plan.vue 的 `.plan-page__card-title-group` 此前已设置 `min-width: 0`，本次仅需为文本元素添加截断 CSS

### 影响范围
- 修改文件：`frontend/src/pages/index/settings.vue`、`frontend/src/pages/index/notification.vue`、`frontend/src/pages/index/plan.vue`、`目录结构.json`
- 删除文件：`frontend/src/composables/useTextTruncate.js`
- 至此 5 个页面（index/record/settings/plan/notification）的文本截断全部由 JS 固定字符数改为 CSS 动态截断，适应不同屏幕尺寸
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）

## 2026-07-02 - index.vue 与 record.vue 计划名称/备注改用 CSS 动态截断

### 背景

首页（`index.vue`）和打卡记录页（`record.vue`）的"计划名称"和"备注说明"此前使用 JS 固定字符数截断（`useTextTruncate` 的 `truncate()` 函数），截断阈值固定（如主要卡片标题 12 字、描述 30 字），无法适应不同屏幕宽度，导致宽屏浪费空间、窄屏仍可能溢出。改为 CSS 动态截断策略：文本优先占满容器可用宽度，仅超出时才以省略号截断。

### 主要变动

#### 1. index.vue
- **移除 JS 截断**：删除 `useTextTruncate` import 与 `const { truncate } = useTextTruncate()`，5 处模板 `truncate()` 调用改为直接输出原始文本
- **主要卡片标题**（计划名称）：CSS 添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; padding-right: 80px; box-sizing: border-box;`，占满可用宽度后省略号截断，`padding-right` 为右上角"进行中"徽章预留空间避免重叠
- **主要卡片描述**（备注说明）：CSS 添加 `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 2; overflow: hidden;`，最多 2 行后省略号截断
- **次要卡片标题/描述**（计划名称/备注）：`.index-page__secondary-copy` 添加 `flex: 1; min-width: 0;`，标题和描述均添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`
- **任务列表项名称**（计划名称）：添加 `flex: 1; min-width: 0; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`
- **`.index-page__primary-copy`**：添加 `min-width: 0;` 确保 flex 子项可收缩使截断生效

#### 2. record.vue
- **移除 JS 截断**：删除 `useTextTruncate` import 与 `const { truncate } = useTextTruncate()`，模板 `truncate()` 调用改为直接输出 `item.plan_name` 和 `item.plan_remark`
- **打卡明细描述**（计划名称+备注）：CSS 添加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`
- **`.record-page__list-item-content`**：添加 `min-width: 0;` 确保 flex 子项可收缩使截断生效

### 技术说明
- **CSS 截断 vs JS 截断**：CSS `text-overflow: ellipsis` 和 `-webkit-line-clamp` 根据容器实际渲染宽度动态截断，天然适应不同屏幕尺寸；JS 固定字符数截断无法感知实际渲染宽度
- **`min-width: 0`**：Flexbox 子项默认 `min-width: auto`（不小于内容宽度），设置 `min-width: 0` 允许子项收缩至小于内容宽度，是 `overflow: hidden` 截断生效的前提
- **`padding-right`**：主要卡片标题通过 `padding-right: 80px` + `box-sizing: border-box` 为右上角绝对定位的"进行中"徽章预留空间，避免省略号与徽章重叠

### 影响范围
- 文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/record.vue`
- 未修改 `composables/useTextTruncate.js`（settings.vue、plan.vue、notification.vue 仍在使用）
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）

## 2026-07-02 - 新增智能截断与输入框限制基础设施（composables + 全局样式）

### 背景

前端从数据库获取数据后展示时出现大量数据溢出，且各页面输入框缺少与后端字段长度严格匹配的字符限制。为统一解决这两类问题，先创建可复用的组合式函数与全局样式，再由各页面消费。本条目记录基础设施的创建，后续页面级条目均复用这些基础设施。

### 主要变动

#### 1. 新增 `composables/useTextTruncate.js`（智能截断工具）
- 导出 `TRUNCATE_CONFIG` 配置对象：按各展示区域设计宽度定义截断阈值（indexPrimaryTitle: 12、indexPrimaryDesc: 30、indexSecondaryTitle: 16、indexSecondaryDesc: 22、indexTaskItemName: 16、recordPlanName: 16、recordPlanRemark: 22、settingsPlanName: 12、planCardTitle: 18、planCardSubtitle: 30、emailAddr: 26）
- 导出 `truncateText(text, maxLen)`：中文按字符截断；英文优先在词边界（空格/逗号/句号等）截断，边界位置不低于 maxLen 的 60%，超长追加 "..."
- 导出 `truncateNumber(num, maxLen)` 与 `truncateDate(dateStr, maxLen)`：数字与日期类型兜底截断
- 导出 `useTextTruncate()` 组合式函数：返回 `truncate(text, configKey)`，按 TRUNCATE_CONFIG 键名自动取阈值

#### 2. 新增 `composables/useInputLimit.js`（输入框字符限制工具）
- 导出 `useInputLimit(maxLength, allowedPattern?)`：返回 `{ count, max, isNear, isFull, hint, filter, sync, handleInput }`
- `isNear`：剩余字符数 ≤ max(3, ceil(maxLength*0.15)) 时为 true
- `isFull`：count >= maxLength 时为 true
- `hint`：接近时返回"还可输入 N 字"，达到时返回"已达上限 N 字"，否则空字符串
- `filter`：白名单正则逐字符过滤非法字符 + 截断到 maxLength
- `handleInput(event)`：从 input 组件事件取值 → 过滤 → 同步计数 → 返回新值
- 参考 Vue 3 Composables 官方文档与微信小程序 input 组件 maxlength 属性

#### 3. `global.scss` 新增 `.input-limit-hint` 全局样式类
- `.input-limit-hint`：12px 字号、右对齐、行高 16px，确保各屏幕尺寸清晰可见
- `.input-limit-hint--near`：橙色 #d97706（接近限制时）
- `.input-limit-hint--full`：红色 var(--color-danger)（达到限制时）
- 配合 useInputLimit.js 在 6 个页面（login/register/forgot-password/profile/plan/notification）复用

### 影响范围
- 新增文件：`frontend/src/composables/useTextTruncate.js`、`frontend/src/composables/useInputLimit.js`
- 修改文件：`frontend/src/assets/styles/global.scss`（新增 3 个 CSS 类）
- `目录结构.json` 已同步更新 composables 部分

## 2026-07-02 - notification.vue 应用智能截断与输入框字符限制

### 背景

通知方式页（`notification.vue`）的邮件渠道卡片副标题（邮箱地址）未做截断处理，超长邮箱会破坏卡片布局；编辑/新建邮件渠道表单的 SMTP 主机、端口、邮箱、客户端专用密码四个输入框未限制字符数，可能输入超出后端字段限制的内容导致后端校验失败。

### 主要变动

#### 1. 数据展示区域智能截断
- 邮件渠道卡片副标题邮箱地址 `{{ ch.email_config?.email || '点击查看配置' }}` 改为 `{{ truncate(ch.email_config?.email || '点击查看配置', 'emailAddr') }}`（阈值 26 字符）
- 导入 `composables/useTextTruncate.js`，超长邮箱截断后追加 "..."

#### 2. 编辑邮件渠道表单输入框字符限制
- 4 个输入框（SMTP 主机、端口、邮箱、客户端专用密码）添加 `:maxlength` 与 `@input` 限制
- 创建 `editHostLimit = useInputLimit(255)`、`editPortLimit = useInputLimit(5, /^\d$/)`（仅数字）、`editEmailLimit = useInputLimit(254)`、`editPwdLimit = useInputLimit(64)` 四个限制器实例
- 接近限制时显示"还可输入 N 字"橙色提示，达到限制时显示"已达上限 N 字"红色提示

#### 3. 新建邮件渠道表单输入框字符限制
- 新建表单的 4 个输入框同样添加字符限制
- 创建 `hostLimit = useInputLimit(255)`、`portLimit = useInputLimit(5, /^\d$/)`、`emailLimit = useInputLimit(254)`、`pwdLimit = useInputLimit(64)` 四个限制器实例
- 提示样式与编辑表单一致

#### 4. 脚本导入
- 新增 `import { useTextTruncate } from '../../composables/useTextTruncate'`
- 新增 `import { useInputLimit } from '../../composables/useInputLimit'`

### 影响范围
- 文件：`frontend/src/pages/index/notification.vue`
- 复用 `composables/useTextTruncate.js`、`composables/useInputLimit.js` 及全局 CSS 类 `input-limit-hint`（均未修改）
- 未新增/删除/移动文件，`目录结构.json` 无需更新
- 构建验证：`npm run build:mp-weixin` 成功

## 2026-07-02 - user 页面输入框统一接入字符限制与友好提示

### 背景

登录、注册、找回密码、个人信息四个用户页面的输入框缺少与后端字段长度严格匹配的字符限制，也未提供接近上限时的友好提示。复用已有 composable `composables/useInputLimit.js`，为所有输入框接入 `:maxlength` 绑定、`@input` 过滤处理及剩余字符数提示，提升输入体验并防止超长提交。

### 主要变动

#### 1. 个人信息页（profile.vue）
- 导入 `useInputLimit`，声明 8 个限制器实例（用户名 15/中英数字、签名 70、旧/新/确认密码各 20、旧邮箱验证码 6/数字、新邮箱 254、新邮箱验证码 6/数字）
- 8 个输入框均添加 `:maxlength` 与 `@input` 事件，原 `maxlength="15"`/`maxlength="70"` 改为动态绑定
- 每个输入框后添加计数提示 text 元素（接近限制橙色、达到限制红色）

#### 2. 登录页（login.vue）
- 导入 `useInputLimit`，声明用户名/邮箱 254、密码 20 两个限制器实例
- 用户名/邮箱与密码输入框添加 `:maxlength`、`@input` 及计数提示，保留已有 `@focus`/`@blur`

#### 3. 注册页（register.vue）
- 导入 `useInputLimit`，声明用户名 15/中英数字、密码 20、确认密码 20、邮箱 254、验证码 6/数字五个限制器实例
- 5 个输入框添加 `:maxlength`、`@input` 及计数提示，保留已有 `@focus`/`@blur`

#### 4. 找回密码页（forgot-password.vue）
- 导入 `useInputLimit`，声明邮箱 254、验证码 6/数字、新密码 20、确认密码 20 四个限制器实例
- 步骤1（邮箱+验证码）与步骤2（新密码+确认密码）共 4 个输入框添加 `:maxlength`、`@input` 及计数提示，保留已有 `@focus`/`@blur`

### 影响范围
- 文件：`frontend/src/pages/user/profile.vue`、`frontend/src/pages/user/login.vue`、`frontend/src/pages/user/register.vue`、`frontend/src/pages/user/forgot-password.vue`
- 复用 `composables/useInputLimit.js` 及全局 CSS 类 `input-limit-hint`（均未修改）
- 未新增/删除/移动文件，`目录结构.json` 无需更新
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）

## 2026-07-02 - index/record/settings 页面应用智能截断功能

### 背景

首页（`index.vue`）、打卡记录页（`record.vue`）、设置页（`settings.vue`）的部分展示文本未做截断处理，超长计划名称/备注会破坏卡片布局或溢出展示区域。复用已有的 `composables/useTextTruncate.js` 工具，按各区域设计宽度对应的阈值截断文本，超长时追加 "..." 并在英文环境下优先于词边界截断以保持语义完整。

### 主要变动

#### 1. 首页（index.vue）
- 导入 `useTextTruncate`，在 `<script setup>` 顶部声明 `const { truncate } = useTextTruncate()`
- 主要卡片标题应用 `truncate(primaryPlan.name, 'indexPrimaryTitle')`（阈值 12）
- 主要卡片描述应用 `truncate(primaryPlan.remark || '无备注', 'indexPrimaryDesc')`（阈值 30）
- 次要卡片标题应用 `truncate(secondaryPlan.name, 'indexSecondaryTitle')`（阈值 16）
- 次要卡片描述应用 `truncate(secondaryPlan.remark || '无备注', 'indexSecondaryDesc')`（阈值 22）
- 任务列表弹层项名称应用 `truncate(plan.name, 'indexTaskItemName')`（阈值 16）

#### 2. 打卡记录页（record.vue）
- 导入 `useTextTruncate`，声明 `const { truncate } = useTextTruncate()`
- 打卡明细描述对计划名称与备注分别截断：`truncate(item.plan_name, 'recordPlanName')`（阈值 16）、`truncate(item.plan_remark, 'recordPlanRemark')`（阈值 22）

#### 3. 设置页（settings.vue）
- 导入 `useTextTruncate`，声明 `const { truncate } = useTextTruncate()`
- 制定计划链接值中的计划名称应用 `truncate(activePlanName, 'settingsPlanName')`（阈值 12）

### 影响范围
- 文件：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/record.vue`、`frontend/src/pages/index/settings.vue`
- 复用 `composables/useTextTruncate.js`（未修改该文件，仅消费其 TRUNCATE_CONFIG 已定义的配置键）
- 未新增/删除/移动文件，`目录结构.json` 无需更新
- 构建验证：`npm run build:mp-weixin` 成功（仅余与本次无关的 Sass 弃用警告）

## 2026-07-02 - plan.vue 应用智能截断功能与输入框字符限制

### 背景

制定计划页（`plan.vue`）的已有计划卡片标题与备注未做截断处理，超长文本会破坏卡片布局；新建/编辑表单的输入框未限制字符数，可能输入超出后端字段限制（计划名称 100 字符、备注 255 字符）的内容导致后端校验失败。

### 主要变动

#### 1. 数据展示区域智能截断
- 已有计划卡片标题 `{{ plan.name }}` 改为 `{{ truncate(plan.name, 'planCardTitle') }}`（阈值 18 字符）
- 已有计划卡片备注 `{{ plan.remark || '无备注' }}` 改为 `{{ truncate(plan.remark || '无备注', 'planCardSubtitle') }}`（阈值 30 字符）
- 复用 `composables/useTextTruncate.js`，超长文本截断后追加 "..."

#### 2. 新建计划表单输入框字符限制
- 计划名称 input 与备注 textarea 添加 `:maxlength` 与 `@input` 限制
- 创建 `nameLimit = useInputLimit(100)` 与 `remarkLimit = useInputLimit(255)` 实例
- 接近限制时显示"还可输入 N 字"橙色提示，达到限制时显示"已达上限 N 字"红色提示

#### 3. 编辑计划表单输入框字符限制
- 编辑表单的计划名称 input 与备注 textarea 同样添加字符限制
- 创建 `editNameLimit = useInputLimit(100)` 与 `editRemarkLimit = useInputLimit(255)` 实例
- 提示样式与新建表单一致

#### 4. 脚本导入
- 新增 `import { useTextTruncate } from '../../composables/useTextTruncate'`
- 新增 `import { useInputLimit } from '../../composables/useInputLimit'`

### 影响范围
- 文件：`frontend/src/pages/index/plan.vue`
- 复用全局 CSS 类 `.input-limit-hint` / `.input-limit-hint--near` / `.input-limit-hint--full`（已在 `global.scss` 定义，未在页面中重复定义）
- 构建验证：`npm run build:mp-weixin` 成功

## 2026-07-02 - record.vue 页面加载自动展示当天打卡详情 + 加载/错误状态反馈（Part 9-7）

### 背景

打卡记录页（`record.vue`）在页面加载时仅刷新月历打卡标记，不自动展示当天打卡明细。用户每次进入页面需手动点击"今天"才能查看当日详情，体验不够流畅。同时异步查询缺少加载中视觉反馈和失败重试机制。

### 主要变动

#### 1. 页面加载时（onShow）自动查询并展示当天打卡详情
- `onShow` 生命周期钩子新增逻辑：在 `loadMonthCheckins()` 之后，当已登录且当前查看月份为当前月份时，自动设置 `selectedDay` 为今天日期并调用 `loadDayCheckins(today)` 加载当日详情
- 其他月份不自动选中日期（"今天"不在该月），需用户手动点击日期单元格触发查询
- 切换年/月、上一月/下一月时仍保持原有行为：清空选中日期与详情，仅刷新月历

#### 2. 异步查询加载状态视觉反馈
- 新增 `isLoadingDay` ref：`loadDayCheckins` 开始时置 true，结束（无论成功/失败）时置 false
- 模板新增加载中状态分支：`v-if="isLoadingDay"` 显示"加载中..."灰色文本，与原有"无数据"/"有数据"分支互斥
- 确保异步查询不阻塞界面渲染，用户在等待数据时看到明确的加载反馈

#### 3. 数据库查询失败错误处理与重试
- 新增 `hasLoadError` ref：`catch` 分支中置 true，标记本次查询失败
- 模板新增加载失败状态分支：`v-else-if="hasLoadError"` 显示"加载失败，点击重试"红色文本，点击触发 `retryLoadDay()` 重新发起查询
- `retryLoadDay()` 函数：读取当前 `selectedDay` 并重新调用 `loadDayCheckins`
- `finally` 块确保 `isLoadingDay` 无论成功失败都复位为 false

#### 4. CSS 样式
- `.record-page__list-loading` / `.record-page__list-loading-text`：加载中状态容器与文本（灰色 #888888，14px）
- `.record-page__list-error` / `.record-page__list-error-text`：加载失败状态容器与文本（红色 #e8553a，14px）

### 交互流程

| 场景 | 触发方式 | 行为 |
|------|----------|------|
| 页面加载（onShow） | 自动 | 已登录+当前月份 → 自动选中今天并加载详情；其他月份 → 仅刷新月历 |
| 点击日期单元格 | 用户点击 | 选中该日期并加载当日详情 |
| 加载中 | 异步查询进行时 | 显示"加载中..." |
| 加载失败 | 数据库异常 | 显示"加载失败，点击重试"，点击重新查询 |
| 有数据 | 查询成功有记录 | 列出所有打卡记录（提醒时间+实际时间+计划名+图标） |
| 无数据 | 查询成功无记录 | 显示"当天暂无打卡记录" |

### 影响范围
- 前端：`frontend/src/pages/index/record.vue`（refs + loadDayCheckins + retryLoadDay + onShow + 模板 + CSS + 注释）
- 验证：前端 `npm run build:mp-weixin` 构建成功

---

## 2026-07-02 - index.vue 页面加载自动检测打卡状态 + 打卡记录表索引优化（Part 9-6）

### 背景

首页加载时无法识别用户当前打卡窗口是否已有打卡记录：用户已打卡后再次进入首页，按钮仍显示"立即打卡"，需手动点击才能转为"已打卡"状态。同时 `checkin_records` 表的索引未针对日期范围查询进行优化，原仅有单列 `idx_user_id`，无法支持 `(user_id, plan_id, actual_time)` 与 `(user_id, actual_time)` 的高效范围扫描。

### 主要变动

#### 1. 前端 `index.vue` 加载阶段自动检测打卡状态
- 修改 `loadTodayCheckins()` 函数：从后端异步查询当前主要卡片计划今日所有打卡记录（返回 `checked_time_ids` 数组）
- 查询成功后比对当前打卡窗口：若 `checkinState.value.status === 'active'` 且 `state.timeId` 存在于 `checkedTimeIds` 集合中，自动设置 `justCheckedInTimeId.value = state.timeId`，触发按钮显示"已打卡"绿色状态
- **异步非阻塞**：`loadTodayCheckins` 为 async 函数，调用时不阻塞 UI 渲染，按钮先按计算属性显示默认状态，数据返回后自动更新
- **错误处理**：`try/catch` 捕获数据库连接异常，仅 `console.warn` 记录，不弹出错误提示，按钮保持默认状态（"立即打卡"或"未到打卡时间"），用户仍可手动尝试打卡
- **触发时机**：`onShow` 页面显示时调用 `loadActivePlans()` 后自动链式调用 `loadTodayCheckins()`；切换任务（`handleSecondaryClick` / `handleSelectTask`）后重新加载

#### 2. 数据库 `checkin_records` 表索引优化
- **`backend/sql/create_yao_db.sql`**：
  - 删除原单列索引 `idx_user_id`（被新的复合索引覆盖）
  - 新增复合索引 `idx_user_plan_actual (user_id, plan_id, actual_time)`：用于 `list_today_by_plan` 按 `(user_id, plan_id, actual_time)` 范围查询
  - 新增复合索引 `idx_user_actual (user_id, actual_time)`：用于 `list_today_by_user` 跨计划按 `(user_id, actual_time)` 范围查询
- **实际数据库迁移**（已执行 ALTER TABLE）：
  ```sql
  ALTER TABLE checkin_records
    DROP INDEX idx_user_id,
    ADD INDEX idx_user_plan_actual (user_id, plan_id, actual_time) COMMENT '用户+计划+时间戳范围索引（用于日期范围查询）',
    ADD INDEX idx_user_actual (user_id, actual_time) COMMENT '用户+时间戳范围索引（用于跨计划日期范围查询）';
  ```
- **索引设计依据**：与 Part 9-5 中 `checkin_service.py` 的 `list_today_by_user` / `list_today_by_plan` 日期范围查询条件（`actual_time >= day_start AND actual_time < day_end`）完全匹配，避免全表扫描；复合索引遵循最左前缀原则，`user_id` 在前以等值过滤，`actual_time` 在最后支持范围扫描

### 查询性能说明
- `list_today_by_plan(user_id, plan_id)`：命中 `idx_user_plan_actual`，等值过滤 `user_id` + `plan_id` 后对 `actual_time` 做范围扫描，预计扫描行数从全表降至单日单计划记录数（通常 ≤ 10 行）
- `list_today_by_user(user_id)`：命中 `idx_user_actual`，等值过滤 `user_id` 后对 `actual_time` 做范围扫描，覆盖用户当日全部计划的打卡记录
- `list_by_month` / `list_day_detail` 同样受益于 `idx_user_actual` 索引（同为 user_id + actual_time 范围查询）

### 影响范围
- 前端：`frontend/src/pages/index/index.vue`（`loadTodayCheckins` 函数）
- 数据库脚本：`backend/sql/create_yao_db.sql`（`checkin_records` 表索引定义）
- 实际数据库：`wuzuniao_yao.checkin_records` 表已执行 ALTER TABLE 完成索引替换
- 验证：前端 `npm run build:mp-weixin` 成功，`SHOW INDEX FROM checkin_records` 确认新索引已生效

---

## 2026-07-02 - 全系统时区统一为上海时区 UTC+8（Part 9-5）

### 背景

项目存在系统性时区隐患：后端无任何显式时区配置，`datetime.now()`/`date.today()` 返回服务器本地时间（开发环境 Windows 为 +08:00，生产环境 Docker 容器默认 UTC），数据库 `DATETIME` 列存储 naive datetime，前端 `now.toISOString()` 将本地时间转为 UTC 后发送给后端。这导致打卡记录存储/查询/显示链路在跨时区环境下出现 8 小时偏差。

### 修复方案：统一使用无时区的上海时间（naive Shanghai datetime）

全链路约定：数据库存储、API 传输、前端展示均使用无时区后缀的上海时间字符串（如 `2026-07-02T14:30:00`）。

### 主要变动

#### 1. 新增时区工具模块 `backend/app/utils/timezone.py`
- `SHANGHAI_TZ = timezone(timedelta(hours=8))`：上海时区常量
- `now_shanghai() -> datetime`：返回当前上海时间的 naive datetime（无 tzinfo，用于写入数据库）
- `today_shanghai() -> date`：返回当前上海日期

#### 2. 数据库连接设置 session time_zone
- `backend/app/core/database.py`：`create_async_engine` 新增 `connect_args={"init_command": "SET time_zone='+08:00'"}`
- 每个连接建立时自动执行 `SET time_zone='+08:00'`，确保 `CURRENT_TIMESTAMP`/`func.now()` 返回上海时间
- 解决开发环境（Windows +08:00）与生产环境（Docker UTC）的时区不一致

#### 3. 后端替换所有 `datetime.now()` / `date.today()`
- `user_service.py`：`datetime.now()` → `now_shanghai()`（2处：purge_expired_deletions 阈值计算、last_login_at 赋值）
- `plan_service.py`：`date.today()` → `today_shanghai()`（1处：auto_close_expired_plans 过期判定）
- `notification_channel_service.py`：`datetime.now()` → `now_shanghai()`（1处：updated_at 手动赋值）
- `checkin_service.py`：`date.today()` → `today_shanghai()`（2处：list_today_by_user、list_today_by_plan）

#### 4. 后端消除所有 `sa_func.DATE()` SQL 函数调用
- `checkin_service.py` 的 `list_today_by_user` 和 `list_today_by_plan`：原使用 `sa_func.DATE(actual_time) == today`，改为日期范围比较 `actual_time >= day_start AND actual_time < day_end`
- 与此前已修复的 `list_by_month` 和 `list_day_detail` 保持一致

#### 5. 后端 API 时间解析保护
- `checkins.py` 的 `create_checkin` 端点：解析 `datetime.fromisoformat()` 后检查 `tzinfo`，若有时区信息则转换为上海时间后去除 tzinfo，确保写入数据库的是 naive Shanghai datetime

#### 6. 前端打卡时间发送改为本地时间字符串
- `index.vue` 的 `handleCheckin`：原 `now.toISOString()`（转为 UTC 带 Z 后缀）改为构造本地时间字符串 `YYYY-MM-DDTHH:MM:SS`（无时区后缀），避免 UTC 转换导致的 8 小时偏差

#### 7. 前端实际打卡时间显示改为字符串提取
- `record.vue` 的 `formatActualTime`：原 `new Date(isoStr).getHours()`（依赖客户端时区解析）改为正则匹配 `T(\d{2}:\d{2})` 直接提取 HH:MM，避免 `new Date()` 时区转换

### 时区一致性保证

| 环节 | 修复前 | 修复后 |
|------|--------|--------|
| 数据库 CURRENT_TIMESTAMP | 依赖服务器 time_zone（开发+08:00/生产UTC） | session time_zone='+08:00'，统一上海时间 |
| Python datetime.now() | 服务器本地时间（开发+08:00/生产UTC） | now_shanghai() 统一上海时间 |
| Python date.today() | 服务器本地日期 | today_shanghai() 统一上海日期 |
| SQL DATE() 函数 | 依赖 session time_zone | 全部改为日期范围比较 |
| 前端发送打卡时间 | toISOString() 转 UTC | 本地时间字符串（无 Z） |
| API 响应时间 | .isoformat() 无时区后缀 | 同左（naive 上海时间，前端直接提取） |
| 前端显示时间 | new Date(isoStr).getHours() | 正则提取 HH:MM |

### 影响范围
- 新增：`backend/app/utils/timezone.py`
- 后端修改：`backend/app/core/database.py`、`backend/app/services/user_service.py`、`backend/app/services/plan_service.py`、`backend/app/services/notification_channel_service.py`、`backend/app/services/checkin_service.py`、`backend/app/api/v1/checkins.py`
- 前端修改：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/record.vue`
- 验证：后端 7 个文件 py_compile 全部通过，前端 npm run build:mp-weixin 成功

---

## 2026-07-02 - index.vue 已完成图标替换 + record.vue 实际打卡时间显示（Part 9-4）

### 主要变动

1. **index.vue 绿色已完成状态图标替换为 daka_1.png**
   - **需求**：将所有绿色"已完成"状态的打卡按钮图标统一替换为 `daka_1.png` 资源
   - 新增 `import checkinDoneIcon from '../../assets/images/daka_1.png'`
   - `checkinIcon` 从常量改为 computed：`isCheckinDone` 为 true 时返回 `daka_1.png`，否则返回 `daka_0.png`
   - 图标尺寸和布局保持不变（`index-page__checkin-icon` 样式未改），仅切换图片资源

2. **record.vue 每行打卡记录添加实际打卡时间显示**
   - **需求**：为每行打卡记录的通知时间字段添加实际打卡时间显示，与通知时间清晰区分
   - 新增 `formatActualTime(isoStr)` 工具函数：将 ISO 时间字符串格式化为 HH:MM
   - 模板：提醒时间行改为 flex 布局（`record-page__list-item-time-row`），已打卡时在通知时间右侧显示 `→ HH:MM` 实际打卡时间
   - 视觉区分：通知时间 16px 深色粗体，实际时间 14px 绿色（#2ead4b）细体，通过 "→" 箭头连接
   - 仅当 `item.checked && item.actual_time` 时显示实际时间，未打卡不显示

### 影响范围
- 前端：`frontend/src/pages/index/index.vue`（import + checkinIcon computed）、`frontend/src/pages/index/record.vue`（模板 + formatActualTime 函数 + CSS）
- 验证：前端 npm run build:mp-weixin 成功

---

## 2026-07-02 - index.vue 打卡成功无弹窗 + record.vue 打卡记录显示修复（Part 9-3）

### 主要变动

1. **index.vue 打卡成功后不弹窗，直接转为绿色"已打卡"状态**
   - **需求**：点击"立即打卡"后不弹出"打卡成功"提示框，按钮直接转为绿色"已打卡"状态
   - 新增 `justCheckedInTimeId` ref：记录刚打卡成功的时间点ID
   - 修改 `isCheckinDone` 计算属性：`active` 状态且 `justCheckedInTimeId === checkinState.timeId` 时也返回 true，按钮显示"已打卡"绿色状态
   - 修改 `checkinText` 计算属性：`isCheckinDone` 为 true 时显示"已打卡"
   - `handleCheckin` 函数移除 `uni.showToast({ title: '打卡成功' })`，成功后设置 `justCheckedInTimeId.value = timeId`
   - `handleSecondaryClick` 和 `handleSelectTask` 切换任务时重置 `justCheckedInTimeId.value = null`

2. **index.vue "已完成"状态禁止单击触发打卡**
   - **需求**：前端对"已完成"状态（含刚打卡成功）禁止单击触发打卡
   - `handleCheckin` 函数开头新增检查：`if (isCheckinDone.value) return`，阻止已完成状态下重复打卡

3. **record.vue 实际打卡记录未显示修复**
   - **需求**：打卡计划和提醒时间展示正常，但实际打卡记录未显示，已完成打卡记录无绿色图标标记
   - **根因**：后端 `list_day_detail` 方法使用 `sa_func.DATE(CheckinRecord.actual_time) == day_date` 查询打卡记录，`DATE()` SQL 函数可能因时区问题导致日期不匹配，返回空记录
   - **修复**：改为日期范围比较 `actual_time >= day_start AND actual_time < day_end`，其中 `day_start` 和 `day_end` 为当天 0 点和次日 0 点的 datetime，与 `list_by_month` 方法保持一致
   - `backend/app/services/checkin_service.py`：import 新增 `time as dt_time, timedelta`；`list_day_detail` 方法打卡记录查询改为日期范围比较

### 影响范围
- 前端：`frontend/src/pages/index/index.vue`（handleCheckin + handleSecondaryClick + handleSelectTask + isCheckinDone + checkinText）
- 后端：`backend/app/services/checkin_service.py`（import + list_day_detail）
- 验证：后端 py_compile 通过，前端 npm run build:mp-weixin 成功

---

## 2026-07-02 - record.vue 打卡记录查询与展示优化（Part 9-2）

### 主要变动

1. **list_by_month 查询逻辑修复**
   - **需求**：确保7月1日的任务打卡记录正确显示绿色标注
   - **根因**：原使用 `extract("year", ...)` 和 `sa_func.DAY()` 可能受时区/兼容性影响
   - **修复**：改为日期范围比较（`actual_time >= first_day AND actual_time < next_month_first`），在 Python 中提取 day，避免 SQL 函数的时区问题

2. **list_day_detail 计划查询条件修改**
   - **需求**：行数 = (当前打卡计划数量 × 提醒时间) + (其他打卡计划数量 × 提醒时间)
   - **根因**：原只查询 `status==1`（进行中）的计划，但7月1日有效的计划可能已被定时任务标记为已结束
   - **修复**：改为按日期范围查询（`start_date <= day_date AND end_date >= day_date`），不限制 status，确保当天有效的所有计划都会显示

3. **多条打卡记录完整展示**
   - **需求**：同一时间点多次打卡时，所有记录完整展示，不截断
   - **修复**：原只取第一条记录（`matched_records[0]`），改为遍历所有记录，每条记录单独占一行

4. **打卡状态标识验证**
   - 已打卡：绿点（`record-page__list-item-dot--completed`）+ `jilu_wc.png` 图标
   - 未打卡：灰点，无图标
   - CSS 无 `max-height`/`overflow` 限制，不会截断

### 影响范围
- 后端：`backend/app/services/checkin_service.py`（list_by_month + list_day_detail 修改）
- 前端：无需修改（显示逻辑已正确）
- 验证：后端 py_compile 通过

---

## 2026-07-02 - 后台定时任务、打卡逻辑重构、日历功能完善（Part 9）

### 主要变动

1. **后端定时任务系统：自动关闭过期计划**
   - **需求**：定期检查所有打卡任务的计划持续起始时间，当前时间超出范围时自动将状态更新为"已结束"
   - `backend/app/services/plan_service.py`：新增 `auto_close_expired_plans()` 方法，查询 status=1 且 end_date < today 的计划，更新为 status=0
   - `backend/app/main.py`：新增 `_auto_close_expired_plans_loop()` 后台任务，每 30 分钟（PLAN_AUTO_CLOSE_INTERVAL=1800秒）执行一次；lifespan 中启动 task2 并在关闭时取消
   - 异常不中断循环，仅记录日志，保证任务可靠性

2. **plan.vue 任务状态切换 UI 实时更新**
   - **需求**：任务状态从"暂停"或"已结束"切换到"进行中"时，卡片左侧色条未实时更新
   - **根因**：编辑表单切换状态时 `editingForm.status` 变化，但卡片色条绑定的是 `plan.status`（来自数据库），未反映编辑中的状态
   - 新增 `getDisplayStatus(plan)` 函数：编辑中时返回 `editingForm.status`，否则返回 `plan.status`
   - 卡片 `:class` 和色条 `:class` 绑定改为调用该函数，实现状态切换时 UI 实时更新

3. **index.vue 移除打卡时间小圆圈**
   - **需求**：移除主要和次要卡片中显示的打卡时间小圆圈元素
   - 移除模板中主要卡片和次要卡片的 `index-page__pills` view 元素
   - 移除 CSS `.index-page__pills` 和 `.index-page__pill` 样式
   - 次要卡片高度从 112px 恢复为 85px（不再需要容纳小圆圈）

4. **index.vue 复杂打卡按钮逻辑**
   - **需求**：实现多状态打卡按钮（无任务/未到打卡时间/立即打卡/已完成）+ 长按3秒重置
   - 新增 `checkinState` 计算属性，返回 4 种状态：
     - `disabled`：未登录/无任务/不在计划日期范围（灰色"无打卡任务"）
     - `waiting`：在日期范围内但未到任何提醒时间的提前一小时窗口（橙色"未到打卡时间"）
     - `active`：在可打卡窗口内（红色"立即打卡"）
     - `done`：多提醒时间间隔≤2小时，处于两次提醒间隔的前半部分（绿色"已完成"）
   - 新增 `isWithinDateRange` 计算属性：检查 start_date <= today <= end_date
   - 新增 `sortedTimes` 计算属性：提醒时间按分钟数排序
   - 打卡窗口逻辑：
     - 单提醒时间：提前一小时内可打卡 [t-60, t]
     - 多提醒时间间隔>2小时：每个提醒时间提前一小时内可打卡
     - 多提醒时间间隔≤2小时：在 [prevT, t] 间隔内，前半部分显示"已完成"，后半部分可打卡
   - 新增 `forceActive` ref + `handleLongPress()` 函数：长按3秒（uni.showLoading 提示）后强制按钮为"立即打卡"状态
   - 添加 CSS `.index-page__checkin-button--done`（绿色）和 `.index-page__checkin-button--waiting`（橙色）样式

5. **record.vue 日历功能完善**
   - **需求**：年月选择器、动态日期、打卡记录查询、详情卡片
   - 模板：
     - 年份选择器（picker mode="selector"，2020-2040，默认当前年份）
     - 月份选择器（picker mode="selector"，1-12，默认当前月份）
     - 动态生成日历日期（含月初前空格占位）
     - 已打卡日期下方小绿点
     - 详情卡片（点击日期后显示）
   - 脚本：
     - `calendarDays` 计算属性：根据年月动态生成日期，标记 isToday/isSelected/checked
     - `loadMonthCheckins()`：调用 `listMonthCheckins` API 获取当月有打卡记录的日期列表
     - `loadDayCheckins(day)`：调用 `listDayCheckins` API 获取当天打卡详情
     - 年月切换/上下月切换时重新加载打卡记录
   - 详情卡片：
     - 已打卡：绿点 + 提醒时间 + 计划名称/备注 + jilu_wc.png 图标
     - 未打卡：灰点 + 提醒时间 + 计划名称/备注（无图标）

6. **后端新增打卡记录查询 API**
   - **需求**：支持日历小绿点和详情卡片功能
   - `backend/app/services/checkin_service.py`：
     - `list_by_month(user_id, year, month)`：查询用户某月有打卡记录的日期列表（DISTINCT DAY(actual_time)）
     - `list_day_detail(user_id, day_date)`：查询用户某天打卡详情（关联计划+提醒时间，返回 checked/actual_time）
   - `backend/app/api/v1/checkins.py`：
     - `GET /{user_id}/month?year=2026&month=6`：返回 `checked_days` 日期列表
     - `GET /{user_id}/day?date=2026-06-15`：返回当天所有进行中计划的提醒时间及打卡状态
   - `frontend/src/api/modules/checkin.js`：新增 `listMonthCheckins` 和 `listDayCheckins` 函数

### 影响范围
- 后端：`backend/app/main.py`、`backend/app/services/plan_service.py`、`backend/app/services/checkin_service.py`、`backend/app/api/v1/checkins.py`
- 前端：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/plan.vue`、`frontend/src/pages/index/record.vue`、`frontend/src/api/modules/checkin.js`
- 验证：后端 py_compile 通过，前端 npm run build:mp-weixin 成功

---

## 2026-07-01 - 首页卡片交换、打卡优化、计划表单优化（Part 8）

### 主要变动

1. **index.vue 卡片内容交换功能**
   - **需求**：点击次级卡片时，次级卡片内容与主要卡片内容互换，原主要卡片内容移至次级卡片位置
   - 新增 `secondaryPlanId` ref 跟踪次要卡片计划ID
   - `handleSecondaryClick()` 改为互换 `primaryPlanId` 和 `secondaryPlanId` 的值
   - `handleSelectTask()` 从任务列表选择任务时，原主要卡片成为次要卡片

2. **index.vue 视觉显示优化**
   - **需求**：仅2个任务时，次级卡片右侧不显示"..."及任何相似字符
   - 移除模板中 `v-else` 分支（2个任务时的线条按钮）
   - 移除 CSS `.index-page__secondary-button` 和 `.index-page__secondary-button-line` 样式

3. **index.vue 内容展示规范**
   - **需求**：主要和次要卡片统一展示"计划名称"、"备注说明"及提醒时间，提醒时间采用小圆圈包裹（参考 plan.vue）
   - 主要卡片：name + remark + notification_times（小圆圈）
   - 次要卡片：name + remark + notification_times（小圆圈）
   - 新增 CSS `.index-page__pills` 和 `.index-page__pill` 样式（参考 plan.vue `.plan-page__pill`）
   - 次要卡片高度从 85px 增加到 112px，容纳新增的备注和时间小圆圈

4. **index.vue 状态样式调整**
   - **需求**："无打卡任务"状态下，打卡按钮不显示图片且背景色为灰色
   - 模板：禁用态隐藏图片 `v-if="!isButtonDisabled"`
   - CSS：禁用态背景色从 `#c1cab5`（浅绿色）改为 `#8a8a8a`（灰色）
   - CSS：禁用态文字 `margin-top: 84px` 垂直居中（无图标时）

5. **index.vue 打卡功能优化**
   - **需求**：取消打卡拦截机制，允许用户即使已有打卡记录仍可进行重复打卡
   - 移除 `isCheckedIn` ref、`longPressTimer`、`isLongPressTriggered` 状态
   - 移除 `handleMultipleReminders()`、`onTouchStart()`、`onTouchEnd()` 函数
   - 移除模板中 `@touchstart`/`@touchend`/`@touchcancel` 事件和 `--done` 类绑定
   - `currentCheckinTime` 不再过滤已打卡时间点
   - `handleCheckin()` 移除已打卡拦截，允许重复打卡
   - 移除 `checkinActiveIcon` 导入（不再需要绿色图标）
   - 移除 CSS `.index-page__checkin-button--done` 样式

6. **plan.vue 防重复提交**
   - **需求**：为"保存计划"按钮添加防频繁点击机制
   - 新增 `isSubmitting` ref 标志位
   - `handleSave()` 和 `handleSaveEdit()` 开头检查 `isSubmitting`，try 前置 true，finally 恢复 false

7. **plan.vue 输入框交互优化**
   - **需求**：输入框文字居中，参考 notification.vue 邮件输入框实现
   - CSS `.plan-page__input`：`padding: 10px 12px 8px` → `padding: 0 12px`，`line-height: 21px` → `line-height: 41px`

8. **plan.vue 布局调整**
   - **需求**：日期控件宽度占满表单单行100%
   - 新建表单和编辑表单的 picker 元素添加 `class="plan-page__date-picker"`
   - 新增 CSS `.plan-page__date-picker { flex: 1; }`

9. **plan.vue 通知方式动态加载**
   - **需求**：每次打开新建计划页面时从数据库获取，仅显示可用状态的通知方式
   - `loadChannels()` 过滤 `ch.enabled`，仅显示可用通知方式
   - `handleNewEntry()` 改为 async，先清空 `selectedChannelIds` 再调用 `loadChannels()` 重新加载

10. **plan.vue 输入框样式调整**
    - **需求**："备注说明"输入框默认高度设置为3行
    - CSS `.plan-page__textarea`：`height: 66px` → `height: 88px`（3行 × 24px + 上下 padding 16px），`padding: 8px 12px 32px` → `padding: 8px 12px`

11. **notification.vue 图标替换**
    - **需求**：邮件通知方式右侧删除图标更换为 shanchu.png
    - 导入从 `shanchu_hui.png` 改为 `shanchu.png`

12. **后端 + 数据库：允许重复打卡**
    - `backend/app/services/checkin_service.py`：移除重复打卡检查逻辑（第55-68行），更新注释
    - `backend/sql/create_yao_db.sql`：`UNIQUE INDEX uk_user_plan_time_day` 改为普通 `INDEX idx_user_plan_time_day`

### 影响范围
- 前端：`frontend/src/pages/index/index.vue`、`frontend/src/pages/index/plan.vue`、`frontend/src/pages/index/notification.vue`
- 后端：`backend/app/services/checkin_service.py`
- 数据库：`backend/sql/create_yao_db.sql`（checkin_records 表索引变更）

---

## 2026-07-01 - 优先级字段、加密存储、打卡记录、首页重构（Part 7）

### 主要变动

1. **数据库新增 priority 优先级字段**
   - **需求**：在 checkin_plans 表中新增优先级字段，数字越小优先级越高
   - `backend/sql/create_yao_db.sql`：checkin_plans 表新增 `priority INT NOT NULL DEFAULT 3`，新增复合索引 `idx_user_status_priority`
   - `backend/app/models/plan.py`：CheckinPlan ORM 新增 `priority` 列（Integer, 默认3, 范围0-7）

2. **后端 AES-256-GCM 加密工具 + 邮件密码加密存储**
   - **需求**：客户端专用密码采用高强度对称加密算法加密后存入数据库，使用时解密
   - `backend/app/utils/crypto.py`（新建）：AES-256-GCM 加密工具，`encrypt(plaintext)` 加密、`decrypt(token)` 解密，存储格式 `base64(nonce(12B) + ciphertext + tag(16B))`
   - `backend/app/core/config.py`：新增 `ENCRYPTION_SECRET_KEY` 配置项（base64 编码的 32 字节密钥）
   - `backend/.env.dev`：新增 `ENCRYPTION_SECRET_KEY` 环境变量
   - `backend/app/services/notification_channel_service.py`：`create_email_channel` 加密密码后存储；`update_email_channel` 密码为空时保留原加密密码，非空时加密新密码
   - `backend/app/schemas/notification_channel.py`：`UpdateEmailChannel.password` 从必填改为 `password: str = ""`（空字符串表示保留原密码）
   - `backend/app/api/v1/notification_channels.py`：`_channel_to_dict` 邮件渠道 `cfg["password"] = ""` 始终返回空值，不暴露解密内容

3. **后端新增打卡记录模块（ORM + Schema + Service + API）**
   - **需求**：支持首页"立即打卡"功能，生成打卡记录写入数据库
   - `backend/app/models/checkin_record.py`（新建）：CheckinRecord ORM 模型（id/user_id/plan_id/plan_time_id/actual_time），外键关联 checkin_plans 和 plan_notification_times
   - `backend/app/schemas/checkin_record.py`（新建）：CreateCheckin（含 actual_time ISO 格式校验）、CheckinOut
   - `backend/app/services/checkin_service.py`（新建）：CheckinService 类，`create_checkin`（校验计划归属/时间点归属 + 今日重复打卡检查）、`list_today_by_user`、`list_today_by_plan`、`get_latest_checkin`
   - `backend/app/api/v1/checkins.py`（新建）：POST /checkins 打卡、GET /checkins/{user_id}/today 今日记录、GET /checkins/{user_id}/today/{plan_id} 某计划今日记录
   - `backend/app/api/v1/__init__.py`：注册 checkins 路由

4. **后端计划排序规则 + priority 字段支持**
   - **需求**：进行中 > 暂停 > 已结束；同状态按 priority 升序；同状态同优先级新创建在前
   - `backend/app/services/plan_service.py`：`list_by_user` 使用 SQL `CASE` 表达式排序（status 1→0, 2→1, 0→2），再按 priority 升序、created_at 降序；`create_plan`/`update_plan` 新增 priority 参数
   - `backend/app/schemas/plan.py`：CreatePlan 新增 `priority: int = 3` 字段及 `validate_priority` 校验器（0-7 范围）
   - `backend/app/api/v1/plans.py`：`_plan_to_dict` 新增 priority 字段；create/update 端点传递 priority

5. **前端 API 模块适配新字段**
   - `frontend/src/api/modules/plan.js`：createPlan 和 updatePlan 新增 priority 参数
   - `frontend/src/api/modules/notification.js`：updateEmailChannel password 空字符串表示保留原密码
   - `frontend/src/api/modules/checkin.js`（新建）：createCheckin/listTodayCheckins/listTodayCheckinsByPlan

6. **plan.vue 优先级字段 + 就地展开编辑 + 排序规则 + 时间宽度修复**
   - **需求**：点击卡片就地展开编辑区域（去除"编辑计划详情"标题）；新建计划隐藏已有计划；提醒时间下方添加优先级单选框（0-7）；修复提醒时间控件宽度（卡片宽度50%）
   - `frontend/src/pages/index/plan.vue`：
     - 新增 `showForm` ref（true 时隐藏已有计划）、`editingPlanId` ref（跟踪展开卡片）、`editingForm` reactive（编辑表单数据隔离）
     - `toggleEditPlan(plan)`：点击卡片展开/收起编辑表单，CSS `border-radius` 衔接实现卡片延伸视觉效果
     - `handleNewEntry()`：点击新建入口隐藏已有计划
     - 优先级单选框：0-7 共8个选项，`v-for="n in 8"` 渲染
     - 时间控件宽度修复：picker 标签添加 `class="plan-page__time-picker"`，样式 `width: 50%`
     - 就地展开样式：`.plan-page__card--editing { border-radius: 12px 12px 0 0 }` + `.plan-page__card-edit { border-radius: 0 0 12px 12px }`

7. **notification.vue UI修复 + 密码字段加密适配**
   - **需求**：修复输入框内容超出下边框问题；修改邮件通知时密码字段始终显示空值，留空提交保留原密码
   - `frontend/src/pages/index/notification.vue`：
     - 输入框 CSS 修复：`padding: 0 12px` + `line-height: 41px`（= height），解决微信小程序原生 input 文本超出下边框问题（含聚焦态）
     - `toggleEmailEdit`：`editForm.password = ''` 显式置空（后端 API 已返回空值）
     - `handleUpdateEmail`：移除 password 必填校验，允许空字符串提交（后端保留原加密密码）
     - 密码字段 placeholder 改为"留空表示不修改，重新输入请填写"

8. **index.vue 重构：动态计划加载 + 任务切换 + 立即打卡按钮**
   - **需求**：主要/次要卡片从数据库获取进行中计划按优先级排序；空状态提示；任务切换（次要卡片点击替换、"..."列出所有任务）；立即打卡按钮三态（灰色无任务/红色立即打卡/绿色已打卡）
   - `frontend/src/pages/index/index.vue`：
     - `onShow` 生命周期加载进行中计划（status=1，按 priority 升序）
     - 空状态：未登录显示"欢迎使用无足鸟按时吃药打卡！"，已登录无计划显示"请先到设置界面创建您的打卡计划..."
     - 任务切换：`primaryPlanId` 跟踪选中任务，次要卡片点击交换，3+任务显示"..."按钮弹出任务列表
     - 打卡按钮三态：`isButtonDisabled`（灰色无任务）、`isCheckedIn`（绿色已打卡）、默认红色"立即打卡"
     - 打卡时段：提醒时间前一小时内 [time-60, time]
     - 打卡功能：`handleCheckin` 调用 createCheckin API 写入数据库
     - 多提醒时间处理：`handleMultipleReminders` 两次打卡间隔不足一小时时动态调整并重置按钮
     - 长按3秒重置：`onTouchStart`/`onTouchEnd` 实现 3 秒长按重置打卡状态
     - 每分钟定时刷新打卡时段状态

### 影响范围
- 后端：新增 crypto.py、checkin_record.py（model/schema/service/api），修改 plan.py（model/schema/service/api）、notification_channel_service.py、notification_channel.py（schema/api）、config.py、.env.dev、__init__.py、create_yao_db.sql
- 前端：新增 checkin.js，重写 index.vue，修改 plan.vue、notification.vue、plan.js、notification.js
- 构建验证：后端 14 个 Python 文件 py_compile 全部通过，前端 npm build 成功

---

## 2026-07-01 - notification 倒计时跳转、plan 编辑功能与 UI 优化、settings 置灰（Part 6-2）

### 主要变动

1. **notification.vue 默认选邮件 + 未绑定邮箱 3 秒倒计时跳转（Part 1）**
   - **需求**：点击"新建通知方式"默认选"邮件"；未绑定邮箱时弹出 3 秒倒计时提示框，结束后自动跳转 profile.vue 绑定邮箱区域（自动展开）
   - `frontend/src/pages/index/notification.vue`：
     - `handleAdd` 函数修改：默认 `formType.value = '邮件'`，并在 `userStore.userInfo.email` 为空时调用 `startEmailCountdown()`
     - 新增 `showCountdown`、`countdown` ref 与 `countdownTimer` 变量
     - 新增 `startEmailCountdown()` 函数：3 秒倒计时（setInterval 每秒递减），到 0 时清除定时器并 `uni.navigateTo({ url: '/pages/user/profile?focus=email' })`
     - 模板新增倒计时弹窗（遮罩 + 提示框 + 倒计时数字），`v-if="showCountdown"` 控制显示
     - 样式新增 `.notification-page__countdown-mask`、`.notification-page__countdown-box`、`.notification-page__countdown-num` 等

2. **profile.vue 支持从 notification 跳转后自动展开邮箱区域（Part 2）**
   - **需求**：从 notification 页通过 `?focus=email` 跳转后，自动展开绑定邮箱区域
   - `frontend/src/pages/user/profile.vue`：
     - 新增 `import { onLoad } from '@dcloudio/uni-app'`
     - 新增 `onLoad` 钩子：接收 `options.focus`，当 `focus === 'email'` 且非删除冷静期时设置 `expandedSections.email = true`

3. **plan.vue UI 优化 + 编辑功能 + 图标替换 + 状态色条（Part 3）**
   - **需求**：日期控件一行完整显示、时间控件宽度 50% 且默认显示当前时间、通知方式多选框站内信默认勾选、点击已有计划动态插入编辑表单、"..."替换为 shanchu.png、状态值 2 左侧深灰色边框
   - `frontend/src/pages/index/plan.vue`：
     - 新增 `editingPlanId` ref（null=新建模式，非 null=编辑模式）与 `formHeading` computed（标题动态切换）
     - 新增 `getCurrentTime()` 函数：使用 `new Date()` 获取当前系统时间格式化为 HH:MM
     - `loadChannels()` 加载渠道后自动将站内信加入 `selectedChannelIds`
     - `handleNewEntry()` 重置时设置 `form.times = [getCurrentTime()]`，并重新勾选站内信
     - 新增 `handleEditPlan(plan)`：填充表单数据切换编辑模式，确保站内信被勾选
     - 修改 `handleSave()`：根据 `editingPlanId` 调用 `createPlan` 或 `updatePlan`
     - 新增 `handleDeletePlan(planId)` 替换原 `handleMenu`（直接二次确认删除）
     - 模板：已有计划卡片新增 `@click="handleEditPlan(plan)"`，右上角"..."替换为 `shanchu.png` 图标，状态 2 卡片左侧色条使用 `.plan-page__card-stripe--paused`（深灰色 `#4a4a4a`）
     - 样式：日期文本新增 `white-space: nowrap` 防换行；时间控件 `width: 50%`；新增 `.plan-page__card-delete`（替换 card-menu/dot）；新增 `.plan-page__card-stripe--paused`

4. **settings.vue 删除冷静期内置灰禁用（Part 4）**
   - **需求**：删除冷静期内"制定计划"和"通知方式"卡片置灰，禁止点击交互
   - `frontend/src/pages/index/settings.vue`：
     - 新增 `isDeletionScheduled` computed（基于 `userStore.userInfo?.status === 0`）
     - 分组 1 添加 `:class="{ 'settings-page__group1--disabled': isDeletionScheduled }"`
     - 卡片点击改为 `@click="!isDeletionScheduled && goPlan()"`、`@click="!isDeletionScheduled && goNotification()"`
     - 新增 `.settings-page__group1--disabled { opacity: 0.5; pointer-events: none; }` 样式

5. **后端更新计划 API（Part 5）**
   - **需求**：新增 PUT 端点支持计划更新（含时间点和渠道关联）
   - `backend/app/schemas/plan.py`：新增 `UpdatePlan(CreatePlan)` 类，复用 CreatePlan 全部字段校验，新增 `plan_id: int` 字段
   - `backend/app/services/plan_service.py`：新增 `update_plan` 方法，更新主表字段 + 删除旧时间点/渠道关联并写入新数据 + 校验渠道归属
   - `backend/app/api/v1/plans.py`：新增 `PUT /{plan_id}` 端点，调用 `service.update_plan`，成功后重新查询加载关联关系并返回
   - `frontend/src/api/modules/plan.js`：新增 `updatePlan(plan_id, {...})` 函数，发送 PUT 请求

### 影响范围

- **通知方式管理**：点击"新建通知方式"默认选邮件，未绑定邮箱时友好引导跳转 profile 绑定邮箱区域
- **计划制定**：支持点击已有计划卡片进入编辑模式更新计划；UI 优化（日期一行显示、时间控件 50% 宽度、默认当前时间、站内信默认勾选、删除图标替换为 shanchu.png、暂停状态深灰色边框）
- **设置页**：删除冷静期内（status=0）"制定计划"和"通知方式"卡片置灰且不可点击
- **个人信息页**：支持从 notification 页跳转后通过 URL 参数 `focus=email` 自动展开绑定邮箱区域
- **后端 API**：新增 `PUT /api/v1/plans/{plan_id}` 端点，支持计划全字段更新

---

## 2026-07-01 - notification/plan/settings 页面优化与功能扩展

### 主要变动

1. **notification.vue 邮件通知交互优化与功能扩展（Part 1）**
   - **需求**：邮件修改输入框垂直居中、新建通知方式排序调整、邮件前置校验、邮件表单新增"是否启用"单选框
   - `frontend/src/pages/index/notification.vue`：
     - `.notification-page__input` padding 由 `10px 12px 8px` 改为 `10px 12px`（上下对称），修复文本不垂直居中问题
     - 新建通知方式表单：通知类型单选框排序调整，"邮件"调至第一个位置，"站内信"调至最后一个位置
     - `selectType()` 函数新增邮件前置校验：选"邮件"时检查 `userStore.userInfo.email`，未绑定邮箱弹窗提示"未绑定邮箱不支持邮件通知，请先绑定邮箱"并阻止切换
     - 新建表单和编辑表单均新增"是否启用"单选框（是/否），与 `enabled` 字段绑定，默认选中"是"
     - `form` 和 `editForm` reactive 对象新增 `enabled: true` 字段
     - `handleSave` 和 `handleUpdateEmail` 提交时传递 `enabled` 参数
     - `toggleEmailEdit` 展开邮件卡片时从渠道数据填充 `editForm.enabled`
     - `handleAdd` 重置表单时同步重置 `form.enabled = true`

2. **plan.vue 新建计划表单优化与任务状态管理（Part 2）**
   - **需求**：日期改为起止时间双控件、时间选择控件 UI 美化、备注输入框宽度占满、新增任务状态单选框
   - `frontend/src/pages/index/plan.vue`：
     - "日期"字段改名为"计划持续起始日期"，原单个 picker 改为起止日期双 picker（开始日期 + "至" + 结束日期）
     - `form` reactive 对象：`date` 单字段改为 `startDate` + `endDate` 双字段，新增 `status: 1` 字段
     - `handleDateChange` 拆分为 `handleStartDateChange` 和 `handleEndDateChange`
     - `handleSave` 校验逻辑：分别校验开始/结束日期非空 + 结束日期不早于开始日期；提交时传递 `start_date`、`end_date`、`status`
     - `handleNewEntry` 重置表单同步重置 `startDate`、`endDate`、`status`
     - 时间选择控件 UI 美化：`.plan-page__time-row` 移除灰色背景，改为与日期选择器统一的 `#f9f9f9` 背景 + `#e8ebe6` 内边框；高度由 40px 调整为 41px；padding 上下对称
     - `.plan-page__time-picker-display` 高度 32px → 41px，padding 改为 `10px 12px`
     - `.plan-page__time-delete` 尺寸调整为 32×41px
     - `.plan-page__textarea` 新增 `width: 100%` 确保备注输入框占满所在行
     - 新增"任务状态"单选框组（进行中=1/暂停=2/已结束=0），默认选中"进行中"
     - 新增 `.plan-page__status-row`、`.plan-page__radio`、`.plan-page__radio-dot` 等单选框样式
     - `.plan-page__picker-display` 新增 `flex: 1` 支持起止日期选择器平分宽度，padding 改为上下对称 `10px 12px`
     - 新增 `.plan-page__date-range` 和 `.plan-page__date-separator` 起止日期布局样式

3. **settings.vue 动态信息展示（Part 3）**
   - **需求**：制定计划和通知方式卡片右侧文字从数据库动态获取，空状态显示为空
   - `frontend/src/pages/index/settings.vue`：
     - 导入 `listNotificationChannels` 和 `listPlans` API，新增 `channels` 和 `plans` ref
     - `onMounted` 调用 `loadChannels()` 和 `loadPlans()` 加载用户数据
     - `activePlanName` 计算属性：返回第一个 `status === 1` 的计划名称，无则返回空字符串
     - `notificationTypeName` 计算属性：返回除"站内信"外第一个通知类型名称，无则返回空字符串
     - "制定计划"卡片右侧：有进行中计划时显示计划名称（绿色）+ "进行中"状态徽章；无则右侧为空且不显示徽章
     - "通知方式"卡片右侧：有非站内信通知方式时显示通知类型名称；无则右侧为空

4. **后端 status 与 enabled 字段支持（Part 4）**
   - **需求**：checkin_plans 表 status 支持"暂停"状态，邮件渠道支持 enabled 字段
   - `backend/sql/create_yao_db.sql`：`checkin_plans.status` 注释从 `1-进行中，0-已结束` 改为 `1-进行中，2-暂停，0-已结束`；`start_date`/`end_date` 注释从"打卡"改为"计划"
   - `backend/app/models/plan.py`：`CheckinPlan.status` 注释同步更新
   - `backend/app/schemas/plan.py`：`CreatePlan` 新增 `status: int = 1` 字段及 `validate_status` 校验器（仅允许 0/1/2）
   - `backend/app/schemas/notification_channel.py`：`CreateEmailChannel` 和 `UpdateEmailChannel` 新增 `enabled: bool = True` 字段
   - `backend/app/services/plan_service.py`：`create_plan` 方法新增 `status: int = 1` 参数，写入主记录时使用该参数
   - `backend/app/services/notification_channel_service.py`：`create_email_channel` 和 `update_email_channel` 新增 `enabled: bool = True` 参数，写入/更新时使用该参数
   - `backend/app/api/v1/plans.py`：`create_plan` 端点传递 `status=payload.status`
   - `backend/app/api/v1/notification_channels.py`：`create_email_channel` 和 `update_email_channel` 端点传递 `enabled=payload.enabled`
   - `frontend/src/api/modules/plan.js`：`createPlan` 函数新增 `status = 1` 参数
   - `frontend/src/api/modules/notification.js`：`createEmailChannel` 和 `updateEmailChannel` 函数新增 `enabled = true` 参数
   - **数据库同步执行**：`ALTER TABLE checkin_plans MODIFY COLUMN status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-进行中，2-暂停，0-已结束'`

### 影响范围

- **通知方式管理**：邮件通知新增启用/停用控制，新建邮件通知时需先绑定邮箱，表单排序优化（邮件优先）
- **计划制定**：日期改为起止日期范围选择，新增任务状态管理（进行中/暂停/已结束），时间选择控件与日期选择器视觉统一
- **设置页**：制定计划和通知方式卡片右侧信息从数据库动态获取，空状态显示为空，反映用户实际配置情况
- **数据库结构**：`checkin_plans.status` 扩展支持 2-暂停 状态；邮件通知渠道支持 `enabled` 字段控制启用/停用

---

## 2026-07-01 - 通知渠道与计划模块从零搭建、登录隐私勾选、表单与数据库重构

### 主要变动

1. **login.vue 微信登录隐私勾选（Part 1）**
   - **需求**：微信一键登录图片下方新增"查看并同意《隐私政策》"单选框，未勾选时点击登录弹出确认对话框，确认后直接执行登录
   - `frontend/src/pages/user/login.vue`：
     - 微信登录区新增独立隐私勾选（`wechatAgree` ref，与账号密码登录的 `remember` 分离互不影响）
     - 新增 `toggleWechatAgree()` 方法切换勾选状态
     - `handleWechatLogin()` 开头加入隐私勾选校验：未勾选时 `uni.showModal`，用户点击"确认"后设置 `wechatAgree=true` 并递归调用 `handleWechatLogin()`
     - 新增 `.login-page__remember--wechat` 样式（居中显示）

2. **notification.vue 通知方式调整（Part 2-4）**
   - **需求**：移除站内信删除图标、新用户注册自动添加站内信、表单通知类型单选、邮件展开 SMTP、卡片动态展示、邮件卡片点击展开配置表单
   - **前端** `frontend/src/pages/index/notification.vue` 完全重写：
     - 通知方式列表改为从数据库动态加载（`loadChannels()` onMounted 调用 `listNotificationChannels`）
     - 站内信卡片：移除删除图标、无点击事件、副标题显示"系统内置通知"
     - 邮件卡片：保留删除图标（`handleDeleteEmail` 二次确认后调用 `deleteNotificationChannel`）；点击卡片展开/收起 SMTP 配置表单（`toggleEmailEdit` 切换 `expandedEmailId`）
     - 邮件配置表单：包含 SMTP服务器地址/端口/发件邮箱地址/客户端专用密码 + 提交按钮，调用 `updateEmailChannel` 更新数据库
     - "新建通知方式"表单：通知类型单选（站内信/邮件），选站内信时保存按钮置灰（`canSave` 计算属性返回 false）；选邮件时显示 SMTP 配置输入框，填写完整后调用 `createEmailChannel`
     - 新增单选框样式（`.notification-page__radio*`，CSS 绘制圆点）
     - 新增保存按钮置灰态样式（`.notification-page__save--disabled`）

3. **新用户注册自动添加站内信通知方式（Part 2.2-2.3）**
   - **需求**：普通注册+微信登录注册的新用户均自动添加站内信通知方式，channel_value=用户ID，channel_type='站内信'
   - `backend/app/services/user_service.py`：
     - `register()` 方法：用户入库后 `flush` 获取ID，调用 `_ensure_znx_channel(user.id)` 自动创建站内信渠道，再 commit
     - `wechat_login()` 方法：新用户分支创建小程序账号后调用 `_ensure_znx_channel(user.id)` 自动创建站内信渠道
     - 新增 `_ensure_znx_channel(user_id)` 方法：查询是否已存在站内信渠道，存在则返回；否则创建 `NotificationChannel(user_id, channel_type='站内信', channel_value=str(user_id), enabled=True)`
     - 导入 `NotificationChannel` 模型和 `CHANNEL_TYPE_ZNX` 常量

4. **数据库表结构变更（Part 5）**
   - **需求**：将 `plan_checkin_times` 表重命名为每日通知时间点表，所有"打卡"相关字段名/注释改为"通知"含义
   - `backend/sql/create_yao_db.sql`：
     - `plan_checkin_times` → `plan_notification_times`（每日通知时间点表）
     - `checkin_time` 字段 → `notification_time`（每日通知时刻 HH:MM:SS）
     - 表注释：'每个计划的多个打卡时间点' → '每个计划的多个通知时间点'
     - `checkin_records.plan_time_id` 字段注释：'对应的打卡时间点ID（plan_checkin_times.id）' → '对应的通知时间点ID（plan_notification_times.id）'
   - **数据库同步执行**：通过 `RENAME TABLE` 和 `ALTER TABLE ... CHANGE COLUMN` 同步实际数据库结构

5. **channel_value 字段扩展（Part 3.4）**
   - **需求**：评估 VARCHAR(255) 是否足以存储邮件配置 JSON，不足则扩展
   - 评估：邮件 JSON 配置 `{"smtp_host":"smtp.exmail.qq.com","smtp_port":465,"email":"user@example.com","password":"xxx"}` 约 100-150 字符，理论 VARCHAR(255) 够用；但考虑密码可能较长、SMTP地址多样，扩展为 TEXT 更稳健
   - `backend/sql/create_yao_db.sql`：`channel_value` 从 `VARCHAR(255)` 改为 `TEXT`
   - 移除原唯一索引 `uk_user_type_value`（TEXT 字段不能直接建唯一索引），新增普通联合索引 `idx_user_type (user_id, channel_type)`
   - `channel_type` 注释从 '通知类型（如 email、sms、wechat）' 改为 '通知类型（站内信、邮件）'
   - **数据库同步执行**：`ALTER TABLE notification_channels MODIFY COLUMN channel_value TEXT NOT NULL` 等

6. **plan.vue 新建计划模块调整（Part 6）**
   - **需求**：日期范围改日期单选（禁止手动输入）、时间起止双控件改单时间+添加时间按钮、通知方式动态加载、保存计划写入三张表
   - `frontend/src/pages/index/plan.vue` 完全重写：
     - 已有计划列表：从数据库动态加载（`loadPlans()` 调用 `listPlans`），按 status 显示绿色/灰色色条
     - 计划名称、备注：保留原 input/textarea
     - 日期：用 `<picker mode="date">` 替代原 input，禁止手动输入
     - 提醒时间：用 `<picker mode="time">` 替代原起止双 input；"添加时间"按钮 `handleAddTime` 动态 push 空字符串到 `form.times` 数组；删除按钮 `handleDeleteTime` 至少保留一个
     - 通知方式：onMounted 调用 `loadChannels()` 加载用户已配置的通知渠道，渲染为复选框列表
     - 保存计划 `handleSave`：前端基础校验（名称/日期/时间/通知方式非空）→ 调用 `createPlan` API（start_date=end_date=单选日期，notification_times=过滤后的有效时间数组，channel_ids=选中的渠道ID数组）
     - 卡片菜单 `handleMenu`：使用 `uni.showActionSheet` 提供删除计划选项，调用 `deletePlan` API

7. **后端基础设施从零搭建**
   - **ORM 模型**（`backend/app/models/`）：
     - `notification_channel.py`：`NotificationChannel` 模型（id/user_id/channel_type/channel_value(TEXT)/enabled/created_at/updated_at）
     - `plan.py`：`CheckinPlan`（主表）+ `PlanNotificationTime`（时间点表）+ `PlanNotificationChannel`（多对多关联表）；CheckinPlan 与后两者建立 `relationship` + `back_populates` 关系，支持 `selectinload` 预加载
   - **Schemas**（`backend/app/schemas/`）：
     - `notification_channel.py`：`EmailChannelValue`（邮件JSON结构）、`CreateEmailChannel`、`UpdateEmailChannel`、`DeleteChannel`；常量 `CHANNEL_TYPE_ZNX='站内信'`、`CHANNEL_TYPE_EMAIL='邮件'`
     - `plan.py`：`CreatePlan`（含 user_id/name/remark/start_date/end_date/notification_times(List[str])/channel_ids(List[int])），Pydantic v2 field_validator 校验时间格式和必填项
   - **Services**（`backend/app/services/`）：
     - `notification_channel_service.py`：`NotificationChannelService` 类，含 `list_by_user`/`get_by_id`/`ensure_znx_channel`/`create_email_channel`/`update_email_channel`/`delete_channel`（站内信禁删）/`parse_email_channel_value` 静态方法
     - `plan_service.py`：`PlanService` 类，含 `list_by_user`（selectinload预加载）/`get_by_id`/`create_plan`（校验渠道归属+写入主表/时间点/关联）/`delete_plan`（级联删除）
   - **API 路由**（`backend/app/api/v1/`）：
     - `notification_channels.py`：`GET /{user_id}/list`、`POST /email`、`PUT /email`、`DELETE /`（请求体含 channel_id+user_id）
     - `plans.py`：`GET /{user_id}/list`、`POST`、`DELETE /{plan_id}?user_id=`
     - `__init__.py` 注册新路由：`notification_channels` 前缀 `/notification-channels`，`plans` 前缀 `/plans`
   - **前端 API 封装**（`frontend/src/api/modules/`）：
     - `notification.js`：`listNotificationChannels`/`createEmailChannel`/`updateEmailChannel`/`deleteNotificationChannel`
     - `plan.js`：`listPlans`/`createPlan`/`deletePlan`

### 影响范围

- **新用户**：无论是普通注册还是微信一键登录注册，系统自动为其创建站内信通知渠道，无需手动配置即可接收系统通知
- **通知方式管理**：用户可查看已配置的通知方式卡片（站内信不可删，邮件可删可改），可添加邮件通知方式（含完整SMTP配置，JSON存储）
- **计划制定**：日期改为单选 picker（禁止手动输入），时间改为多个单时间 picker（可动态增删），通知方式从数据库动态加载用户已配置的渠道
- **数据库结构**：`plan_checkin_times` 表重命名为 `plan_notification_times`，字段 `checkin_time` 改为 `notification_time`；`notification_channels.channel_value` 从 VARCHAR(255) 扩展为 TEXT；唯一索引 `uk_user_type_value` 替换为普通索引 `idx_user_type`
- **API 端点新增**：通知渠道 4 个端点（列表/创建邮件/更新邮件/删除）+ 计划 3 个端点（列表/创建/删除）

---

## 2026-07-01 - 账号合并动态更新、验证码异步发送、账号删除前端状态同步

### 主要变动

1. **账号合并后动态更新（Task 1）**
   - **问题**：`bind_email` 接口仅返回 id/username/email，账号合并后头像、签名等关联信息未同步更新
   - `backend/app/api/v1/users.py` `bind_email` 端点：响应扩展为完整用户信息（新增 signature/avatar_url/has_password/status 字段，与登录接口格式一致）
   - `frontend/src/pages/user/profile.vue` `handleChangeEmail` bindEmail 分支：改用 `userStore.setUser(result.data)` 替代手动字段赋值，确保合并后所有关联信息（头像、昵称、签名、绑定状态等）实时更新

2. **获取验证码异步优化（Task 2）**
   - **问题根因**：`send_code_for_purpose` 同步调用 `Email().send_verification_code()` 执行 SMTP_SSL 连接（timeout=10s），阻塞 FastAPI 事件循环 2-5 秒，导致前端点击"获取验证码"后延迟数秒才弹出提示并开始倒计时
   - `backend/app/services/user_service.py`：
     - 新增 `import asyncio` 和 `from ..utils.logger import logger`
     - `send_code_for_purpose`：邮件发送改为 `asyncio.create_task(self._send_email_async(email, code))`（fire-and-forget），生成验证码并入库后立即返回，不等待 SMTP
     - 新增 `_send_email_async` 方法：通过 `asyncio.to_thread` 在线程池中执行同步 SMTP 调用，失败仅记录日志不阻塞响应
   - **影响范围**：所有涉及"获取验证码"的页面（register.vue、forgot-password.vue、profile.vue）均自动受益，无需逐页修改前端代码

3. **账号删除后前端状态同步（Task 3）**
   - **问题**：后端 1 分钟后自动删除 status=0 的账号，但前端无法感知，缓存残留导致状态不同步
   - `backend/app/api/v1/users.py`：新增 `GET /{user_id}/info` 端点，返回用户完整信息或 404（用于前端验证账号是否存在）
   - `frontend/src/api/modules/user.js`：新增 `getUserInfo(user_id)` 函数
   - `frontend/src/store/modules/user.js`：
     - 新增 `deletionTimer` 定时器：`startDeletionCountdown()` 在 scheduleDeletion 成功后启动 60 秒倒计时，到期自动 `clearUser()` + `reLaunch` 到登录页
     - 新增 `clearDeletionTimer()`：cancelDeletion 成功后清除倒计时
     - 新增 `verifyUserExists()`：调用 `GET /{user_id}/info`，404 时清除前端状态并跳转登录页；账号仍存在时刷新本地信息
   - `frontend/src/App.vue` `onShow`：应用回到前台时，若 `userInfo.status === 0`，调用 `verifyUserExists()` 检测账号是否已被后端删除
   - `frontend/src/pages/user/profile.vue`：
     - `handleDeletion` scheduleDeletion 分支：成功后调用 `userStore.startDeletionCountdown()`
     - `handleDeletion` cancelDeletion 分支：成功后调用 `userStore.clearDeletionTimer()`
   - **覆盖场景**：正常删除（前端 60s 定时器自动清理）、应用后台/关闭后恢复（App.vue onShow 检测）、异常中断（verifyUserExists 兜底）

### 影响范围

- 账号合并后用户界面立即显示主账号的完整信息（头像、昵称、签名、邮箱、密码状态等），无需重新登录
- 所有"获取验证码"操作响应时间从 2-5 秒降至毫秒级，用户体验显著提升
- 账号删除后前端自动切换至未登录状态，覆盖正常删除、应用后台恢复、异常中断等全部场景

---

## 2026-07-01 - 签名实时同步、邮箱绑定合并流程优化、数据库 deletion_scheduled_at 字段清理

### 主要变动

1. **settings.vue 签名实时同步（Task 1）**
   - **问题根因**：`profile.vue` 的 `handleUpdateSignature` 调用后端 API 成功后未更新本地 `userStore.userInfo.signature`，导致用户返回 settings.vue 时签名显示仍为旧值
   - `frontend/src/pages/user/profile.vue` `handleUpdateSignature`：API 成功后同步 `userStore.userInfo.signature = signatureForm.value.trim()` 并持久化到本地存储
   - `frontend/src/pages/index/settings.vue` 无需改动：`displaySlogan` 为基于 `userStore.userInfo.signature` 的 computed，store 更新后自动刷新

2. **邮箱绑定合并流程优化（Task 2）**
   - **问题根因**：`send_change_email_new_code` 对所有场景都拒绝已存在的邮箱，导致无邮箱用户绑定已有邮箱（触发账号合并）时无法获取验证码
   - `backend/app/services/user_service.py` `send_change_email_new_code`：新增 `allow_existing: bool = False` 参数，仅当 `allow_existing=False` 时校验邮箱唯一性
   - `backend/app/schemas/user.py` `SendChangeEmailNewCode`：新增 `allow_existing: bool = False` 字段
   - `backend/app/api/v1/users.py` `/send-change-email-new-code`：传递 `payload.allow_existing` 到 service 层
   - `frontend/src/api/modules/user.js` `sendChangeEmailNewCode`：新增第二参数 `allow_existing = false`，传入请求 body
   - `frontend/src/pages/user/profile.vue` `handleGetNewEmailCode`：传 `!hasEmail.value` 作为 `allow_existing`（绑定邮箱场景允许已存在以触发合并，修改邮箱场景禁止已存在）
   - 业务逻辑保持一致：已有邮箱用户修改邮箱仍拒绝已存在邮箱；无邮箱用户绑定已有邮箱后由 `bind_email` 执行账号合并

3. **数据库 deletion_scheduled_at 字段清理（Task 3）**
   - 执行 SQL：`ALTER TABLE wuzuniao_yonghu.users DROP COLUMN deletion_scheduled_at;`
   - 验证：users 表剩余列与 ORM 模型完全一致（id/username/email/password_hash/signature/avatar_url/status/last_login_at/created_at/updated_at）

### 影响范围

- 用户在 profile.vue 修改签名后，返回 settings.vue 即可看到最新签名，无需重新登录
- 无邮箱用户（如微信登录用户）绑定已有邮箱时不再被"该邮箱已被注册"拦截，可正常获取验证码并完成账号合并
- 数据库 users 表结构精简，deletion_scheduled_at 字段已彻底移除，与 ORM 模型和建表 SQL 完全一致

---

## 2026-07-01 - status 字段替代 deletion_scheduled_at、字段长度限制调整、签名3行截断、profile.vue 置灰逻辑

### 主要变动

1. **settings.vue 签名3行截断（Task 1）**
   - `frontend/src/pages/index/settings.vue`：`.settings-page__profile-slogan` 由 2 行截断改为 3 行截断（`-webkit-line-clamp: 3`）

2. **数据库字段长度限制调整（Task 2）**
   - 字段长度计算依据：基于 settings.vue 资料卡文本区（375px 标准屏幕约 191px 宽）的可视字符数 × 1.5
   - `backend/app/models/user.py`：`username` 长度 `String(50)` → `String(15)`（28px 字体约 9 可视字符 × 1.5 ≈ 15）；`signature` 长度 `String(255)` → `String(70)`（16px 字体 3 行约 46 可视字符 × 1.5 ≈ 70）
   - `backend/sql/create_user_db.sql`：同步更新 `username VARCHAR(15)`、`signature VARCHAR(70)`
   - `backend/app/schemas/user.py`：`RegisterUser.validate_username`、`UpdateUsername.validate_new_username` 校验由 2-50 改为 2-15；`UpdateSignature.validate_signature` 由 255 改为 70；`ScheduleDeletion` 注释由"计划注销账号"改为"计划删除账号"
   - `frontend/src/pages/user/register.vue`：用户名校验由 2-50 改为 2-15
   - `frontend/src/pages/user/profile.vue`：用户名输入框 maxlength 50→15、placeholder "2-50"→"2-15"、校验消息同步；签名输入框 maxlength 255→70、placeholder "255"→"70"、校验消息同步

3. **用 status 字段替代 deletion_scheduled_at（Task 3）**
   - **字段语义**：`status` 1-正常，0-待删除（用户确认删除后置 0，后台任务 1 分钟后清理）
   - `backend/app/models/user.py`：删除 `deletion_scheduled_at` 字段，更新 `status` 字段注释
   - `backend/sql/create_user_db.sql`：删除 `deletion_scheduled_at` 列，更新 `status` 注释
   - `backend/app/services/user_service.py`：
     - `schedule_deletion`：改为 `user.status = 0`（不再设置 deletion_scheduled_at）
     - `cancel_deletion`：改为 `user.status = 1`
     - `purge_expired_deletions`：查询条件改为 `status == 0 AND updated_at <= (now - 1min)`，利用 updated_at 字段判断 1 分钟到期
   - `backend/app/api/v1/users.py`：login、wechat-login、schedule-deletion、cancel-deletion 端点响应中 `deletion_scheduled_at` 字段替换为 `status`；schedule-deletion/cancel-deletion 端点文档字符串与提示消息由"注销"更新为"删除"
   - `backend/app/main.py`：更新后台清理任务注释，反映新的 status+updated_at 查询条件
   - `frontend/src/store/modules/user.js`：`setUser` 存储 `status` 替代 `deletion_scheduled_at`（默认值 1）
   - `frontend/src/api/modules/user.js`：更新 scheduleDeletion/cancelDeletion 函数注释

4. **profile.vue 状态检测与置灰逻辑（Task 4）**
   - `frontend/src/pages/user/profile.vue`：
     - `isDeletionScheduled` 计算属性改为基于 `userStore.userInfo?.status === 0`
     - 分组1（资料修改）容器添加 `:class="{ 'profile-page__group--disabled': isDeletionScheduled }"`，账号待删除状态下整体置灰（opacity: 0.5）并禁用所有交互（pointer-events: none）
     - `toggleSection` 函数添加守卫：status=0 时禁止展开任何修改项
     - `handleDeletion` 中 scheduleDeletion 成功后设置 `userStore.userInfo.status = result.data.status`；cancelDeletion 成功后设置 `userStore.userInfo.status = 1`
     - 新增 CSS `.profile-page__group--disabled`：opacity 0.5 + pointer-events none
   - 仅保留"退出登录"和"取消删除"两个可交互按钮（分组2不受置灰影响）
   - 取消删除后 status 恢复为 1，所有控件自动恢复正常显示与交互

### 影响范围

- 用户名/签名长度限制更贴合界面可视范围（约 1.5 倍），减少超长文本对布局的冲击
- 账号删除机制改用 status 字段标识，逻辑更清晰；删除 deletion_scheduled_at 字段，数据库结构简化
- profile.vue 在账号待删除状态下自动置灰所有修改入口，防止用户在删除倒计时期间修改资料；取消删除后自动恢复
- 前后端字段长度限制提示统一为 2-15（用户名）和 70（签名）

---

## 2026-07-01 - 账号删除机制修复、绑定邮箱合并逻辑、文本统一与资料卡截断优化

### 主要变动

1. **profile.vue 文本统一（Task 1）**
   - `frontend/src/pages/user/profile.vue`：将所有"注销账号"统一替换为"删除账号"，"取消注销"替换为"取消删除"，相关提示（弹窗标题/内容/按钮/toast）中的"注销"全部更新为"删除"（共11处）

2. **修复账号定时删除机制（Task 2）**
   - **问题根因**：`schedule_deletion` 方法仅设置 `deletion_scheduled_at` 时间戳，但后端没有任何后台任务轮询并执行实际删除，导致到期后账号数据未被清除
   - `backend/app/services/user_service.py`：新增 `purge_expired_deletions` 方法，查询 `deletion_scheduled_at <= now()` 的到期账号，删除其主记录及关联的 `user_miniapp_accounts` 记录
   - `backend/app/main.py`：新增 `lifespan` 上下文管理器，启动 asyncio 后台任务 `_purge_expired_deletions_loop`，每 30 秒轮询一次数据库清理到期账号；异常不中断循环仅记录日志，应用关闭时取消任务

3. **绑定邮箱账号合并逻辑（Task 3）**
   - `backend/app/services/user_service.py` `bind_email` 方法重构：
     - 邮箱不存在：直接绑定到当前账号（原有逻辑）
     - 邮箱已存在：触发账号合并，以已有邮箱账号为主账号，合并当前无邮箱账号的信息（username/password_hash/signature/avatar_url 字段为空时填充，冲突时保留主账号；last_login_at 取较新值），转移从账号的小程序绑定记录到主账号，删除从账号
   - `frontend/src/pages/user/profile.vue` `handleChangeEmail`：bindEmail 分支更新为使用后端返回的 id/username/email 同步本地状态（合并场景下 id 可能变化）

4. **settings.vue 资料卡截断优化（Task 4）**
   - `frontend/src/pages/index/settings.vue`：修复用户名或个性签名字符过多时文本超出边界、头像框被挤压消失的问题
   - `.settings-page__profile-info` 添加 `min-width: 0`（flexbox 文本截断关键）
   - `.settings-page__profile-name` 添加单行截断（`overflow: hidden; text-overflow: ellipsis; white-space: nowrap`）
   - `.settings-page__profile-slogan` 改为2行截断（`display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden`），去掉 `white-space: pre-line`

### 影响范围

- 账号删除功能现已可靠工作：到期后 30 秒内自动清除账号及关联数据
- 绑定邮箱时若邮箱已存在，自动合并账号信息，避免数据孤立
- profile.vue 中"注销"相关文案统一为"删除"，术语更清晰
- settings.vue 资料卡长文本正确截断显示省略号，布局不再被撑开

---

## 2026-07-01 - profile.vue 功能扩展与默认值/布局优化

### 主要变动

1. **页面布局统一调整（Task 1）**
   - 由于 NoticeButton 组件已增加约 5px 顶部相对距离（0.6vh），将所有 12 个页面的第一个正式元素 `padding-top` 由 100px 统一调整为 105px
   - 涉及页面：`index/record.vue`、`index/settings.vue`、`index/plan.vue`、`index/notification.vue`、`index/index.vue`、`user/login.vue`、`user/register.vue`、`user/forgot-password.vue`、`user/contact.vue`、`user/profile.vue`、`user/help.vue`、`user/privacy.vue`

2. **index.vue 打卡按钮定位优化（Task 2）**
   - `pages/index/index.vue`：将"立即打卡"大圆形按钮调整为距底部导航栏顶部 20% 页面高度（20vh）的位置
   - 使用 `min-height: calc(100vh - 17px)` + `margin-top: auto` + `margin-bottom: calc(20vh + 101px)` 实现响应式定位（101px = BottomNav 高 86px + 底部偏移 15px），保证不同屏幕尺寸下相对位置一致

3. **settings.vue 头像与用户名显示逻辑修复（Task 3）**
   - `pages/index/settings.vue`：
     - 头像逻辑：未登录使用默认头像 `hong`；已登录从数据库 `avatar_url` 字段获取，空值则不显示头像（添加 `v-if="avatarUrl"`）
     - 用户名逻辑：已登录用户无用户名时显示空字符串，而非"请登录"（仅未登录时显示"请登录"）

4. **微信登录注册账号默认值（Task 4）**
   - `backend/app/services/user_service.py` `wechat_login`：新用户自动设置默认用户名（"无足鸟"+自增数字，通过 `_generate_default_username` 查询数据库生成）、默认头像 `lan`、默认签名"行有不得，反求诸己。"

5. **普通用户注册默认值（Task 5）**
   - `backend/app/services/user_service.py` `register`：新增默认头像 `hei` 与默认签名"蜗角虚名，蝇头微利，算来著甚干忙。事皆前定，谁弱又谁强。"

6. **profile.vue 功能逻辑调整（Task 6）**
   - **修改用户名**：在"修改头像"前新增"修改用户名"功能，前端格式校验（2-50 字符，中英文数字）+ 后端唯一性校验（`update_username` 方法，排除自身）
   - **注销账号**：冷静期由 24 小时调整为 1 分钟（`schedule_deletion` 方法 `timedelta(minutes=1)`）
   - **修改密码 / 设置密码**：根据 `has_password` 字段动态显示"修改密码"或"设置密码"；无密码用户隐藏旧密码输入框，直接调用 `set_password` 接口设置新密码；设置成功后更新本地 `has_password` 状态
   - **修改邮箱 / 绑定邮箱**：根据 `email` 字段动态显示"修改邮箱"或"绑定邮箱"；无邮箱用户跳过旧邮箱验证步骤，直接显示新邮箱+验证码表单，调用 `bind_email` 接口绑定；绑定成功后更新本地 `email` 状态

### 后端新增

- `backend/app/schemas/user.py`：新增 `UpdateUsername`、`SetPassword`、`BindEmail` 三个 Schema
- `backend/app/services/user_service.py`：新增 `_generate_default_username`、`update_username`、`set_password`、`bind_email` 四个方法
- `backend/app/api/v1/users.py`：新增 `PUT /update-username`、`PUT /set-password`、`PUT /bind-email` 三个接口；`/login` 和 `/wechat-login` 响应新增 `email` 和 `has_password` 字段；`/schedule-deletion` 消息更新为"1分钟后"

### 前端新增

- `frontend/src/store/modules/user.js`：`setUser` 方法新增 `email` 和 `has_password` 字段存储
- `frontend/src/api/modules/user.js`：新增 `updateUsername`、`setPassword`、`bindEmail` 三个 API 函数
- `frontend/src/pages/user/profile.vue`：
  - 新增 `hasPassword`、`hasEmail` computed 属性
  - 新增 `usernameForm` 状态、`validateUsername` 校验函数、`handleUpdateUsername` 处理函数
  - `handleChangePassword` 根据 `hasPassword` 分支调用 `changePassword` 或 `setPassword`
  - `handleChangeEmail` 根据 `hasEmail` 分支调用 `changeEmail` 或 `bindEmail`
  - 注销账号弹窗消息从"24小时/一天"更新为"1分钟"

### 影响范围

- 微信登录用户现可享受完整的资料管理功能（设置密码、绑定邮箱、修改用户名）
- 普通注册用户和微信登录用户均有默认头像和签名，无需手动设置
- 所有页面顶部内容与通知组件保持 5px 间距，布局更协调
- 打卡按钮在不同屏幕尺寸下保持距底部导航栏 20% 的相对位置

---

## 2026-06-30 - 新增微信一键登录功能

### 主要变动

1. **后端新增功能（backend）**
   - `requirements.txt`：新增 `httpx>=0.24.0` 依赖（用于异步调用微信 jscode2session 接口）
   - `app/core/config.py`：Settings 新增 `WX_APPID`、`WX_APP_SECRET` 配置项
   - `app/models/user_miniapp_account.py`：新增 UserMiniappAccount ORM 模型（对应 user_miniapp_accounts 表，存储 openid + session_key）
   - `app/schemas/user.py`：新增 `WeChatLogin` Schema（校验 code 非空）
   - `app/services/user_service.py`：新增 `_code2session`（调用微信接口）和 `wechat_login`（查找/创建用户 + 存储 session_key）方法
   - `app/api/v1/users.py`：新增 `POST /wechat-login` 接口

2. **前端新增功能（frontend）**
   - `api/modules/user.js`：新增 `wechatLogin` API 调用函数
   - `pages/user/login.vue`：实现 `handleWechatLogin`（wx.login 获取 code → 发送后端 → 写入状态 → 跳转）

### 影响范围
- 微信登录用户自动创建账号（username/email/password 为空），通过 openid 关联
- session_key 安全存储于后端 user_miniapp_accounts 表，不下发到前端
- 支持已绑定微信用户再次登录时更新 session_key

---

## 2026-06-30 - 新增修改头像、注销账号功能及响应式布局优化

### 主要变动

1. **后端新增功能（backend）**
   - `app/models/user.py`：User 模型新增 `deletion_scheduled_at` 字段（账号注销计划时间）
   - `app/services/user_service.py`：新增 `update_avatar`、`schedule_deletion`、`cancel_deletion` 方法
   - `app/schemas/user.py`：新增 `UpdateAvatar`、`ScheduleDeletion` Schema
   - `app/api/v1/users.py`：新增 `/update-avatar`、`/schedule-deletion`、`/cancel-deletion` 接口；登录接口返回 `deletion_scheduled_at` 字段
   - `.env.dev`：新增 `WX_APPID`、`WX_APP_SECRET` 环境变量
   - `sql/create_user_db.sql`：同步更新建表脚本，新增 `deletion_scheduled_at` 字段

2. **前端新增功能（frontend）**
   - `pages/user/profile.vue`：新增"修改头像"功能项（头像列表选择+提交）；新增"注销账号"功能项（二次确认+24小时冷静期+取消注销）
   - `pages/index/settings.vue`：默认头像路径改为 `touxiang/hong.png`；头像显示改为 key 映射方式
   - `api/modules/user.js`：新增 `updateAvatar`、`scheduleDeletion`、`cancelDeletion` 函数
   - `store/modules/user.js`：`setUser` 方法支持 `deletion_scheduled_at` 字段
   - `components/NoticeButton.vue`：顶部间距改为基于 `statusBarHeight` 动态计算，新机型自动增加约 10px

### 影响范围
- 用户个人信息管理（头像、注销）
- 设置页头像显示
- 全局通知按钮顶部间距适配

---

## 2026-06-29 - 实现用户登录功能与状态管理

### 主要变动

1. **后端登录接口（backend）**
   - `app/schemas/user.py`：新增 `LoginUser` Schema（用户名或邮箱 + 密码校验）
   - `app/services/user_service.py`：新增 `login` 方法（支持用户名或邮箱查找，密码校验）
   - `app/api/v1/users.py`：新增 `/login` 接口（返回用户信息：id、username、signature、avatar_url）

2. **前端状态管理（frontend）**
   - 安装 Pinia 2（兼容 Vue 3.4.21）
   - 创建 `store/index.js`：Pinia 实例创建与导出
   - 创建 `store/modules/user.js`：用户状态管理（userInfo、setUser、clearUser、本地存储持久化）
   - 修改 `main.js`：注册 Pinia

3. **前端登录页（frontend/src/pages/user/login.vue）**
   - 参照 register.vue 实现前后端双重验证机制：
     - 前端验证：用户名/密码非空校验，失焦实时校验，错误视觉反馈（红色边框 + 文字提示）
     - 后端验证：调用 `/login` 接口验证用户名/邮箱 + 密码
   - 登录成功：写入 Pinia 用户状态 → toast 提示 → 1.5秒后跳转 settings.vue

4. **设置页用户状态显示（frontend/src/pages/index/settings.vue）**
   - 用户资料卡动态显示登录状态：
     - 已登录：显示用户 username、signature、avatar_url，点击跳转 profile.vue
     - 未登录：用户名显示"请登录"，点击跳转 login.vue

5. **个人信息页退出登录（frontend/src/pages/user/profile.vue）**
   - 退出登录时调用 `userStore.clearUser()` 清除用户状态
   - toast 提示 → 1.5秒后跳转 settings.vue

6. **前端 API（frontend/src/api/modules/user.js）**
   - 新增 `loginUser` 接口：调用后端 `/login`

### 影响范围：
- 后端：schemas/user.py、services/user_service.py、api/v1/users.py（新增登录功能）
- 前端：新增 store 目录（Pinia 状态管理）、修改 main.js（注册 Pinia）、修改 login.vue（实现登录验证）、修改 settings.vue（用户状态显示）、修改 profile.vue（退出登录清除状态）、修改 api/modules/user.js（新增 loginUser）
- 文档：目录结构.json（新增 store 目录描述）、更新记录.md（本次变更）

---

## 2026-06-29 - 修复注册流程双重故障（网络错误 + 密码哈希失败）

### 问题现象
1. Phase 1：注册页点击"获取验证码"提示"网络请求失败:fail"（已勾选"不校验合法域名"）
2. Phase 2：网络修复后，验证码能收到，但提交注册提示"验证码错误或已过期"（1分钟内操作，未超时）

### 根因分析（科学调试流程 + 插桩取证）

#### Phase 1: 网络请求失败
1. `netstat -ano | findstr :8000` → 无输出，8000 端口无监听进程
2. `python -c "import pydantic_settings"` → ModuleNotFoundError，后端依赖未安装
3. `pip install -r requirements.txt` → 失败，`python-jose` 在 Python 3.14 上无可用版本
4. 根因链：python-jose 不可用 → pip install 整体失败 → 依赖缺失 → 后端无法启动 → 端口无监听 → 连接拒绝 → request:fail

#### Phase 2: 密码哈希失败（通过 verify_code 插桩日志定位）
1. 插桩日志显示第一次提交 `verify_code 验证通过`，但随后 `(trapped) error reading bcrypt version`
2. `get_password_hash('Test1234!')` 直接测试 → `ValueError: password cannot be longer than 72 bytes`
3. 根因：passlib 1.7.4 访问 `bcrypt.__about__.__version__`（bcrypt 5.0.0 已移除该属性）→ passlib 内部 `detect_wrap_bug` 调用 `bcrypt.hashpw` 抛异常
4. 连锁反应：哈希失败 → register 抛 ValueError → 400 → 验证码被 verify_code 消费（一次性）→ 第二三次提交"记录不存在"

### 修复方案

#### Phase 1: 网络请求失败
1. **安装缺失依赖**：pip install pydantic-settings passlib[bcrypt] python-multipart email-validator（清华镜像源）
2. **替换 python-jose 为 pyjwt**：`requirements.txt` 中 `python-jose[cryptography]>=3.3.0` → `pyjwt>=2.8.0`
   - 原因：python-jose 已过时且不支持 Python 3.14；当前代码未使用 JWT；pyjwt 是现代替代方案
3. **启动后端服务**：uvicorn app.main:app --host 0.0.0.0 --port 8000

#### Phase 2: 密码哈希失败
1. **移除 passlib，直接使用 bcrypt 原生 API**（`app/core/security.py`）：
   - `passlib.CryptContext` → `bcrypt.gensalt()` + `bcrypt.hashpw()` + `bcrypt.checkpw()`
   - 原因：passlib 1.7.4 自 2020 年停止维护，与 bcrypt 5.0.0 不兼容；bcrypt 5.0.0 原生 API 更简洁
2. **更新 requirements.txt**：`passlib[bcrypt]>=1.7.4` → `bcrypt>=4.0.0`

### 验证结果
- Phase 1: send-code 端点 200 `{"code":0,"msg":"验证码已发送"}`，邮件成功发送
- Phase 2: 端到端服务层测试通过：
  - 验证码生成 → verify_code 验证通过 → INSERT users → 注册成功（id=1）→ 密码校验 True
- 完整注册流程无错误，密码哈希正常工作

### 影响范围：
- 后端：requirements.txt（python-jose→pyjwt, passlib→bcrypt）、app/core/security.py（重写为 bcrypt 原生 API）
- 前端：无修改（前端代码无缺陷，根因为后端依赖问题）
- 调试记录：debug-sendcode-network-error.md（已标记 [FIXED]，含完整插桩证据）

---

## 2026-06-29 - 注册页失焦验证 + 获取验证码网络错误修复

### 主要变动：

1. **表单失焦验证逻辑优化（register.vue）**
   - 新增 `errors` reactive 对象，按字段存储错误信息
   - 新增 `validateField(field)` 函数：失焦时按字段触发对应校验策略并写入 errors
   - 新增 `handleBlur(field)` 包装函数：先恢复 placeholder 原始色，再触发字段校验；密码字段失焦时联动重新校验确认密码
   - 新增 `handleFocus(field)` 函数：聚焦时清空该字段错误（输入清错）
   - 新增 `validateConfirmPassword` 函数：校验非空 + 与密码一致性
   - `handleRegister` 最终校验改为调用 `validateField` 写入 errors，同步展示视觉反馈
   - 所有输入框 `@blur` 改为 `handleBlur('字段名')`，`@focus` 改为 `handleFocus('字段名')`

2. **错误视觉反馈（register.vue 模板 + 样式）**
   - 输入框错误态：`:class="{ 'register-page__input--error': errors.字段 }"` 绑定红色边框
   - 验证码输入框错误态：`register-page__code-input--error` 红色边框
   - 每个字段下方新增 `<text v-if="errors.字段">` 错误文字提示（红色 #e5484d，12px）
   - 新增 CSS：`.register-page__input--error` / `.register-page__code-input--error`（box-shadow 红色边框）、`.register-page__error-text`（红色错误文字）

3. **获取验证码网络错误修复（request.js）**
   - 根因：微信小程序默认校验域名白名单，请求 `http://localhost:8000` 被拦截触发 fail 回调，原代码丢弃 errMsg 诊断信息
   - 修复 `fail` 回调：保留 `err.errMsg` 诊断信息，针对常见错误给出明确指引：
     - 域名未配置 → 提示在微信开发者工具勾选「不校验合法域名」
     - 连接拒绝 → 提示确认后端已启动（localhost:8000）
     - 超时 → 提示检查后端服务
   - Error 对象新增 `isNetworkError` 标识，区分网络错误与业务错误

4. **获取验证码重试机制（register.vue）**
   - 抽取 `requestCode()` 函数，`handleGetCode` 校验邮箱后调用
   - 网络错误（`isNetworkError`）：弹出 modal 提示原因 + 「重试/取消」按钮，确认则重新调用
   - 业务错误（如邮箱已注册）：toast 提示，不触发重试

### 影响范围：
- 前端：register.vue（脚本 + 模板 + 样式）、api/request.js（fail 回调错误处理）
- 构建验证：`npm run build:mp-weixin` 通过（exit 0，仅预存 Sass 警告）；register.vue 诊断无错误
- 使用提示：开发期需在微信开发者工具 → 详情 → 本地设置，勾选「不校验合法域名、web-view、TLS 版本以及 HTTPS 证书」，否则请求 localhost 会报网络错误

---

## 2026-06-29 - 实现用户注册功能（前后端联通 + 输入校验 + 邮箱验证码）

### 主要变动：

1. **后端：环境配置与依赖**
   - `requirements.txt`：新增 `email-validator>=2.0.0`（Pydantic EmailStr 依赖）
   - `.env.dev`：新增腾讯企业邮 SMTP 配置字段（SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASSWORD/SMTP_SENDER_NAME），SMTP_USER 与 SMTP_PASSWORD 留空待填写
   - `app/core/config.py`：Settings 新增 SMTP_* 配置项（默认值匹配腾讯企业邮 smtp.exmail.qq.com:465 SSL）

2. **后端：ORM 模型对齐 SQL 结构**
   - `app/models/user.py`：更新 User 模型匹配 `create_user_db.sql`
     - id 改为 BigInteger、字段名 `hashed_password` → `password_hash`
     - 补全 signature/avatar_url/status/last_login_at 字段
     - 添加 `__table_args__ = {"schema": "wuzuniao_yonghu"}` 实现跨库访问（用户表位于独立用户库，业务库通过跨库查询访问）

3. **后端：输入校验 Schema**
   - `app/schemas/user.py`：新增 RegisterUser / SendCode Schema
     - 用户名：field_validator 校验仅中文/英文/数字 + 长度 2-50
     - 密码：长度 8-20 + 大小写字母/数字/特殊符号至少三种
     - 验证码：6 位数字
     - 邮箱：EmailStr 标准格式

4. **后端：Email 类（邮箱操作）**
   - 新建 `app/services/email_service.py`：`Email` 类封装 SMTP 邮件发送
     - 使用 `smtplib.SMTP_SSL` 经腾讯企业邮（端口 465）发送
     - 源邮箱地址/授权码从环境变量读取
     - `send_verification_code(to_email, code)`：向用户邮箱发送 6 位验证码邮件
     - 未配置 SMTP_USER/SMTP_PASSWORD 时抛出明确错误提示

5. **后端：User 类（用户业务逻辑）**
   - 重构 `app/services/user_service.py` 为 `User` 类
     - `send_code(email)`：生成 6 位验证码 → 调用 Email 类发送 → 进程内暂存（5 分钟有效期）
     - `verify_code(email, code)`：注册时后端二次校验验证码
     - `register(username, password, email, code)`：验证码校验 → 用户名/邮箱唯一性 → 哈希密码入库
     - 已注册邮箱不再发送验证码

6. **后端：API 路由层**
   - `app/api/v1/users.py`：新增 POST `/api/v1/users/register`（注册）与 `/api/v1/users/send-code`（发送验证码）接口
   - `app/api/v1/__init__.py`：挂载 users 路由（prefix="/users"）
   - `app/api/__init__.py`：移除重复的 `/v1` 前缀（main.py 已通过 API_V1_STR 添加 /api/v1），修复路径为 /api/v1/users/*

7. **前端：API 请求层**
   - 新建 `src/api/request.js`：统一封装 uni.request（baseURL 拼接、错误解析兼容 FastAPI 校验错误/HTTPException/业务格式）
   - 新建 `src/api/modules/user.js`：导出 sendRegisterCode / registerUser 请求函数

8. **前端：register.vue 注册页重构**
   - 新增前端输入校验（与后端规则一致）：用户名/密码/邮箱/验证码
   - 获取验证码：前端校验邮箱 → 调用后端 send-code 接口 → 60 秒倒计时
   - 隐私政策同意机制：未勾选时弹出确认弹窗（确认/取消）；确认则自动勾选并提交，取消则关闭不提交
   - 注册提交：前端校验通过 → 调用后端 register 接口 → 成功跳转登录页

### 影响范围：
- 后端：新增 email_service.py，修改 user_service.py/users.py/models/user.py/schemas/user.py/config.py/.env.dev/requirements.txt/api 路由挂载
- 前端：新增 api/ 目录（request.js + modules/user.js），重构 register.vue 脚本逻辑
- 数据库：无需新增表（验证码进程内暂存），用户表结构已由 create_user_db.sql 定义
- 构建验证：`npm run build:mp-weixin` 通过（exit 0，仅预存 Sass 警告）；后端 `py_compile` 全部通过
- 待办：使用前需在 `.env.dev` 填写腾讯企业邮 SMTP_USER 与 SMTP_PASSWORD（客户端专用密码）

---

## 2026-06-28 - 修复 App.vue 末尾换行 + record.vue 日历点击选中功能

### 主要变动：

1. **App.vue：文件末尾补充换行符（Issue1）**
   - `</style>` 后添加空行，符合 POSIX 文本文件规范与编辑器代码规范要求

2. **record.vue：日历单元格绑定点击事件，实现日期选中切换（Issue2）**
   - v-for 渲染的 `.record-page__calendar-cell` 添加 `@click="handleSelectDay(day)"`
   - 新增 `handleSelectDay(day)` 函数：day 为 null（月首尾占位）直接返回；清除所有日期 isSelected，将点击日期置为选中
   - 新增 `selectedDate` ref（初始 '6月16日' 匹配 calendarDays 中 isSelected=true 的 16 日），点击日期后同步更新为 '6月{date}日'
   - 列表标题由静态 "6月15日" 改为动态 `{{ selectedDate }}`，与日历选中状态联动

### 影响范围：
- App.vue 末尾换行（代码规范）
- record.vue 日历交互功能补全：用户点击任意有效日期可切换选中状态并联动更新当日明细列表标题
- 构建验证：`npm run build:mp-weixin` 通过（exit 0，DONE Build complete）

---

## 2026-06-28 - 修复空规则报错 + 抽象复用样式/组件 + 页面功能注释

### 主要变动：

1. **修复 notification.vue 空规则报错（Do not use empty rule）**
   - 移除空的 `.notification-page__form-wrap {}` CSS 选择器
   - 同步移除模板中无样式的冗余 `notification-page__form-wrap` class（v-if 逻辑保留）
   - 经 GetDiagnostics 验证：notification.vue 诊断结果为空数组，报错彻底解决

2. **抽象复用 CSS 到 global.scss（仅抽象 2+ 页面重复样式）**
   - 扩展 CSS 设计令牌（CSS Custom Properties，参考 W3C css-variables 标准）：文本色、品牌色、卡片色、阴影、圆角、页面间距
   - 新增 `.u-arrow-right` 工具类（CSS border 三角形，参考 MDN 方案），替代 settings.vue / profile.vue 中重复的右箭头样式
   - 保留 page 作用域变量定义，App.vue 全局引入后所有页面可用 var(--xxx)

3. **新建 composables/usePlaceholder.js（4 页面输入框交互复用）**
   - 抽象 login.vue / register.vue / plan.vue / notification.vue 共有的 placeholder 聚焦交互逻辑
   - 导出 `{ activeField, onFocus, onBlur, phStyle }`，聚焦变浅灰 #c0c0c0，失焦恢复 placeholder-class 原始色
   - 参考 Vue 3 Composables 官方文档

4. **新建 components/PageHeader.vue（4 页面标题区复用）**
   - 抽象 notification.vue / profile.vue / help.vue / contact.vue 共有的标题区结构（标题 32px/36px/600 + 描述 16px/24px/pre-line）
   - Props：title（必填）、desc（可选，支持 \n 换行）
   - plan.vue 因 desc 有特殊 padding-top 4px 保留原结构以保持视觉不变

5. **重构现有页面应用抽象**
   - login.vue / register.vue / plan.vue / notification.vue：placeholder 逻辑改用 usePlaceholder，移除重复的 activeField/onFocus/onBlur/phStyle 定义
   - notification.vue / profile.vue / help.vue / contact.vue：标题区改用 PageHeader 组件，移除重复的 header/title/desc(subtitle) CSS
   - settings.vue / profile.vue：右箭头改用 .u-arrow-right 工具类，settings 保留绿色变体（--green）仅覆盖 border-left-color

6. **为 11 个页面主要功能添加详细注释**
   - 每个页面 `<script setup>` 顶部添加 JSDoc 块注释，说明页面功能、模块划分、交互逻辑
   - 注释清晰介绍功能作用，避免无意义内容（如 index/record/plan/notification/settings/profile/login/register/help/contact/privacy）

### 影响范围：
- 前端代码质量：消除空规则报错，减少 4 处 placeholder 逻辑重复、4 处标题区结构重复、2 处箭头样式重复
- 新增文件：frontend/src/composables/usePlaceholder.js、frontend/src/components/PageHeader.vue
- 修改文件：global.scss + 10 个页面 .vue（plan/notification/login/register 应用 usePlaceholder；notification/profile/help/contact 应用 PageHeader；settings/profile 应用 .u-arrow-right；全部 11 页添加功能注释）
- 构建验证：`npm run build:mp-weixin` 通过（exit 0，DONE Build complete），仅有预存在的 Sass @import 弃用警告

---

## 2026-06-28 - 头像去圆形边框 + design 目录清理未引用资源

### 主要变动：

1. **settings.vue：用户头像去除圆形边框效果**
   - 移除 `.settings-page__profile-avatar` 的 `border-radius: 9999px` 与 `box-shadow: inset 0 2px 4px 0 rgba(0,0,0,0.05)`
   - 头像现以原始矩形形状显示，外部无任何圆形边框/阴影
   - 头像 wrap 容器无圆形样式，确认无残留

2. **login.vue / register.vue / plan.vue / notification.vue：输入框 mouseout(=blur) 行为确认**
   - 小程序无原生 mouseout 事件，现有 `@blur` 失焦处理已等效覆盖"鼠标移出"场景
   - 颜色恢复：`onBlur()` 清空 `activeField`，`phStyle(key)` 返回空串，placeholder-class 接管恢复原始颜色（login/register 为 #454745，plan/notification 为 #868685）
   - 垂直对齐：各页 placeholder-class 仅含 `color` + `font-size`，从未改变对齐方式，placeholder 始终保持默认垂直居中
   - 现状已满足"未输入内容移出后恢复原始颜色与对齐"要求，无需改动

3. **design 目录清理：删除 5 个未被引用的设计稿参考图**
   - 经 Grep 全项目搜索确认，`frontend/src/assets/design/` 下 5 个文件（Container-x1.png、Container-x1-1.png、Container-x1-2.png、Icon-x1.png、reference.png）均未被任何页面/组件引用
   - 删除 5 个原文件，保留空 design 目录以便后续存放设计资源

### 影响范围：
- settings 页用户资料卡头像显示效果（去圆形边框）
- 项目静态资源清理（design 目录瘦身，无功能影响）
- 新增项目根目录备份文件 design_backup_20260628.zip

---

## 2026-06-28 - 首页高度自适应 + 输入框交互 + 卡片切换 + 通知卡 + 个人信息页

### 主要变动：

1. **index.vue：高度自适应，去除无内容时的滚动条**
   - `.index-page__frame` 的 `min-height` 由固定 884px 改为 `100vh`，`padding-top: 17px` + `box-sizing: border-box`
   - `.index-page__main-canvas` 的 `padding-bottom` 由 272px 调整为 120px，去除强制滚动
   - 不改变现有内容视觉位置与布局结构，仅消除多余滚动

2. **login.vue / register.vue / plan.vue：输入框 placeholder 聚焦交互**
   - 新增 `activeField` ref + `onFocus(key)` / `onBlur()` / `phStyle(key)` 逻辑
   - 聚焦时通过 `:placeholder-style` 将提示文字颜色设为浅灰 `#c0c0c0`
   - 失焦后（未输入内容）通过 `placeholder-class` 恢复原始颜色与样式
   - 覆盖三页全部输入框（登录 2、注册 4、计划 8，共 14 个）

3. **plan.vue：卡片切换功能**
   - 新增 `showForm` ref，默认 `false`：默认隐藏"新建计划详情"卡片，仅显示"新建计划"入口卡
   - 点击"新建计划"入口卡 → `handleNewEntry()` 置 `showForm=true`，入口卡隐藏、详情卡显示
   - 详情卡使用 `v-if` + `.plan-page__form--fade-in` 淡入动画（0.3s，translateY 8px），过渡自然

4. **notification.vue：新增"新建通知方式"卡片**
   - 新增 `showForm` ref + `handleAdd()` 切换逻辑：默认显示"添加新的通知方式"入口卡，点击后切换为"新建通知方式"表单卡
   - 表单卡含"通知名称"输入框、"接收地址"输入框、"保存通知"按钮
   - 视觉样式参照 plan.vue"新建计划详情"卡片（同圆角 12px、同阴影、同输入框样式、同淡入动画）
   - 输入框同样实现聚焦 placeholder 变灰交互

5. **profile.vue：个人信息页开发（原占位文件重写）**
   - 顶部 NoticeButton + 页面标题区（"个人信息" + 描述）
   - 分组 1：修改签名 / 修改密码 / 修改邮箱（3 项，带分隔线，点击 toast 占位）
   - 分组 2：退出登录（单独分组，红色文字提示危险操作，点击弹 `uni.showModal` 二次确认）
   - 分组卡片样式参照 settings 页分组 2（圆角 24px、行高 49px、右箭头 CSS 三角形）

### 影响范围：
- 首页滚动条问题修复（index.vue）
- 3 个页面输入框交互体验统一（login/register/plan）
- plan.vue 卡片切换交互（plan.vue）
- notification.vue 新增表单卡功能（notification.vue）
- profile.vue 从占位文件升级为完整页面（profile.vue）

---

## 2026-06-28 - 通知组件 top 调整 + 全局 padding-top 统一 + 底部导航栏固定

### 主要变动：

1. **NoticeButton.vue：top 45px**
   - 默认 `top` 从 `40px` 改为 `45px`（CSS 与 prop 默认值同步更新）
   - 按钮底部落在 85px 处

2. **全部 10 个页面主内容区 padding-top 统一为 100px**
   - index.vue（96px → 100px）、record.vue（80px → 100px）、settings.vue（88px → 100px）
   - plan.vue（104px → 100px）、notification.vue（104px → 100px）
   - help.vue（104px → 100px）、contact.vue（104px → 100px）
   - login.vue（88px → 100px）、register.vue（88px → 100px）、privacy.vue（88px → 100px）
   - 统一留 15px 间隙避免与通知按钮（底部 85px）重叠

3. **BottomNav.vue：固定在页面底部，不随滚动**
   - `position: absolute` → `position: fixed`（相对视口定位，不随页面内容滚动）
   - 新增 `z-index: 100`，确保始终浮于内容之上、保持在可视区域底部
   - 3 个使用 BottomNav 的页面（index/record/settings）已有 padding-bottom（177px/272px）为固定导航预留空间，内容不会被遮挡

### 影响范围：
- 全部页面顶部留白一致性（通知按钮与内容间距）
- 3 个含底部导航的页面（首页/记录/设置）导航栏交互体验（滚动时始终可见）

---

## 2026-06-28 - 通知组件全局统一 + 用户模块隐私政策跳转与样式统一

### 主要变动：

1. **NoticeButton.vue 全局配置统一**
   - 默认 `top` 从 `17px` 改为 `40px`（等于组件自身高度 40px）
   - 移除 7 个页面（index/record/settings/notification/plan/help/contact）的显式 `top` prop，统一使用默认值
   - 确保通知按钮在所有页面顶部间距一致（top 40px + 高 40px = 底部 80px）

2. **login.vue 多项调整**
   - "记住我"文本替换为"查看并同意《隐私政策》"，并拆分为普通文本 + 可点击链接
   - 新增 `goPrivacy()` 跳转 `/pages/user/privacy`（含 fail toast）
   - 微信登录图标路径：`ewm_wx.png` → `dl_wx.png`
   - 主容器 `padding-top` 从 `33.5px` 改为 `88px`，避免通知按钮与卡片重叠（修复 top 变更引入的潜在重叠）
   - 新增 `.login-page__agree-link` 样式

3. **register.vue 多项调整**
   - "查看并同意《隐私政策》"拆分为普通文本 + 可点击链接，新增 `goPrivacy()` 跳转
   - 主容器 `padding-top` 从 `4px` 改为 `88px`，修复白色卡片与通知按钮的重叠
   - 输入框边框颜色统一为 login 风格：`#0e0f0c` → `#c1cab5`（input/code-input/code-btn/checkbox 四处）
   - code-input/code-btn 高度从 `50px` 统一为 `49px`（与 login 一致）
   - 新增 `.register-page__agree-link` 样式

4. **privacy.vue 多项调整**
   - 主容器 `padding-top` 从 `24px` 改为 `88px`，修复标题与通知按钮的重叠
   - "我们如何共享您的信息"段落表格改为横向滚动：用 `scroll-view scroll-x` 包裹，表格固定宽度 `600px`，允许向左滑动查看被截断的右侧内容
   - 新增 `.privacy-page__table-scroll` 样式，调整 `.privacy-page__table` 为 flex 列布局并设固定宽度

### 影响范围：
- 通知组件视觉一致性（全部页面）
- 登录/注册页交互（隐私政策跳转、输入框样式）
- 隐私政策页排版（标题间距、表格可读性）

---

## 2026-06-28 - 实现用户模块页面跳转逻辑

### 主要变动：

1. **settings.vue 新增 4 处跳转**
   - 用户资料卡片：`@click="goLogin"` → `/pages/user/login`
   - 帮助中心：`@click="goHelp"` → `/pages/user/help`
   - 联系我们：`@click="goContact"` → `/pages/user/contact`
   - 隐私政策：`@click="goPrivacy"` → `/pages/user/privacy`
   - 新增统一导航辅助函数 `navigate(url)`，封装 `uni.navigateTo` 的 `fail` 回调（toast 提示"页面跳转失败"），避免静默失败

2. **login.vue 新增 1 处跳转**
   - 底部"还没有账号？ 立即注册"：`@click="goRegister"` → `/pages/user/register`
   - `goRegister` 函数含 `fail` 回调 toast 提示

3. **register.vue 无需修改**
   - 底部"已有账号？去登录"已绑定 `goLogin`（第 95 行），跳转 `/pages/user/login`，功能已存在

### 技术说明：
- 项目为 uni-app（非 Vue Router），按 AGENTS.md 6.2 规范使用 `uni.navigateTo`，与已有 `goPlan`/`goNotification` 风格一致
- 错误处理：`fail` 回调显示 toast，主要应对页面栈超限（uni-app 限制 10 层）等罕见场景
- 路径正确性：所有目标路径均已在 `pages.json` 注册（login/register/help/contact/privacy）
- 平台兼容：`@click` 与 `uni.navigateTo` 在微信小程序、H5、App 端均原生支持

### 影响范围：
- 修改文件：`frontend/src/pages/index/settings.vue`、`frontend/src/pages/user/login.vue`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增隐私政策页面（像素级还原 Figma 1-91）

### 主要变动：

1. **新增隐私政策页面**
   - 新建 `frontend/src/pages/user/privacy.vue`，按 Figma 设计稿（node 1-91）像素级还原
   - 页面结构（长文静态展示，无交互逻辑）：
     - 主容器：padding 24px 16px 48px
     - 标题区 "隐私政策"（32px/36px/600/#0e0f0c，padding 8px 0 16px）
     - 前言段落（20px/32.6px/#0e0f0c，padding-bottom 24px）
     - 主内容卡片（padding 24px，gap 32px，圆角 24px，阴影 `0 1 2 rgba(0,0,0,0.05)`）：
       - 一、我们如何收集和使用您的个人信息（含 2 个子标题 + 2 个项目列表）
       - 二、我们如何共享您的信息（含第三方信息共享表格）
       - 三、我们如何存储和保护您的信息（含项目列表）
       - 四、您的权利（含项目列表）
       - 五、未成年人保护
       - 六、本政策的更新
       - 七、如何联系我们（含邮箱联系方式 + 回复时效说明）
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="16px"`），不含系统导航栏与 `BottomNav` 组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 章节标题 24px/32px/600/#0e0f0c，padding-bottom 12px
   - 普通段落 16px/24px/#454745，padding-bottom 16px（含换行 white-space: pre-line）
   - 子标题 16px/24px/600/#0e0f0c，padding-bottom 8px
   - 项目列表 padding-left 20px，gap 8px，bullet 7px + 文字 16px/24px/#454745
   - 第三方共享信息表格：
     - 列宽：第三方名称 114.48px / 涉及个人信息 266.95px / 使用目的 218.57px
     - 单元格 padding 12px 8px，边框 #c1cab5
     - 表头文字 #0e0f0c 600，数据文字 #454745 400
   - 无交互逻辑，纯静态展示文字和表格

2. **路由注册**
   - `frontend/src/pages.json` — 在 `subPackages` 的 `pages/user` 分包下新增 `privacy` 路由，`navigationStyle: custom`

### 影响范围：
- 新增文件：`frontend/src/pages/user/privacy.vue`
- 修改文件：`frontend/src/pages.json`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增帮助中心页面（像素级还原 Figma 1-569）

### 主要变动：

1. **新增帮助中心页面**
   - 新建 `frontend/src/pages/user/help.vue`，按 Figma 设计稿（node 1-569）像素级还原
   - 页面结构（静态展示，无交互逻辑）：
     - 主内容画布：padding 104px 24px 32px，gap 32px
     - 页面标题区（Page Header，342x92，padding 0 8px，gap 8px）：
       - 主标题 "帮助中心"（32px/36px/600/#0e0f0c）
       - 副标题 "找到常见问题的解答，或了解如何更好地管理\n您的习惯。"（16px/24px/#454745，含换行）
     - FAQ 卡片列表（342x884，gap 16px）：
       - Card 1 "如何修改打卡时间？" 342x164
       - Card 2 "如何添加多个时间段？" 342x164
       - Card 3 "通知收不到怎么办？" 342x190
       - Card 4 "可以恢复误删的记录吗？" 342x138
       - Card 5 "如何导出我的历史数据？" 342x164
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="12px"`），不含系统导航栏与 `BottomNav` 组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 本页为纯文本静态展示，无需额外图标资源（顶栏铃铛由 NoticeButton 内置 `tongzhi_0.png` 提供，已在前序任务验证 40x40 #9fe870 匹配设计稿）
   - 卡片样式：padding 16px，白底，圆角 12px，阴影 `0 1 2 rgba(0,0,0,0.05)`
   - 卡片内部 gap 4px：标题 16px/24px/600/#0e0f0c + 正文 16px/26px/#454745（含换行 white-space: pre-line）
   - 无交互逻辑，纯静态展示文字

2. **路由注册**
   - `frontend/src/pages.json` — 在 `subPackages` 的 `pages/user` 分包下新增 `help` 路由，`navigationStyle: custom`

### 影响范围：
- 新增文件：`frontend/src/pages/user/help.vue`
- 修改文件：`frontend/src/pages.json`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增联系我们页面（像素级还原 Figma 1-532）

### 主要变动：

1. **新增联系我们页面**
   - 新建 `frontend/src/pages/user/contact.vue`，按 Figma 设计稿（node 1-532）像素级还原
   - 页面结构（静态展示，无交互逻辑）：
     - 主内容画布：padding 104px 24px 32px，gap 32px
     - 标题区（Contact Card，padding 0 8px，gap 8px）：
       - 主标题 "联系我们"（32px/36px/600/#0e0f0c）
       - 副标题 "如果您在使用过程中遇到任何问题，欢迎随时\n联系我们。"（16px/24px/#454745，含换行）
     - 白色卡片（Article，342x572，padding 24px，圆角 24px，阴影 `0 1 2 rgba(0,0,0,0.05)`）：
       - 联系方式列表（294x524，gap 24px）：
         - 客服邮箱：标签 14px/20px #5e5f5a + 内容 "xpg@wuzuniao.com" 16px/24px #0e0f0c
         - 微信公众号：标签 + "无足鸟ICT" + 140x140 二维码区（#f3f3f4 背景，圆角 8px，padding-top 12px）
         - QQ群：标签 + "278634838" + 140x140 二维码区（#f3f3f4 背景，圆角 8px，padding-top 12px）
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="12px"`），不含系统导航栏与 `BottomNav` 组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 图标资源均从 `assets/images/` 目录选取并验证尺寸颜色匹配设计稿：
     - `tongzhi_0.png`（40x40 #9fe870）→ 顶栏铃铛（NoticeButton 内置）
     - `ewm_wx.png`（140x140）→ 微信公众号二维码占位（设计稿容器 140x140）
     - `ewm_qq.png`（140x140）→ QQ群二维码占位（设计稿容器 140x140）
   - 二维码容器：140x140，背景 #f3f3f4，圆角 8px，flex 居中，图标 140x140 aspectFit
   - 标签文字 14px/20px #5e5f5a，内容文字 16px/24px #0e0f0c
   - 无交互逻辑，纯静态展示文字和图片

2. **路由注册**
   - `frontend/src/pages.json` — 在 `subPackages` 的 `pages/user` 分包下新增 `contact` 路由，`navigationStyle: custom`

### 影响范围：
- 新增文件：`frontend/src/pages/user/contact.vue`
- 修改文件：`frontend/src/pages.json`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增用户注册页面（像素级还原 Figma 5-57）

### 主要变动：

1. **新增用户注册页面**
   - 新建 `frontend/src/pages/user/register.vue`，按 Figma 设计稿（node 5-57）像素级还原
   - 页面结构（垂直居中布局）：
     - 白色卡片容器（342x742，padding 24px，gap 24px，圆角 24px，白底，阴影）
       - 标题区（gap 8px 居中）：主标题 "注册新账号"（32px/36px/600/#0e0f0c）+ 副标题 "加入我们，开始您的健康之旅。"（16px/24px/#454745）
       - 注册表单（gap 16px）：用户名 / 密码 / 确认密码 / 电子邮箱 / 验证码（输入框 190x50 + 获取验证码按钮 96x50）/ 协议勾选行 / 注册按钮
     - 底部登录链接 "已有账号？去登录"（margin-top 24px，16px/24px/#454745 居中）
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="14px"`），不含系统导航栏与 `BottomNav` 组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 输入框：白底、圆角 12px、inset 边框 #0e0f0c（设计稿为黑色边框，区别于登录页的 #c1cab5）、padding 14px 12px 12px、高 49px
   - 验证码输入框高 50px（与获取验证码按钮等高），按钮 96x50 inset 边框 #0e0f0c 圆角 12px
   - 协议复选框 16x16 圆角 4px，未选白底 inset 边框 #0e0f0c，选中 #2f6c00 + CSS 白色对勾
   - 协议文字 "查看并同意《隐私政策》" 12px/16px #454745
   - 注册按钮 #9fe870 圆角 24px 高 48px，文字 #0e0f0c 16px
   - 交互：协议勾选切换、验证码 60 秒倒计时、注册校验（必填 + 两次密码一致 + 协议勾选）、跳转登录页

2. **路由注册**
   - `frontend/src/pages.json` — 在 `subPackages` 的 `pages/user` 分包下新增 `register` 路由，`navigationStyle: custom`

### 影响范围：
- 新增文件：`frontend/src/pages/user/register.vue`
- 修改文件：`frontend/src/pages.json`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增登录页面（像素级还原 Figma 5-112）

### 主要变动：

1. **新增登录页面**
   - 新建 `frontend/src/pages/user/login.vue`，按 Figma 设计稿（node 5-112）像素级还原
   - 页面结构（垂直居中布局）：
     - 氛围背景装饰圆（312x312 #e2f6d5，绝对定位居中）
     - 主登录卡片（342x463，padding 24px，gap 24px，圆角 24px，白底，阴影）
       - 标题 "欢迎回来"（32px/36px/600/#0e0f0c 居中）
       - 表单：用户名/邮箱字段 + 密码字段（含眼睛图标 + 忘记密码链接）+ 记住我复选框 + 登录按钮
       - 底部注册链接 "还没有账号？ 立即注册"
     - 微信一键登录区（342x128，padding 24px，gap 12px，圆角 24px，白底，阴影）
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="12px"`），不含系统导航栏与 `BottomNav` 组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 图标资源均从 `assets/images/` 目录选取并验证尺寸颜色匹配设计稿：
     - `tongzhi_0.png`（40x40 #9fe870 无红点）→ 顶栏铃铛（NoticeButton 内置）
     - `mima_1.png`（19x13 #454745）→ 密码眼睛图标（设计 18.33x12.5）
     - `ewm_wx.png`（140x140）→ 微信图标（CSS 缩放至 23.33x23.33）
   - 输入框：白底、圆角 12px、inset 边框 #c1cab5、padding 14px 12px 12px
   - 密码输入框右侧 padding 24px 为眼睛图标留位，眼睛图标绝对定位
   - "忘记密码？"链接 12px/16px #454745 右对齐
   - 记住我复选框 16x16 圆角 4px，选中态 #2f6c00 + CSS 白色对勾
   - 登录按钮 #9fe870 圆角 24px 高 48px，文字 #0e0f0c 16px
   - 微信按钮 48x48 #eeeeee 圆形背景 + 微信图标居中
   - 交互：密码显隐切换、记住我勾选、登录校验提示、忘记密码/微信登录 toast 占位

2. **路由注册**
   - `frontend/src/pages.json` — 在 `subPackages` 的 `pages/user` 分包下新增 `login` 路由，`navigationStyle: custom`

### 影响范围：
- 新增文件：`frontend/src/pages/user/login.vue`
- 修改文件：`frontend/src/pages.json`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增制定计划页面（像素级还原 Figma 6-2）

### 主要变动：

1. **新增制定计划页面**
   - 新建 `frontend/src/pages/index/plan.vue`，按 Figma 设计稿（node 6-2）像素级还原
   - 页面结构：
     - 标题区（"制定计划" + 描述文案，保留换行）
     - 已有计划列表（2 张计划卡 + 1 张"新建计划"入口卡）
     - 新建计划表单（计划名称 / 备注说明 / 日期范围 / 提醒时间 / 通知方式 / 保存按钮）
   - 复用 `NoticeButton` 组件（`:has-notification="false"`，`top="12px"`），不含 `BottomNav` 导航组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 图标资源均从 `assets/images/` 目录选取并验证尺寸颜色匹配设计稿：
     - `tongzhi_0.png`（40x40 #9fe870 无红点）→ 顶栏铃铛
     - `jia_jihua.png`（14x14 #2f6c00）→ "新建计划"加号
     - `jia_shijian.png`（14x14 #2f6c00）→ "添加时间"加号
     - `shanchu.png`（16x18 #ba1a1a）→ 时间段删除
     - `baocun_jihua.png`（20x20 #ffffff）→ "保存计划"图标
   - 计划卡片左侧色条：激活态 `#2f6c00`、未激活态 `#e1e0da`，圆角 12px 0 0 12px
   - 时间标签药丸：`#e8ebe6` 背景、`#41493a` 文字、圆角 9999px
   - 表单输入框：`#f9f9f9` 背景、`#e8ebe6` inset 边框、圆角 6px
   - 时间段行：`#e8ebe6` 灰底容器 + 白色内输入框（圆角 4px）
   - 通知方式复选框：选中 `#2f6c00` + CSS 白色对勾，未选白底 `#e8ebe6` 边框
   - 保存按钮：`#2f6c00` 圆角胶囊 + 双层阴影，白色图标 + 白色文字
   - "..."菜单图标、日期范围右箭头、复选框对勾均采用 CSS 绘制（避免引入额外二进制资源）
   - 通知方式复选框支持点击切换（reactive 状态）

2. **路由注册**
   - `frontend/src/pages.json` — 新增 `pages/index/plan` 路由，`navigationStyle: custom`

3. **设置页跳转接线**
   - `frontend/src/pages/index/settings.vue` — "制定计划"链接卡新增 `@click="goPlan"`，通过 `uni.navigateTo` 跳转至计划页

### 影响范围：
- 新增文件：`frontend/src/pages/index/plan.vue`
- 修改文件：`frontend/src/pages.json`、`frontend/src/pages/index/settings.vue`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 新增通知方式页面（像素级还原 Figma 1-487）

### 主要变动：

1. **新增通知方式页面**
   - 新建 `frontend/src/pages/index/notification.vue`，按 Figma 设计稿（node 1-487）像素级还原
   - 页面结构：标题区（"通知方式" + 描述文案）+ 通知方式卡片列表（站内信 / 邮件）+ "添加新方式"按钮
   - 复用 `NoticeButton` 组件（`:has-notification="true"`，`top="12px"`），不含 `BottomNav` 导航组件
   - 应用全局背景色 `var(--page-bg-color)` 保证风格统一
   - 站内信图标使用指定资源 `assets/images/tz_znx.png`（48x48，已含绿色圆背景）
   - 邮件图标使用指定资源 `assets/images/tz_yx.png`（48x48，已含灰色圆背景）
   - 删除图标使用 `assets/images/shanchu_hui.png`（16x18，灰色）
   - "添加新方式"加号图标采用 CSS 双 view 绘制（避免引入额外二进制资源），颜色 #2f6c00
   - 卡片/按钮圆角 12px，添加按钮含 inset 边框 #c1cab5

2. **路由注册**
   - `frontend/src/pages.json` — 新增 `pages/index/notification` 路由，`navigationStyle: custom`

3. **设置页跳转接线**
   - `frontend/src/pages/index/settings.vue` — "通知方式"链接卡新增 `@click="goNotification"`，通过 `uni.navigateTo` 跳转至通知页

### 影响范围：
- 新增文件：`frontend/src/pages/index/notification.vue`
- 修改文件：`frontend/src/pages.json`、`frontend/src/pages/index/settings.vue`、`目录结构.json`、`更新记录.md`
- 构建验证：`npm run build:mp-weixin` 通过（`DONE Build complete.`）

---

## 2026-06-28 - 设置页面移除头像绿色圆环背景

### 主要变动：

1. **移除用户头像下方的绿色圆环背景**
   - `pages/index/settings.vue` — 删除模板中的 `<view class="settings-page__profile-avatar-bg">` 节点
   - 删除对应 CSS 规则 `.settings-page__profile-avatar-bg`（原 128x128 #e2f6d5 装饰圆，绝对定位于头像下方）
   - 头像本身（`.profile-avatar`）样式保持不变，仅移除外层装饰圆

2. **保留项（未变更）**
   - 资料卡片右上角绿色渐变 `.settings-page__profile-card-glow` 完整保留
   - 页面背景色、其他卡片、分组2、导航、通知按钮等所有其他视觉元素、布局与交互维持原样

### 影响范围：
- `frontend/src/pages/index/settings.vue`（手术式删除：1 个模板节点 + 1 个 CSS 规则）
- `更新记录.md`（文档同步）

---

## 2026-06-28 - 设置页面样式优化（背景色/头像/卡片渐变）

### 主要变动：

1. **页面背景色统一**
   - `pages/index/settings.vue` 中 `.settings-page` 背景色由硬编码 `#ffffff` 改为 `var(--page-bg-color)`，与 index、record 页面保持视觉一致

2. **用户头像去着色**
   - 移除 `.settings-page__profile-avatar` 的 `background: #eeeeee`，保持 PNG 头像原有色彩与透明度，不再有灰色底色渗透

3. **资料卡片右上角绿色渐变点缀**
   - 新增 `.settings-page__profile-card-glow` 渐变层（linear-gradient 225deg，#9fe870→#e2f6d5→透明），绝对定位于卡片右上角（160x160，右上圆角 24px 匹配卡片）
   - `.settings-page__profile-card` 新增 `position: relative` 与 `overflow: hidden`，约束渐变不溢出圆角
   - 调整层级：渐变层 z-index:0，文字 `.profile-info` 与头像 `.profile-avatar-wrap` 均为 z-index:1，确保渐变仅作背景装饰、不遮挡头像

### 影响范围：
- `frontend/src/pages/index/settings.vue`（样式调整，无结构破坏）
- `更新记录.md`（文档同步）

---

## 2026-06-28 - 开发设置页面 & 路由调整

### 主要变动：

1. **开发设置页面 `pages/index/settings.vue`**
   - 按 Figma 设计稿（node-id=1-364）像素级还原，包含：用户资料卡片（昵称、签名、头像）、制定计划卡片（绿色高亮+进行中状态）、通知方式卡片、分组2（帮助中心/联系我们/隐私政策）
   - 复用 `NoticeButton` 通知组件和 `BottomNav` 导航组件
   - 用户头像使用 `assets/images/touxiang.png`，头像背景装饰圆使用 #e2f6d5
   - 右箭头使用 CSS 三角形实现，绿色卡片箭头 #2f6c00，普通卡片箭头 #868685

2. **设置页路由从子包移至主包**
   - `pages.json` — 将 settings 从 `subPackages/pages/user` 移至主包 `pages/index/settings`，使用自定义导航栏（navigationStyle: custom）
   - `pages/user/` 下仅保留 `profile.vue`

3. **组件导航逻辑同步调整**
   - `components/BottomNav.vue` — settings 跳转方式由 `navigateTo('/pages/user/settings')` 改为 `reLaunch('/pages/index/settings')`，与 home/record 一致
   - `components/NoticeButton.vue` — 点击跳转由 `navigateTo('/pages/user/settings')` 改为 `reLaunch('/pages/index/settings')`；新增 `top` 属性（默认 17px）支持不同页面通知按钮的垂直位置定制

### 影响范围：
- `frontend/src/pages/index/settings.vue`（全新开发）
- `frontend/src/pages.json`（路由调整）
- `frontend/src/components/BottomNav.vue`（settings 跳转方式）
- `frontend/src/components/NoticeButton.vue`（跳转方式 + 新增 top 属性）
- `目录结构.json`、`更新记录.md`（文档同步）

---

## 2026-06-28 - 组件交互逻辑内聚化 & 修复列表项分隔线

### 主要变动：

1. **组件交互逻辑内聚（模内紧耦，模间解耦）**
   - 将两页面重复的导航逻辑下沉到对应组件内部，消除页面间重复代码：
   - `components/BottomNav.vue` — 内部封装 home/record/settings 跳转逻辑（home、record 用 reLaunch 切换主 tab，settings 用 navigateTo 进入子包页），点击当前已激活项时不重复跳转；移除 `change` 事件抛出
   - `components/NoticeButton.vue` — 内部封装点击跳转设置页逻辑；移除 `click` 事件抛出；并将两页面重复的 top-bar 定位样式（position:absolute; top:17px; left:24px; z-index:10）下沉到组件自身
   - `pages/index/index.vue` — 移除 `handleNoticeClick`、`handleNavChange`、`.index-page__top-bar` 包裹层及样式、模板上的 `@click`/`@change` 绑定
   - `pages/index/record.vue` — 同上，移除重复的处理函数、`.record-page__top-bar` 包裹层及样式、事件绑定

2. **修复列表项分隔线（参考 Figma 设计稿渲染图采样验证）**
   - `pages/index/record.vue` 中 `.record-page__list-item--bordered` 原 `box-shadow: inset 0 0 0 1px #e8ebe6` 会在四周绘制边框线，与设计稿不符
   - 对 reference.png 像素采样确认：首项顶部/左侧/右侧均为纯白(255,255,255)，仅底部 y=683-684 存在分隔线（约 #EDF0EC，为 #E8EBE6 抗锯齿结果）
   - 改为 `border-bottom: 1px solid #e8ebe6`，仅显示底部分隔线，与设计稿一致

3. **构建验证**
   - 构建成功，输出中 `handleNoticeClick`/`handleNavChange` 已从页面 JS 消失，BottomNav/NoticeButton JS 内含 reLaunch/navigateTo 调用，NoticeButton.wxss 含定位样式，record.wxss 列表项使用 border-bottom

---

## 2026-06-28 - 抽象页面背景色到全局CSS文件并修正背景色

### 主要变动：
1. **新增全局样式文件**
   - 新增 `frontend/src/assets/styles/global.scss` - 定义全局CSS变量 `--page-bg-color`，在 `page` 选择器上声明，供各页面按需引用

2. **全局引入**
   - 更新 `frontend/src/App.vue` - 在全局样式中 `@import` 引入 `global.scss`，使变量对所有页面可用

3. **修正错误的背景色**（原为白色 #ffffff，与设计稿不符）
   - 经对 Figma 设计稿参考图 `reference.png` 采样，实际画布背景为 `rgb(232, 235, 230)` = `#e8ebe6`（Figma MCP 报告的 frame 自身填充为白色，但卡片悬浮于 #e8ebe6 画布之上）
   - 更新 `frontend/src/pages/index/record.vue` - 根容器背景由 `#ffffff` 改为 `var(--page-bg-color)`
   - 更新 `frontend/src/pages/index/index.vue` - 根容器及 frame 容器背景由 `#ffffff` 改为 `var(--page-bg-color)`（移除 `.index-page__frame` 的硬编码白色背景）
   - 仅 index 目录两个页面引用该变量，settings/profile 等其他页面不受影响

4. **构建验证**
   - 构建成功，`app.wxss` 中正确输出 `page{--page-bg-color: #e8ebe6}`，record/index 页面 wxss 正确引用 `var(--page-bg-color)`

---

## 2026-06-28 - 记录页面按设计稿像素级还原

### 主要变动：
1. **页面重构**
   - 重写 `frontend/src/pages/index/record.vue` - 严格按 Figma 设计稿（node-id=1-220）像素级还原，删除与设计稿不一致的元素

2. **删除的元素**（设计稿中不存在）
   - 删除"用药记录"页面标题
   - 删除统计卡片（连续打卡天数、按时吃药率）

3. **日历卡片调整**
   - 卡片样式：圆角 12px、内边距 24px、阴影 0 1px 2px rgba(0,0,0,0.05)
   - 今日（15日）：绿色描边圆圈（inset 2px #2f6c00）
   - 选中日（16日）：浅绿背景圆圈（#e2f6d5）+ 深绿文字（#225100）
   - 已打卡日期（3、5、8、10、12、15）：底部绿色圆点（4×4 #2ead4b）
   - 月份标题"2026年6月"：24px/32px，字重 600
   - 日期数字：16px/24px，字重 500

4. **任务列表卡片调整**
   - 卡片样式：圆角 12px、内边距 24px、阴影
   - 列表标题：图标 + "6月15日"（18px/24px，字重 600）
   - 任务项：时间 + 描述（非药品名），符合设计稿
     - 已完成项：绿色圆点 + "09:05" + "饭后半小时服用" + 完成图标
     - 未完成项：灰色圆点 + "19:30" + "睡前服用"
   - 首项底部边框分隔（inset 1px #e8ebe6）

5. **构建验证**
   - 构建成功，无编译错误

---

## 2026-06-28 - 修复首页导航栏跳转

### 主要变动：
1. **Bug修复**
   - 更新 `frontend/src/pages/index/index.vue` - 修复底部导航栏点击"记录"按钮的跳转路径，从 `/pages/user/profile` 改为 `/pages/index/record`

2. **构建验证**
   - 构建成功，无编译错误

---

## 2026-06-28 - 记录页面像素级还原优化

### 主要变动：
1. **页面优化**
   - 更新 `frontend/src/pages/index/record.vue` - 按 Figma 设计稿像素级还原，修复日历箭头样式（使用 CSS border 三角形替代 clip-path），调整任务卡片布局和样式，优化统计卡片展示

2. **图标使用**
   - 使用 `jilu_xq.png` 作为任务卡片日期左边图标
   - 使用 `jilu_wc.png` 作为任务完成状态图标

3. **组件复用**
   - 复用 NoticeButton 通知组件
   - 复用 BottomNav 底部导航栏组件

4. **构建验证**
   - 构建成功，无编译错误

---

## 2026-06-28 - 底部导航栏组件抽象与复用

### 主要变动：
1. **新增组件**
   - 创建 `frontend/src/components/BottomNav.vue` - 可复用底部导航栏组件，包含首页、记录、设置三个导航项，支持激活状态切换

2. **前端页面**
   - 更新 `frontend/src/pages/index/index.vue` - 使用 BottomNav 组件替代内联底部导航，清理冗余样式代码，添加导航跳转逻辑

3. **文档更新**
   - 更新 `目录结构.json` - 添加 BottomNav.vue 组件描述

---

## 2026-06-28 - 通知组件抽象与复用

### 主要变动：
1. **新增组件**
   - 创建 `frontend/src/components/NoticeButton.vue` - 可复用通知按钮组件，支持未读状态切换和点击事件

2. **前端页面**
   - 更新 `frontend/src/pages/index/index.vue` - 使用 NoticeButton 组件替代内联通知按钮，清理冗余样式代码，添加点击跳转逻辑

3. **文档更新**
   - 更新 `目录结构.json` - 添加 components 目录及 NoticeButton.vue 描述

---

## 2026-06-28 - 首页按 Figma 设计稿还原

### 主要变动：
1. **前端页面**
   - 更新 `frontend/src/pages/index/index.vue` - 按首页 Figma 设计稿重构顶部通知、双层任务卡片、打卡主按钮和底部导航布局

2. **路由配置**
   - 更新 `frontend/pages.json` - 为首页开启自定义导航样式，避免原生导航栏影响页面还原

---

## 2026-06-28 - 重命名Agents.md为AGENTS.md

### 主要变动：
1. **文件重命名**
   - 将 `Agents.md` 重命名为 `AGENTS.md`
   - 同步更新 AGENTS.md 内部对自身的引用
   - 更新 `目录结构.json` 中的文件名

---

## 2026-06-28 - 更新AGENTS.md，添加新会话初始化要求

### 主要变动：
1. **文档**
   - 更新 `AGENTS.md` - 新增"新会话初始化要求（强制）"章节
   - 调整后续章节编号（原第4-11节顺延为第5-12节）
   - 调整子章节编号（原5.1-5.3顺延为6.1-6.3）

2. **新增要求**
   - 每次新会话开启时，必须检查并加载 `AGENTS.md` 和 `目录结构.json`
   - 若文档不存在或不确定，必须使用 Read 工具重新读取
   - 确保使用文档的最新版本

---

## 2026-06-28 - 数据库创建和SQL脚本完善

### 主要变动：
1. **数据库**
   - 创建 `wuzuniao_yonghu` 用户库 - 包含users和user_miniapp_accounts表
   - 重建 `wuzuniao_yao` 业务库 - 包含checkin_plans、plan_checkin_times、notification_channels、plan_notification_channels、notification_logs、checkin_records表
   - 调整notification_logs表status字段：0-成功/1-失败/2-未读

2. **SQL脚本**
   - 完善 `backend/sql/create_user_db.sql` - 用户库创建脚本
   - 完善 `backend/sql/create_yao_db.sql` - 业务库创建脚本

3. **文档**
   - 更新 `目录结构.json` - 添加backend/sql目录描述

---

## 2026-06-26 - 优化Agents.md并合并CLAUDE.md

### 主要变动：
1. **文档合并**
   - 合并 `CLAUDE.md` 通用行为指南至 `Agents.md` 第 3 节核心原则
   - 优化表述并增强约束力
   - 删除 `CLAUDE.md` 文件，消除文档冗余

2. **新增约束**
   - 新增简体中文交流要求：与用户交互（包括问答、注释、文档、提交信息等）必须使用简体中文

3. **文档更新**
   - 更新 `Agents.md` 变更记录
   - 更新 `Agents.md` 第 9 节，移除对 `CLAUDE.md` 的交叉引用

---

## 2026-06-25 - 创建Agents.md及数据库配置更新

### 主要变动：
1. **文档**
   - 创建 `Agents.md` - AI编程助手开发指南与约束文件
   - 更新 `目录结构.json` - 添加Agents.md描述

2. **数据库配置**
   - 更新 `backend/.env.dev` - 数据库名从mydb改为wuzuniao_yao
   - 更新 `backend/.env.prod` - 数据库名从mydb改为wuzuniao_yao
   - 更新 `scripts/test_db_connection.py` - 数据库名从mydb改为wuzuniao_yao

3. **配置说明**
   - 明确数据库结构为演进式设计，允许在开发过程中修改

---

## 2026-06-23 - 数据库连接测试成功

### 主要变动：
1. **脚本**
   - 更新 `scripts/test_db_connection.py` - 添加自动创建数据库功能、SQLAlchemy text() 修复

2. **测试结果**
   - MariaDB 12.3.2 连接成功
   - 创建 'mydb' 数据库成功
   - 数据库连接验证成功

---

## 2026-06-23 - 数据库连接配置与测试脚本

### 主要变动：
1. **脚本**
   - 创建 `scripts/test_db_connection.py` - 数据库连接测试脚本
   - 修复测试脚本编码问题

2. **配置文件**
   - 更新 `backend/.env.dev` - 更新数据库连接配置为 root/root@127.0.0.1:3306
   - 更新 `目录结构.json` - 添加测试脚本描述

---

## 2026-06-23 - 删除商品相关模块

### 主要变动：
1. **后端**
   - 删除 `backend/app/api/v1/products.py` - 商品模块API路由

2. **前端**
   - 删除 `frontend/src/api/modules/product.js` - 商品相关API模块
   - 删除 `frontend/src/pages/product/` 目录 - 商品模块页面
   - 修改 `frontend/pages.json` - 移除商品子包配置

3. **配置文件**
   - 更新 `目录结构.json` - 移除商品相关描述

---

## 2026-06-23 - 项目初始化

### 主要变动：
1. **项目目录结构搭建**
   - 创建完整的Monorepo项目结构
   - 划分backend、frontend、scripts三大模块

2. **后端核心文件创建**
   - `backend/app/core/config.py` - 环境配置管理，自动切换开发/生产环境
   - `backend/app/core/database.py` - SQLAlchemy异步数据库连接
   - `backend/app/core/security.py` - 密码加密工具
   - `backend/app/utils/logger.py` - 日志配置
   - `backend/app/main.py` - FastAPI应用入口，配置CORS中间件
   - `backend/app/api/v1/users.py` - 用户模块API路由
   - `backend/app/models/user.py` - 用户数据表模型
   - `backend/app/schemas/user.py` - 用户相关Schema
   - `backend/app/services/user_service.py` - 用户业务逻辑服务

3. **前端核心文件创建**
   - `frontend/src/api/request.js` - uni.request统一封装
   - `frontend/src/api/modules/user.js` - 用户相关API
   - `frontend/src/App.vue` - 应用根组件
   - `frontend/src/main.js` - 应用入口
   - `frontend/src/pages/index/index.vue` - 首页
   - `frontend/src/pages/user/profile.vue` - 个人信息页面
   - `frontend/src/pages/user/settings.vue` - 设置页面
   - `frontend/manifest.json` - 小程序配置
   - `frontend/pages.json` - 路由配置（包含用户子包）
   - `frontend/uni.scss` - 全局样式变量
   - `frontend/package.json` - 依赖配置

4. **辅助脚本与配置文件**
   - `scripts/dev.bat` - Windows一键启动后端
   - `scripts/init_db.py` - 数据库表初始化
   - `backend/.env.dev` - 开发环境变量
   - `backend/.env.prod` - 生产环境变量模板
   - `frontend/.env.dev` - 前端开发环境变量
   - `frontend/.env.prod` - 前端生产环境变量
   - `backend/requirements.txt` - Python依赖
   - `.gitignore` - Git忽略规则
   - `目录结构.json` - 项目结构说明
   - `更新记录.md` - 本文件

---

## 记录规范

### 格式要求：
```
## YYYY-MM-DD - 更新标题

### 主要变动：
1. **模块名**
   - 文件路径1 - 简要说明
   - 文件路径2 - 简要说明
   - ...

2. **模块名**
   - ...
```

### 注意事项：
- 日期格式：YYYY-MM-DD
- 按时间倒序排列（最新记录在最上面）
- 简要清晰地描述变更内容
- 涉及文件变更时，注明文件路径
- 重大功能变更可添加详细说明
