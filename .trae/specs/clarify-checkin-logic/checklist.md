# Checklist

以下检查点用于验证 `spec.md` 是否完整、准确地梳理了首页打卡逻辑（期望行为）。逐项核对，全部通过即视为梳理完成。

- [x] 按钮四种基础状态（disabled/waiting/active/done）的视觉表现已用表格列出（背景色、图标、文字、可点击性）
- [x] waiting 状态标注为**不显示图标**（文字垂直居中）
- [x] 已覆盖"用户未登录"场景的按钮状态与空状态文本
- [x] 已覆盖"已登录但无进行中计划"场景的按钮状态与空状态文本
- [x] 已覆盖"有计划但不在计划日期范围内"场景的按钮状态
- [x] 已覆盖"在日期范围内但无提醒时间"场景的按钮状态
- [x] 已覆盖"未到开始打卡时间"场景（waiting）及长按重置能力
- [x] 已覆盖"到达开始打卡时间且未打卡"场景（active），并说明超过提醒时间 t 仍为 active
- [x] 已覆盖"当前提醒已匹配打卡记录"场景（done）及长按重置能力
- [x] 已明确"开始打卡时间" = 提醒时间前 1 小时（t-60），且可打卡状态持续到用户打卡为止（不因超过 t 而结束）
- [x] 已说明小间隔（≤120 分钟）情况下中点划分：前半段看前一个提醒是否已打卡，后半段看当前提醒是否已打卡
- [x] 已说明情况 7 前半段未打卡时保持 active（非强制 done）
- [x] 已描述"打卡记录匹配判定"规则：单条提醒看全天任意记录，多条提醒看 `[t-60, t+60]` 区间记录
- [x] 已描述打卡成功后按钮即时转为绿色"已打卡"且不弹窗
- [x] 已描述 done 状态持续时长：到下一次提醒开始打卡时间、或当日 24:00 后重置为 waiting、或切换任务、或长按重置
- [x] 已描述每分钟定时器的作用（同步数据库打卡记录并重新判定）
- [x] 已说明 `new Date()` 非响应式，时间流逝不自动触发状态重算的边界
- [x] 已描述长按 3 秒重置机制（waiting/done → active，含 loading 与 toast 提示）
- [x] 已描述 `onShow` 时的数据加载与定时器启动
- [x] 已描述任务切换（次要卡片互换、任务列表选择）对 `forceActive` / `justCheckedInTimeId` 的重置与重新加载行为
- [x] 已在所有与当前代码不一致之处用 `⚠️ 当前实现差异` 标注，作为后续代码修改输入
