# Checklist

以下检查点用于验证 `spec.md` 是否完整、准确地梳理了首页打卡逻辑（以用户本次给出的新逻辑为准）。逐项核对，全部通过即视为梳理完成。

## 按钮基础状态
- [x] 按钮四种基础状态（disabled/waiting/active/done）的视觉表现已用表格列出（背景色、图标、文字、可点击性）
- [x] waiting 和 disabled 状态标注为不显示图标（文字垂直居中）

## 三种 disabled 场景
- [x] 已覆盖"用户未登录"场景的按钮状态与空状态文本
- [x] 已覆盖"已登录但未创建计划"场景的按钮状态与空状态文本
- [x] 已覆盖"已登录但当前时间不属于打卡日期范围"场景的按钮状态

## 打卡记录匹配区间划分规则（新逻辑）
- [x] 已明确"开始打卡时间" = 第一个提醒时间前 2 小时（t1-120），替代旧的 t-60
- [x] 已描述第一次提醒的匹配区间从 **0:00** 开始（因之前无其他提醒）
- [x] 已描述匹配区间**统一按相邻提醒的中点划分**，不再区分间隔>4h 和 ≤4h
- [x] 已描述第一次提醒匹配区间结束 = midpoint(t1, t2)
- [x] 已描述中间提醒匹配区间：[midpoint(t_{i-1}, t_i), midpoint(t_i, t_{i+1})]
- [x] 已描述最后一次提醒的匹配区间到 **24:00** 结束（因之后无其他提醒）
- [x] 已说明匹配区间内未打卡为 active（立即打卡）
- [x] 已说明**全天 0:00-24:00 无间隙、无留白**，任意时刻打卡记录都能归属到对应提醒

## 打卡记录匹配判定（新逻辑）
- [x] 已描述按匹配区间判定打卡记录，替代旧的 [t-60, t+60] 区间
- [x] 已说明第一次提醒查询 [0:00, midpoint] 区间
- [x] 已说明最后一次提醒查询 [midpoint, 24:00] 区间
- [x] 已说明中间提醒查询对应匹配区间 [midpoint, midpoint]

## 一天时间流程图
- [x] 已包含单提醒（12 点）的时间流程图，覆盖未打卡/7点打卡/11点打卡/16点打卡场景
- [x] 已包含双提醒（8点+20点）的时间流程图，覆盖未打卡/上午打卡/上下午都打卡场景
- [x] 双提醒示例已包含每分钟检查说明，标注用户在线时才检查
- [x] 已包含多提醒通用规则流程图，统一按中点划分匹配区间
- [x] 多提醒通用规则已补充每分钟检查说明（参考双提醒时间）
- [x] 多提醒通用规则已说明用户不在线时不进行检查
- [x] 时间流程图中已标注匹配区间起止（中点划分）、状态转换时机

## 打卡成功与状态持续
- [x] 已描述打卡成功后按钮即时转为绿色"已打卡"且不弹窗
- [x] 已描述 done 状态持续时长：到匹配区间结束、或 24:00、或切换任务、或长按重置

## 长按重置与防抖
- [x] 已描述长按 3 秒重置机制（waiting/done → active，disabled/active 不可重置）
- [x] 已描述新增防抖规则：同一任务 3 秒内只允许点击一次

## 刷新机制
- [x] 已描述每分钟定时器的作用（同步数据库打卡记录并重新判定）
- [x] 已说明 new Date() 非响应式，时间流逝不自动触发状态重算的边界
- [x] 已描述 onShow 时的数据加载与定时器启动
- [x] 已描述新增规则：用户不在线（页面不在前台）时不进行检查，onHide 清除定时器

## 任务卡片交互逻辑
- [x] 已说明打卡只针对主要卡片任务
- [x] 已说明点击主要卡片无反应，不设点击事件
- [x] 已说明点击次要卡片互换，再次点击再互换
- [x] 已说明点击"..."展开任务列表，选择任务替换到主要卡片

## 任务卡片默认显示顺序
- [x] 已说明只显示进行中任务（当前时间在打卡日期范围内）
- [x] 已说明 1 个任务只显示主要卡片
- [x] 已说明 2 个任务显示主要+次要，不显示"..."
- [x] 已说明 3+任务显示主要+次要+"..."
- [x] 已说明主要卡片优先级规则：当前时间在提醒时间前后 2 小时范围内的优先；都在范围内时优先级高的为主；优先级相同时先创建的为主

## 冲突对照
- [x] 已在附录中列出新旧逻辑冲突点对照表，明确以新逻辑为准
- [x] 对照表已包含匹配区间规则（统一按中点划分，全天无留白）
- [x] 对照表已包含刷新机制差异（onHide 清除定时器）
